//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-05-27 08:35:22 CEST

package windows_win32_graphics_gdi

const (
	/*
		ABORTDOC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ABORTDOC = 0x2

	/*
		ABSOLUTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ABSOLUTE = 0x1

	/*
		AC_SRC_ALPHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AC_SRC_ALPHA = 0x1

	/*
		AC_SRC_OVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AC_SRC_OVER = 0x0

	/*
		ASPECT_FILTERING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ASPECT_FILTERING = 0x1

	/*
		BANDINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BANDINFO = 0x18

	/*
		BEGIN_PATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BEGIN_PATH = 0x1000

	/*
		BKMODE_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BKMODE_LAST = 0x2

	/*
		CA_LOG_FILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CA_LOG_FILTER = 0x2

	/*
		CA_NEGATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CA_NEGATIVE = 0x1

	/*
		CBM_INIT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBM_INIT = 0x4

	/*
		CCHFORMNAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCHFORMNAME = 0x20

	/*
		CC_CHORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_CHORD = 0x4

	/*
		CC_CIRCLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_CIRCLES = 0x1

	/*
		CC_ELLIPSES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_ELLIPSES = 0x8

	/*
		CC_INTERIORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_INTERIORS = 0x80

	/*
		CC_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_NONE = 0x0

	/*
		CC_PIE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_PIE = 0x2

	/*
		CC_ROUNDRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_ROUNDRECT = 0x100

	/*
		CC_STYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_STYLED = 0x20

	/*
		CC_WIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_WIDE = 0x10

	/*
		CC_WIDESTYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CC_WIDESTYLED = 0x40

	/*
		CHARSET_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHARSET_DEFAULT = 0x1

	/*
		CHARSET_GLYPHIDX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHARSET_GLYPHIDX = 0x3

	/*
		CHECKJPEGFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKJPEGFORMAT = 0x1017

	/*
		CHECKPNGFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKPNGFORMAT = 0x1018

	/*
		CLEARTYPE_NATURAL_QUALITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLEARTYPE_NATURAL_QUALITY = 0x6

	/*
		CLIP_TO_PATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLIP_TO_PATH = 0x1001

	/*
		CLOSECHANNEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLOSECHANNEL = 0x1010

	/*
		CLR_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLR_INVALID = 0xffffffff

	/*
		CM_CMYK_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CM_CMYK_COLOR = 0x4

	/*
		CM_DEVICE_ICM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CM_DEVICE_ICM = 0x1

	/*
		CM_GAMMA_RAMP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CM_GAMMA_RAMP = 0x2

	/*
		CM_IN_GAMUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CM_IN_GAMUT = 0x0

	/*
		CM_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CM_NONE = 0x0

	/*
		CM_OUT_OF_GAMUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CM_OUT_OF_GAMUT = 0xff

	/*
		COLORMATCHTOTARGET_EMBEDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COLORMATCHTOTARGET_EMBEDED = 0x1

	/*
		CP_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_NONE = 0x0

	/*
		CP_RECTANGLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_RECTANGLE = 0x1

	/*
		CP_REGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_REGION = 0x2

	/*
		CREATECOLORSPACE_EMBEDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATECOLORSPACE_EMBEDED = 0x1

	/*
		DCBA_FACEDOWNCENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEDOWNCENTER = 0x101

	/*
		DCBA_FACEDOWNLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEDOWNLEFT = 0x102

	/*
		DCBA_FACEDOWNNONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEDOWNNONE = 0x100

	/*
		DCBA_FACEDOWNRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEDOWNRIGHT = 0x103

	/*
		DCBA_FACEUPCENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEUPCENTER = 0x1

	/*
		DCBA_FACEUPLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEUPLEFT = 0x2

	/*
		DCBA_FACEUPNONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEUPNONE = 0x0

	/*
		DCBA_FACEUPRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCBA_FACEUPRIGHT = 0x3

	/*
		DCTT_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCTT_BITMAP = 0x1

	/*
		DCTT_DOWNLOAD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCTT_DOWNLOAD = 0x2

	/*
		DCTT_DOWNLOAD_OUTLINE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCTT_DOWNLOAD_OUTLINE = 0x8

	/*
		DCTT_SUBDEV from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCTT_SUBDEV = 0x4

	/*
		DC_BINADJUST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_BINADJUST = 0x13

	/*
		DC_DATATYPE_PRODUCED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_DATATYPE_PRODUCED = 0x15

	/*
		DC_EMF_COMPLIANT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_EMF_COMPLIANT = 0x14

	/*
		DC_MANUFACTURER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_MANUFACTURER = 0x17

	/*
		DC_MODEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_MODEL = 0x18

	/*
		DEVICEDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEDATA = 0x13

	/*
		DEVICE_FONTTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICE_FONTTYPE = 0x2

	/*
		DISPLAYCONFIG_MAXPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_MAXPATH = 0x400

	/*
		DISPLAYCONFIG_PATH_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_ACTIVE = 0x1

	/*
		DISPLAYCONFIG_PATH_CLONE_GROUP_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_CLONE_GROUP_INVALID = 0xffff

	/*
		DISPLAYCONFIG_PATH_DESKTOP_IMAGE_IDX_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_DESKTOP_IMAGE_IDX_INVALID = 0xffff

	/*
		DISPLAYCONFIG_PATH_MODE_IDX_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_MODE_IDX_INVALID = 0xffffffff

	/*
		DISPLAYCONFIG_PATH_PREFERRED_UNSCALED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_PREFERRED_UNSCALED = 0x4

	/*
		DISPLAYCONFIG_PATH_SOURCE_MODE_IDX_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_SOURCE_MODE_IDX_INVALID = 0xffff

	/*
		DISPLAYCONFIG_PATH_SUPPORT_VIRTUAL_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_SUPPORT_VIRTUAL_MODE = 0x8

	/*
		DISPLAYCONFIG_PATH_TARGET_MODE_IDX_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_TARGET_MODE_IDX_INVALID = 0xffff

	/*
		DISPLAYCONFIG_PATH_VALID_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_PATH_VALID_FLAGS = 0x1d

	/*
		DISPLAYCONFIG_SOURCE_IN_USE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_SOURCE_IN_USE = 0x1

	/*
		DISPLAYCONFIG_TARGET_FORCED_AVAILABILITY_BOOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_TARGET_FORCED_AVAILABILITY_BOOT = 0x4

	/*
		DISPLAYCONFIG_TARGET_FORCED_AVAILABILITY_PATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_TARGET_FORCED_AVAILABILITY_PATH = 0x8

	/*
		DISPLAYCONFIG_TARGET_FORCED_AVAILABILITY_SYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_TARGET_FORCED_AVAILABILITY_SYSTEM = 0x10

	/*
		DISPLAYCONFIG_TARGET_FORCIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_TARGET_FORCIBLE = 0x2

	/*
		DISPLAYCONFIG_TARGET_IN_USE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_TARGET_IN_USE = 0x1

	/*
		DISPLAYCONFIG_TARGET_IS_HMD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYCONFIG_TARGET_IS_HMD = 0x20

	/*
		DISPLAY_DEVICE_ACC_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_ACC_DRIVER = 0x40

	/*
		DISPLAY_DEVICE_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_ACTIVE = 0x1

	/*
		DISPLAY_DEVICE_ATTACHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_ATTACHED = 0x2

	/*
		DISPLAY_DEVICE_ATTACHED_TO_DESKTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_ATTACHED_TO_DESKTOP = 0x1

	/*
		DISPLAY_DEVICE_DISCONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_DISCONNECT = 0x2000000

	/*
		DISPLAY_DEVICE_MIRRORING_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_MIRRORING_DRIVER = 0x8

	/*
		DISPLAY_DEVICE_MODESPRUNED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_MODESPRUNED = 0x8000000

	/*
		DISPLAY_DEVICE_MULTI_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_MULTI_DRIVER = 0x2

	/*
		DISPLAY_DEVICE_PRIMARY_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_PRIMARY_DEVICE = 0x4

	/*
		DISPLAY_DEVICE_RDPUDD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_RDPUDD = 0x1000000

	/*
		DISPLAY_DEVICE_REMOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_REMOTE = 0x4000000

	/*
		DISPLAY_DEVICE_REMOVABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_REMOVABLE = 0x20

	/*
		DISPLAY_DEVICE_TS_COMPATIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_TS_COMPATIBLE = 0x200000

	/*
		DISPLAY_DEVICE_UNSAFE_MODES_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_UNSAFE_MODES_ON = 0x80000

	/*
		DISPLAY_DEVICE_VGA_COMPATIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAY_DEVICE_VGA_COMPATIBLE = 0x10

	/*
		DI_APPBANDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DI_APPBANDING = 0x1

	/*
		DI_ROPS_READ_DESTINATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DI_ROPS_READ_DESTINATION = 0x2

	/*
		DMBIN_AUTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_AUTO = 0x7

	/*
		DMBIN_CASSETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_CASSETTE = 0xe

	/*
		DMBIN_ENVELOPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_ENVELOPE = 0x5

	/*
		DMBIN_ENVMANUAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_ENVMANUAL = 0x6

	/*
		DMBIN_FORMSOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_FORMSOURCE = 0xf

	/*
		DMBIN_LARGECAPACITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_LARGECAPACITY = 0xb

	/*
		DMBIN_LARGEFMT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_LARGEFMT = 0xa

	/*
		DMBIN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_LAST = 0xf

	/*
		DMBIN_LOWER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_LOWER = 0x2

	/*
		DMBIN_MANUAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_MANUAL = 0x4

	/*
		DMBIN_MIDDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_MIDDLE = 0x3

	/*
		DMBIN_ONLYONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_ONLYONE = 0x1

	/*
		DMBIN_SMALLFMT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_SMALLFMT = 0x9

	/*
		DMBIN_TRACTOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_TRACTOR = 0x8

	/*
		DMBIN_UPPER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_UPPER = 0x1

	/*
		DMBIN_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMBIN_USER = 0x100

	/*
		DMDISPLAYFLAGS_TEXTMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDISPLAYFLAGS_TEXTMODE = 0x4

	/*
		DMDITHER_COARSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_COARSE = 0x2

	/*
		DMDITHER_ERRORDIFFUSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_ERRORDIFFUSION = 0x5

	/*
		DMDITHER_FINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_FINE = 0x3

	/*
		DMDITHER_GRAYSCALE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_GRAYSCALE = 0xa

	/*
		DMDITHER_LINEART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_LINEART = 0x4

	/*
		DMDITHER_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_NONE = 0x1

	/*
		DMDITHER_RESERVED6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_RESERVED6 = 0x6

	/*
		DMDITHER_RESERVED7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_RESERVED7 = 0x7

	/*
		DMDITHER_RESERVED8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_RESERVED8 = 0x8

	/*
		DMDITHER_RESERVED9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_RESERVED9 = 0x9

	/*
		DMDITHER_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMDITHER_USER = 0x100

	/*
		DMICMMETHOD_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICMMETHOD_DEVICE = 0x4

	/*
		DMICMMETHOD_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICMMETHOD_DRIVER = 0x3

	/*
		DMICMMETHOD_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICMMETHOD_NONE = 0x1

	/*
		DMICMMETHOD_SYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICMMETHOD_SYSTEM = 0x2

	/*
		DMICMMETHOD_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICMMETHOD_USER = 0x100

	/*
		DMICM_ABS_COLORIMETRIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICM_ABS_COLORIMETRIC = 0x4

	/*
		DMICM_COLORIMETRIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICM_COLORIMETRIC = 0x3

	/*
		DMICM_CONTRAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICM_CONTRAST = 0x2

	/*
		DMICM_SATURATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICM_SATURATE = 0x1

	/*
		DMICM_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMICM_USER = 0x100

	/*
		DMMEDIA_GLOSSY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMMEDIA_GLOSSY = 0x3

	/*
		DMMEDIA_STANDARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMMEDIA_STANDARD = 0x1

	/*
		DMMEDIA_TRANSPARENCY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMMEDIA_TRANSPARENCY = 0x2

	/*
		DMMEDIA_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMMEDIA_USER = 0x100

	/*
		DMNUP_ONEUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMNUP_ONEUP = 0x2

	/*
		DMNUP_SYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMNUP_SYSTEM = 0x1

	/*
		DMORIENT_LANDSCAPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMORIENT_LANDSCAPE = 0x2

	/*
		DMORIENT_PORTRAIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMORIENT_PORTRAIT = 0x1

	/*
		DMPAPER_10X11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_10X11 = 0x2d

	/*
		DMPAPER_10X14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_10X14 = 0x10

	/*
		DMPAPER_11X17 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_11X17 = 0x11

	/*
		DMPAPER_12X11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_12X11 = 0x5a

	/*
		DMPAPER_15X11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_15X11 = 0x2e

	/*
		DMPAPER_9X11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_9X11 = 0x2c

	/*
		DMPAPER_A2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A2 = 0x42

	/*
		DMPAPER_A3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A3 = 0x8

	/*
		DMPAPER_A3_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A3_EXTRA = 0x3f

	/*
		DMPAPER_A3_EXTRA_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A3_EXTRA_TRANSVERSE = 0x44

	/*
		DMPAPER_A3_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A3_ROTATED = 0x4c

	/*
		DMPAPER_A3_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A3_TRANSVERSE = 0x43

	/*
		DMPAPER_A4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A4 = 0x9

	/*
		DMPAPER_A4SMALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A4SMALL = 0xa

	/*
		DMPAPER_A4_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A4_EXTRA = 0x35

	/*
		DMPAPER_A4_PLUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A4_PLUS = 0x3c

	/*
		DMPAPER_A4_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A4_ROTATED = 0x4d

	/*
		DMPAPER_A4_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A4_TRANSVERSE = 0x37

	/*
		DMPAPER_A5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A5 = 0xb

	/*
		DMPAPER_A5_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A5_EXTRA = 0x40

	/*
		DMPAPER_A5_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A5_ROTATED = 0x4e

	/*
		DMPAPER_A5_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A5_TRANSVERSE = 0x3d

	/*
		DMPAPER_A6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A6 = 0x46

	/*
		DMPAPER_A6_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A6_ROTATED = 0x53

	/*
		DMPAPER_A_PLUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_A_PLUS = 0x39

	/*
		DMPAPER_B4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B4 = 0xc

	/*
		DMPAPER_B4_JIS_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B4_JIS_ROTATED = 0x4f

	/*
		DMPAPER_B5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B5 = 0xd

	/*
		DMPAPER_B5_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B5_EXTRA = 0x41

	/*
		DMPAPER_B5_JIS_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B5_JIS_ROTATED = 0x50

	/*
		DMPAPER_B5_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B5_TRANSVERSE = 0x3e

	/*
		DMPAPER_B6_JIS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B6_JIS = 0x58

	/*
		DMPAPER_B6_JIS_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B6_JIS_ROTATED = 0x59

	/*
		DMPAPER_B_PLUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_B_PLUS = 0x3a

	/*
		DMPAPER_CSHEET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_CSHEET = 0x18

	/*
		DMPAPER_DBL_JAPANESE_POSTCARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_DBL_JAPANESE_POSTCARD = 0x45

	/*
		DMPAPER_DBL_JAPANESE_POSTCARD_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_DBL_JAPANESE_POSTCARD_ROTATED = 0x52

	/*
		DMPAPER_DSHEET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_DSHEET = 0x19

	/*
		DMPAPER_ENV_10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_10 = 0x14

	/*
		DMPAPER_ENV_11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_11 = 0x15

	/*
		DMPAPER_ENV_12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_12 = 0x16

	/*
		DMPAPER_ENV_14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_14 = 0x17

	/*
		DMPAPER_ENV_9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_9 = 0x13

	/*
		DMPAPER_ENV_B4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_B4 = 0x21

	/*
		DMPAPER_ENV_B5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_B5 = 0x22

	/*
		DMPAPER_ENV_B6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_B6 = 0x23

	/*
		DMPAPER_ENV_C3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_C3 = 0x1d

	/*
		DMPAPER_ENV_C4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_C4 = 0x1e

	/*
		DMPAPER_ENV_C5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_C5 = 0x1c

	/*
		DMPAPER_ENV_C6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_C6 = 0x1f

	/*
		DMPAPER_ENV_C65 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_C65 = 0x20

	/*
		DMPAPER_ENV_DL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_DL = 0x1b

	/*
		DMPAPER_ENV_INVITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_INVITE = 0x2f

	/*
		DMPAPER_ENV_ITALY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_ITALY = 0x24

	/*
		DMPAPER_ENV_MONARCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_MONARCH = 0x25

	/*
		DMPAPER_ENV_PERSONAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ENV_PERSONAL = 0x26

	/*
		DMPAPER_ESHEET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ESHEET = 0x1a

	/*
		DMPAPER_EXECUTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_EXECUTIVE = 0x7

	/*
		DMPAPER_FANFOLD_LGL_GERMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_FANFOLD_LGL_GERMAN = 0x29

	/*
		DMPAPER_FANFOLD_STD_GERMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_FANFOLD_STD_GERMAN = 0x28

	/*
		DMPAPER_FANFOLD_US from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_FANFOLD_US = 0x27

	/*
		DMPAPER_FOLIO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_FOLIO = 0xe

	/*
		DMPAPER_ISO_B4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_ISO_B4 = 0x2a

	/*
		DMPAPER_JAPANESE_POSTCARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JAPANESE_POSTCARD = 0x2b

	/*
		DMPAPER_JAPANESE_POSTCARD_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JAPANESE_POSTCARD_ROTATED = 0x51

	/*
		DMPAPER_JENV_CHOU3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_CHOU3 = 0x49

	/*
		DMPAPER_JENV_CHOU3_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_CHOU3_ROTATED = 0x56

	/*
		DMPAPER_JENV_CHOU4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_CHOU4 = 0x4a

	/*
		DMPAPER_JENV_CHOU4_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_CHOU4_ROTATED = 0x57

	/*
		DMPAPER_JENV_KAKU2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_KAKU2 = 0x47

	/*
		DMPAPER_JENV_KAKU2_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_KAKU2_ROTATED = 0x54

	/*
		DMPAPER_JENV_KAKU3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_KAKU3 = 0x48

	/*
		DMPAPER_JENV_KAKU3_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_KAKU3_ROTATED = 0x55

	/*
		DMPAPER_JENV_YOU4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_YOU4 = 0x5b

	/*
		DMPAPER_JENV_YOU4_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_JENV_YOU4_ROTATED = 0x5c

	/*
		DMPAPER_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LAST = 0x76

	/*
		DMPAPER_LEDGER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LEDGER = 0x4

	/*
		DMPAPER_LEGAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LEGAL = 0x5

	/*
		DMPAPER_LEGAL_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LEGAL_EXTRA = 0x33

	/*
		DMPAPER_LETTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTER = 0x1

	/*
		DMPAPER_LETTERSMALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTERSMALL = 0x2

	/*
		DMPAPER_LETTER_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTER_EXTRA = 0x32

	/*
		DMPAPER_LETTER_EXTRA_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTER_EXTRA_TRANSVERSE = 0x38

	/*
		DMPAPER_LETTER_PLUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTER_PLUS = 0x3b

	/*
		DMPAPER_LETTER_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTER_ROTATED = 0x4b

	/*
		DMPAPER_LETTER_TRANSVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_LETTER_TRANSVERSE = 0x36

	/*
		DMPAPER_NOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_NOTE = 0x12

	/*
		DMPAPER_P16K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_P16K = 0x5d

	/*
		DMPAPER_P16K_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_P16K_ROTATED = 0x6a

	/*
		DMPAPER_P32K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_P32K = 0x5e

	/*
		DMPAPER_P32KBIG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_P32KBIG = 0x5f

	/*
		DMPAPER_P32KBIG_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_P32KBIG_ROTATED = 0x6c

	/*
		DMPAPER_P32K_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_P32K_ROTATED = 0x6b

	/*
		DMPAPER_PENV_1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_1 = 0x60

	/*
		DMPAPER_PENV_10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_10 = 0x69

	/*
		DMPAPER_PENV_10_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_10_ROTATED = 0x76

	/*
		DMPAPER_PENV_1_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_1_ROTATED = 0x6d

	/*
		DMPAPER_PENV_2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_2 = 0x61

	/*
		DMPAPER_PENV_2_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_2_ROTATED = 0x6e

	/*
		DMPAPER_PENV_3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_3 = 0x62

	/*
		DMPAPER_PENV_3_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_3_ROTATED = 0x6f

	/*
		DMPAPER_PENV_4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_4 = 0x63

	/*
		DMPAPER_PENV_4_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_4_ROTATED = 0x70

	/*
		DMPAPER_PENV_5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_5 = 0x64

	/*
		DMPAPER_PENV_5_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_5_ROTATED = 0x71

	/*
		DMPAPER_PENV_6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_6 = 0x65

	/*
		DMPAPER_PENV_6_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_6_ROTATED = 0x72

	/*
		DMPAPER_PENV_7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_7 = 0x66

	/*
		DMPAPER_PENV_7_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_7_ROTATED = 0x73

	/*
		DMPAPER_PENV_8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_8 = 0x67

	/*
		DMPAPER_PENV_8_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_8_ROTATED = 0x74

	/*
		DMPAPER_PENV_9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_9 = 0x68

	/*
		DMPAPER_PENV_9_ROTATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_PENV_9_ROTATED = 0x75

	/*
		DMPAPER_QUARTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_QUARTO = 0xf

	/*
		DMPAPER_RESERVED_48 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_RESERVED_48 = 0x30

	/*
		DMPAPER_RESERVED_49 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_RESERVED_49 = 0x31

	/*
		DMPAPER_STATEMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_STATEMENT = 0x6

	/*
		DMPAPER_TABLOID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_TABLOID = 0x3

	/*
		DMPAPER_TABLOID_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_TABLOID_EXTRA = 0x34

	/*
		DMPAPER_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMPAPER_USER = 0x100

	/*
		DMRES_DRAFT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMRES_DRAFT = -1

	/*
		DMRES_HIGH from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMRES_HIGH = -4

	/*
		DMRES_LOW from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMRES_LOW = -2

	/*
		DMRES_MEDIUM from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMRES_MEDIUM = -3

	/*
		DOWNLOADFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOWNLOADFACE = 0x202

	/*
		DOWNLOADHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOWNLOADHEADER = 0x100f

	/*
		DRAFTMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRAFTMODE = 0x7

	/*
		DRAWPATTERNRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRAWPATTERNRECT = 0x19

	/*
		DT_CHARSTREAM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_CHARSTREAM = 0x4

	/*
		DT_DISPFILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_DISPFILE = 0x6

	/*
		DT_METAFILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_METAFILE = 0x5

	/*
		DT_PLOTTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_PLOTTER = 0x0

	/*
		DT_RASCAMERA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_RASCAMERA = 0x3

	/*
		DT_RASDISPLAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_RASDISPLAY = 0x1

	/*
		DT_RASPRINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DT_RASPRINTER = 0x2

	/*
		ELF_CULTURE_LATIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ELF_CULTURE_LATIN = 0x0

	/*
		ELF_VENDOR_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ELF_VENDOR_SIZE = 0x4

	/*
		ELF_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ELF_VERSION = 0x0

	/*
		ENABLEDUPLEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENABLEDUPLEX = 0x1c

	/*
		ENABLEPAIRKERNING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENABLEPAIRKERNING = 0x301

	/*
		ENABLERELATIVEWIDTHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENABLERELATIVEWIDTHS = 0x300

	/*
		ENCAPSULATED_POSTSCRIPT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENCAPSULATED_POSTSCRIPT = 0x1014

	/*
		ENDDOC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDDOC = 0xb

	/*
		END_PATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	END_PATH = 0x1002

	/*
		ENHMETA_SIGNATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENHMETA_SIGNATURE = 0x464d4520

	/*
		ENHMETA_STOCK_OBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENHMETA_STOCK_OBJECT = 0x80000000

	/*
		ENUMPAPERBINS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENUMPAPERBINS = 0x1f

	/*
		ENUMPAPERMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENUMPAPERMETRICS = 0x22

	/*
		EPSPRINTING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EPSPRINTING = 0x21

	/*
		EPS_SIGNATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EPS_SIGNATURE = 0x46535045

	/*
		ERROR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERROR = 0x0

	/*
		ERR_FORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_FORMAT = 0x3ee

	/*
		ERR_GENERIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_GENERIC = 0x3e8

	/*
		ERR_INVALID_BASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_BASE = 0x43d

	/*
		ERR_INVALID_CMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_CMAP = 0x424

	/*
		ERR_INVALID_DELTA_FORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_DELTA_FORMAT = 0x3f5

	/*
		ERR_INVALID_EBLC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_EBLC = 0x43e

	/*
		ERR_INVALID_GDEF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_GDEF = 0x43b

	/*
		ERR_INVALID_GLYF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_GLYF = 0x425

	/*
		ERR_INVALID_GPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_GPOS = 0x43a

	/*
		ERR_INVALID_GSUB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_GSUB = 0x439

	/*
		ERR_INVALID_HDMX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_HDMX = 0x441

	/*
		ERR_INVALID_HEAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_HEAD = 0x426

	/*
		ERR_INVALID_HHEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_HHEA = 0x427

	/*
		ERR_INVALID_HHEA_OR_VHEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_HHEA_OR_VHEA = 0x430

	/*
		ERR_INVALID_HMTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_HMTX = 0x428

	/*
		ERR_INVALID_HMTX_OR_VMTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_HMTX_OR_VMTX = 0x431

	/*
		ERR_INVALID_JSTF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_JSTF = 0x43c

	/*
		ERR_INVALID_LOCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_LOCA = 0x429

	/*
		ERR_INVALID_LTSH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_LTSH = 0x43f

	/*
		ERR_INVALID_MAXP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_MAXP = 0x42a

	/*
		ERR_INVALID_MERGE_CHECKSUMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_MERGE_CHECKSUMS = 0x3f3

	/*
		ERR_INVALID_MERGE_FORMATS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_MERGE_FORMATS = 0x3f2

	/*
		ERR_INVALID_MERGE_NUMGLYPHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_MERGE_NUMGLYPHS = 0x3f4

	/*
		ERR_INVALID_NAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_NAME = 0x42b

	/*
		ERR_INVALID_OS2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_OS2 = 0x42d

	/*
		ERR_INVALID_POST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_POST = 0x42c

	/*
		ERR_INVALID_TTC_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_TTC_INDEX = 0x3f7

	/*
		ERR_INVALID_TTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_TTO = 0x438

	/*
		ERR_INVALID_VDMX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_VDMX = 0x440

	/*
		ERR_INVALID_VHEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_VHEA = 0x42e

	/*
		ERR_INVALID_VMTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_INVALID_VMTX = 0x42f

	/*
		ERR_MEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MEM = 0x3ed

	/*
		ERR_MISSING_CMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_CMAP = 0x406

	/*
		ERR_MISSING_EBDT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_EBDT = 0x414

	/*
		ERR_MISSING_GLYF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_GLYF = 0x407

	/*
		ERR_MISSING_HEAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_HEAD = 0x408

	/*
		ERR_MISSING_HHEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_HHEA = 0x409

	/*
		ERR_MISSING_HHEA_OR_VHEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_HHEA_OR_VHEA = 0x412

	/*
		ERR_MISSING_HMTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_HMTX = 0x40a

	/*
		ERR_MISSING_HMTX_OR_VMTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_HMTX_OR_VMTX = 0x413

	/*
		ERR_MISSING_LOCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_LOCA = 0x40b

	/*
		ERR_MISSING_MAXP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_MAXP = 0x40c

	/*
		ERR_MISSING_NAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_NAME = 0x40d

	/*
		ERR_MISSING_OS2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_OS2 = 0x40f

	/*
		ERR_MISSING_POST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_POST = 0x40e

	/*
		ERR_MISSING_VHEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_VHEA = 0x410

	/*
		ERR_MISSING_VMTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_MISSING_VMTX = 0x411

	/*
		ERR_NOT_TTC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_NOT_TTC = 0x3f6

	/*
		ERR_NO_GLYPHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_NO_GLYPHS = 0x3f1

	/*
		ERR_PARAMETER0 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER0 = 0x44c

	/*
		ERR_PARAMETER1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER1 = 0x44d

	/*
		ERR_PARAMETER10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER10 = 0x456

	/*
		ERR_PARAMETER11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER11 = 0x457

	/*
		ERR_PARAMETER12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER12 = 0x458

	/*
		ERR_PARAMETER13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER13 = 0x459

	/*
		ERR_PARAMETER14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER14 = 0x45a

	/*
		ERR_PARAMETER15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER15 = 0x45b

	/*
		ERR_PARAMETER16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER16 = 0x45c

	/*
		ERR_PARAMETER2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER2 = 0x44e

	/*
		ERR_PARAMETER3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER3 = 0x44f

	/*
		ERR_PARAMETER4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER4 = 0x450

	/*
		ERR_PARAMETER5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER5 = 0x451

	/*
		ERR_PARAMETER6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER6 = 0x452

	/*
		ERR_PARAMETER7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER7 = 0x453

	/*
		ERR_PARAMETER8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER8 = 0x454

	/*
		ERR_PARAMETER9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_PARAMETER9 = 0x455

	/*
		ERR_READCONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_READCONTROL = 0x3eb

	/*
		ERR_READOUTOFBOUNDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_READOUTOFBOUNDS = 0x3e9

	/*
		ERR_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_VERSION = 0x3f0

	/*
		ERR_WOULD_GROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_WOULD_GROW = 0x3ef

	/*
		ERR_WRITECONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_WRITECONTROL = 0x3ec

	/*
		ERR_WRITEOUTOFBOUNDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERR_WRITEOUTOFBOUNDS = 0x3ea

	/*
		EXTTEXTOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EXTTEXTOUT = 0x200

	/*
		EXT_DEVICE_CAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EXT_DEVICE_CAPS = 0x1003

	/*
		E_ADDFONTFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ADDFONTFAILED = 0x200

	/*
		E_API_NOTIMPL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_API_NOTIMPL = 0x1

	/*
		E_CHARCODECOUNTINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_CHARCODECOUNTINVALID = 0x2

	/*
		E_CHARCODESETINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_CHARCODESETINVALID = 0x3

	/*
		E_CHARSETINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_CHARSETINVALID = 0x15

	/*
		E_COULDNTCREATETEMPFILE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_COULDNTCREATETEMPFILE = 0x201

	/*
		E_DEVICETRUETYPEFONT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_DEVICETRUETYPEFONT = 0x4

	/*
		E_ERRORACCESSINGEXCLUDELIST from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORACCESSINGEXCLUDELIST = 0x112

	/*
		E_ERRORACCESSINGFACENAME from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORACCESSINGFACENAME = 0xd

	/*
		E_ERRORACCESSINGFONTDATA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORACCESSINGFONTDATA = 0xc

	/*
		E_ERRORCOMPRESSINGFONTDATA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORCOMPRESSINGFONTDATA = 0x100

	/*
		E_ERRORCONVERTINGCHARS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORCONVERTINGCHARS = 0x12

	/*
		E_ERRORCREATINGFONTFILE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORCREATINGFONTFILE = 0x10d

	/*
		E_ERRORDECOMPRESSINGFONTDATA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORDECOMPRESSINGFONTDATA = 0x111

	/*
		E_ERROREXPANDINGFONTDATA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERROREXPANDINGFONTDATA = 0x207

	/*
		E_ERRORGETTINGDC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORGETTINGDC = 0x208

	/*
		E_ERRORREADINGFONTDATA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORREADINGFONTDATA = 0x10b

	/*
		E_ERRORUNICODECONVERSION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_ERRORUNICODECONVERSION = 0x11

	/*
		E_EXCEPTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_EXCEPTION = 0x13

	/*
		E_EXCEPTIONINCOMPRESSION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_EXCEPTIONINCOMPRESSION = 0x20a

	/*
		E_EXCEPTIONINDECOMPRESSION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_EXCEPTIONINDECOMPRESSION = 0x209

	/*
		E_FACENAMEINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FACENAMEINVALID = 0x113

	/*
		E_FILE_NOT_FOUND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FILE_NOT_FOUND = 0x17

	/*
		E_FLAGSINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FLAGSINVALID = 0x10c

	/*
		E_FONTALREADYEXISTS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTALREADYEXISTS = 0x10e

	/*
		E_FONTDATAINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTDATAINVALID = 0x102

	/*
		E_FONTFAMILYNAMENOTINFULL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTFAMILYNAMENOTINFULL = 0x11d

	/*
		E_FONTFILECREATEFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTFILECREATEFAILED = 0x203

	/*
		E_FONTFILENOTFOUND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTFILENOTFOUND = 0x205

	/*
		E_FONTINSTALLFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTINSTALLFAILED = 0x110

	/*
		E_FONTNAMEALREADYEXISTS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTNAMEALREADYEXISTS = 0x10f

	/*
		E_FONTNOTEMBEDDABLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTNOTEMBEDDABLE = 0x104

	/*
		E_FONTREFERENCEINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTREFERENCEINVALID = 0x8

	/*
		E_FONTVARIATIONSIMULATED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_FONTVARIATIONSIMULATED = 0x11b

	/*
		E_HDCINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_HDCINVALID = 0x6

	/*
		E_INPUTPARAMINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_INPUTPARAMINVALID = 0x19

	/*
		E_NAMECHANGEFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_NAMECHANGEFAILED = 0x103

	/*
		E_NOFREEMEMORY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_NOFREEMEMORY = 0x7

	/*
		E_NONE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_NONE = 0x0

	/*
		E_NOOS2 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_NOOS2 = 0x109

	/*
		E_NOTATRUETYPEFONT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_NOTATRUETYPEFONT = 0xa

	/*
		E_PBENABLEDINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_PBENABLEDINVALID = 0x118

	/*
		E_PERMISSIONSINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_PERMISSIONSINVALID = 0x117

	/*
		E_PRIVSINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_PRIVSINVALID = 0x105

	/*
		E_PRIVSTATUSINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_PRIVSTATUSINVALID = 0x116

	/*
		E_READFROMSTREAMFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_READFROMSTREAMFAILED = 0x107

	/*
		E_RESERVEDPARAMNOTNULL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_RESERVEDPARAMNOTNULL = 0x14

	/*
		E_RESOURCEFILECREATEFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_RESOURCEFILECREATEFAILED = 0x206

	/*
		E_SAVETOSTREAMFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_SAVETOSTREAMFAILED = 0x108

	/*
		E_STATUSINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_STATUSINVALID = 0x115

	/*
		E_STREAMINVALID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_STREAMINVALID = 0x114

	/*
		E_SUBSETTINGEXCEPTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_SUBSETTINGEXCEPTION = 0x119

	/*
		E_SUBSETTINGFAILED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_SUBSETTINGFAILED = 0x106

	/*
		E_SUBSTRING_TEST_FAIL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_SUBSTRING_TEST_FAIL = 0x11a

	/*
		E_T2NOFREEMEMORY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_T2NOFREEMEMORY = 0x10a

	/*
		E_TTC_INDEX_OUT_OF_RANGE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_TTC_INDEX_OUT_OF_RANGE = 0x18

	/*
		E_WINDOWSAPI from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	E_WINDOWSAPI = 0x204

	/*
		FEATURESETTING_CUSTPAPER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_CUSTPAPER = 0x3

	/*
		FEATURESETTING_MIRROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_MIRROR = 0x4

	/*
		FEATURESETTING_NEGATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_NEGATIVE = 0x5

	/*
		FEATURESETTING_NUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_NUP = 0x0

	/*
		FEATURESETTING_OUTPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_OUTPUT = 0x1

	/*
		FEATURESETTING_PRIVATE_BEGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_PRIVATE_BEGIN = 0x1000

	/*
		FEATURESETTING_PRIVATE_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_PRIVATE_END = 0x1fff

	/*
		FEATURESETTING_PROTOCOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_PROTOCOL = 0x6

	/*
		FEATURESETTING_PSLEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FEATURESETTING_PSLEVEL = 0x2

	/*
		FLI_GLYPHS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLI_GLYPHS = 0x40000

	/*
		FLI_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLI_MASK = 0x103b

	/*
		FLUSHOUTPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLUSHOUTPUT = 0x6

	/*
		FONTMAPPER_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FONTMAPPER_MAX = 0xa

	/*
		FS_ARABIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_ARABIC = 0x40

	/*
		FS_BALTIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_BALTIC = 0x80

	/*
		FS_CHINESESIMP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_CHINESESIMP = 0x40000

	/*
		FS_CHINESETRAD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_CHINESETRAD = 0x100000

	/*
		FS_CYRILLIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_CYRILLIC = 0x4

	/*
		FS_GREEK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_GREEK = 0x8

	/*
		FS_HEBREW from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_HEBREW = 0x20

	/*
		FS_JISJAPAN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_JISJAPAN = 0x20000

	/*
		FS_JOHAB from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_JOHAB = 0x200000

	/*
		FS_LATIN1 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_LATIN1 = 0x1

	/*
		FS_LATIN2 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_LATIN2 = 0x2

	/*
		FS_SYMBOL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_SYMBOL = -2147483648

	/*
		FS_THAI from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_THAI = 0x10000

	/*
		FS_TURKISH from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_TURKISH = 0x10

	/*
		FS_VIETNAMESE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_VIETNAMESE = 0x100

	/*
		FS_WANSUNG from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_WANSUNG = 0x80000

	/*
		GCPCLASS_ARABIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_ARABIC = 0x2

	/*
		GCPCLASS_HEBREW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_HEBREW = 0x2

	/*
		GCPCLASS_LATIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_LATIN = 0x1

	/*
		GCPCLASS_LATINNUMBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_LATINNUMBER = 0x5

	/*
		GCPCLASS_LATINNUMERICSEPARATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_LATINNUMERICSEPARATOR = 0x7

	/*
		GCPCLASS_LATINNUMERICTERMINATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_LATINNUMERICTERMINATOR = 0x6

	/*
		GCPCLASS_LOCALNUMBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_LOCALNUMBER = 0x4

	/*
		GCPCLASS_NEUTRAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_NEUTRAL = 0x3

	/*
		GCPCLASS_NUMERICSEPARATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_NUMERICSEPARATOR = 0x8

	/*
		GCPCLASS_POSTBOUNDLTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_POSTBOUNDLTR = 0x20

	/*
		GCPCLASS_POSTBOUNDRTL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_POSTBOUNDRTL = 0x10

	/*
		GCPCLASS_PREBOUNDLTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_PREBOUNDLTR = 0x80

	/*
		GCPCLASS_PREBOUNDRTL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPCLASS_PREBOUNDRTL = 0x40

	/*
		GCPGLYPH_LINKAFTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPGLYPH_LINKAFTER = 0x4000

	/*
		GCPGLYPH_LINKBEFORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCPGLYPH_LINKBEFORE = 0x8000

	/*
		GCP_DBCS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCP_DBCS = 0x1

	/*
		GCP_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCP_ERROR = 0x8000

	/*
		GCP_JUSTIFYIN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCP_JUSTIFYIN = 0x200000

	/*
		GDICOMMENT_BEGINGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_BEGINGROUP = 0x2

	/*
		GDICOMMENT_ENDGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_ENDGROUP = 0x3

	/*
		GDICOMMENT_IDENTIFIER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_IDENTIFIER = 0x43494447

	/*
		GDICOMMENT_MULTIFORMATS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_MULTIFORMATS = 0x40000004

	/*
		GDICOMMENT_UNICODE_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_UNICODE_END = 0x80

	/*
		GDICOMMENT_UNICODE_STRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_UNICODE_STRING = 0x40

	/*
		GDICOMMENT_WINDOWS_METAFILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDICOMMENT_WINDOWS_METAFILE = 0x80000001

	/*
		GDIPLUS_TS_QUERYVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDIPLUS_TS_QUERYVER = 0x101a

	/*
		GDIPLUS_TS_RECORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDIPLUS_TS_RECORD = 0x101b

	/*
		GDIREGISTERDDRAWPACKETVERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDIREGISTERDDRAWPACKETVERSION = 0x1

	/*
		GDI_ERROR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDI_ERROR = -1

	/*
		GETCOLORTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETCOLORTABLE = 0x5

	/*
		GETDEVICEUNITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETDEVICEUNITS = 0x2a

	/*
		GETEXTENDEDTEXTMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETEXTENDEDTEXTMETRICS = 0x100

	/*
		GETEXTENTTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETEXTENTTABLE = 0x101

	/*
		GETFACENAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETFACENAME = 0x201

	/*
		GETPAIRKERNTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETPAIRKERNTABLE = 0x102

	/*
		GETPENWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETPENWIDTH = 0x10

	/*
		GETPHYSPAGESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETPHYSPAGESIZE = 0xc

	/*
		GETPRINTINGOFFSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETPRINTINGOFFSET = 0xd

	/*
		GETSCALINGFACTOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETSCALINGFACTOR = 0xe

	/*
		GETSETPAPERBINS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETSETPAPERBINS = 0x1d

	/*
		GETSETPAPERMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETSETPAPERMETRICS = 0x23

	/*
		GETSETPRINTORIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETSETPRINTORIENT = 0x1e

	/*
		GETSETSCREENPARAMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETSETSCREENPARAMS = 0xc00

	/*
		GETTECHNOLGY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETTECHNOLGY = 0x14

	/*
		GETTECHNOLOGY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETTECHNOLOGY = 0x14

	/*
		GETTRACKKERNTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETTRACKKERNTABLE = 0x103

	/*
		GETVECTORBRUSHSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETVECTORBRUSHSIZE = 0x1b

	/*
		GETVECTORPENSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETVECTORPENSIZE = 0x1a

	/*
		GET_PS_FEATURESETTING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GET_PS_FEATURESETTING = 0x1019

	/*
		GGI_MARK_NONEXISTING_GLYPHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GGI_MARK_NONEXISTING_GLYPHS = 0x1

	/*
		GM_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GM_LAST = 0x2

	/*
		GRADIENT_FILL_OP_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GRADIENT_FILL_OP_FLAG = 0xff

	/*
		GS_8BIT_INDICES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GS_8BIT_INDICES = 0x1

	/*
		HS_API_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HS_API_MAX = 0xc

	/*
		ILLUMINANT_A from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_A = 0x1

	/*
		ILLUMINANT_B from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_B = 0x2

	/*
		ILLUMINANT_C from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_C = 0x3

	/*
		ILLUMINANT_D50 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_D50 = 0x4

	/*
		ILLUMINANT_D55 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_D55 = 0x5

	/*
		ILLUMINANT_D65 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_D65 = 0x6

	/*
		ILLUMINANT_D75 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_D75 = 0x7

	/*
		ILLUMINANT_DAYLIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_DAYLIGHT = 0x3

	/*
		ILLUMINANT_DEVICE_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_DEVICE_DEFAULT = 0x0

	/*
		ILLUMINANT_F2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_F2 = 0x8

	/*
		ILLUMINANT_FLUORESCENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_FLUORESCENT = 0x8

	/*
		ILLUMINANT_MAX_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_MAX_INDEX = 0x8

	/*
		ILLUMINANT_NTSC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_NTSC = 0x3

	/*
		ILLUMINANT_TUNGSTEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILLUMINANT_TUNGSTEN = 0x1

	/*
		LAYOUT_BTT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LAYOUT_BTT = 0x2

	/*
		LAYOUT_VBH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LAYOUT_VBH = 0x4

	/*
		LCS_GM_ABS_COLORIMETRIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LCS_GM_ABS_COLORIMETRIC = 0x8

	/*
		LCS_GM_BUSINESS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LCS_GM_BUSINESS = 0x1

	/*
		LCS_GM_GRAPHICS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LCS_GM_GRAPHICS = 0x2

	/*
		LCS_GM_IMAGES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LCS_GM_IMAGES = 0x4

	/*
		LC_INTERIORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_INTERIORS = 0x80

	/*
		LC_MARKER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_MARKER = 0x4

	/*
		LC_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_NONE = 0x0

	/*
		LC_POLYLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_POLYLINE = 0x2

	/*
		LC_POLYMARKER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_POLYMARKER = 0x8

	/*
		LC_STYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_STYLED = 0x20

	/*
		LC_WIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_WIDE = 0x10

	/*
		LC_WIDESTYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LC_WIDESTYLED = 0x40

	/*
		LF_FACESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LF_FACESIZE = 0x20

	/*
		LF_FULLFACESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LF_FULLFACESIZE = 0x40

	/*
		LPD_DOUBLEBUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_DOUBLEBUFFER = 0x1

	/*
		LPD_SHARE_ACCUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SHARE_ACCUM = 0x100

	/*
		LPD_SHARE_DEPTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SHARE_DEPTH = 0x40

	/*
		LPD_SHARE_STENCIL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SHARE_STENCIL = 0x80

	/*
		LPD_STEREO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_STEREO = 0x2

	/*
		LPD_SUPPORT_GDI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SUPPORT_GDI = 0x10

	/*
		LPD_SUPPORT_OPENGL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SUPPORT_OPENGL = 0x20

	/*
		LPD_SWAP_COPY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SWAP_COPY = 0x400

	/*
		LPD_SWAP_EXCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_SWAP_EXCHANGE = 0x200

	/*
		LPD_TRANSPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_TRANSPARENT = 0x1000

	/*
		LPD_TYPE_COLORINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_TYPE_COLORINDEX = 0x1

	/*
		LPD_TYPE_RGBA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPD_TYPE_RGBA = 0x0

	/*
		MAXSTRETCHBLTMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXSTRETCHBLTMODE = 0x4

	/*
		METAFILE_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	METAFILE_DRIVER = 0x801

	/*
		META_ANIMATEPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_ANIMATEPALETTE = 0x436

	/*
		META_ARC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_ARC = 0x817

	/*
		META_BITBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_BITBLT = 0x922

	/*
		META_CHORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CHORD = 0x830

	/*
		META_CREATEBRUSHINDIRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CREATEBRUSHINDIRECT = 0x2fc

	/*
		META_CREATEFONTINDIRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CREATEFONTINDIRECT = 0x2fb

	/*
		META_CREATEPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CREATEPALETTE = 0xf7

	/*
		META_CREATEPATTERNBRUSH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CREATEPATTERNBRUSH = 0x1f9

	/*
		META_CREATEPENINDIRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CREATEPENINDIRECT = 0x2fa

	/*
		META_CREATEREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_CREATEREGION = 0x6ff

	/*
		META_DELETEOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_DELETEOBJECT = 0x1f0

	/*
		META_DIBBITBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_DIBBITBLT = 0x940

	/*
		META_DIBCREATEPATTERNBRUSH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_DIBCREATEPATTERNBRUSH = 0x142

	/*
		META_DIBSTRETCHBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_DIBSTRETCHBLT = 0xb41

	/*
		META_ELLIPSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_ELLIPSE = 0x418

	/*
		META_ESCAPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_ESCAPE = 0x626

	/*
		META_EXCLUDECLIPRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_EXCLUDECLIPRECT = 0x415

	/*
		META_EXTFLOODFILL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_EXTFLOODFILL = 0x548

	/*
		META_EXTTEXTOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_EXTTEXTOUT = 0xa32

	/*
		META_FILLREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_FILLREGION = 0x228

	/*
		META_FLOODFILL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_FLOODFILL = 0x419

	/*
		META_FRAMEREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_FRAMEREGION = 0x429

	/*
		META_INTERSECTCLIPRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_INTERSECTCLIPRECT = 0x416

	/*
		META_INVERTREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_INVERTREGION = 0x12a

	/*
		META_LINETO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_LINETO = 0x213

	/*
		META_MOVETO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_MOVETO = 0x214

	/*
		META_OFFSETCLIPRGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_OFFSETCLIPRGN = 0x220

	/*
		META_OFFSETVIEWPORTORG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_OFFSETVIEWPORTORG = 0x211

	/*
		META_OFFSETWINDOWORG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_OFFSETWINDOWORG = 0x20f

	/*
		META_PAINTREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_PAINTREGION = 0x12b

	/*
		META_PATBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_PATBLT = 0x61d

	/*
		META_PIE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_PIE = 0x81a

	/*
		META_POLYGON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_POLYGON = 0x324

	/*
		META_POLYLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_POLYLINE = 0x325

	/*
		META_POLYPOLYGON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_POLYPOLYGON = 0x538

	/*
		META_REALIZEPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_REALIZEPALETTE = 0x35

	/*
		META_RECTANGLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_RECTANGLE = 0x41b

	/*
		META_RESIZEPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_RESIZEPALETTE = 0x139

	/*
		META_RESTOREDC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_RESTOREDC = 0x127

	/*
		META_ROUNDRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_ROUNDRECT = 0x61c

	/*
		META_SAVEDC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SAVEDC = 0x1e

	/*
		META_SCALEVIEWPORTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SCALEVIEWPORTEXT = 0x412

	/*
		META_SCALEWINDOWEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SCALEWINDOWEXT = 0x410

	/*
		META_SELECTCLIPREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SELECTCLIPREGION = 0x12c

	/*
		META_SELECTOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SELECTOBJECT = 0x12d

	/*
		META_SELECTPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SELECTPALETTE = 0x234

	/*
		META_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETBKCOLOR = 0x201

	/*
		META_SETBKMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETBKMODE = 0x102

	/*
		META_SETDIBTODEV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETDIBTODEV = 0xd33

	/*
		META_SETLAYOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETLAYOUT = 0x149

	/*
		META_SETMAPMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETMAPMODE = 0x103

	/*
		META_SETMAPPERFLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETMAPPERFLAGS = 0x231

	/*
		META_SETPALENTRIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETPALENTRIES = 0x37

	/*
		META_SETPIXEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETPIXEL = 0x41f

	/*
		META_SETPOLYFILLMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETPOLYFILLMODE = 0x106

	/*
		META_SETRELABS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETRELABS = 0x105

	/*
		META_SETROP2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETROP2 = 0x104

	/*
		META_SETSTRETCHBLTMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETSTRETCHBLTMODE = 0x107

	/*
		META_SETTEXTALIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETTEXTALIGN = 0x12e

	/*
		META_SETTEXTCHAREXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETTEXTCHAREXTRA = 0x108

	/*
		META_SETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETTEXTCOLOR = 0x209

	/*
		META_SETTEXTJUSTIFICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETTEXTJUSTIFICATION = 0x20a

	/*
		META_SETVIEWPORTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETVIEWPORTEXT = 0x20e

	/*
		META_SETVIEWPORTORG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETVIEWPORTORG = 0x20d

	/*
		META_SETWINDOWEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETWINDOWEXT = 0x20c

	/*
		META_SETWINDOWORG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_SETWINDOWORG = 0x20b

	/*
		META_STRETCHBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_STRETCHBLT = 0xb23

	/*
		META_STRETCHDIB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_STRETCHDIB = 0xf43

	/*
		META_TEXTOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	META_TEXTOUT = 0x521

	/*
		MFCOMMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MFCOMMENT = 0xf

	/*
		MILCORE_TS_QUERYVER_RESULT_FALSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MILCORE_TS_QUERYVER_RESULT_FALSE = 0x0

	/*
		MILCORE_TS_QUERYVER_RESULT_TRUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MILCORE_TS_QUERYVER_RESULT_TRUE = 0x7fffffff

	/*
		MM_MAX_AXES_NAMELEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MM_MAX_AXES_NAMELEN = 0x10

	/*
		MM_MAX_NUMAXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MM_MAX_NUMAXES = 0x10

	/*
		MONO_FONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MONO_FONT = 0x8

	/*
		MOUSETRAILS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSETRAILS = 0x27

	/*
		NEWFRAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEWFRAME = 0x1

	/*
		NEWTRANSPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEWTRANSPARENT = 0x3

	/*
		NEXTBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEXTBAND = 0x3

	/*
		NTM_BOLD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_BOLD = 0x20

	/*
		NTM_DSIG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_DSIG = 0x200000

	/*
		NTM_ITALIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_ITALIC = 0x1

	/*
		NTM_MULTIPLEMASTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_MULTIPLEMASTER = 0x80000

	/*
		NTM_NONNEGATIVE_AC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_NONNEGATIVE_AC = 0x10000

	/*
		NTM_PS_OPENTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_PS_OPENTYPE = 0x20000

	/*
		NTM_REGULAR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_REGULAR = 0x40

	/*
		NTM_TT_OPENTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_TT_OPENTYPE = 0x40000

	/*
		NTM_TYPE1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTM_TYPE1 = 0x100000

	/*
		OPENCHANNEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OPENCHANNEL = 0x100e

	/*
		PANOSE_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PANOSE_COUNT = 0xa

	/*
		PAN_ANY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_ANY = 0x0

	/*
		PAN_ARMSTYLE_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_ARMSTYLE_INDEX = 0x6

	/*
		PAN_CULTURE_LATIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_CULTURE_LATIN = 0x0

	/*
		PAN_FAMILYTYPE_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_FAMILYTYPE_INDEX = 0x0

	/*
		PAN_LETTERFORM_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_LETTERFORM_INDEX = 0x7

	/*
		PAN_NO_FIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_NO_FIT = 0x1

	/*
		PAN_PROPORTION_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_PROPORTION_INDEX = 0x3

	/*
		PAN_SERIFSTYLE_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_SERIFSTYLE_INDEX = 0x1

	/*
		PAN_STROKEVARIATION_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAN_STROKEVARIATION_INDEX = 0x5

	/*
		PASSTHROUGH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PASSTHROUGH = 0x13

	/*
		PC_EXPLICIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_EXPLICIT = 0x2

	/*
		PC_INTERIORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_INTERIORS = 0x80

	/*
		PC_NOCOLLAPSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_NOCOLLAPSE = 0x4

	/*
		PC_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_NONE = 0x0

	/*
		PC_PATHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_PATHS = 0x200

	/*
		PC_POLYGON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_POLYGON = 0x1

	/*
		PC_POLYPOLYGON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_POLYPOLYGON = 0x100

	/*
		PC_RECTANGLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_RECTANGLE = 0x2

	/*
		PC_RESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_RESERVED = 0x1

	/*
		PC_SCANLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_SCANLINE = 0x8

	/*
		PC_STYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_STYLED = 0x20

	/*
		PC_TRAPEZOID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_TRAPEZOID = 0x4

	/*
		PC_WIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_WIDE = 0x10

	/*
		PC_WIDESTYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_WIDESTYLED = 0x40

	/*
		PC_WINDPOLYGON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PC_WINDPOLYGON = 0x4

	/*
		POLYFILL_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POLYFILL_LAST = 0x2

	/*
		POSTSCRIPT_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POSTSCRIPT_DATA = 0x25

	/*
		POSTSCRIPT_IDENTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POSTSCRIPT_IDENTIFY = 0x1015

	/*
		POSTSCRIPT_IGNORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POSTSCRIPT_IGNORE = 0x26

	/*
		POSTSCRIPT_INJECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POSTSCRIPT_INJECTION = 0x1016

	/*
		POSTSCRIPT_PASSTHROUGH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POSTSCRIPT_PASSTHROUGH = 0x1013

	/*
		PRINTRATEUNIT_CPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRINTRATEUNIT_CPS = 0x2

	/*
		PRINTRATEUNIT_IPM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRINTRATEUNIT_IPM = 0x4

	/*
		PRINTRATEUNIT_LPM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRINTRATEUNIT_LPM = 0x3

	/*
		PRINTRATEUNIT_PPM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRINTRATEUNIT_PPM = 0x1

	/*
		PR_JOBSTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PR_JOBSTATUS = 0x0

	/*
		PSIDENT_GDICENTRIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSIDENT_GDICENTRIC = 0x0

	/*
		PSIDENT_PSCENTRIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSIDENT_PSCENTRIC = 0x1

	/*
		PSINJECT_DLFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSINJECT_DLFONT = 0xdddddddd

	/*
		PSPROTOCOL_ASCII from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSPROTOCOL_ASCII = 0x0

	/*
		PSPROTOCOL_BCP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSPROTOCOL_BCP = 0x1

	/*
		PSPROTOCOL_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSPROTOCOL_BINARY = 0x3

	/*
		PSPROTOCOL_TBCP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSPROTOCOL_TBCP = 0x2

	/*
		PT_BEZIERTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PT_BEZIERTO = 0x4

	/*
		PT_CLOSEFIGURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PT_CLOSEFIGURE = 0x1

	/*
		PT_LINETO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PT_LINETO = 0x2

	/*
		PT_MOVETO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PT_MOVETO = 0x6

	/*
		QDI_DIBTOSCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QDI_DIBTOSCREEN = 0x4

	/*
		QDI_GETDIBITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QDI_GETDIBITS = 0x2

	/*
		QDI_SETDIBITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QDI_SETDIBITS = 0x1

	/*
		QDI_STRETCHDIB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QDI_STRETCHDIB = 0x8

	/*
		QUERYDIBSUPPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERYDIBSUPPORT = 0xc01

	/*
		QUERYESCSUPPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERYESCSUPPORT = 0x8

	/*
		QUERYROPSUPPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERYROPSUPPORT = 0x28

	/*
		RASTER_FONTTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RASTER_FONTTYPE = 0x1

	/*
		RC_BANDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_BANDING = 0x2

	/*
		RC_BIGFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_BIGFONT = 0x400

	/*
		RC_BITBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_BITBLT = 0x1

	/*
		RC_BITMAP64 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_BITMAP64 = 0x8

	/*
		RC_DEVBITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_DEVBITS = 0x8000

	/*
		RC_DIBTODEV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_DIBTODEV = 0x200

	/*
		RC_DI_BITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_DI_BITMAP = 0x80

	/*
		RC_FLOODFILL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_FLOODFILL = 0x1000

	/*
		RC_GDI20_OUTPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_GDI20_OUTPUT = 0x10

	/*
		RC_GDI20_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_GDI20_STATE = 0x20

	/*
		RC_OP_DX_OUTPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_OP_DX_OUTPUT = 0x4000

	/*
		RC_PALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_PALETTE = 0x100

	/*
		RC_SAVEBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_SAVEBITMAP = 0x40

	/*
		RC_SCALING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_SCALING = 0x4

	/*
		RC_STRETCHBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_STRETCHBLT = 0x800

	/*
		RC_STRETCHDIB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RC_STRETCHDIB = 0x2000

	/*
		RDH_RECTANGLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RDH_RECTANGLES = 0x1

	/*
		RELATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RELATIVE = 0x2

	/*
		RESTORE_CTM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RESTORE_CTM = 0x1004

	/*
		SAVE_CTM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SAVE_CTM = 0x1005

	/*
		SB_CONST_ALPHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_CONST_ALPHA = 0x1

	/*
		SB_GRAD_RECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GRAD_RECT = 0x10

	/*
		SB_GRAD_TRI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GRAD_TRI = 0x20

	/*
		SB_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_NONE = 0x0

	/*
		SB_PIXEL_ALPHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_PIXEL_ALPHA = 0x2

	/*
		SB_PREMULT_ALPHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_PREMULT_ALPHA = 0x4

	/*
		SC_SCREENSAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_SCREENSAVE = 0xf140

	/*
		SELECTDIB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELECTDIB = 0x29

	/*
		SELECTPAPERSOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELECTPAPERSOURCE = 0x12

	/*
		SETABORTPROC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETABORTPROC = 0x9

	/*
		SETALLJUSTVALUES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETALLJUSTVALUES = 0x303

	/*
		SETCHARSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETCHARSET = 0x304

	/*
		SETCOLORTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETCOLORTABLE = 0x4

	/*
		SETCOPYCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETCOPYCOUNT = 0x11

	/*
		SETDIBSCALING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETDIBSCALING = 0x20

	/*
		SETICMPROFILE_EMBEDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETICMPROFILE_EMBEDED = 0x1

	/*
		SETKERNTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETKERNTRACK = 0x302

	/*
		SETLINECAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETLINECAP = 0x15

	/*
		SETLINEJOIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETLINEJOIN = 0x16

	/*
		SETMITERLIMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETMITERLIMIT = 0x17

	/*
		SET_ARC_DIRECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_ARC_DIRECTION = 0x1006

	/*
		SET_BACKGROUND_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_BACKGROUND_COLOR = 0x1007

	/*
		SET_BOUNDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_BOUNDS = 0x100d

	/*
		SET_CLIP_BOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_CLIP_BOX = 0x100c

	/*
		SET_MIRROR_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_MIRROR_MODE = 0x100e

	/*
		SET_POLY_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_POLY_MODE = 0x1008

	/*
		SET_SCREEN_ANGLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_SCREEN_ANGLE = 0x1009

	/*
		SET_SPREAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SET_SPREAD = 0x100a

	/*
		SPCLPASSTHROUGH2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPCLPASSTHROUGH2 = 0x11d8

	/*
		SP_APPABORT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_APPABORT = -2

	/*
		SP_ERROR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_ERROR = -1

	/*
		SP_NOTREPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_NOTREPORTED = 0x4000

	/*
		SP_OUTOFDISK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_OUTOFDISK = -4

	/*
		SP_OUTOFMEMORY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_OUTOFMEMORY = -5

	/*
		SP_USERABORT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_USERABORT = -3

	/*
		STARTDOC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STARTDOC = 0xa

	/*
		STOCK_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STOCK_LAST = 0x13

	/*
		STRETCHBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRETCHBLT = 0x800

	/*
		SYSPAL_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYSPAL_ERROR = 0x0

	/*
		SYSRGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYSRGN = 0x4

	/*
		TC_CP_STROKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_CP_STROKE = 0x4

	/*
		TC_CR_90 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_CR_90 = 0x8

	/*
		TC_CR_ANY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_CR_ANY = 0x10

	/*
		TC_EA_DOUBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_EA_DOUBLE = 0x200

	/*
		TC_IA_ABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_IA_ABLE = 0x400

	/*
		TC_OP_CHARACTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_OP_CHARACTER = 0x1

	/*
		TC_OP_STROKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_OP_STROKE = 0x2

	/*
		TC_RA_ABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_RA_ABLE = 0x2000

	/*
		TC_RESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_RESERVED = 0x8000

	/*
		TC_SA_CONTIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SA_CONTIN = 0x100

	/*
		TC_SA_DOUBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SA_DOUBLE = 0x40

	/*
		TC_SA_INTEGER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SA_INTEGER = 0x80

	/*
		TC_SCROLLBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SCROLLBLT = 0x10000

	/*
		TC_SF_X_YINDEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SF_X_YINDEP = 0x20

	/*
		TC_SO_ABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SO_ABLE = 0x1000

	/*
		TC_UA_ABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_UA_ABLE = 0x800

	/*
		TC_VA_ABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_VA_ABLE = 0x4000

	/*
		TRANSFORM_CTM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSFORM_CTM = 0x100b

	/*
		TRUETYPE_FONTTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRUETYPE_FONTTYPE = 0x4

	/*
		TTDELETE_DONTREMOVEFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTDELETE_DONTREMOVEFONT = 0x1

	/*
		TTEMBED_EUDCEMBEDDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTEMBED_EUDCEMBEDDED = 0x2

	/*
		TTEMBED_FAILIFVARIATIONSIMULATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTEMBED_FAILIFVARIATIONSIMULATED = 0x10

	/*
		TTEMBED_SUBSETCANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTEMBED_SUBSETCANCEL = 0x4

	/*
		TTEMBED_VARIATIONSIMULATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTEMBED_VARIATIONSIMULATED = 0x1

	/*
		TTEMBED_WEBOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTEMBED_WEBOBJECT = 0x80

	/*
		TTEMBED_XORENCRYPTDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTEMBED_XORENCRYPTDATA = 0x10000000

	/*
		TTFCFP_APPLE_PLATFORMID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_APPLE_PLATFORMID = 0x1

	/*
		TTFCFP_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_DELTA = 0x2

	/*
		TTFCFP_DONT_CARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_DONT_CARE = 0xffff

	/*
		TTFCFP_FLAGS_COMPRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_FLAGS_COMPRESS = 0x2

	/*
		TTFCFP_FLAGS_GLYPHLIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_FLAGS_GLYPHLIST = 0x8

	/*
		TTFCFP_FLAGS_SUBSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_FLAGS_SUBSET = 0x1

	/*
		TTFCFP_FLAGS_TTC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_FLAGS_TTC = 0x4

	/*
		TTFCFP_LANG_KEEP_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_LANG_KEEP_ALL = 0x0

	/*
		TTFCFP_MS_PLATFORMID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_MS_PLATFORMID = 0x3

	/*
		TTFCFP_SUBSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_SUBSET = 0x0

	/*
		TTFCFP_SUBSET1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFCFP_SUBSET1 = 0x1

	/*
		TTFMFP_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFMFP_DELTA = 0x2

	/*
		TTFMFP_SUBSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFMFP_SUBSET = 0x0

	/*
		TTFMFP_SUBSET1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTFMFP_SUBSET1 = 0x1

	/*
		TTLOAD_EUDC_OVERWRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTLOAD_EUDC_OVERWRITE = 0x2

	/*
		TTLOAD_EUDC_SET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTLOAD_EUDC_SET = 0x4

	/*
		TTLOAD_PRIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTLOAD_PRIVATE = 0x1

	/*
		TT_AVAILABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TT_AVAILABLE = 0x1

	/*
		TT_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TT_ENABLED = 0x2

	/*
		TT_POLYGON_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TT_POLYGON_TYPE = 0x18

	/*
		TT_PRIM_CSPLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TT_PRIM_CSPLINE = 0x3

	/*
		TT_PRIM_LINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TT_PRIM_LINE = 0x1

	/*
		TT_PRIM_QSPLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TT_PRIM_QSPLINE = 0x2

	/*
		WGL_FONT_LINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_FONT_LINES = 0x0

	/*
		WGL_FONT_POLYGONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_FONT_POLYGONS = 0x1

	/*
		WGL_SWAPMULTIPLE_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAPMULTIPLE_MAX = 0x10

	/*
		WGL_SWAP_MAIN_PLANE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_MAIN_PLANE = 0x1

	/*
		WGL_SWAP_OVERLAY1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY1 = 0x2

	/*
		WGL_SWAP_OVERLAY10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY10 = 0x400

	/*
		WGL_SWAP_OVERLAY11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY11 = 0x800

	/*
		WGL_SWAP_OVERLAY12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY12 = 0x1000

	/*
		WGL_SWAP_OVERLAY13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY13 = 0x2000

	/*
		WGL_SWAP_OVERLAY14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY14 = 0x4000

	/*
		WGL_SWAP_OVERLAY15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY15 = 0x8000

	/*
		WGL_SWAP_OVERLAY2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY2 = 0x4

	/*
		WGL_SWAP_OVERLAY3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY3 = 0x8

	/*
		WGL_SWAP_OVERLAY4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY4 = 0x10

	/*
		WGL_SWAP_OVERLAY5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY5 = 0x20

	/*
		WGL_SWAP_OVERLAY6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY6 = 0x40

	/*
		WGL_SWAP_OVERLAY7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY7 = 0x80

	/*
		WGL_SWAP_OVERLAY8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY8 = 0x100

	/*
		WGL_SWAP_OVERLAY9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_OVERLAY9 = 0x200

	/*
		WGL_SWAP_UNDERLAY1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY1 = 0x10000

	/*
		WGL_SWAP_UNDERLAY10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY10 = 0x2000000

	/*
		WGL_SWAP_UNDERLAY11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY11 = 0x4000000

	/*
		WGL_SWAP_UNDERLAY12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY12 = 0x8000000

	/*
		WGL_SWAP_UNDERLAY13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY13 = 0x10000000

	/*
		WGL_SWAP_UNDERLAY14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY14 = 0x20000000

	/*
		WGL_SWAP_UNDERLAY15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY15 = 0x40000000

	/*
		WGL_SWAP_UNDERLAY2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY2 = 0x20000

	/*
		WGL_SWAP_UNDERLAY3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY3 = 0x40000

	/*
		WGL_SWAP_UNDERLAY4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY4 = 0x80000

	/*
		WGL_SWAP_UNDERLAY5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY5 = 0x100000

	/*
		WGL_SWAP_UNDERLAY6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY6 = 0x200000

	/*
		WGL_SWAP_UNDERLAY7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY7 = 0x400000

	/*
		WGL_SWAP_UNDERLAY8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY8 = 0x800000

	/*
		WGL_SWAP_UNDERLAY9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WGL_SWAP_UNDERLAY9 = 0x1000000
)

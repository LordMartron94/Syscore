//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_system_kernel

const (
	/*
		MAXUCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXUCHAR = 0xff

	/*
		MAXULONG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXULONG = 0xffffffff

	/*
		MAXUSHORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXUSHORT = 0xffff

	/*
		NULL64 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NULL64 = 0x0

	/*
		OBJ_CASE_INSENSITIVE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_CASE_INSENSITIVE = 0x40

	/*
		OBJ_DONT_REPARSE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_DONT_REPARSE = 0x1000

	/*
		OBJ_EXCLUSIVE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_EXCLUSIVE = 0x20

	/*
		OBJ_FORCE_ACCESS_CHECK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_FORCE_ACCESS_CHECK = 0x400

	/*
		OBJ_HANDLE_TAGBITS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_HANDLE_TAGBITS = 0x3

	/*
		OBJ_IGNORE_IMPERSONATED_DEVICEMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_IGNORE_IMPERSONATED_DEVICEMAP = 0x800

	/*
		OBJ_INHERIT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_INHERIT = 0x2

	/*
		OBJ_KERNEL_HANDLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_KERNEL_HANDLE = 0x200

	/*
		OBJ_OPENIF from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_OPENIF = 0x80

	/*
		OBJ_OPENLINK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_OPENLINK = 0x100

	/*
		OBJ_PERMANENT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_PERMANENT = 0x10

	/*
		OBJ_VALID_ATTRIBUTES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBJ_VALID_ATTRIBUTES = 0x1ff2

	/*
		RTL_BALANCED_NODE_RESERVED_PARENT_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_BALANCED_NODE_RESERVED_PARENT_MASK = 0x3
)

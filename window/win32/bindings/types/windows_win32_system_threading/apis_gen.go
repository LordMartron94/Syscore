//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-05-27 08:35:22 CEST

package windows_win32_system_threading

const (
	/*
		ALL_PROCESSOR_GROUPS from Windows.Win32 metadata (originally typed uint16). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALL_PROCESSOR_GROUPS = 0xffff

	/*
		CONDITION_VARIABLE_LOCKMODE_SHARED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONDITION_VARIABLE_LOCKMODE_SHARED = 0x1

	/*
		CREATE_MUTEX_INITIAL_OWNER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATE_MUTEX_INITIAL_OWNER = 0x1

	/*
		CREATE_WAITABLE_TIMER_HIGH_RESOLUTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATE_WAITABLE_TIMER_HIGH_RESOLUTION = 0x2

	/*
		CREATE_WAITABLE_TIMER_MANUAL_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATE_WAITABLE_TIMER_MANUAL_RESET = 0x1

	/*
		FLS_OUT_OF_INDEXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLS_OUT_OF_INDEXES = 0xffffffff

	/*
		INFINITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFINITE = 0xffffffff

	/*
		INIT_ONCE_ASYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INIT_ONCE_ASYNC = 0x2

	/*
		INIT_ONCE_CHECK_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INIT_ONCE_CHECK_ONLY = 0x1

	/*
		INIT_ONCE_CTX_RESERVED_BITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INIT_ONCE_CTX_RESERVED_BITS = 0x2

	/*
		INIT_ONCE_INIT_FAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INIT_ONCE_INIT_FAILED = 0x4

	/*
		PME_CURRENT_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PME_CURRENT_VERSION = 0x1

	/*
		PME_FAILFAST_ON_COMMIT_FAIL_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PME_FAILFAST_ON_COMMIT_FAIL_DISABLE = 0x0

	/*
		PME_FAILFAST_ON_COMMIT_FAIL_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PME_FAILFAST_ON_COMMIT_FAIL_ENABLE = 0x1

	/*
		PRIVATE_NAMESPACE_FLAG_DESTROY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIVATE_NAMESPACE_FLAG_DESTROY = 0x1

	/*
		PROCESS_LEAP_SECOND_INFO_FLAG_ENABLE_SIXTY_SECOND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_LEAP_SECOND_INFO_FLAG_ENABLE_SIXTY_SECOND = 0x1

	/*
		PROCESS_LEAP_SECOND_INFO_VALID_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_LEAP_SECOND_INFO_VALID_FLAGS = 0x1

	/*
		PROCESS_POWER_THROTTLING_CURRENT_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_POWER_THROTTLING_CURRENT_VERSION = 0x1

	/*
		PROCESS_POWER_THROTTLING_EXECUTION_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_POWER_THROTTLING_EXECUTION_SPEED = 0x1

	/*
		PROCESS_POWER_THROTTLING_IGNORE_TIMER_RESOLUTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_POWER_THROTTLING_IGNORE_TIMER_RESOLUTION = 0x4

	/*
		PROC_THREAD_ATTRIBUTE_ALL_APPLICATION_PACKAGES_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_ALL_APPLICATION_PACKAGES_POLICY = 0x2000f

	/*
		PROC_THREAD_ATTRIBUTE_CHILD_PROCESS_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_CHILD_PROCESS_POLICY = 0x2000e

	/*
		PROC_THREAD_ATTRIBUTE_COMPONENT_FILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_COMPONENT_FILTER = 0x2001a

	/*
		PROC_THREAD_ATTRIBUTE_DESKTOP_APP_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_DESKTOP_APP_POLICY = 0x20012

	/*
		PROC_THREAD_ATTRIBUTE_ENABLE_OPTIONAL_XSTATE_FEATURES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_ENABLE_OPTIONAL_XSTATE_FEATURES = 0x3001b

	/*
		PROC_THREAD_ATTRIBUTE_GROUP_AFFINITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_GROUP_AFFINITY = 0x30003

	/*
		PROC_THREAD_ATTRIBUTE_HANDLE_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_HANDLE_LIST = 0x20002

	/*
		PROC_THREAD_ATTRIBUTE_IDEAL_PROCESSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_IDEAL_PROCESSOR = 0x30005

	/*
		PROC_THREAD_ATTRIBUTE_JOB_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_JOB_LIST = 0x2000d

	/*
		PROC_THREAD_ATTRIBUTE_MACHINE_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_MACHINE_TYPE = 0x20019

	/*
		PROC_THREAD_ATTRIBUTE_MITIGATION_AUDIT_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_MITIGATION_AUDIT_POLICY = 0x20018

	/*
		PROC_THREAD_ATTRIBUTE_MITIGATION_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_MITIGATION_POLICY = 0x20007

	/*
		PROC_THREAD_ATTRIBUTE_PARENT_PROCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_PARENT_PROCESS = 0x20000

	/*
		PROC_THREAD_ATTRIBUTE_PREFERRED_NODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_PREFERRED_NODE = 0x20004

	/*
		PROC_THREAD_ATTRIBUTE_PROTECTION_LEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_PROTECTION_LEVEL = 0x2000b

	/*
		PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE = 0x20016

	/*
		PROC_THREAD_ATTRIBUTE_REPLACE_VALUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_REPLACE_VALUE = 0x1

	/*
		PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES = 0x20009

	/*
		PROC_THREAD_ATTRIBUTE_UMS_THREAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_UMS_THREAD = 0x30006

	/*
		PROC_THREAD_ATTRIBUTE_WIN32K_FILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_WIN32K_FILTER = 0x20010

	/*
		RTL_CRITICAL_SECTION_ALL_FLAG_BITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_ALL_FLAG_BITS = 0xff000000

	/*
		RTL_CRITICAL_SECTION_DEBUG_FLAG_STATIC_INIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_DEBUG_FLAG_STATIC_INIT = 0x1

	/*
		RTL_CRITICAL_SECTION_FLAG_DYNAMIC_SPIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_FLAG_DYNAMIC_SPIN = 0x2000000

	/*
		RTL_CRITICAL_SECTION_FLAG_FORCE_DEBUG_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_FLAG_FORCE_DEBUG_INFO = 0x10000000

	/*
		RTL_CRITICAL_SECTION_FLAG_NO_DEBUG_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_FLAG_NO_DEBUG_INFO = 0x1000000

	/*
		RTL_CRITICAL_SECTION_FLAG_RESOURCE_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_FLAG_RESOURCE_TYPE = 0x8000000

	/*
		RTL_CRITICAL_SECTION_FLAG_STATIC_INIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTL_CRITICAL_SECTION_FLAG_STATIC_INIT = 0x4000000

	/*
		SYNCHRONIZATION_BARRIER_FLAGS_BLOCK_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYNCHRONIZATION_BARRIER_FLAGS_BLOCK_ONLY = 0x2

	/*
		SYNCHRONIZATION_BARRIER_FLAGS_NO_DELETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYNCHRONIZATION_BARRIER_FLAGS_NO_DELETE = 0x4

	/*
		SYNCHRONIZATION_BARRIER_FLAGS_SPIN_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYNCHRONIZATION_BARRIER_FLAGS_SPIN_ONLY = 0x1

	/*
		THREAD_POWER_THROTTLING_CURRENT_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THREAD_POWER_THROTTLING_CURRENT_VERSION = 0x1

	/*
		THREAD_POWER_THROTTLING_EXECUTION_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THREAD_POWER_THROTTLING_EXECUTION_SPEED = 0x1

	/*
		THREAD_POWER_THROTTLING_VALID_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THREAD_POWER_THROTTLING_VALID_FLAGS = 0x1

	/*
		TLS_OUT_OF_INDEXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TLS_OUT_OF_INDEXES = 0xffffffff
)

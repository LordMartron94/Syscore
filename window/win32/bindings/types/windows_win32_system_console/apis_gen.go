//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-05-27 08:35:22 CEST

package windows_win32_system_console

const (
	/*
		ALTNUMPAD_BIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALTNUMPAD_BIT = 0x4000000

	/*
		ATTACH_PARENT_PROCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ATTACH_PARENT_PROCESS = 0xffffffff

	/*
		CAPSLOCK_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CAPSLOCK_ON = 0x80

	/*
		CONSOLE_FULLSCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_FULLSCREEN = 0x1

	/*
		CONSOLE_FULLSCREEN_HARDWARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_FULLSCREEN_HARDWARE = 0x2

	/*
		CONSOLE_FULLSCREEN_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_FULLSCREEN_MODE = 0x1

	/*
		CONSOLE_MOUSE_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_MOUSE_DOWN = 0x8

	/*
		CONSOLE_MOUSE_SELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_MOUSE_SELECTION = 0x4

	/*
		CONSOLE_NO_SELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_NO_SELECTION = 0x0

	/*
		CONSOLE_SELECTION_IN_PROGRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_SELECTION_IN_PROGRESS = 0x1

	/*
		CONSOLE_SELECTION_NOT_EMPTY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_SELECTION_NOT_EMPTY = 0x2

	/*
		CONSOLE_TEXTMODE_BUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_TEXTMODE_BUFFER = 0x1

	/*
		CONSOLE_WINDOWED_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_WINDOWED_MODE = 0x2

	/*
		CTRL_BREAK_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTRL_BREAK_EVENT = 0x1

	/*
		CTRL_CLOSE_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTRL_CLOSE_EVENT = 0x2

	/*
		CTRL_C_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTRL_C_EVENT = 0x0

	/*
		CTRL_LOGOFF_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTRL_LOGOFF_EVENT = 0x5

	/*
		CTRL_SHUTDOWN_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTRL_SHUTDOWN_EVENT = 0x6

	/*
		DOUBLE_CLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOUBLE_CLICK = 0x2

	/*
		ENHANCED_KEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENHANCED_KEY = 0x100

	/*
		FOCUS_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FOCUS_EVENT = 0x10

	/*
		FROM_LEFT_1ST_BUTTON_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FROM_LEFT_1ST_BUTTON_PRESSED = 0x1

	/*
		FROM_LEFT_2ND_BUTTON_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FROM_LEFT_2ND_BUTTON_PRESSED = 0x4

	/*
		FROM_LEFT_3RD_BUTTON_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FROM_LEFT_3RD_BUTTON_PRESSED = 0x8

	/*
		FROM_LEFT_4TH_BUTTON_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FROM_LEFT_4TH_BUTTON_PRESSED = 0x10

	/*
		HISTORY_NO_DUP_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HISTORY_NO_DUP_FLAG = 0x1

	/*
		KEY_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KEY_EVENT = 0x1

	/*
		LEFT_ALT_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LEFT_ALT_PRESSED = 0x2

	/*
		LEFT_CTRL_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LEFT_CTRL_PRESSED = 0x8

	/*
		MENU_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MENU_EVENT = 0x8

	/*
		MOUSE_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSE_EVENT = 0x2

	/*
		MOUSE_HWHEELED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSE_HWHEELED = 0x8

	/*
		MOUSE_MOVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSE_MOVED = 0x1

	/*
		MOUSE_WHEELED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSE_WHEELED = 0x4

	/*
		NLS_ALPHANUMERIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_ALPHANUMERIC = 0x0

	/*
		NLS_DBCSCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_DBCSCHAR = 0x10000

	/*
		NLS_HIRAGANA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_HIRAGANA = 0x40000

	/*
		NLS_IME_CONVERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_IME_CONVERSION = 0x800000

	/*
		NLS_IME_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_IME_DISABLE = 0x20000000

	/*
		NLS_KATAKANA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_KATAKANA = 0x20000

	/*
		NLS_ROMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NLS_ROMAN = 0x400000

	/*
		NUMLOCK_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NUMLOCK_ON = 0x20

	/*
		PSEUDOCONSOLE_INHERIT_CURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSEUDOCONSOLE_INHERIT_CURSOR = 0x1

	/*
		RIGHTMOST_BUTTON_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIGHTMOST_BUTTON_PRESSED = 0x2

	/*
		RIGHT_ALT_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIGHT_ALT_PRESSED = 0x1

	/*
		RIGHT_CTRL_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIGHT_CTRL_PRESSED = 0x4

	/*
		SCROLLLOCK_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCROLLLOCK_ON = 0x40

	/*
		SHIFT_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHIFT_PRESSED = 0x10

	/*
		WINDOW_BUFFER_SIZE_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINDOW_BUFFER_SIZE_EVENT = 0x4
)

//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_storage_packaging_appx

const (
	/*
		APPLICATION_USER_MODEL_ID_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	APPLICATION_USER_MODEL_ID_MAX_LENGTH = 0x82

	/*
		APPLICATION_USER_MODEL_ID_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	APPLICATION_USER_MODEL_ID_MIN_LENGTH = 0x14

	/*
		PACKAGE_APPLICATIONS_MAX_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_APPLICATIONS_MAX_COUNT = 0x64

	/*
		PACKAGE_APPLICATIONS_MIN_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_APPLICATIONS_MIN_COUNT = 0x0

	/*
		PACKAGE_ARCHITECTURE_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_ARCHITECTURE_MAX_LENGTH = 0x7

	/*
		PACKAGE_ARCHITECTURE_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_ARCHITECTURE_MIN_LENGTH = 0x3

	/*
		PACKAGE_DEPENDENCY_RANK_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_DEPENDENCY_RANK_DEFAULT = 0x0

	/*
		PACKAGE_FAMILY_MAX_RESOURCE_PACKAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FAMILY_MAX_RESOURCE_PACKAGES = 0x200

	/*
		PACKAGE_FAMILY_MIN_RESOURCE_PACKAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FAMILY_MIN_RESOURCE_PACKAGES = 0x0

	/*
		PACKAGE_FAMILY_NAME_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FAMILY_NAME_MAX_LENGTH = 0x40

	/*
		PACKAGE_FAMILY_NAME_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FAMILY_NAME_MIN_LENGTH = 0x11

	/*
		PACKAGE_FILTER_ALL_LOADED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_ALL_LOADED = 0x0

	/*
		PACKAGE_FILTER_BUNDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_BUNDLE = 0x80

	/*
		PACKAGE_FILTER_DIRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_DIRECT = 0x20

	/*
		PACKAGE_FILTER_DYNAMIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_DYNAMIC = 0x100000

	/*
		PACKAGE_FILTER_HEAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_HEAD = 0x10

	/*
		PACKAGE_FILTER_HOSTRUNTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_HOSTRUNTIME = 0x200000

	/*
		PACKAGE_FILTER_IS_IN_RELATED_SET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_IS_IN_RELATED_SET = 0x40000

	/*
		PACKAGE_FILTER_OPTIONAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_OPTIONAL = 0x20000

	/*
		PACKAGE_FILTER_RESOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_RESOURCE = 0x40

	/*
		PACKAGE_FILTER_STATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FILTER_STATIC = 0x80000

	/*
		PACKAGE_FULL_NAME_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FULL_NAME_MAX_LENGTH = 0x7f

	/*
		PACKAGE_FULL_NAME_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_FULL_NAME_MIN_LENGTH = 0x1e

	/*
		PACKAGE_GRAPH_MAX_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_GRAPH_MAX_SIZE = 0x281

	/*
		PACKAGE_GRAPH_MIN_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_GRAPH_MIN_SIZE = 0x1

	/*
		PACKAGE_INFORMATION_BASIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_INFORMATION_BASIC = 0x0

	/*
		PACKAGE_INFORMATION_FULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_INFORMATION_FULL = 0x100

	/*
		PACKAGE_MAX_DEPENDENCIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_MAX_DEPENDENCIES = 0x80

	/*
		PACKAGE_MIN_DEPENDENCIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_MIN_DEPENDENCIES = 0x0

	/*
		PACKAGE_NAME_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_NAME_MAX_LENGTH = 0x32

	/*
		PACKAGE_NAME_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_NAME_MIN_LENGTH = 0x3

	/*
		PACKAGE_PROPERTY_BUNDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_BUNDLE = 0x4

	/*
		PACKAGE_PROPERTY_DEVELOPMENT_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_DEVELOPMENT_MODE = 0x10000

	/*
		PACKAGE_PROPERTY_DYNAMIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_DYNAMIC = 0x100000

	/*
		PACKAGE_PROPERTY_FRAMEWORK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_FRAMEWORK = 0x1

	/*
		PACKAGE_PROPERTY_HOSTRUNTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_HOSTRUNTIME = 0x200000

	/*
		PACKAGE_PROPERTY_IS_IN_RELATED_SET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_IS_IN_RELATED_SET = 0x40000

	/*
		PACKAGE_PROPERTY_OPTIONAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_OPTIONAL = 0x8

	/*
		PACKAGE_PROPERTY_RESOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_RESOURCE = 0x2

	/*
		PACKAGE_PROPERTY_STATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PROPERTY_STATIC = 0x80000

	/*
		PACKAGE_PUBLISHERID_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PUBLISHERID_MAX_LENGTH = 0xd

	/*
		PACKAGE_PUBLISHERID_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PUBLISHERID_MIN_LENGTH = 0xd

	/*
		PACKAGE_PUBLISHER_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PUBLISHER_MAX_LENGTH = 0x2000

	/*
		PACKAGE_PUBLISHER_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_PUBLISHER_MIN_LENGTH = 0x3

	/*
		PACKAGE_RELATIVE_APPLICATION_ID_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_RELATIVE_APPLICATION_ID_MAX_LENGTH = 0x41

	/*
		PACKAGE_RELATIVE_APPLICATION_ID_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_RELATIVE_APPLICATION_ID_MIN_LENGTH = 0x2

	/*
		PACKAGE_RESOURCEID_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_RESOURCEID_MAX_LENGTH = 0x1e

	/*
		PACKAGE_RESOURCEID_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_RESOURCEID_MIN_LENGTH = 0x0

	/*
		PACKAGE_VERSION_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_VERSION_MAX_LENGTH = 0x17

	/*
		PACKAGE_VERSION_MIN_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PACKAGE_VERSION_MIN_LENGTH = 0x7
)

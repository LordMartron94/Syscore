//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-05-27 08:35:22 CEST

package windows_win32_devices_communication

const (
	/*
		MAXLENGTH_NAI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXLENGTH_NAI = 0x48

	/*
		MAXLENGTH_UICCDATASTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXLENGTH_UICCDATASTORE = 0xa

	/*
		MDM_ANALOG_RLP_OFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_ANALOG_RLP_OFF = 0x1

	/*
		MDM_ANALOG_RLP_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_ANALOG_RLP_ON = 0x0

	/*
		MDM_ANALOG_V34 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_ANALOG_V34 = 0x2

	/*
		MDM_AUTO_ML_2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_AUTO_ML_2 = 0x2

	/*
		MDM_AUTO_ML_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_AUTO_ML_DEFAULT = 0x0

	/*
		MDM_AUTO_ML_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_AUTO_ML_NONE = 0x1

	/*
		MDM_AUTO_SPEED_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_AUTO_SPEED_DEFAULT = 0x0

	/*
		MDM_BEARERMODE_ANALOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_BEARERMODE_ANALOG = 0x0

	/*
		MDM_BEARERMODE_GSM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_BEARERMODE_GSM = 0x2

	/*
		MDM_BEARERMODE_ISDN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_BEARERMODE_ISDN = 0x1

	/*
		MDM_BLIND_DIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_BLIND_DIAL = 0x200

	/*
		MDM_CCITT_OVERRIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_CCITT_OVERRIDE = 0x40

	/*
		MDM_CELLULAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_CELLULAR = 0x8

	/*
		MDM_COMPRESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_COMPRESSION = 0x1

	/*
		MDM_DIAGNOSTICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_DIAGNOSTICS = 0x800

	/*
		MDM_ERROR_CONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_ERROR_CONTROL = 0x2

	/*
		MDM_FLOWCONTROL_HARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_FLOWCONTROL_HARD = 0x10

	/*
		MDM_FLOWCONTROL_SOFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_FLOWCONTROL_SOFT = 0x20

	/*
		MDM_FORCED_EC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_FORCED_EC = 0x4

	/*
		MDM_HDLCPPP_AUTH_CHAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_AUTH_CHAP = 0x3

	/*
		MDM_HDLCPPP_AUTH_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_AUTH_DEFAULT = 0x0

	/*
		MDM_HDLCPPP_AUTH_MSCHAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_AUTH_MSCHAP = 0x4

	/*
		MDM_HDLCPPP_AUTH_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_AUTH_NONE = 0x1

	/*
		MDM_HDLCPPP_AUTH_PAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_AUTH_PAP = 0x2

	/*
		MDM_HDLCPPP_ML_2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_ML_2 = 0x2

	/*
		MDM_HDLCPPP_ML_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_ML_DEFAULT = 0x0

	/*
		MDM_HDLCPPP_ML_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_ML_NONE = 0x1

	/*
		MDM_HDLCPPP_SPEED_56K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_SPEED_56K = 0x2

	/*
		MDM_HDLCPPP_SPEED_64K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_SPEED_64K = 0x1

	/*
		MDM_HDLCPPP_SPEED_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_HDLCPPP_SPEED_DEFAULT = 0x0

	/*
		MDM_MASK_AUTO_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_AUTO_SPEED = 0x7

	/*
		MDM_MASK_BEARERMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_BEARERMODE = 0xf000

	/*
		MDM_MASK_HDLCPPP_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_HDLCPPP_SPEED = 0x7

	/*
		MDM_MASK_PROTOCOLDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_PROTOCOLDATA = 0xff00000

	/*
		MDM_MASK_PROTOCOLID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_PROTOCOLID = 0xf0000

	/*
		MDM_MASK_V110_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_V110_SPEED = 0xf

	/*
		MDM_MASK_V120_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_V120_SPEED = 0x7

	/*
		MDM_MASK_X75_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_MASK_X75_DATA = 0x7

	/*
		MDM_PIAFS_INCOMING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PIAFS_INCOMING = 0x0

	/*
		MDM_PIAFS_OUTGOING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PIAFS_OUTGOING = 0x1

	/*
		MDM_PROTOCOLID_ANALOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_ANALOG = 0x7

	/*
		MDM_PROTOCOLID_AUTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_AUTO = 0x6

	/*
		MDM_PROTOCOLID_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_DEFAULT = 0x0

	/*
		MDM_PROTOCOLID_GPRS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_GPRS = 0x8

	/*
		MDM_PROTOCOLID_HDLCPPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_HDLCPPP = 0x1

	/*
		MDM_PROTOCOLID_PIAFS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_PIAFS = 0x9

	/*
		MDM_PROTOCOLID_V110 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_V110 = 0x4

	/*
		MDM_PROTOCOLID_V120 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_V120 = 0x5

	/*
		MDM_PROTOCOLID_V128 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_V128 = 0x2

	/*
		MDM_PROTOCOLID_X75 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_PROTOCOLID_X75 = 0x3

	/*
		MDM_SHIFT_AUTO_ML from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_AUTO_ML = 0x6

	/*
		MDM_SHIFT_AUTO_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_AUTO_SPEED = 0x0

	/*
		MDM_SHIFT_BEARERMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_BEARERMODE = 0xc

	/*
		MDM_SHIFT_EXTENDEDINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_EXTENDEDINFO = 0xc

	/*
		MDM_SHIFT_HDLCPPP_AUTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_HDLCPPP_AUTH = 0x3

	/*
		MDM_SHIFT_HDLCPPP_ML from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_HDLCPPP_ML = 0x6

	/*
		MDM_SHIFT_HDLCPPP_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_HDLCPPP_SPEED = 0x0

	/*
		MDM_SHIFT_PROTOCOLDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_PROTOCOLDATA = 0x14

	/*
		MDM_SHIFT_PROTOCOLID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_PROTOCOLID = 0x10

	/*
		MDM_SHIFT_PROTOCOLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_PROTOCOLINFO = 0x10

	/*
		MDM_SHIFT_V110_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_V110_SPEED = 0x0

	/*
		MDM_SHIFT_V120_ML from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_V120_ML = 0x6

	/*
		MDM_SHIFT_V120_SPEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_V120_SPEED = 0x0

	/*
		MDM_SHIFT_X75_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SHIFT_X75_DATA = 0x0

	/*
		MDM_SPEED_ADJUST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_SPEED_ADJUST = 0x80

	/*
		MDM_TONE_DIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_TONE_DIAL = 0x100

	/*
		MDM_V110_SPEED_12DOT0K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_12DOT0K = 0x5

	/*
		MDM_V110_SPEED_14DOT4K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_14DOT4K = 0x6

	/*
		MDM_V110_SPEED_19DOT2K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_19DOT2K = 0x7

	/*
		MDM_V110_SPEED_1DOT2K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_1DOT2K = 0x1

	/*
		MDM_V110_SPEED_28DOT8K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_28DOT8K = 0x8

	/*
		MDM_V110_SPEED_2DOT4K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_2DOT4K = 0x2

	/*
		MDM_V110_SPEED_38DOT4K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_38DOT4K = 0x9

	/*
		MDM_V110_SPEED_4DOT8K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_4DOT8K = 0x3

	/*
		MDM_V110_SPEED_57DOT6K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_57DOT6K = 0xa

	/*
		MDM_V110_SPEED_9DOT6K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_9DOT6K = 0x4

	/*
		MDM_V110_SPEED_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V110_SPEED_DEFAULT = 0x0

	/*
		MDM_V120_ML_2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V120_ML_2 = 0x2

	/*
		MDM_V120_ML_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V120_ML_DEFAULT = 0x0

	/*
		MDM_V120_ML_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V120_ML_NONE = 0x1

	/*
		MDM_V120_SPEED_56K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V120_SPEED_56K = 0x2

	/*
		MDM_V120_SPEED_64K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V120_SPEED_64K = 0x1

	/*
		MDM_V120_SPEED_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V120_SPEED_DEFAULT = 0x0

	/*
		MDM_V23_OVERRIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_V23_OVERRIDE = 0x400

	/*
		MDM_X75_DATA_128K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_X75_DATA_128K = 0x2

	/*
		MDM_X75_DATA_64K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_X75_DATA_64K = 0x1

	/*
		MDM_X75_DATA_BTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_X75_DATA_BTX = 0x4

	/*
		MDM_X75_DATA_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_X75_DATA_DEFAULT = 0x0

	/*
		MDM_X75_DATA_T_70 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDM_X75_DATA_T_70 = 0x3
)

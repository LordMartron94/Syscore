//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_system_diagnostics_debug

const (
	/*
		API_VERSION_NUMBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	API_VERSION_NUMBER = 0xc

	/*
		BIND_ALL_IMAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BIND_ALL_IMAGES = 0x4

	/*
		BIND_CACHE_IMPORT_DLLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BIND_CACHE_IMPORT_DLLS = 0x8

	/*
		BIND_NO_BOUND_IMPORTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BIND_NO_BOUND_IMPORTS = 0x1

	/*
		BIND_NO_UPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BIND_NO_UPDATE = 0x2

	/*
		BIND_REPORT_64BIT_VA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BIND_REPORT_64BIT_VA = 0x10

	/*
		CBA_CHECK_ARM_MACHINE_THUMB_TYPE_OVERRIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_CHECK_ARM_MACHINE_THUMB_TYPE_OVERRIDE = 0x80000000

	/*
		CBA_CHECK_ENGOPT_DISALLOW_NETWORK_PATHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_CHECK_ENGOPT_DISALLOW_NETWORK_PATHS = 0x70000000

	/*
		CBA_DEBUG_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DEBUG_INFO = 0x10000000

	/*
		CBA_DEFERRED_SYMBOL_LOAD_CANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DEFERRED_SYMBOL_LOAD_CANCEL = 0x7

	/*
		CBA_DEFERRED_SYMBOL_LOAD_COMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DEFERRED_SYMBOL_LOAD_COMPLETE = 0x2

	/*
		CBA_DEFERRED_SYMBOL_LOAD_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DEFERRED_SYMBOL_LOAD_FAILURE = 0x3

	/*
		CBA_DEFERRED_SYMBOL_LOAD_PARTIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DEFERRED_SYMBOL_LOAD_PARTIAL = 0x20

	/*
		CBA_DEFERRED_SYMBOL_LOAD_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DEFERRED_SYMBOL_LOAD_START = 0x1

	/*
		CBA_DUPLICATE_SYMBOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_DUPLICATE_SYMBOL = 0x5

	/*
		CBA_ENGINE_PRESENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_ENGINE_PRESENT = 0x60000000

	/*
		CBA_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_EVENT = 0x10

	/*
		CBA_MAP_JIT_SYMBOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_MAP_JIT_SYMBOL = 0xa0000000

	/*
		CBA_READ_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_READ_MEMORY = 0x6

	/*
		CBA_SET_OPTIONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_SET_OPTIONS = 0x8

	/*
		CBA_SRCSRV_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_SRCSRV_EVENT = 0x40000000

	/*
		CBA_SRCSRV_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_SRCSRV_INFO = 0x20000000

	/*
		CBA_SYMBOLS_UNLOADED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_SYMBOLS_UNLOADED = 0x4

	/*
		CBA_UPDATE_STATUS_BAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_UPDATE_STATUS_BAR = 0x50000000

	/*
		CBA_XML_LOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBA_XML_LOG = 0x90000000

	/*
		CERT_PE_IMAGE_DIGEST_ALL_IMPORT_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CERT_PE_IMAGE_DIGEST_ALL_IMPORT_INFO = 0x4

	/*
		CERT_PE_IMAGE_DIGEST_DEBUG_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CERT_PE_IMAGE_DIGEST_DEBUG_INFO = 0x1

	/*
		CERT_PE_IMAGE_DIGEST_NON_PE_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CERT_PE_IMAGE_DIGEST_NON_PE_INFO = 0x8

	/*
		CERT_PE_IMAGE_DIGEST_RESOURCES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CERT_PE_IMAGE_DIGEST_RESOURCES = 0x2

	/*
		CERT_SECTION_TYPE_ANY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CERT_SECTION_TYPE_ANY = 0xff

	/*
		CHECKSUM_MAPVIEW_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKSUM_MAPVIEW_FAILURE = 0x3

	/*
		CHECKSUM_MAP_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKSUM_MAP_FAILURE = 0x2

	/*
		CHECKSUM_OPEN_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKSUM_OPEN_FAILURE = 0x1

	/*
		CHECKSUM_SUCCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKSUM_SUCCESS = 0x0

	/*
		CHECKSUM_UNICODE_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHECKSUM_UNICODE_FAILURE = 0x4

	/*
		DBHHEADER_PDBGUID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBHHEADER_PDBGUID = 0x3

	/*
		DMP_CONTEXT_RECORD_SIZE_32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_CONTEXT_RECORD_SIZE_32 = 0x4b0

	/*
		DMP_CONTEXT_RECORD_SIZE_64 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_CONTEXT_RECORD_SIZE_64 = 0xbb8

	/*
		DMP_HEADER_COMMENT_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_HEADER_COMMENT_SIZE = 0x80

	/*
		DMP_PHYSICAL_MEMORY_BLOCK_SIZE_32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_PHYSICAL_MEMORY_BLOCK_SIZE_32 = 0x2bc

	/*
		DMP_PHYSICAL_MEMORY_BLOCK_SIZE_64 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_PHYSICAL_MEMORY_BLOCK_SIZE_64 = 0x2bc

	/*
		DMP_RESERVED_0_SIZE_32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_RESERVED_0_SIZE_32 = 0x6e0

	/*
		DMP_RESERVED_0_SIZE_64 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_RESERVED_0_SIZE_64 = 0xfa8

	/*
		DMP_RESERVED_2_SIZE_32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_RESERVED_2_SIZE_32 = 0x10

	/*
		DMP_RESERVED_3_SIZE_32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMP_RESERVED_3_SIZE_32 = 0x38

	/*
		DSLFLAG_MISMATCHED_DBG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSLFLAG_MISMATCHED_DBG = 0x2

	/*
		DSLFLAG_MISMATCHED_PDB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSLFLAG_MISMATCHED_PDB = 0x1

	/*
		DUMP_SUMMARY_VALID_CURRENT_USER_VA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DUMP_SUMMARY_VALID_CURRENT_USER_VA = 0x2

	/*
		DUMP_SUMMARY_VALID_KERNEL_VA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DUMP_SUMMARY_VALID_KERNEL_VA = 0x1

	/*
		ERROR_IMAGE_NOT_STRIPPED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERROR_IMAGE_NOT_STRIPPED = 0x8800

	/*
		ERROR_NO_DBG_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERROR_NO_DBG_POINTER = 0x8801

	/*
		ERROR_NO_PDB_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ERROR_NO_PDB_POINTER = 0x8802

	/*
		ESLFLAG_FULLPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ESLFLAG_FULLPATH = 0x1

	/*
		ESLFLAG_INLINE_SITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ESLFLAG_INLINE_SITE = 0x10

	/*
		ESLFLAG_NEAREST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ESLFLAG_NEAREST = 0x2

	/*
		ESLFLAG_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ESLFLAG_NEXT = 0x8

	/*
		ESLFLAG_PREV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ESLFLAG_PREV = 0x4

	/*
		EVENT_SRCSPEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SRCSPEW = 0x64

	/*
		EVENT_SRCSPEW_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SRCSPEW_END = 0xc7

	/*
		EVENT_SRCSPEW_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SRCSPEW_START = 0x64

	/*
		EXCEPTION_CONTINUE_EXECUTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EXCEPTION_CONTINUE_EXECUTION = -1

	/*
		EXCEPTION_CONTINUE_SEARCH from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EXCEPTION_CONTINUE_SEARCH = 0x0

	/*
		EXCEPTION_EXECUTE_HANDLER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EXCEPTION_EXECUTE_HANDLER = 0x1

	/*
		EXT_OUTPUT_VER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EXT_OUTPUT_VER = 0x1

	/*
		FLAG_ENGINE_PRESENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLAG_ENGINE_PRESENT = 0x4

	/*
		FLAG_ENGOPT_DISALLOW_NETWORK_PATHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLAG_ENGOPT_DISALLOW_NETWORK_PATHS = 0x8

	/*
		FLAG_OVERRIDE_ARM_MACHINE_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FLAG_OVERRIDE_ARM_MACHINE_TYPE = 0x10

	/*
		IMAGEHLP_MODULE_REGION_ADDITIONAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_MODULE_REGION_ADDITIONAL = 0x4

	/*
		IMAGEHLP_MODULE_REGION_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_MODULE_REGION_ALL = 0xff

	/*
		IMAGEHLP_MODULE_REGION_DLLBASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_MODULE_REGION_DLLBASE = 0x1

	/*
		IMAGEHLP_MODULE_REGION_DLLRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_MODULE_REGION_DLLRANGE = 0x2

	/*
		IMAGEHLP_MODULE_REGION_JIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_MODULE_REGION_JIT = 0x8

	/*
		IMAGEHLP_RMAP_BIG_ENDIAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_BIG_ENDIAN = 0x2

	/*
		IMAGEHLP_RMAP_FIXUP_ARM64X from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_FIXUP_ARM64X = 0x10000000

	/*
		IMAGEHLP_RMAP_FIXUP_IMAGEBASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_FIXUP_IMAGEBASE = 0x80000000

	/*
		IMAGEHLP_RMAP_IGNORE_MISCOMPARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_IGNORE_MISCOMPARE = 0x4

	/*
		IMAGEHLP_RMAP_LOAD_RW_DATA_SECTIONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_LOAD_RW_DATA_SECTIONS = 0x20000000

	/*
		IMAGEHLP_RMAP_MAPPED_FLAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_MAPPED_FLAT = 0x1

	/*
		IMAGEHLP_RMAP_OMIT_SHARED_RW_DATA_SECTIONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_RMAP_OMIT_SHARED_RW_DATA_SECTIONS = 0x40000000

	/*
		IMAGEHLP_SYMBOL_FUNCTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_FUNCTION = 0x800

	/*
		IMAGEHLP_SYMBOL_INFO_CONSTANT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_CONSTANT = 0x100

	/*
		IMAGEHLP_SYMBOL_INFO_FRAMERELATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_FRAMERELATIVE = 0x20

	/*
		IMAGEHLP_SYMBOL_INFO_LOCAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_LOCAL = 0x80

	/*
		IMAGEHLP_SYMBOL_INFO_PARAMETER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_PARAMETER = 0x40

	/*
		IMAGEHLP_SYMBOL_INFO_REGISTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_REGISTER = 0x8

	/*
		IMAGEHLP_SYMBOL_INFO_REGRELATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_REGRELATIVE = 0x10

	/*
		IMAGEHLP_SYMBOL_INFO_TLSRELATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_TLSRELATIVE = 0x4000

	/*
		IMAGEHLP_SYMBOL_INFO_VALUEPRESENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_INFO_VALUEPRESENT = 0x1

	/*
		IMAGEHLP_SYMBOL_THUNK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_THUNK = 0x2000

	/*
		IMAGEHLP_SYMBOL_VIRTUAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGEHLP_SYMBOL_VIRTUAL = 0x1000

	/*
		INLINE_FRAME_CONTEXT_IGNORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INLINE_FRAME_CONTEXT_IGNORE = 0xffffffff

	/*
		INLINE_FRAME_CONTEXT_INIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INLINE_FRAME_CONTEXT_INIT = 0x0

	/*
		INTERFACESAFE_FOR_UNTRUSTED_CALLER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INTERFACESAFE_FOR_UNTRUSTED_CALLER = 0x1

	/*
		INTERFACESAFE_FOR_UNTRUSTED_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INTERFACESAFE_FOR_UNTRUSTED_DATA = 0x2

	/*
		INTERFACE_USES_DISPEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INTERFACE_USES_DISPEX = 0x4

	/*
		INTERFACE_USES_SECURITY_MANAGER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INTERFACE_USES_SECURITY_MANAGER = 0x8

	/*
		IOCTL_IPMI_INTERNAL_RECORD_SEL_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_IPMI_INTERNAL_RECORD_SEL_EVENT = 0x221000

	/*
		IPMI_IOCTL_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPMI_IOCTL_INDEX = 0x400

	/*
		IPMI_OS_SEL_RECORD_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPMI_OS_SEL_RECORD_MASK = 0xffff

	/*
		IPMI_OS_SEL_RECORD_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPMI_OS_SEL_RECORD_VERSION = 0x1

	/*
		IPMI_OS_SEL_RECORD_VERSION_1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPMI_OS_SEL_RECORD_VERSION_1 = 0x1

	/*
		MAX_SYM_NAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_SYM_NAME = 0x7d0

	/*
		MINIDUMP_MISC1_PROCESSOR_POWER_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC1_PROCESSOR_POWER_INFO = 0x4

	/*
		MINIDUMP_MISC3_PROCESS_EXECUTE_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC3_PROCESS_EXECUTE_FLAGS = 0x20

	/*
		MINIDUMP_MISC3_PROCESS_INTEGRITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC3_PROCESS_INTEGRITY = 0x10

	/*
		MINIDUMP_MISC3_PROTECTED_PROCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC3_PROTECTED_PROCESS = 0x80

	/*
		MINIDUMP_MISC3_TIMEZONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC3_TIMEZONE = 0x40

	/*
		MINIDUMP_MISC4_BUILDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC4_BUILDSTRING = 0x100

	/*
		MINIDUMP_MISC5_PROCESS_COOKIE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_MISC5_PROCESS_COOKIE = 0x200

	/*
		MINIDUMP_PROCESS_VM_COUNTERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_PROCESS_VM_COUNTERS = 0x1

	/*
		MINIDUMP_PROCESS_VM_COUNTERS_EX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_PROCESS_VM_COUNTERS_EX = 0x4

	/*
		MINIDUMP_PROCESS_VM_COUNTERS_EX2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_PROCESS_VM_COUNTERS_EX2 = 0x8

	/*
		MINIDUMP_PROCESS_VM_COUNTERS_JOB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_PROCESS_VM_COUNTERS_JOB = 0x10

	/*
		MINIDUMP_PROCESS_VM_COUNTERS_VIRTUALSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_PROCESS_VM_COUNTERS_VIRTUALSIZE = 0x2

	/*
		MINIDUMP_SYSMEMINFO1_BASICPERF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_SYSMEMINFO1_BASICPERF = 0x2

	/*
		MINIDUMP_SYSMEMINFO1_FILECACHE_TRANSITIONREPURPOSECOUNT_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_SYSMEMINFO1_FILECACHE_TRANSITIONREPURPOSECOUNT_FLAGS = 0x1

	/*
		MINIDUMP_SYSMEMINFO1_PERF_CCTOTALDIRTYPAGES_CCDIRTYPAGETHRESHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_SYSMEMINFO1_PERF_CCTOTALDIRTYPAGES_CCDIRTYPAGETHRESHOLD = 0x4

	/*
		MINIDUMP_SYSMEMINFO1_PERF_RESIDENTAVAILABLEPAGES_SHAREDCOMMITPAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_SYSMEMINFO1_PERF_RESIDENTAVAILABLEPAGES_SHAREDCOMMITPAGES = 0x8

	/*
		MINIDUMP_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIDUMP_VERSION = 0xa793

	/*
		NUM_SSRVOPTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NUM_SSRVOPTS = 0x20

	/*
		SPLITSYM_EXTRACT_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPLITSYM_EXTRACT_ALL = 0x2

	/*
		SPLITSYM_REMOVE_PRIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPLITSYM_REMOVE_PRIVATE = 0x1

	/*
		SPLITSYM_SYMBOLPATH_IS_SRC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPLITSYM_SYMBOLPATH_IS_SRC = 0x4

	/*
		SSRVACTION_CHECKSUMSTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_CHECKSUMSTATUS = 0x8

	/*
		SSRVACTION_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_EVENT = 0x3

	/*
		SSRVACTION_EVENTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_EVENTW = 0x4

	/*
		SSRVACTION_HTTPSTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_HTTPSTATUS = 0x6

	/*
		SSRVACTION_QUERYCANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_QUERYCANCEL = 0x2

	/*
		SSRVACTION_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_SIZE = 0x5

	/*
		SSRVACTION_TRACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_TRACE = 0x1

	/*
		SSRVACTION_XMLOUTPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVACTION_XMLOUTPUT = 0x7

	/*
		SSRVOPT_CALLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_CALLBACK = 0x1

	/*
		SSRVOPT_CALLBACKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_CALLBACKW = 0x10000

	/*
		SSRVOPT_DISABLE_PING_HOST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_DISABLE_PING_HOST = 0x4000000

	/*
		SSRVOPT_DISABLE_TIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_DISABLE_TIMEOUT = 0x8000000

	/*
		SSRVOPT_DONT_UNCOMPRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_DONT_UNCOMPRESS = 0x2000000

	/*
		SSRVOPT_DOWNSTREAM_STORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_DOWNSTREAM_STORE = 0x2000

	/*
		SSRVOPT_ENABLE_COMM_MSG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_ENABLE_COMM_MSG = 0x10000000

	/*
		SSRVOPT_FAVOR_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_FAVOR_COMPRESSED = 0x200000

	/*
		SSRVOPT_FLAT_DEFAULT_STORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_FLAT_DEFAULT_STORE = 0x20000

	/*
		SSRVOPT_GETPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_GETPATH = 0x40

	/*
		SSRVOPT_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_MAX = 0x80000000

	/*
		SSRVOPT_MESSAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_MESSAGE = 0x80000

	/*
		SSRVOPT_NOCOPY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_NOCOPY = 0x40

	/*
		SSRVOPT_OLDGUIDPTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_OLDGUIDPTR = 0x10

	/*
		SSRVOPT_OVERWRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_OVERWRITE = 0x4000

	/*
		SSRVOPT_PARAMTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_PARAMTYPE = 0x100

	/*
		SSRVOPT_PARENTWIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_PARENTWIN = 0x80

	/*
		SSRVOPT_PROXY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_PROXY = 0x1000

	/*
		SSRVOPT_PROXYW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_PROXYW = 0x40000

	/*
		SSRVOPT_RESETTOU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_RESETTOU = 0x8000

	/*
		SSRVOPT_RETRY_APP_HANG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_RETRY_APP_HANG = 0x80000000

	/*
		SSRVOPT_SECURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_SECURE = 0x200

	/*
		SSRVOPT_SERVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_SERVICE = 0x100000

	/*
		SSRVOPT_SETCONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_SETCONTEXT = 0x800

	/*
		SSRVOPT_STRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_STRING = 0x400000

	/*
		SSRVOPT_TRACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_TRACE = 0x400

	/*
		SSRVOPT_UNATTENDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_UNATTENDED = 0x20

	/*
		SSRVOPT_URI_FILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_URI_FILTER = 0x20000000

	/*
		SSRVOPT_URI_TIERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_URI_TIERS = 0x40000000

	/*
		SSRVOPT_WINHTTP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_WINHTTP = 0x800000

	/*
		SSRVOPT_WININET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVOPT_WININET = 0x1000000

	/*
		SSRVURI_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_ALL = 0xff

	/*
		SSRVURI_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_COMPRESSED = 0x2

	/*
		SSRVURI_FILEPTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_FILEPTR = 0x4

	/*
		SSRVURI_HTTP_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_HTTP_COMPRESSED = 0x2

	/*
		SSRVURI_HTTP_FILEPTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_HTTP_FILEPTR = 0x4

	/*
		SSRVURI_HTTP_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_HTTP_MASK = 0xf

	/*
		SSRVURI_HTTP_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_HTTP_NORMAL = 0x1

	/*
		SSRVURI_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_NORMAL = 0x1

	/*
		SSRVURI_UNC_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_UNC_COMPRESSED = 0x20

	/*
		SSRVURI_UNC_FILEPTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_UNC_FILEPTR = 0x40

	/*
		SSRVURI_UNC_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_UNC_MASK = 0xf0

	/*
		SSRVURI_UNC_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SSRVURI_UNC_NORMAL = 0x10

	/*
		SYMENUM_OPTIONS_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMENUM_OPTIONS_DEFAULT = 0x1

	/*
		SYMENUM_OPTIONS_INLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMENUM_OPTIONS_INLINE = 0x2

	/*
		SYMFLAG_FIXUP_ARM64X from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_FIXUP_ARM64X = 0x1000000

	/*
		SYMFLAG_FUNC_NO_RETURN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_FUNC_NO_RETURN = 0x100000

	/*
		SYMFLAG_GLOBAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_GLOBAL = 0x2000000

	/*
		SYMFLAG_NULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_NULL = 0x80000

	/*
		SYMFLAG_PUBLIC_CODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_PUBLIC_CODE = 0x400000

	/*
		SYMFLAG_REGREL_ALIASINDIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_REGREL_ALIASINDIR = 0x800000

	/*
		SYMFLAG_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_RESET = 0x80000000

	/*
		SYMFLAG_SYNTHETIC_ZEROBASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMFLAG_SYNTHETIC_ZEROBASE = 0x200000

	/*
		SYMF_CONSTANT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_CONSTANT = 0x100

	/*
		SYMF_EXPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_EXPORT = 0x200

	/*
		SYMF_FORWARDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_FORWARDER = 0x400

	/*
		SYMF_FRAMEREL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_FRAMEREL = 0x20

	/*
		SYMF_FUNCTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_FUNCTION = 0x800

	/*
		SYMF_LOCAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_LOCAL = 0x80

	/*
		SYMF_OMAP_GENERATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_OMAP_GENERATED = 0x1

	/*
		SYMF_OMAP_MODIFIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_OMAP_MODIFIED = 0x2

	/*
		SYMF_PARAMETER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_PARAMETER = 0x40

	/*
		SYMF_REGISTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_REGISTER = 0x8

	/*
		SYMF_REGREL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_REGREL = 0x10

	/*
		SYMF_THUNK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_THUNK = 0x2000

	/*
		SYMF_TLSREL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_TLSREL = 0x4000

	/*
		SYMF_VIRTUAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMF_VIRTUAL = 0x1000

	/*
		SYMOPT_ALLOW_ABSOLUTE_SYMBOLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_ALLOW_ABSOLUTE_SYMBOLS = 0x800

	/*
		SYMOPT_ALLOW_ZERO_ADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_ALLOW_ZERO_ADDRESS = 0x1000000

	/*
		SYMOPT_AUTO_PUBLICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_AUTO_PUBLICS = 0x10000

	/*
		SYMOPT_CASE_INSENSITIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_CASE_INSENSITIVE = 0x1

	/*
		SYMOPT_DEBUG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_DEBUG = 0x80000000

	/*
		SYMOPT_DEFERRED_LOADS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_DEFERRED_LOADS = 0x4

	/*
		SYMOPT_DISABLE_FAST_SYMBOLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_DISABLE_FAST_SYMBOLS = 0x10000000

	/*
		SYMOPT_DISABLE_SRVSTAR_ON_STARTUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_DISABLE_SRVSTAR_ON_STARTUP = 0x40000000

	/*
		SYMOPT_DISABLE_SYMSRV_AUTODETECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_DISABLE_SYMSRV_AUTODETECT = 0x2000000

	/*
		SYMOPT_DISABLE_SYMSRV_TIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_DISABLE_SYMSRV_TIMEOUT = 0x20000000

	/*
		SYMOPT_EXACT_SYMBOLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_EXACT_SYMBOLS = 0x400

	/*
		SYMOPT_FAIL_CRITICAL_ERRORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_FAIL_CRITICAL_ERRORS = 0x200

	/*
		SYMOPT_FAVOR_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_FAVOR_COMPRESSED = 0x800000

	/*
		SYMOPT_FLAT_DIRECTORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_FLAT_DIRECTORY = 0x400000

	/*
		SYMOPT_IGNORE_CVREC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_IGNORE_CVREC = 0x80

	/*
		SYMOPT_IGNORE_IMAGEDIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_IGNORE_IMAGEDIR = 0x200000

	/*
		SYMOPT_IGNORE_NT_SYMPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_IGNORE_NT_SYMPATH = 0x1000

	/*
		SYMOPT_INCLUDE_32BIT_MODULES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_INCLUDE_32BIT_MODULES = 0x2000

	/*
		SYMOPT_LOAD_ANYTHING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_LOAD_ANYTHING = 0x40

	/*
		SYMOPT_LOAD_LINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_LOAD_LINES = 0x10

	/*
		SYMOPT_NO_CPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_NO_CPP = 0x8

	/*
		SYMOPT_NO_IMAGE_SEARCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_NO_IMAGE_SEARCH = 0x20000

	/*
		SYMOPT_NO_PROMPTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_NO_PROMPTS = 0x80000

	/*
		SYMOPT_NO_PUBLICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_NO_PUBLICS = 0x8000

	/*
		SYMOPT_NO_UNQUALIFIED_LOADS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_NO_UNQUALIFIED_LOADS = 0x100

	/*
		SYMOPT_OMAP_FIND_NEAREST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_OMAP_FIND_NEAREST = 0x20

	/*
		SYMOPT_OVERWRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_OVERWRITE = 0x100000

	/*
		SYMOPT_PUBLICS_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_PUBLICS_ONLY = 0x4000

	/*
		SYMOPT_READONLY_CACHE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_READONLY_CACHE = 0x4000000

	/*
		SYMOPT_SECURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_SECURE = 0x40000

	/*
		SYMOPT_SYMPATH_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_SYMPATH_LAST = 0x8000000

	/*
		SYMOPT_UNDNAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMOPT_UNDNAME = 0x2

	/*
		SYMSEARCH_ALLITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSEARCH_ALLITEMS = 0x8

	/*
		SYMSEARCH_GLOBALSONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSEARCH_GLOBALSONLY = 0x4

	/*
		SYMSEARCH_MASKOBJS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSEARCH_MASKOBJS = 0x1

	/*
		SYMSEARCH_RECURSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSEARCH_RECURSE = 0x2

	/*
		SYMSRV_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSRV_VERSION = 0x2

	/*
		SYMSTOREOPT_ALT_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSTOREOPT_ALT_INDEX = 0x10

	/*
		SYMSTOREOPT_UNICODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYMSTOREOPT_UNICODE = 0x20

	/*
		SYM_INLINE_COMP_DIFFERENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_INLINE_COMP_DIFFERENT = 0x5

	/*
		SYM_INLINE_COMP_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_INLINE_COMP_ERROR = 0x0

	/*
		SYM_INLINE_COMP_IDENTICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_INLINE_COMP_IDENTICAL = 0x1

	/*
		SYM_INLINE_COMP_STEPIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_INLINE_COMP_STEPIN = 0x2

	/*
		SYM_INLINE_COMP_STEPOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_INLINE_COMP_STEPOUT = 0x3

	/*
		SYM_INLINE_COMP_STEPOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_INLINE_COMP_STEPOVER = 0x4

	/*
		SYM_STKWALK_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_STKWALK_DEFAULT = 0x0

	/*
		SYM_STKWALK_FORCE_FRAMEPTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_STKWALK_FORCE_FRAMEPTR = 0x1

	/*
		SYM_STKWALK_ZEROEXTEND_PTRS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYM_STKWALK_ZEROEXTEND_PTRS = 0x2

	/*
		SevMax from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SevMax = 0x4

	/*
		TARGET_ATTRIBUTE_PACMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TARGET_ATTRIBUTE_PACMASK = 0x1

	/*
		UNDNAME_32_BIT_DECODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_32_BIT_DECODE = 0x800

	/*
		UNDNAME_COMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_COMPLETE = 0x0

	/*
		UNDNAME_NAME_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NAME_ONLY = 0x1000

	/*
		UNDNAME_NO_ACCESS_SPECIFIERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_ACCESS_SPECIFIERS = 0x80

	/*
		UNDNAME_NO_ALLOCATION_LANGUAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_ALLOCATION_LANGUAGE = 0x10

	/*
		UNDNAME_NO_ALLOCATION_MODEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_ALLOCATION_MODEL = 0x8

	/*
		UNDNAME_NO_ARGUMENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_ARGUMENTS = 0x2000

	/*
		UNDNAME_NO_CV_THISTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_CV_THISTYPE = 0x40

	/*
		UNDNAME_NO_FUNCTION_RETURNS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_FUNCTION_RETURNS = 0x4

	/*
		UNDNAME_NO_LEADING_UNDERSCORES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_LEADING_UNDERSCORES = 0x1

	/*
		UNDNAME_NO_MEMBER_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_MEMBER_TYPE = 0x200

	/*
		UNDNAME_NO_MS_KEYWORDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_MS_KEYWORDS = 0x2

	/*
		UNDNAME_NO_MS_THISTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_MS_THISTYPE = 0x20

	/*
		UNDNAME_NO_RETURN_UDT_MODEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_RETURN_UDT_MODEL = 0x400

	/*
		UNDNAME_NO_SPECIAL_SYMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_SPECIAL_SYMS = 0x4000

	/*
		UNDNAME_NO_THISTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_THISTYPE = 0x60

	/*
		UNDNAME_NO_THROW_SIGNATURES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNDNAME_NO_THROW_SIGNATURES = 0x100

	/*
		WCT_MAX_NODE_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WCT_MAX_NODE_COUNT = 0x10

	/*
		WCT_NETWORK_IO_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WCT_NETWORK_IO_FLAG = 0x8

	/*
		WCT_OBJNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WCT_OBJNAME_LENGTH = 0x80

	/*
		WHEA_BAD_PAGE_LIST_LOCATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_BAD_PAGE_LIST_LOCATION = 0xf

	/*
		WHEA_BAD_PAGE_LIST_MAX_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_BAD_PAGE_LIST_MAX_SIZE = 0xe

	/*
		WHEA_CMCI_THRESHOLD_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_CMCI_THRESHOLD_COUNT = 0xa

	/*
		WHEA_CMCI_THRESHOLD_POLL_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_CMCI_THRESHOLD_POLL_COUNT = 0xc

	/*
		WHEA_CMCI_THRESHOLD_TIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_CMCI_THRESHOLD_TIME = 0xb

	/*
		WHEA_DEVICE_DRIVER_BUFFER_SET_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_BUFFER_SET_MAX = 0x1

	/*
		WHEA_DEVICE_DRIVER_BUFFER_SET_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_BUFFER_SET_MIN = 0x1

	/*
		WHEA_DEVICE_DRIVER_BUFFER_SET_V1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_BUFFER_SET_V1 = 0x1

	/*
		WHEA_DEVICE_DRIVER_CONFIG_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_CONFIG_MAX = 0x2

	/*
		WHEA_DEVICE_DRIVER_CONFIG_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_CONFIG_MIN = 0x1

	/*
		WHEA_DEVICE_DRIVER_CONFIG_V1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_CONFIG_V1 = 0x1

	/*
		WHEA_DEVICE_DRIVER_CONFIG_V2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DEVICE_DRIVER_CONFIG_V2 = 0x2

	/*
		WHEA_DISABLE_DUMMY_WRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DISABLE_DUMMY_WRITE = 0x6

	/*
		WHEA_DISABLE_OFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_DISABLE_OFFLINE = 0x0

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_AERBRIDGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_AERBRIDGE = 0x8

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_AERENDPOINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_AERENDPOINT = 0x7

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_AERROOTPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_AERROOTPORT = 0x6

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_GENERIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_GENERIC = 0x9

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_GENERIC_V2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_GENERIC_V2 = 0xa

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_IPFCMC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_IPFCMC = 0x4

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_IPFCPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_IPFCPE = 0x5

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_IPFMCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_IPFMCA = 0x3

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_XPFCMC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_XPFCMC = 0x1

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_XPFMCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_XPFMCE = 0x0

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_XPFNMI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_TYPE_XPFNMI = 0x2

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_VERSION_10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_VERSION_10 = 0xa

	/*
		WHEA_ERROR_SOURCE_DESCRIPTOR_VERSION_11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_DESCRIPTOR_VERSION_11 = 0xb

	/*
		WHEA_ERROR_SOURCE_FLAG_DEFAULTSOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_FLAG_DEFAULTSOURCE = 0x80000000

	/*
		WHEA_ERROR_SOURCE_FLAG_FIRMWAREFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_FLAG_FIRMWAREFIRST = 0x1

	/*
		WHEA_ERROR_SOURCE_FLAG_GHES_ASSIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_FLAG_GHES_ASSIST = 0x4

	/*
		WHEA_ERROR_SOURCE_FLAG_GLOBAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_FLAG_GLOBAL = 0x2

	/*
		WHEA_ERROR_SOURCE_INVALID_RELATED_SOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ERROR_SOURCE_INVALID_RELATED_SOURCE = 0xffff

	/*
		WHEA_MAX_MC_BANKS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_MAX_MC_BANKS = 0x20

	/*
		WHEA_MEM_PERSISTOFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_MEM_PERSISTOFFLINE = 0x1

	/*
		WHEA_MEM_PFA_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_MEM_PFA_DISABLE = 0x2

	/*
		WHEA_MEM_PFA_PAGECOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_MEM_PFA_PAGECOUNT = 0x3

	/*
		WHEA_MEM_PFA_THRESHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_MEM_PFA_THRESHOLD = 0x4

	/*
		WHEA_MEM_PFA_TIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_MEM_PFA_TIMEOUT = 0x5

	/*
		WHEA_NOTIFICATION_TYPE_ARMV8_SEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_ARMV8_SEA = 0x8

	/*
		WHEA_NOTIFICATION_TYPE_ARMV8_SEI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_ARMV8_SEI = 0x9

	/*
		WHEA_NOTIFICATION_TYPE_CMCI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_CMCI = 0x5

	/*
		WHEA_NOTIFICATION_TYPE_EXTERNALINTERRUPT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_EXTERNALINTERRUPT = 0x1

	/*
		WHEA_NOTIFICATION_TYPE_EXTERNALINTERRUPT_GSIV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_EXTERNALINTERRUPT_GSIV = 0xa

	/*
		WHEA_NOTIFICATION_TYPE_GPIO_SIGNAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_GPIO_SIGNAL = 0x7

	/*
		WHEA_NOTIFICATION_TYPE_LOCALINTERRUPT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_LOCALINTERRUPT = 0x2

	/*
		WHEA_NOTIFICATION_TYPE_MCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_MCE = 0x6

	/*
		WHEA_NOTIFICATION_TYPE_NMI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_NMI = 0x4

	/*
		WHEA_NOTIFICATION_TYPE_POLLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_POLLED = 0x0

	/*
		WHEA_NOTIFICATION_TYPE_SCI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_SCI = 0x3

	/*
		WHEA_NOTIFICATION_TYPE_SDEI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFICATION_TYPE_SDEI = 0xb

	/*
		WHEA_NOTIFY_ALL_OFFLINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_NOTIFY_ALL_OFFLINES = 0x10

	/*
		WHEA_PENDING_PAGE_LIST_SZ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_PENDING_PAGE_LIST_SZ = 0xd

	/*
		WHEA_RESTORE_CMCI_ATTEMPTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_RESTORE_CMCI_ATTEMPTS = 0x8

	/*
		WHEA_RESTORE_CMCI_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_RESTORE_CMCI_ENABLED = 0x7

	/*
		WHEA_RESTORE_CMCI_ERR_LIMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_RESTORE_CMCI_ERR_LIMIT = 0x9

	/*
		WHEA_ROW_FAIL_CHECK_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ROW_FAIL_CHECK_ENABLE = 0x12

	/*
		WHEA_ROW_FAIL_CHECK_EXTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ROW_FAIL_CHECK_EXTENT = 0x11

	/*
		WHEA_ROW_FAIL_CHECK_THRESHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_ROW_FAIL_CHECK_THRESHOLD = 0x13

	/*
		WHEA_XPF_MC_BANK_STATUSFORMAT_AMD64MCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_XPF_MC_BANK_STATUSFORMAT_AMD64MCA = 0x2

	/*
		WHEA_XPF_MC_BANK_STATUSFORMAT_IA32MCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_XPF_MC_BANK_STATUSFORMAT_IA32MCA = 0x0

	/*
		WHEA_XPF_MC_BANK_STATUSFORMAT_Intel64MCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEA_XPF_MC_BANK_STATUSFORMAT_Intel64MCA = 0x1

	/*
		WOW64_MAXIMUM_SUPPORTED_EXTENSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOW64_MAXIMUM_SUPPORTED_EXTENSION = 0x200

	/*
		WOW64_SIZE_OF_80387_REGISTERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOW64_SIZE_OF_80387_REGISTERS = 0x50
)

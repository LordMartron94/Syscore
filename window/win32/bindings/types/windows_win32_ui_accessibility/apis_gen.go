//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_ui_accessibility

const (
	/*
		ANRUS_PRIORITY_AUDIO_DYNAMIC_DUCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ANRUS_PRIORITY_AUDIO_DYNAMIC_DUCK = 0x10

	/*
		DISPID_ACC_CHILD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_CHILD = -5002

	/*
		DISPID_ACC_CHILDCOUNT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_CHILDCOUNT = -5001

	/*
		DISPID_ACC_DEFAULTACTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_DEFAULTACTION = -5013

	/*
		DISPID_ACC_DESCRIPTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_DESCRIPTION = -5005

	/*
		DISPID_ACC_DODEFAULTACTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_DODEFAULTACTION = -5018

	/*
		DISPID_ACC_FOCUS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_FOCUS = -5011

	/*
		DISPID_ACC_HELP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_HELP = -5008

	/*
		DISPID_ACC_HELPTOPIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_HELPTOPIC = -5009

	/*
		DISPID_ACC_HITTEST from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_HITTEST = -5017

	/*
		DISPID_ACC_KEYBOARDSHORTCUT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_KEYBOARDSHORTCUT = -5010

	/*
		DISPID_ACC_LOCATION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_LOCATION = -5015

	/*
		DISPID_ACC_NAME from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_NAME = -5003

	/*
		DISPID_ACC_NAVIGATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_NAVIGATE = -5016

	/*
		DISPID_ACC_PARENT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_PARENT = -5000

	/*
		DISPID_ACC_ROLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_ROLE = -5006

	/*
		DISPID_ACC_SELECT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_SELECT = -5014

	/*
		DISPID_ACC_SELECTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_SELECTION = -5012

	/*
		DISPID_ACC_STATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_STATE = -5007

	/*
		DISPID_ACC_VALUE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPID_ACC_VALUE = -5004

	/*
		MSAA_MENU_SIG from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSAA_MENU_SIG = -1441927155

	/*
		NAVDIR_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_DOWN = 0x2

	/*
		NAVDIR_FIRSTCHILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_FIRSTCHILD = 0x7

	/*
		NAVDIR_LASTCHILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_LASTCHILD = 0x8

	/*
		NAVDIR_LEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_LEFT = 0x3

	/*
		NAVDIR_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_MAX = 0x9

	/*
		NAVDIR_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_MIN = 0x0

	/*
		NAVDIR_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_NEXT = 0x5

	/*
		NAVDIR_PREVIOUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_PREVIOUS = 0x6

	/*
		NAVDIR_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_RIGHT = 0x4

	/*
		NAVDIR_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NAVDIR_UP = 0x1

	/*
		ROLE_SYSTEM_ALERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_ALERT = 0x8

	/*
		ROLE_SYSTEM_ANIMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_ANIMATION = 0x36

	/*
		ROLE_SYSTEM_APPLICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_APPLICATION = 0xe

	/*
		ROLE_SYSTEM_BORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_BORDER = 0x13

	/*
		ROLE_SYSTEM_BUTTONDROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_BUTTONDROPDOWN = 0x38

	/*
		ROLE_SYSTEM_BUTTONDROPDOWNGRID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_BUTTONDROPDOWNGRID = 0x3a

	/*
		ROLE_SYSTEM_BUTTONMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_BUTTONMENU = 0x39

	/*
		ROLE_SYSTEM_CARET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CARET = 0x7

	/*
		ROLE_SYSTEM_CELL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CELL = 0x1d

	/*
		ROLE_SYSTEM_CHARACTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CHARACTER = 0x20

	/*
		ROLE_SYSTEM_CHART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CHART = 0x11

	/*
		ROLE_SYSTEM_CHECKBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CHECKBUTTON = 0x2c

	/*
		ROLE_SYSTEM_CLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CLIENT = 0xa

	/*
		ROLE_SYSTEM_CLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CLOCK = 0x3d

	/*
		ROLE_SYSTEM_COLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_COLUMN = 0x1b

	/*
		ROLE_SYSTEM_COLUMNHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_COLUMNHEADER = 0x19

	/*
		ROLE_SYSTEM_COMBOBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_COMBOBOX = 0x2e

	/*
		ROLE_SYSTEM_CURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_CURSOR = 0x6

	/*
		ROLE_SYSTEM_DIAGRAM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_DIAGRAM = 0x35

	/*
		ROLE_SYSTEM_DIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_DIAL = 0x31

	/*
		ROLE_SYSTEM_DIALOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_DIALOG = 0x12

	/*
		ROLE_SYSTEM_DOCUMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_DOCUMENT = 0xf

	/*
		ROLE_SYSTEM_DROPLIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_DROPLIST = 0x2f

	/*
		ROLE_SYSTEM_EQUATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_EQUATION = 0x37

	/*
		ROLE_SYSTEM_GRAPHIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_GRAPHIC = 0x28

	/*
		ROLE_SYSTEM_GRIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_GRIP = 0x4

	/*
		ROLE_SYSTEM_GROUPING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_GROUPING = 0x14

	/*
		ROLE_SYSTEM_HELPBALLOON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_HELPBALLOON = 0x1f

	/*
		ROLE_SYSTEM_HOTKEYFIELD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_HOTKEYFIELD = 0x32

	/*
		ROLE_SYSTEM_INDICATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_INDICATOR = 0x27

	/*
		ROLE_SYSTEM_IPADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_IPADDRESS = 0x3f

	/*
		ROLE_SYSTEM_LINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_LINK = 0x1e

	/*
		ROLE_SYSTEM_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_LIST = 0x21

	/*
		ROLE_SYSTEM_LISTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_LISTITEM = 0x22

	/*
		ROLE_SYSTEM_MENUBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_MENUBAR = 0x2

	/*
		ROLE_SYSTEM_MENUITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_MENUITEM = 0xc

	/*
		ROLE_SYSTEM_MENUPOPUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_MENUPOPUP = 0xb

	/*
		ROLE_SYSTEM_OUTLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_OUTLINE = 0x23

	/*
		ROLE_SYSTEM_OUTLINEBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_OUTLINEBUTTON = 0x40

	/*
		ROLE_SYSTEM_OUTLINEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_OUTLINEITEM = 0x24

	/*
		ROLE_SYSTEM_PAGETAB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_PAGETAB = 0x25

	/*
		ROLE_SYSTEM_PAGETABLIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_PAGETABLIST = 0x3c

	/*
		ROLE_SYSTEM_PANE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_PANE = 0x10

	/*
		ROLE_SYSTEM_PROGRESSBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_PROGRESSBAR = 0x30

	/*
		ROLE_SYSTEM_PROPERTYPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_PROPERTYPAGE = 0x26

	/*
		ROLE_SYSTEM_PUSHBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_PUSHBUTTON = 0x2b

	/*
		ROLE_SYSTEM_RADIOBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_RADIOBUTTON = 0x2d

	/*
		ROLE_SYSTEM_ROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_ROW = 0x1c

	/*
		ROLE_SYSTEM_ROWHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_ROWHEADER = 0x1a

	/*
		ROLE_SYSTEM_SCROLLBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_SCROLLBAR = 0x3

	/*
		ROLE_SYSTEM_SEPARATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_SEPARATOR = 0x15

	/*
		ROLE_SYSTEM_SLIDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_SLIDER = 0x33

	/*
		ROLE_SYSTEM_SOUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_SOUND = 0x5

	/*
		ROLE_SYSTEM_SPINBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_SPINBUTTON = 0x34

	/*
		ROLE_SYSTEM_SPLITBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_SPLITBUTTON = 0x3e

	/*
		ROLE_SYSTEM_STATICTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_STATICTEXT = 0x29

	/*
		ROLE_SYSTEM_STATUSBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_STATUSBAR = 0x17

	/*
		ROLE_SYSTEM_TABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_TABLE = 0x18

	/*
		ROLE_SYSTEM_TEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_TEXT = 0x2a

	/*
		ROLE_SYSTEM_TITLEBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_TITLEBAR = 0x1

	/*
		ROLE_SYSTEM_TOOLBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_TOOLBAR = 0x16

	/*
		ROLE_SYSTEM_TOOLTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_TOOLTIP = 0xd

	/*
		ROLE_SYSTEM_WHITESPACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_WHITESPACE = 0x3b

	/*
		ROLE_SYSTEM_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ROLE_SYSTEM_WINDOW = 0x9

	/*
		SELFLAG_ADDSELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_ADDSELECTION = 0x8

	/*
		SELFLAG_EXTENDSELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_EXTENDSELECTION = 0x4

	/*
		SELFLAG_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_NONE = 0x0

	/*
		SELFLAG_REMOVESELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_REMOVESELECTION = 0x10

	/*
		SELFLAG_TAKEFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_TAKEFOCUS = 0x1

	/*
		SELFLAG_TAKESELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_TAKESELECTION = 0x2

	/*
		SELFLAG_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SELFLAG_VALID = 0x1f

	/*
		STATE_SYSTEM_HASPOPUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_HASPOPUP = 0x40000000

	/*
		STATE_SYSTEM_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_NORMAL = 0x0

	/*
		UIA_E_ELEMENTNOTAVAILABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_ELEMENTNOTAVAILABLE = 0x80040201

	/*
		UIA_E_ELEMENTNOTENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_ELEMENTNOTENABLED = 0x80040200

	/*
		UIA_E_INVALIDOPERATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_INVALIDOPERATION = 0x80131509

	/*
		UIA_E_NOCLICKABLEPOINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_NOCLICKABLEPOINT = 0x80040202

	/*
		UIA_E_NOTSUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_NOTSUPPORTED = 0x80040204

	/*
		UIA_E_PROXYASSEMBLYNOTLOADED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_PROXYASSEMBLYNOTLOADED = 0x80040203

	/*
		UIA_E_TIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_E_TIMEOUT = 0x80131505

	/*
		UIA_IAFP_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_IAFP_DEFAULT = 0x0

	/*
		UIA_IAFP_UNWRAP_BRIDGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_IAFP_UNWRAP_BRIDGE = 0x1

	/*
		UIA_PFIA_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_PFIA_DEFAULT = 0x0

	/*
		UIA_PFIA_UNWRAP_BRIDGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIA_PFIA_UNWRAP_BRIDGE = 0x1

	/*
		UiaAppendRuntimeId from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UiaAppendRuntimeId = 0x3

	/*
		UiaRootObjectId from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UiaRootObjectId = -25
)

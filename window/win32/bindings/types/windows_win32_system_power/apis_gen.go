//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-05-27 08:35:22 CEST

package windows_win32_system_power

const (
	/*
		ACPI_TIME_ADJUST_DAYLIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACPI_TIME_ADJUST_DAYLIGHT = 0x1

	/*
		ACPI_TIME_IN_DAYLIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACPI_TIME_IN_DAYLIGHT = 0x2

	/*
		ACPI_TIME_ZONE_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACPI_TIME_ZONE_UNKNOWN = 0x7ff

	/*
		ACTIVE_COOLING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTIVE_COOLING = 0x0

	/*
		BATTERY_CAPACITY_RELATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_CAPACITY_RELATIVE = 0x40000000

	/*
		BATTERY_CHARGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_CHARGING = 0x4

	/*
		BATTERY_CLASS_MAJOR_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_CLASS_MAJOR_VERSION = 0x1

	/*
		BATTERY_CLASS_MINOR_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_CLASS_MINOR_VERSION = 0x0

	/*
		BATTERY_CLASS_MINOR_VERSION_1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_CLASS_MINOR_VERSION_1 = 0x1

	/*
		BATTERY_CRITICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_CRITICAL = 0x8

	/*
		BATTERY_DISCHARGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_DISCHARGING = 0x2

	/*
		BATTERY_IS_SHORT_TERM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_IS_SHORT_TERM = 0x20000000

	/*
		BATTERY_MINIPORT_UPDATE_DATA_VER_1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_MINIPORT_UPDATE_DATA_VER_1 = 0x1

	/*
		BATTERY_MINIPORT_UPDATE_DATA_VER_2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_MINIPORT_UPDATE_DATA_VER_2 = 0x2

	/*
		BATTERY_POWER_ON_LINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_POWER_ON_LINE = 0x1

	/*
		BATTERY_SEALED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_SEALED = 0x10000000

	/*
		BATTERY_SET_CHARGER_ID_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_SET_CHARGER_ID_SUPPORTED = 0x8

	/*
		BATTERY_SET_CHARGE_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_SET_CHARGE_SUPPORTED = 0x1

	/*
		BATTERY_SET_CHARGINGSOURCE_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_SET_CHARGINGSOURCE_SUPPORTED = 0x4

	/*
		BATTERY_SET_DISCHARGE_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_SET_DISCHARGE_SUPPORTED = 0x2

	/*
		BATTERY_SYSTEM_BATTERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_SYSTEM_BATTERY = 0x80000000

	/*
		BATTERY_TAG_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_TAG_INVALID = 0x0

	/*
		BATTERY_UNKNOWN_CAPACITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_UNKNOWN_CAPACITY = 0xffffffff

	/*
		BATTERY_UNKNOWN_CURRENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_UNKNOWN_CURRENT = 0xffffffff

	/*
		BATTERY_UNKNOWN_RATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_UNKNOWN_RATE = 0x80000000

	/*
		BATTERY_UNKNOWN_TIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_UNKNOWN_TIME = 0xffffffff

	/*
		BATTERY_UNKNOWN_VOLTAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_UNKNOWN_VOLTAGE = 0xffffffff

	/*
		BATTERY_USB_CHARGER_STATUS_FN_DEFAULT_USB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_USB_CHARGER_STATUS_FN_DEFAULT_USB = 0x1

	/*
		BATTERY_USB_CHARGER_STATUS_UCM_PD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_USB_CHARGER_STATUS_UCM_PD = 0x2

	/*
		DEVICEPOWER_AND_OPERATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_AND_OPERATION = 0x40000000

	/*
		DEVICEPOWER_CLEAR_WAKEENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_CLEAR_WAKEENABLED = 0x2

	/*
		DEVICEPOWER_FILTER_DEVICES_PRESENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_FILTER_DEVICES_PRESENT = 0x20000000

	/*
		DEVICEPOWER_FILTER_HARDWARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_FILTER_HARDWARE = 0x10000000

	/*
		DEVICEPOWER_FILTER_ON_NAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_FILTER_ON_NAME = 0x2000000

	/*
		DEVICEPOWER_FILTER_WAKEENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_FILTER_WAKEENABLED = 0x8000000

	/*
		DEVICEPOWER_FILTER_WAKEPROGRAMMABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_FILTER_WAKEPROGRAMMABLE = 0x4000000

	/*
		DEVICEPOWER_HARDWAREID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_HARDWAREID = 0x80000000

	/*
		DEVICEPOWER_SET_WAKEENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVICEPOWER_SET_WAKEENABLED = 0x1

	/*
		EFFECTIVE_POWER_MODE_V1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFFECTIVE_POWER_MODE_V1 = 0x1

	/*
		EFFECTIVE_POWER_MODE_V2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFFECTIVE_POWER_MODE_V2 = 0x2

	/*
		EMI_NAME_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EMI_NAME_MAX = 0x10

	/*
		EMI_VERSION_V1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EMI_VERSION_V1 = 0x1

	/*
		EMI_VERSION_V2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EMI_VERSION_V2 = 0x2

	/*
		EnableMultiBatteryDisplay from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EnableMultiBatteryDisplay = 0x2

	/*
		EnablePasswordLogon from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EnablePasswordLogon = 0x4

	/*
		EnableSysTrayBatteryMeter from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EnableSysTrayBatteryMeter = 0x1

	/*
		EnableVideoDimDisplay from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EnableVideoDimDisplay = 0x10

	/*
		EnableWakeOnRing from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EnableWakeOnRing = 0x8

	/*
		IOCTL_ACPI_GET_REAL_TIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_ACPI_GET_REAL_TIME = 0x294210

	/*
		IOCTL_ACPI_SET_REAL_TIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_ACPI_SET_REAL_TIME = 0x298214

	/*
		IOCTL_BATTERY_CHARGING_SOURCE_CHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_BATTERY_CHARGING_SOURCE_CHANGE = 0x294050

	/*
		IOCTL_BATTERY_QUERY_INFORMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_BATTERY_QUERY_INFORMATION = 0x294044

	/*
		IOCTL_BATTERY_QUERY_STATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_BATTERY_QUERY_STATUS = 0x29404c

	/*
		IOCTL_BATTERY_QUERY_TAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_BATTERY_QUERY_TAG = 0x294040

	/*
		IOCTL_BATTERY_SET_INFORMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_BATTERY_SET_INFORMATION = 0x298048

	/*
		IOCTL_EMI_GET_MEASUREMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_EMI_GET_MEASUREMENT = 0x22400c

	/*
		IOCTL_EMI_GET_METADATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_EMI_GET_METADATA = 0x224008

	/*
		IOCTL_EMI_GET_METADATA_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_EMI_GET_METADATA_SIZE = 0x224004

	/*
		IOCTL_EMI_GET_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_EMI_GET_VERSION = 0x224000

	/*
		IOCTL_GET_ACPI_TIME_AND_ALARM_CAPABILITIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_ACPI_TIME_AND_ALARM_CAPABILITIES = 0x29421c

	/*
		IOCTL_GET_PROCESSOR_OBJ_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_PROCESSOR_OBJ_INFO = 0x294180

	/*
		IOCTL_GET_SYS_BUTTON_CAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_SYS_BUTTON_CAPS = 0x294140

	/*
		IOCTL_GET_SYS_BUTTON_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_SYS_BUTTON_EVENT = 0x294144

	/*
		IOCTL_GET_WAKE_ALARM_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_WAKE_ALARM_POLICY = 0x29c20c

	/*
		IOCTL_GET_WAKE_ALARM_SYSTEM_POWERSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_WAKE_ALARM_SYSTEM_POWERSTATE = 0x294218

	/*
		IOCTL_GET_WAKE_ALARM_VALUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_GET_WAKE_ALARM_VALUE = 0x29c208

	/*
		IOCTL_NOTIFY_SWITCH_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_NOTIFY_SWITCH_EVENT = 0x294100

	/*
		IOCTL_QUERY_LID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_QUERY_LID = 0x2940c0

	/*
		IOCTL_RUN_ACTIVE_COOLING_METHOD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_RUN_ACTIVE_COOLING_METHOD = 0x298088

	/*
		IOCTL_SET_SYS_MESSAGE_INDICATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_SET_SYS_MESSAGE_INDICATOR = 0x2981c0

	/*
		IOCTL_SET_WAKE_ALARM_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_SET_WAKE_ALARM_POLICY = 0x298204

	/*
		IOCTL_SET_WAKE_ALARM_VALUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_SET_WAKE_ALARM_VALUE = 0x298200

	/*
		IOCTL_THERMAL_QUERY_INFORMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_THERMAL_QUERY_INFORMATION = 0x294080

	/*
		IOCTL_THERMAL_READ_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_THERMAL_READ_POLICY = 0x294094

	/*
		IOCTL_THERMAL_READ_TEMPERATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_THERMAL_READ_TEMPERATURE = 0x294090

	/*
		IOCTL_THERMAL_SET_COOLING_POLICY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_THERMAL_SET_COOLING_POLICY = 0x298084

	/*
		IOCTL_THERMAL_SET_PASSIVE_LIMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_THERMAL_SET_PASSIVE_LIMIT = 0x29808c

	/*
		MAX_ACTIVE_COOLING_LEVELS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_ACTIVE_COOLING_LEVELS = 0xa

	/*
		MAX_BATTERY_STRING_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_BATTERY_STRING_SIZE = 0x80

	/*
		PASSIVE_COOLING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PASSIVE_COOLING = 0x1

	/*
		PDCAP_S0_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_S0_SUPPORTED = 0x10000

	/*
		PDCAP_S1_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_S1_SUPPORTED = 0x20000

	/*
		PDCAP_S2_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_S2_SUPPORTED = 0x40000

	/*
		PDCAP_S3_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_S3_SUPPORTED = 0x80000

	/*
		PDCAP_S4_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_S4_SUPPORTED = 0x1000000

	/*
		PDCAP_S5_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_S5_SUPPORTED = 0x2000000

	/*
		PDCAP_WAKE_FROM_S0_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_WAKE_FROM_S0_SUPPORTED = 0x100000

	/*
		PDCAP_WAKE_FROM_S1_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_WAKE_FROM_S1_SUPPORTED = 0x200000

	/*
		PDCAP_WAKE_FROM_S2_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_WAKE_FROM_S2_SUPPORTED = 0x400000

	/*
		PDCAP_WAKE_FROM_S3_SUPPORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDCAP_WAKE_FROM_S3_SUPPORTED = 0x800000

	/*
		POWER_ATTRIBUTE_HIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POWER_ATTRIBUTE_HIDE = 0x1

	/*
		POWER_ATTRIBUTE_SHOW_AOAC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POWER_ATTRIBUTE_SHOW_AOAC = 0x2

	/*
		PPM_FIRMWARE_ACPI1C2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_ACPI1C2 = 0x1

	/*
		PPM_FIRMWARE_ACPI1C3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_ACPI1C3 = 0x2

	/*
		PPM_FIRMWARE_ACPI1TSTATES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_ACPI1TSTATES = 0x4

	/*
		PPM_FIRMWARE_CPC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_CPC = 0x40000

	/*
		PPM_FIRMWARE_CSD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_CSD = 0x10

	/*
		PPM_FIRMWARE_CST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_CST = 0x8

	/*
		PPM_FIRMWARE_LPI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_LPI = 0x80000

	/*
		PPM_FIRMWARE_OSC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_OSC = 0x10000

	/*
		PPM_FIRMWARE_PCCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PCCH = 0x4000

	/*
		PPM_FIRMWARE_PCCP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PCCP = 0x8000

	/*
		PPM_FIRMWARE_PCT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PCT = 0x20

	/*
		PPM_FIRMWARE_PDC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PDC = 0x20000

	/*
		PPM_FIRMWARE_PPC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PPC = 0x100

	/*
		PPM_FIRMWARE_PSD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PSD = 0x200

	/*
		PPM_FIRMWARE_PSS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PSS = 0x40

	/*
		PPM_FIRMWARE_PTC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_PTC = 0x400

	/*
		PPM_FIRMWARE_TPC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_TPC = 0x1000

	/*
		PPM_FIRMWARE_TSD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_TSD = 0x2000

	/*
		PPM_FIRMWARE_TSS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_TSS = 0x800

	/*
		PPM_FIRMWARE_XPSS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_FIRMWARE_XPSS = 0x80

	/*
		PPM_IDLE_IMPLEMENTATION_CSTATES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_IDLE_IMPLEMENTATION_CSTATES = 0x1

	/*
		PPM_IDLE_IMPLEMENTATION_LPISTATES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_IDLE_IMPLEMENTATION_LPISTATES = 0x4

	/*
		PPM_IDLE_IMPLEMENTATION_MICROPEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_IDLE_IMPLEMENTATION_MICROPEP = 0x3

	/*
		PPM_IDLE_IMPLEMENTATION_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_IDLE_IMPLEMENTATION_NONE = 0x0

	/*
		PPM_IDLE_IMPLEMENTATION_PEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_IDLE_IMPLEMENTATION_PEP = 0x2

	/*
		PPM_PERFORMANCE_IMPLEMENTATION_CPPC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_PERFORMANCE_IMPLEMENTATION_CPPC = 0x3

	/*
		PPM_PERFORMANCE_IMPLEMENTATION_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_PERFORMANCE_IMPLEMENTATION_NONE = 0x0

	/*
		PPM_PERFORMANCE_IMPLEMENTATION_PCCV1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_PERFORMANCE_IMPLEMENTATION_PCCV1 = 0x2

	/*
		PPM_PERFORMANCE_IMPLEMENTATION_PEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_PERFORMANCE_IMPLEMENTATION_PEP = 0x4

	/*
		PPM_PERFORMANCE_IMPLEMENTATION_PSTATES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPM_PERFORMANCE_IMPLEMENTATION_PSTATES = 0x1

	/*
		SYS_BUTTON_LID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_LID = 0x4

	/*
		SYS_BUTTON_LID_CHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_LID_CHANGED = 0x80000

	/*
		SYS_BUTTON_LID_CLOSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_LID_CLOSED = 0x20000

	/*
		SYS_BUTTON_LID_INITIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_LID_INITIAL = 0x40000

	/*
		SYS_BUTTON_LID_OPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_LID_OPEN = 0x10000

	/*
		SYS_BUTTON_LID_STATE_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_LID_STATE_MASK = 0x30000

	/*
		SYS_BUTTON_POWER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_POWER = 0x1

	/*
		SYS_BUTTON_SLEEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_SLEEP = 0x2

	/*
		SYS_BUTTON_WAKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYS_BUTTON_WAKE = 0x80000000

	/*
		THERMAL_COOLING_INTERFACE_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THERMAL_COOLING_INTERFACE_VERSION = 0x1

	/*
		THERMAL_DEVICE_INTERFACE_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THERMAL_DEVICE_INTERFACE_VERSION = 0x1

	/*
		THERMAL_EVENT_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THERMAL_EVENT_VERSION = 0x1

	/*
		THERMAL_POLICY_VERSION_1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THERMAL_POLICY_VERSION_1 = 0x1

	/*
		THERMAL_POLICY_VERSION_2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THERMAL_POLICY_VERSION_2 = 0x2

	/*
		TZ_ACTIVATION_REASON_CURRENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TZ_ACTIVATION_REASON_CURRENT = 0x2

	/*
		TZ_ACTIVATION_REASON_THERMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TZ_ACTIVATION_REASON_THERMAL = 0x1

	/*
		UNKNOWN_CAPACITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNKNOWN_CAPACITY = 0xffffffff

	/*
		UNKNOWN_CURRENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNKNOWN_CURRENT = 0xffffffff

	/*
		UNKNOWN_RATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNKNOWN_RATE = 0x80000000

	/*
		UNKNOWN_VOLTAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNKNOWN_VOLTAGE = 0xffffffff
)

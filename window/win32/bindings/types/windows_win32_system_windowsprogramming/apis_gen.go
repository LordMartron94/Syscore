//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_system_windowsprogramming

const (
	/*
		AADBE_ADD_ENTRY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AADBE_ADD_ENTRY = 0x1

	/*
		AADBE_DEL_ENTRY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AADBE_DEL_ENTRY = 0x2

	/*
		ACTCTX_FLAG_APPLICATION_NAME_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_APPLICATION_NAME_VALID = 0x20

	/*
		ACTCTX_FLAG_ASSEMBLY_DIRECTORY_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_ASSEMBLY_DIRECTORY_VALID = 0x4

	/*
		ACTCTX_FLAG_HMODULE_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_HMODULE_VALID = 0x80

	/*
		ACTCTX_FLAG_LANGID_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_LANGID_VALID = 0x2

	/*
		ACTCTX_FLAG_PROCESSOR_ARCHITECTURE_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_PROCESSOR_ARCHITECTURE_VALID = 0x1

	/*
		ACTCTX_FLAG_RESOURCE_NAME_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_RESOURCE_NAME_VALID = 0x8

	/*
		ACTCTX_FLAG_SET_PROCESS_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_SET_PROCESS_DEFAULT = 0x10

	/*
		ACTCTX_FLAG_SOURCE_IS_ASSEMBLYREF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTCTX_FLAG_SOURCE_IS_ASSEMBLYREF = 0x40

	/*
		ACTIVATION_CONTEXT_BASIC_INFORMATION_DEFINED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACTIVATION_CONTEXT_BASIC_INFORMATION_DEFINED = 0x1

	/*
		AC_LINE_BACKUP_POWER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AC_LINE_BACKUP_POWER = 0x2

	/*
		AC_LINE_OFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AC_LINE_OFFLINE = 0x0

	/*
		AC_LINE_ONLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AC_LINE_ONLINE = 0x1

	/*
		AC_LINE_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AC_LINE_UNKNOWN = 0xff

	/*
		ADN_DEL_IF_EMPTY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ADN_DEL_IF_EMPTY = 0x1

	/*
		ADN_DEL_UNC_PATHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ADN_DEL_UNC_PATHS = 0x8

	/*
		ADN_DONT_DEL_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ADN_DONT_DEL_DIR = 0x4

	/*
		ADN_DONT_DEL_SUBDIRS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ADN_DONT_DEL_SUBDIRS = 0x2

	/*
		AFSR_BACKNEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_BACKNEW = 0x2

	/*
		AFSR_EXTRAINCREFCNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_EXTRAINCREFCNT = 0x800

	/*
		AFSR_NODELETENEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_NODELETENEW = 0x4

	/*
		AFSR_NOMESSAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_NOMESSAGES = 0x8

	/*
		AFSR_NOPROGRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_NOPROGRESS = 0x10

	/*
		AFSR_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_RESTORE = 0x1

	/*
		AFSR_UPDREFCNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_UPDREFCNT = 0x200

	/*
		AFSR_USEREFCNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AFSR_USEREFCNT = 0x400

	/*
		AIF_FORCE_FILE_IN_USE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_FORCE_FILE_IN_USE = 0x8

	/*
		AIF_NOLANGUAGECHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_NOLANGUAGECHECK = 0x10000000

	/*
		AIF_NOOVERWRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_NOOVERWRITE = 0x10

	/*
		AIF_NOSKIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_NOSKIP = 0x2

	/*
		AIF_NOVERSIONCHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_NOVERSIONCHECK = 0x4

	/*
		AIF_NO_VERSION_DIALOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_NO_VERSION_DIALOG = 0x20

	/*
		AIF_QUIET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_QUIET = 0x20000000

	/*
		AIF_REPLACEONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_REPLACEONLY = 0x400

	/*
		AIF_WARNIFSKIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AIF_WARNIFSKIP = 0x1

	/*
		ALINF_BKINSTALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_BKINSTALL = 0x20

	/*
		ALINF_CHECKBKDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_CHECKBKDATA = 0x80

	/*
		ALINF_DELAYREGISTEROCX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_DELAYREGISTEROCX = 0x200

	/*
		ALINF_NGCONV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_NGCONV = 0x8

	/*
		ALINF_QUIET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_QUIET = 0x4

	/*
		ALINF_ROLLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_ROLLBACK = 0x40

	/*
		ALINF_ROLLBKDOALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_ROLLBKDOALL = 0x100

	/*
		ALINF_UPDHLPDLLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ALINF_UPDHLPDLLS = 0x10

	/*
		ARSR_NOMESSAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARSR_NOMESSAGES = 0x8

	/*
		ARSR_REGSECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARSR_REGSECTION = 0x80

	/*
		ARSR_REMOVREGBKDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARSR_REMOVREGBKDATA = 0x1000

	/*
		ARSR_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARSR_RESTORE = 0x1

	/*
		ATOM_FLAG_GLOBAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ATOM_FLAG_GLOBAL = 0x2

	/*
		AT_ARP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AT_ARP = 0x280

	/*
		AT_NULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	AT_NULL = 0x282

	/*
		BACKUP_GHOSTED_FILE_EXTENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BACKUP_GHOSTED_FILE_EXTENTS = 0xb

	/*
		BACKUP_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BACKUP_INVALID = 0x0

	/*
		BASE_SEARCH_PATH_DISABLE_SAFE_SEARCHMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BASE_SEARCH_PATH_DISABLE_SAFE_SEARCHMODE = 0x10000

	/*
		BASE_SEARCH_PATH_ENABLE_SAFE_SEARCHMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BASE_SEARCH_PATH_ENABLE_SAFE_SEARCHMODE = 0x1

	/*
		BASE_SEARCH_PATH_PERMANENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BASE_SEARCH_PATH_PERMANENT = 0x8000

	/*
		BATTERY_FLAG_CHARGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_FLAG_CHARGING = 0x8

	/*
		BATTERY_FLAG_CRITICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_FLAG_CRITICAL = 0x4

	/*
		BATTERY_FLAG_HIGH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_FLAG_HIGH = 0x1

	/*
		BATTERY_FLAG_LOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_FLAG_LOW = 0x2

	/*
		BATTERY_FLAG_NO_BATTERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_FLAG_NO_BATTERY = 0x80

	/*
		BATTERY_FLAG_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_FLAG_UNKNOWN = 0xff

	/*
		BATTERY_LIFE_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_LIFE_UNKNOWN = 0xffffffff

	/*
		BATTERY_PERCENTAGE_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BATTERY_PERCENTAGE_UNKNOWN = 0xff

	/*
		BAUD_075 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_075 = 0x1

	/*
		BAUD_110 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_110 = 0x2

	/*
		BAUD_115200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_115200 = 0x20000

	/*
		BAUD_1200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_1200 = 0x40

	/*
		BAUD_128K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_128K = 0x10000

	/*
		BAUD_134_5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_134_5 = 0x4

	/*
		BAUD_14400 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_14400 = 0x1000

	/*
		BAUD_150 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_150 = 0x8

	/*
		BAUD_1800 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_1800 = 0x80

	/*
		BAUD_19200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_19200 = 0x2000

	/*
		BAUD_2400 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_2400 = 0x100

	/*
		BAUD_300 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_300 = 0x10

	/*
		BAUD_38400 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_38400 = 0x4000

	/*
		BAUD_4800 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_4800 = 0x200

	/*
		BAUD_56K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_56K = 0x8000

	/*
		BAUD_57600 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_57600 = 0x40000

	/*
		BAUD_600 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_600 = 0x20

	/*
		BAUD_7200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_7200 = 0x400

	/*
		BAUD_9600 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_9600 = 0x800

	/*
		BAUD_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BAUD_USER = 0x10000000

	/*
		CBR_110 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_110 = 0x6e

	/*
		CBR_115200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_115200 = 0x1c200

	/*
		CBR_1200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_1200 = 0x4b0

	/*
		CBR_128000 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_128000 = 0x1f400

	/*
		CBR_14400 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_14400 = 0x3840

	/*
		CBR_19200 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_19200 = 0x4b00

	/*
		CBR_2400 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_2400 = 0x960

	/*
		CBR_256000 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_256000 = 0x3e800

	/*
		CBR_300 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_300 = 0x12c

	/*
		CBR_38400 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_38400 = 0x9600

	/*
		CBR_4800 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_4800 = 0x12c0

	/*
		CBR_56000 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_56000 = 0xdac0

	/*
		CBR_57600 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_57600 = 0xe100

	/*
		CBR_600 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_600 = 0x258

	/*
		CBR_9600 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBR_9600 = 0x2580

	/*
		CE_DNS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CE_DNS = 0x800

	/*
		CE_IOE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CE_IOE = 0x400

	/*
		CE_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CE_MODE = 0x8000

	/*
		CE_OOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CE_OOP = 0x1000

	/*
		CE_PTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CE_PTO = 0x200

	/*
		CE_TXFULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CE_TXFULL = 0x100

	/*
		CL_NL_IP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CL_NL_IP = 0x303

	/*
		CL_NL_IPX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CL_NL_IPX = 0x301

	/*
		CL_TL_NBF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CL_TL_NBF = 0x401

	/*
		CL_TL_UDP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CL_TL_UDP = 0x403

	/*
		CODEINTEGRITY_OPTION_DEBUGMODE_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_DEBUGMODE_ENABLED = 0x80

	/*
		CODEINTEGRITY_OPTION_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_ENABLED = 0x1

	/*
		CODEINTEGRITY_OPTION_FLIGHTING_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_FLIGHTING_ENABLED = 0x200

	/*
		CODEINTEGRITY_OPTION_FLIGHT_BUILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_FLIGHT_BUILD = 0x100

	/*
		CODEINTEGRITY_OPTION_HVCI_IUM_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_HVCI_IUM_ENABLED = 0x2000

	/*
		CODEINTEGRITY_OPTION_HVCI_KMCI_AUDITMODE_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_HVCI_KMCI_AUDITMODE_ENABLED = 0x800

	/*
		CODEINTEGRITY_OPTION_HVCI_KMCI_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_HVCI_KMCI_ENABLED = 0x400

	/*
		CODEINTEGRITY_OPTION_HVCI_KMCI_STRICTMODE_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_HVCI_KMCI_STRICTMODE_ENABLED = 0x1000

	/*
		CODEINTEGRITY_OPTION_PREPRODUCTION_BUILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_PREPRODUCTION_BUILD = 0x40

	/*
		CODEINTEGRITY_OPTION_TESTSIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_TESTSIGN = 0x2

	/*
		CODEINTEGRITY_OPTION_TEST_BUILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_TEST_BUILD = 0x20

	/*
		CODEINTEGRITY_OPTION_UMCI_AUDITMODE_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_UMCI_AUDITMODE_ENABLED = 0x8

	/*
		CODEINTEGRITY_OPTION_UMCI_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_UMCI_ENABLED = 0x4

	/*
		CODEINTEGRITY_OPTION_UMCI_EXCLUSIONPATHS_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CODEINTEGRITY_OPTION_UMCI_EXCLUSIONPATHS_ENABLED = 0x10

	/*
		COMMPROP_INITIALIZED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COMMPROP_INITIALIZED = 0xe73cf52e

	/*
		CONTEXT_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONTEXT_SIZE = 0x10

	/*
		COPYFILE2_IO_CYCLE_SIZE_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPYFILE2_IO_CYCLE_SIZE_MAX = 0x40000000

	/*
		COPYFILE2_IO_CYCLE_SIZE_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPYFILE2_IO_CYCLE_SIZE_MIN = 0x1000

	/*
		COPYFILE2_IO_RATE_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPYFILE2_IO_RATE_MIN = 0x200

	/*
		COPYFILE2_MESSAGE_COPY_OFFLOAD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPYFILE2_MESSAGE_COPY_OFFLOAD = 0x1

	/*
		COPY_FILE2_V2_DONT_COPY_JUNCTIONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE2_V2_DONT_COPY_JUNCTIONS = 0x1

	/*
		COPY_FILE2_V2_VALID_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE2_V2_VALID_FLAGS = 0x1

	/*
		COPY_FILE_ALLOW_DECRYPTED_DESTINATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_ALLOW_DECRYPTED_DESTINATION = 0x8

	/*
		COPY_FILE_COPY_SYMLINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_COPY_SYMLINK = 0x800

	/*
		COPY_FILE_DIRECTORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_DIRECTORY = 0x80

	/*
		COPY_FILE_DISABLE_PRE_ALLOCATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_DISABLE_PRE_ALLOCATION = 0x4000000

	/*
		COPY_FILE_DONT_REQUEST_DEST_WRITE_DAC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_DONT_REQUEST_DEST_WRITE_DAC = 0x2000000

	/*
		COPY_FILE_ENABLE_LOW_FREE_SPACE_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_ENABLE_LOW_FREE_SPACE_MODE = 0x8000000

	/*
		COPY_FILE_ENABLE_SPARSE_COPY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_ENABLE_SPARSE_COPY = 0x20000000

	/*
		COPY_FILE_FAIL_IF_EXISTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_FAIL_IF_EXISTS = 0x1

	/*
		COPY_FILE_IGNORE_EDP_BLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_IGNORE_EDP_BLOCK = 0x400000

	/*
		COPY_FILE_IGNORE_SOURCE_ENCRYPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_IGNORE_SOURCE_ENCRYPTION = 0x800000

	/*
		COPY_FILE_NO_BUFFERING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_NO_BUFFERING = 0x1000

	/*
		COPY_FILE_NO_OFFLOAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_NO_OFFLOAD = 0x40000

	/*
		COPY_FILE_OPEN_AND_COPY_REPARSE_POINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_OPEN_AND_COPY_REPARSE_POINT = 0x200000

	/*
		COPY_FILE_OPEN_SOURCE_FOR_WRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_OPEN_SOURCE_FOR_WRITE = 0x4

	/*
		COPY_FILE_REQUEST_COMPRESSED_TRAFFIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_REQUEST_COMPRESSED_TRAFFIC = 0x10000000

	/*
		COPY_FILE_REQUEST_SECURITY_PRIVILEGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_REQUEST_SECURITY_PRIVILEGES = 0x2000

	/*
		COPY_FILE_RESTARTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_RESTARTABLE = 0x2

	/*
		COPY_FILE_RESUME_FROM_PAUSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_RESUME_FROM_PAUSE = 0x4000

	/*
		COPY_FILE_SKIP_ALTERNATE_STREAMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COPY_FILE_SKIP_ALTERNATE_STREAMS = 0x8000

	/*
		CO_TL_NBF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CO_TL_NBF = 0x400

	/*
		CO_TL_SPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CO_TL_SPP = 0x406

	/*
		CO_TL_SPX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CO_TL_SPX = 0x402

	/*
		CO_TL_TCP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CO_TL_TCP = 0x404

	/*
		CP_DIRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_DIRECT = 0x2

	/*
		CP_HWND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_HWND = 0x0

	/*
		CP_LEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_LEVEL = 0x3

	/*
		CP_OPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_OPEN = 0x1

	/*
		CREATE_FOR_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATE_FOR_DIR = 0x2

	/*
		CREATE_FOR_IMPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATE_FOR_IMPORT = 0x1

	/*
		CRITICAL_SECTION_NO_DEBUG_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CRITICAL_SECTION_NO_DEBUG_INFO = 0x1000000

	/*
		DCICREATEOFFSCREENSURFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCICREATEOFFSCREENSURFACE = 0x2

	/*
		DCICREATEOVERLAYSURFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCICREATEOVERLAYSURFACE = 0x3

	/*
		DCICREATEPRIMARYSURFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCICREATEPRIMARYSURFACE = 0x1

	/*
		DCIENUMSURFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCIENUMSURFACE = 0x4

	/*
		DCIESCAPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCIESCAPE = 0x5

	/*
		DCI_1632_ACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_1632_ACCESS = 0x40

	/*
		DCI_ASYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ASYNC = 0x400

	/*
		DCI_CANOVERLAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_CANOVERLAY = 0x10000

	/*
		DCI_CAN_STRETCHX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_CAN_STRETCHX = 0x1000

	/*
		DCI_CAN_STRETCHXN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_CAN_STRETCHXN = 0x4000

	/*
		DCI_CAN_STRETCHY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_CAN_STRETCHY = 0x2000

	/*
		DCI_CAN_STRETCHYN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_CAN_STRETCHYN = 0x8000

	/*
		DCI_CHROMAKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_CHROMAKEY = 0x20

	/*
		DCI_DWORDALIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_DWORDALIGN = 0x100

	/*
		DCI_DWORDSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_DWORDSIZE = 0x80

	/*
		DCI_ERR_CURRENTLYNOTAVAIL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_CURRENTLYNOTAVAIL = -5

	/*
		DCI_ERR_HEIGHTALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_HEIGHTALIGN = -21

	/*
		DCI_ERR_INVALIDCLIPLIST from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_INVALIDCLIPLIST = -15

	/*
		DCI_ERR_INVALIDPOSITION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_INVALIDPOSITION = -13

	/*
		DCI_ERR_INVALIDRECT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_INVALIDRECT = -6

	/*
		DCI_ERR_INVALIDSTRETCH from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_INVALIDSTRETCH = -14

	/*
		DCI_ERR_OUTOFMEMORY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_OUTOFMEMORY = -12

	/*
		DCI_ERR_SURFACEISOBSCURED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_SURFACEISOBSCURED = -16

	/*
		DCI_ERR_TOOBIGHEIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_TOOBIGHEIGHT = -9

	/*
		DCI_ERR_TOOBIGSIZE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_TOOBIGSIZE = -11

	/*
		DCI_ERR_TOOBIGWIDTH from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_TOOBIGWIDTH = -10

	/*
		DCI_ERR_UNSUPPORTEDFORMAT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_UNSUPPORTEDFORMAT = -7

	/*
		DCI_ERR_UNSUPPORTEDMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_UNSUPPORTEDMASK = -8

	/*
		DCI_ERR_WIDTHALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_WIDTHALIGN = -20

	/*
		DCI_ERR_XALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_XALIGN = -17

	/*
		DCI_ERR_XYALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_XYALIGN = -19

	/*
		DCI_ERR_YALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_ERR_YALIGN = -18

	/*
		DCI_FAIL_GENERIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_FAIL_GENERIC = -1

	/*
		DCI_FAIL_INVALIDSURFACE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_FAIL_INVALIDSURFACE = -3

	/*
		DCI_FAIL_UNSUPPORTED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_FAIL_UNSUPPORTED = -4

	/*
		DCI_FAIL_UNSUPPORTEDVERSION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_FAIL_UNSUPPORTEDVERSION = -2

	/*
		DCI_OFFSCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_OFFSCREEN = 0x1

	/*
		DCI_OK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_OK = 0x0

	/*
		DCI_OVERLAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_OVERLAY = 0x2

	/*
		DCI_PRIMARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_PRIMARY = 0x0

	/*
		DCI_STATUS_CHROMAKEYCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_STATUS_CHROMAKEYCHANGED = 0x10

	/*
		DCI_STATUS_FORMATCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_STATUS_FORMATCHANGED = 0x4

	/*
		DCI_STATUS_POINTERCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_STATUS_POINTERCHANGED = 0x1

	/*
		DCI_STATUS_STRIDECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_STATUS_STRIDECHANGED = 0x2

	/*
		DCI_STATUS_SURFACEINFOCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_STATUS_SURFACEINFOCHANGED = 0x8

	/*
		DCI_STATUS_WASSTILLDRAWING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_STATUS_WASSTILLDRAWING = 0x20

	/*
		DCI_SURFACE_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_SURFACE_TYPE = 0xf

	/*
		DCI_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_VERSION = 0x100

	/*
		DCI_VISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_VISIBLE = 0x10

	/*
		DCI_WRITEONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCI_WRITEONLY = 0x200

	/*
		DEACTIVATE_ACTCTX_FLAG_FORCE_EARLY_DEACTIVATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEACTIVATE_ACTCTX_FLAG_FORCE_EARLY_DEACTIVATION = 0x1

	/*
		DELAYLOAD_GPA_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELAYLOAD_GPA_FAILURE = 0x4

	/*
		DELETE_BROWSING_HISTORY_COOKIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_COOKIES = 0x2

	/*
		DELETE_BROWSING_HISTORY_DOWNLOADHISTORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_DOWNLOADHISTORY = 0x40

	/*
		DELETE_BROWSING_HISTORY_FORMDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_FORMDATA = 0x8

	/*
		DELETE_BROWSING_HISTORY_HISTORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_HISTORY = 0x1

	/*
		DELETE_BROWSING_HISTORY_PASSWORDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_PASSWORDS = 0x10

	/*
		DELETE_BROWSING_HISTORY_PRESERVEFAVORITES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_PRESERVEFAVORITES = 0x20

	/*
		DELETE_BROWSING_HISTORY_TIF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DELETE_BROWSING_HISTORY_TIF = 0x4

	/*
		DOCKINFO_DOCKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOCKINFO_DOCKED = 0x2

	/*
		DOCKINFO_UNDOCKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOCKINFO_UNDOCKED = 0x1

	/*
		DOCKINFO_USER_SUPPLIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOCKINFO_USER_SUPPLIED = 0x4

	/*
		DRIVE_CDROM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_CDROM = 0x5

	/*
		DRIVE_FIXED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_FIXED = 0x3

	/*
		DRIVE_NO_ROOT_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_NO_ROOT_DIR = 0x1

	/*
		DRIVE_RAMDISK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_RAMDISK = 0x6

	/*
		DRIVE_REMOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_REMOTE = 0x4

	/*
		DRIVE_REMOVABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_REMOVABLE = 0x2

	/*
		DRIVE_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRIVE_UNKNOWN = 0x0

	/*
		DTR_CONTROL_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTR_CONTROL_DISABLE = 0x0

	/*
		DTR_CONTROL_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTR_CONTROL_ENABLE = 0x1

	/*
		DTR_CONTROL_HANDSHAKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTR_CONTROL_HANDSHAKE = 0x2

	/*
		EFSRPC_SECURE_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFSRPC_SECURE_ONLY = 0x8

	/*
		EFS_DROP_ALTERNATE_STREAMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_DROP_ALTERNATE_STREAMS = 0x10

	/*
		EFS_USE_RECOVERY_KEYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_USE_RECOVERY_KEYS = 0x1

	/*
		ENTITY_LIST_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENTITY_LIST_ID = 0x0

	/*
		ENTITY_TYPE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENTITY_TYPE_ID = 0x1

	/*
		ER_ICMP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ER_ICMP = 0x380

	/*
		EVENTLOG_FULL_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENTLOG_FULL_INFO = 0x0

	/*
		FAIL_FAST_GENERATE_EXCEPTION_ADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FAIL_FAST_GENERATE_EXCEPTION_ADDRESS = 0x1

	/*
		FAIL_FAST_NO_HARD_ERROR_DLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FAIL_FAST_NO_HARD_ERROR_DLG = 0x2

	/*
		FIBER_FLAG_FLOAT_SWITCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FIBER_FLAG_FLOAT_SWITCH = 0x1

	/*
		FILE_CREATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_CREATED = 0x2

	/*
		FILE_DIR_DISALLOWED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_DIR_DISALLOWED = 0x9

	/*
		FILE_DOES_NOT_EXIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_DOES_NOT_EXIST = 0x5

	/*
		FILE_ENCRYPTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_ENCRYPTABLE = 0x0

	/*
		FILE_EXISTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_EXISTS = 0x4

	/*
		FILE_FLAG_IGNORE_IMPERSONATED_DEVICEMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_FLAG_IGNORE_IMPERSONATED_DEVICEMAP = 0x20000

	/*
		FILE_FLAG_OPEN_REQUIRING_OPLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_FLAG_OPEN_REQUIRING_OPLOCK = 0x40000

	/*
		FILE_IS_ENCRYPTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_IS_ENCRYPTED = 0x1

	/*
		FILE_MAXIMUM_DISPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_MAXIMUM_DISPOSITION = 0x5

	/*
		FILE_NO_COMPRESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_NO_COMPRESSION = 0x8000

	/*
		FILE_OPENED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_OPENED = 0x1

	/*
		FILE_OPEN_NO_RECALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_OPEN_NO_RECALL = 0x400000

	/*
		FILE_OPEN_REMOTE_INSTANCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_OPEN_REMOTE_INSTANCE = 0x400

	/*
		FILE_OVERWRITTEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_OVERWRITTEN = 0x3

	/*
		FILE_READ_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_READ_ONLY = 0x8

	/*
		FILE_RENAME_FLAG_POSIX_SEMANTICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_RENAME_FLAG_POSIX_SEMANTICS = 0x2

	/*
		FILE_RENAME_FLAG_REPLACE_IF_EXISTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_RENAME_FLAG_REPLACE_IF_EXISTS = 0x1

	/*
		FILE_RENAME_FLAG_SUPPRESS_PIN_STATE_INHERITANCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_RENAME_FLAG_SUPPRESS_PIN_STATE_INHERITANCE = 0x4

	/*
		FILE_ROOT_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_ROOT_DIR = 0x3

	/*
		FILE_SKIP_COMPLETION_PORT_ON_SUCCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_SKIP_COMPLETION_PORT_ON_SUCCESS = 0x1

	/*
		FILE_SKIP_SET_EVENT_ON_HANDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_SKIP_SET_EVENT_ON_HANDLE = 0x2

	/*
		FILE_SUPERSEDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_SUPERSEDED = 0x0

	/*
		FILE_SYSTEM_ATTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_SYSTEM_ATTR = 0x2

	/*
		FILE_SYSTEM_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_SYSTEM_DIR = 0x4

	/*
		FILE_SYSTEM_NOT_SUPPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_SYSTEM_NOT_SUPPORT = 0x6

	/*
		FILE_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_UNKNOWN = 0x5

	/*
		FILE_USER_DISALLOWED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_USER_DISALLOWED = 0x7

	/*
		FILE_VALID_MAILSLOT_OPTION_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_VALID_MAILSLOT_OPTION_FLAGS = 0x32

	/*
		FILE_VALID_OPTION_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_VALID_OPTION_FLAGS = 0xffffff

	/*
		FILE_VALID_PIPE_OPTION_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_VALID_PIPE_OPTION_FLAGS = 0x32

	/*
		FILE_VALID_SET_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_VALID_SET_FLAGS = 0x36

	/*
		FIND_ACTCTX_SECTION_KEY_RETURN_ASSEMBLY_METADATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FIND_ACTCTX_SECTION_KEY_RETURN_ASSEMBLY_METADATA = 0x4

	/*
		FIND_ACTCTX_SECTION_KEY_RETURN_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FIND_ACTCTX_SECTION_KEY_RETURN_FLAGS = 0x2

	/*
		FIND_ACTCTX_SECTION_KEY_RETURN_HACTCTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FIND_ACTCTX_SECTION_KEY_RETURN_HACTCTX = 0x1

	/*
		FORMAT_MESSAGE_MAX_WIDTH_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FORMAT_MESSAGE_MAX_WIDTH_MASK = 0xff

	/*
		FS_CASE_IS_PRESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_CASE_IS_PRESERVED = 0x2

	/*
		FS_CASE_SENSITIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_CASE_SENSITIVE = 0x1

	/*
		FS_FILE_COMPRESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_FILE_COMPRESSION = 0x10

	/*
		FS_FILE_ENCRYPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_FILE_ENCRYPTION = 0x20000

	/*
		FS_PERSISTENT_ACLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_PERSISTENT_ACLS = 0x8

	/*
		FS_UNICODE_STORED_ON_DISK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_UNICODE_STORED_ON_DISK = 0x4

	/*
		FS_VOL_IS_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FS_VOL_IS_COMPRESSED = 0x8000

	/*
		GMEM_DDESHARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_DDESHARE = 0x2000

	/*
		GMEM_DISCARDABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_DISCARDABLE = 0x100

	/*
		GMEM_DISCARDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_DISCARDED = 0x4000

	/*
		GMEM_INVALID_HANDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_INVALID_HANDLE = 0x8000

	/*
		GMEM_LOCKCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_LOCKCOUNT = 0xff

	/*
		GMEM_LOWER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_LOWER = 0x1000

	/*
		GMEM_MODIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_MODIFY = 0x80

	/*
		GMEM_NOCOMPACT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_NOCOMPACT = 0x10

	/*
		GMEM_NODISCARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_NODISCARD = 0x20

	/*
		GMEM_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_NOTIFY = 0x4000

	/*
		GMEM_NOT_BANKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_NOT_BANKED = 0x1000

	/*
		GMEM_SHARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_SHARE = 0x2000

	/*
		GMEM_VALID_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMEM_VALID_FLAGS = 0x7f72

	/*
		HANJA_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HANJA_WINDOW = 0x2

	/*
		HINSTANCE_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HINSTANCE_ERROR = 0x20

	/*
		HW_PROFILE_GUIDLEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HW_PROFILE_GUIDLEN = 0x27

	/*
		IE4_BACKNEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_BACKNEW = 0x2

	/*
		IE4_EXTRAINCREFCNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_EXTRAINCREFCNT = 0x800

	/*
		IE4_FRDOALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_FRDOALL = 0x100

	/*
		IE4_NODELETENEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_NODELETENEW = 0x4

	/*
		IE4_NOENUMKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_NOENUMKEY = 0x20

	/*
		IE4_NOMESSAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_NOMESSAGES = 0x8

	/*
		IE4_NOPROGRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_NOPROGRESS = 0x10

	/*
		IE4_NO_CRC_MAPPING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_NO_CRC_MAPPING = 0x40

	/*
		IE4_REGSECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_REGSECTION = 0x80

	/*
		IE4_REMOVREGBKDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_REMOVREGBKDATA = 0x1000

	/*
		IE4_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_RESTORE = 0x1

	/*
		IE4_UPDREFCNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_UPDREFCNT = 0x200

	/*
		IE4_USEREFCNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE4_USEREFCNT = 0x400

	/*
		IE_BADID from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_BADID = -1

	/*
		IE_BAUDRATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_BAUDRATE = -12

	/*
		IE_BYTESIZE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_BYTESIZE = -11

	/*
		IE_DEFAULT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_DEFAULT = -5

	/*
		IE_HARDWARE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_HARDWARE = -10

	/*
		IE_MEMORY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_MEMORY = -4

	/*
		IE_NOPEN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_NOPEN = -3

	/*
		IE_OPEN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IE_OPEN = -2

	/*
		IF_GENERIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IF_GENERIC = 0x200

	/*
		IF_MIB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IF_MIB = 0x202

	/*
		IGNORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IGNORE = 0x0

	/*
		IMEA_INIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMEA_INIT = 0x1

	/*
		IMEA_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMEA_NEXT = 0x2

	/*
		IMEA_PREV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMEA_PREV = 0x3

	/*
		IME_BANJAtoJUNJA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_BANJAtoJUNJA = 0x13

	/*
		IME_ENABLE_CONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_ENABLE_CONVERT = 0x2

	/*
		IME_ENTERWORDREGISTERMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_ENTERWORDREGISTERMODE = 0x18

	/*
		IME_GETCONVERSIONMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_GETCONVERSIONMODE = 0x11

	/*
		IME_GETIMECAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_GETIMECAPS = 0x3

	/*
		IME_GETOPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_GETOPEN = 0x5

	/*
		IME_GETVERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_GETVERSION = 0x7

	/*
		IME_JOHABtoKS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_JOHABtoKS = 0x15

	/*
		IME_JUNJAtoBANJA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_JUNJAtoBANJA = 0x14

	/*
		IME_KStoJOHAB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_KStoJOHAB = 0x16

	/*
		IME_MAXPROCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MAXPROCESS = 0x20

	/*
		IME_MODE_ALPHANUMERIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_ALPHANUMERIC = 0x1

	/*
		IME_MODE_CODEINPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_CODEINPUT = 0x80

	/*
		IME_MODE_DBCSCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_DBCSCHAR = 0x10

	/*
		IME_MODE_HANJACONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_HANJACONVERT = 0x4

	/*
		IME_MODE_HIRAGANA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_HIRAGANA = 0x4

	/*
		IME_MODE_KATAKANA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_KATAKANA = 0x2

	/*
		IME_MODE_NOCODEINPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_NOCODEINPUT = 0x100

	/*
		IME_MODE_NOROMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_NOROMAN = 0x40

	/*
		IME_MODE_ROMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_ROMAN = 0x20

	/*
		IME_MODE_SBCSCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MODE_SBCSCHAR = 0x2

	/*
		IME_MOVEIMEWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_MOVEIMEWINDOW = 0x8

	/*
		IME_REQUEST_CONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_REQUEST_CONVERT = 0x1

	/*
		IME_RS_DISKERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_DISKERROR = 0xe

	/*
		IME_RS_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_ERROR = 0x1

	/*
		IME_RS_ILLEGAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_ILLEGAL = 0x6

	/*
		IME_RS_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_INVALID = 0x11

	/*
		IME_RS_NEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_NEST = 0x12

	/*
		IME_RS_NOIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_NOIME = 0x2

	/*
		IME_RS_NOROOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_NOROOM = 0xa

	/*
		IME_RS_NOTFOUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_NOTFOUND = 0x7

	/*
		IME_RS_SYSTEMMODAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_SYSTEMMODAL = 0x13

	/*
		IME_RS_TOOLONG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_RS_TOOLONG = 0x5

	/*
		IME_SENDVKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_SENDVKEY = 0x13

	/*
		IME_SETCONVERSIONFONTEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_SETCONVERSIONFONTEX = 0x19

	/*
		IME_SETCONVERSIONMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_SETCONVERSIONMODE = 0x10

	/*
		IME_SETCONVERSIONWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_SETCONVERSIONWINDOW = 0x8

	/*
		IME_SETOPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_SETOPEN = 0x4

	/*
		IME_SET_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IME_SET_MODE = 0x12

	/*
		INFO_CLASS_GENERIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFO_CLASS_GENERIC = 0x100

	/*
		INFO_CLASS_IMPLEMENTATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFO_CLASS_IMPLEMENTATION = 0x300

	/*
		INFO_CLASS_PROTOCOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFO_CLASS_PROTOCOL = 0x200

	/*
		INFO_TYPE_ADDRESS_OBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFO_TYPE_ADDRESS_OBJECT = 0x200

	/*
		INFO_TYPE_CONNECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFO_TYPE_CONNECTION = 0x300

	/*
		INFO_TYPE_PROVIDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFO_TYPE_PROVIDER = 0x100

	/*
		INTERIM_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INTERIM_WINDOW = 0x0

	/*
		INVALID_ENTITY_INSTANCE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INVALID_ENTITY_INSTANCE = -1

	/*
		IOCTL_TDI_TL_IO_CONTROL_ENDPOINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_TDI_TL_IO_CONTROL_ENDPOINT = 0x210038

	/*
		IR_CHANGECONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_CHANGECONVERT = 0x121

	/*
		IR_CLOSECONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_CLOSECONVERT = 0x122

	/*
		IR_DBCSCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_DBCSCHAR = 0x160

	/*
		IR_FULLCONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_FULLCONVERT = 0x123

	/*
		IR_IMESELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_IMESELECT = 0x130

	/*
		IR_MODEINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_MODEINFO = 0x190

	/*
		IR_OPENCONVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_OPENCONVERT = 0x120

	/*
		IR_STRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_STRING = 0x140

	/*
		IR_STRINGEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_STRINGEND = 0x101

	/*
		IR_STRINGEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_STRINGEX = 0x180

	/*
		IR_STRINGSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_STRINGSTART = 0x100

	/*
		IR_UNDETERMINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IR_UNDETERMINE = 0x170

	/*
		LIS_NOGRPCONV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LIS_NOGRPCONV = 0x2

	/*
		LIS_QUIET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LIS_QUIET = 0x1

	/*
		LOGON32_PROVIDER_VIRTUAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOGON32_PROVIDER_VIRTUAL = 0x4

	/*
		LOGON32_PROVIDER_WINNT35 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOGON32_PROVIDER_WINNT35 = 0x1

	/*
		LOGON_ZERO_PASSWORD_BUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOGON_ZERO_PASSWORD_BUFFER = 0x80000000

	/*
		LPTx from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LPTx = 0x80

	/*
		MAXINTATOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXINTATOM = 0xc000

	/*
		MAX_COMPUTERNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_COMPUTERNAME_LENGTH = 0xf

	/*
		MAX_TDI_ENTITIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_TDI_ENTITIES = 0x1000

	/*
		MCW_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCW_DEFAULT = 0x0

	/*
		MCW_HIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCW_HIDDEN = 0x10

	/*
		MCW_RECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCW_RECT = 0x1

	/*
		MCW_SCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCW_SCREEN = 0x4

	/*
		MCW_VERTICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCW_VERTICAL = 0x8

	/*
		MCW_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCW_WINDOW = 0x2

	/*
		MICROSOFT_WINBASE_H_DEFINE_INTERLOCKED_CPLUSPLUS_OVERLOADS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MICROSOFT_WINBASE_H_DEFINE_INTERLOCKED_CPLUSPLUS_OVERLOADS = 0x0

	/*
		MICROSOFT_WINDOWS_WINBASE_H_DEFINE_INTERLOCKED_CPLUSPLUS_OVERLOADS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MICROSOFT_WINDOWS_WINBASE_H_DEFINE_INTERLOCKED_CPLUSPLUS_OVERLOADS = 0x0

	/*
		MODE_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MODE_WINDOW = 0x1

	/*
		OFS_MAXPATHNAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OFS_MAXPATHNAME = 0x80

	/*
		OPERATION_API_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OPERATION_API_VERSION = 0x1

	/*
		OVERWRITE_HIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OVERWRITE_HIDDEN = 0x4

	/*
		PCF_16BITMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_16BITMODE = 0x200

	/*
		PCF_DTRDSR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_DTRDSR = 0x1

	/*
		PCF_INTTIMEOUTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_INTTIMEOUTS = 0x80

	/*
		PCF_PARITY_CHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_PARITY_CHECK = 0x8

	/*
		PCF_RLSD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_RLSD = 0x4

	/*
		PCF_RTSCTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_RTSCTS = 0x2

	/*
		PCF_SETXCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_SETXCHAR = 0x20

	/*
		PCF_SPECIALCHARS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_SPECIALCHARS = 0x100

	/*
		PCF_TOTALTIMEOUTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_TOTALTIMEOUTS = 0x40

	/*
		PCF_XONXOFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PCF_XONXOFF = 0x10

	/*
		PROCESS_CREATION_ALL_APPLICATION_PACKAGES_OPT_OUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_ALL_APPLICATION_PACKAGES_OPT_OUT = 0x1

	/*
		PROCESS_CREATION_CHILD_PROCESS_OVERRIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_CHILD_PROCESS_OVERRIDE = 0x2

	/*
		PROCESS_CREATION_CHILD_PROCESS_RESTRICTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_CHILD_PROCESS_RESTRICTED = 0x1

	/*
		PROCESS_CREATION_CHILD_PROCESS_RESTRICTED_UNLESS_SECURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_CHILD_PROCESS_RESTRICTED_UNLESS_SECURE = 0x4

	/*
		PROCESS_CREATION_DESKTOP_APP_BREAKAWAY_DISABLE_PROCESS_TREE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_DESKTOP_APP_BREAKAWAY_DISABLE_PROCESS_TREE = 0x2

	/*
		PROCESS_CREATION_DESKTOP_APP_BREAKAWAY_ENABLE_PROCESS_TREE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_DESKTOP_APP_BREAKAWAY_ENABLE_PROCESS_TREE = 0x1

	/*
		PROCESS_CREATION_DESKTOP_APP_BREAKAWAY_OVERRIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_DESKTOP_APP_BREAKAWAY_OVERRIDE = 0x4

	/*
		PROCESS_CREATION_MITIGATION_POLICY_DEP_ATL_THUNK_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_MITIGATION_POLICY_DEP_ATL_THUNK_ENABLE = 0x2

	/*
		PROCESS_CREATION_MITIGATION_POLICY_DEP_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_MITIGATION_POLICY_DEP_ENABLE = 0x1

	/*
		PROCESS_CREATION_MITIGATION_POLICY_SEHOP_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROCESS_CREATION_MITIGATION_POLICY_SEHOP_ENABLE = 0x4

	/*
		PROC_THREAD_ATTRIBUTE_ADDITIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_ADDITIVE = 0x40000

	/*
		PROC_THREAD_ATTRIBUTE_INPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_INPUT = 0x20000

	/*
		PROC_THREAD_ATTRIBUTE_NUMBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_NUMBER = 0xffff

	/*
		PROC_THREAD_ATTRIBUTE_THREAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROC_THREAD_ATTRIBUTE_THREAD = 0x10000

	/*
		PROGRESS_CANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROGRESS_CANCEL = 0x1

	/*
		PROGRESS_CONTINUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROGRESS_CONTINUE = 0x0

	/*
		PROGRESS_QUIET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROGRESS_QUIET = 0x3

	/*
		PROGRESS_STOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROGRESS_STOP = 0x2

	/*
		PROTECTION_LEVEL_SAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROTECTION_LEVEL_SAME = 0xffffffff

	/*
		PST_FAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_FAX = 0x21

	/*
		PST_LAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_LAT = 0x101

	/*
		PST_MODEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_MODEM = 0x6

	/*
		PST_NETWORK_BRIDGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_NETWORK_BRIDGE = 0x100

	/*
		PST_PARALLELPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_PARALLELPORT = 0x2

	/*
		PST_RS232 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_RS232 = 0x1

	/*
		PST_RS422 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_RS422 = 0x3

	/*
		PST_RS423 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_RS423 = 0x4

	/*
		PST_RS449 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_RS449 = 0x5

	/*
		PST_SCANNER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_SCANNER = 0x22

	/*
		PST_TCPIP_TELNET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_TCPIP_TELNET = 0x102

	/*
		PST_UNSPECIFIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_UNSPECIFIED = 0x0

	/*
		PST_X25 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PST_X25 = 0x103

	/*
		QUERY_ACTCTX_FLAG_ACTCTX_IS_ADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERY_ACTCTX_FLAG_ACTCTX_IS_ADDRESS = 0x10

	/*
		QUERY_ACTCTX_FLAG_ACTCTX_IS_HMODULE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERY_ACTCTX_FLAG_ACTCTX_IS_HMODULE = 0x8

	/*
		QUERY_ACTCTX_FLAG_NO_ADDREF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERY_ACTCTX_FLAG_NO_ADDREF = 0x80000000

	/*
		QUERY_ACTCTX_FLAG_USE_ACTIVE_ACTCTX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QUERY_ACTCTX_FLAG_USE_ACTIVE_ACTCTX = 0x4

	/*
		RECOVERY_DEFAULT_PING_INTERVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RECOVERY_DEFAULT_PING_INTERVAL = 0x1388

	/*
		REMOTE_PROTOCOL_INFO_FLAG_LOOPBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	REMOTE_PROTOCOL_INFO_FLAG_LOOPBACK = 0x1

	/*
		REMOTE_PROTOCOL_INFO_FLAG_OFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	REMOTE_PROTOCOL_INFO_FLAG_OFFLINE = 0x2

	/*
		REMOTE_PROTOCOL_INFO_FLAG_PERSISTENT_HANDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	REMOTE_PROTOCOL_INFO_FLAG_PERSISTENT_HANDLE = 0x4

	/*
		RESETDEV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RESETDEV = 0x7

	/*
		RESTART_MAX_CMD_LINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RESTART_MAX_CMD_LINE = 0x400

	/*
		RPI_FLAG_SMB2_SHARECAP_CLUSTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_FLAG_SMB2_SHARECAP_CLUSTER = 0x40

	/*
		RPI_FLAG_SMB2_SHARECAP_CONTINUOUS_AVAILABILITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_FLAG_SMB2_SHARECAP_CONTINUOUS_AVAILABILITY = 0x10

	/*
		RPI_FLAG_SMB2_SHARECAP_DFS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_FLAG_SMB2_SHARECAP_DFS = 0x8

	/*
		RPI_FLAG_SMB2_SHARECAP_SCALEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_FLAG_SMB2_SHARECAP_SCALEOUT = 0x20

	/*
		RPI_FLAG_SMB2_SHARECAP_TIMEWARP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_FLAG_SMB2_SHARECAP_TIMEWARP = 0x2

	/*
		RPI_SMB2_FLAG_SERVERCAP_DFS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_FLAG_SERVERCAP_DFS = 0x1

	/*
		RPI_SMB2_FLAG_SERVERCAP_DIRECTORY_LEASING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_FLAG_SERVERCAP_DIRECTORY_LEASING = 0x20

	/*
		RPI_SMB2_FLAG_SERVERCAP_LARGEMTU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_FLAG_SERVERCAP_LARGEMTU = 0x4

	/*
		RPI_SMB2_FLAG_SERVERCAP_LEASING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_FLAG_SERVERCAP_LEASING = 0x2

	/*
		RPI_SMB2_FLAG_SERVERCAP_MULTICHANNEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_FLAG_SERVERCAP_MULTICHANNEL = 0x8

	/*
		RPI_SMB2_FLAG_SERVERCAP_PERSISTENT_HANDLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_FLAG_SERVERCAP_PERSISTENT_HANDLES = 0x10

	/*
		RPI_SMB2_SHAREFLAG_COMPRESS_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_SHAREFLAG_COMPRESS_DATA = 0x2

	/*
		RPI_SMB2_SHAREFLAG_ENCRYPT_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RPI_SMB2_SHAREFLAG_ENCRYPT_DATA = 0x1

	/*
		RSC_FLAG_DELAYREGISTEROCX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_DELAYREGISTEROCX = 0x200

	/*
		RSC_FLAG_INF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_INF = 0x1

	/*
		RSC_FLAG_NGCONV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_NGCONV = 0x8

	/*
		RSC_FLAG_QUIET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_QUIET = 0x4

	/*
		RSC_FLAG_SETUPAPI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_SETUPAPI = 0x400

	/*
		RSC_FLAG_SKIPDISKSPACECHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_SKIPDISKSPACECHECK = 0x2

	/*
		RSC_FLAG_UPDHLPDLLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RSC_FLAG_UPDHLPDLLS = 0x10

	/*
		RTS_CONTROL_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTS_CONTROL_DISABLE = 0x0

	/*
		RTS_CONTROL_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTS_CONTROL_ENABLE = 0x1

	/*
		RTS_CONTROL_HANDSHAKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTS_CONTROL_HANDSHAKE = 0x2

	/*
		RTS_CONTROL_TOGGLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RTS_CONTROL_TOGGLE = 0x3

	/*
		RUNCMDS_DELAYPOSTCMD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RUNCMDS_DELAYPOSTCMD = 0x4

	/*
		RUNCMDS_NOWAIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RUNCMDS_NOWAIT = 0x2

	/*
		RUNCMDS_QUIET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RUNCMDS_QUIET = 0x1

	/*
		SCS_32BIT_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_32BIT_BINARY = 0x0

	/*
		SCS_64BIT_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_64BIT_BINARY = 0x6

	/*
		SCS_DOS_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_DOS_BINARY = 0x1

	/*
		SCS_OS216_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_OS216_BINARY = 0x5

	/*
		SCS_PIF_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_PIF_BINARY = 0x3

	/*
		SCS_POSIX_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_POSIX_BINARY = 0x4

	/*
		SCS_THIS_PLATFORM_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_THIS_PLATFORM_BINARY = 0x6

	/*
		SCS_WOW_BINARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCS_WOW_BINARY = 0x2

	/*
		SHUTDOWN_NORETRY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHUTDOWN_NORETRY = 0x1

	/*
		SP_BAUD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_BAUD = 0x2

	/*
		SP_DATABITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_DATABITS = 0x4

	/*
		SP_HANDSHAKING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_HANDSHAKING = 0x10

	/*
		SP_PARITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_PARITY = 0x1

	/*
		SP_PARITY_CHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_PARITY_CHECK = 0x20

	/*
		SP_RLSD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_RLSD = 0x40

	/*
		SP_SERIALCOMM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_SERIALCOMM = 0x1

	/*
		SP_STOPBITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SP_STOPBITS = 0x8

	/*
		STARTF_HOLOGRAPHIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STARTF_HOLOGRAPHIC = 0x40000

	/*
		STORAGE_INFO_FLAGS_ALIGNED_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STORAGE_INFO_FLAGS_ALIGNED_DEVICE = 0x1

	/*
		STORAGE_INFO_FLAGS_PARTITION_ALIGNED_ON_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STORAGE_INFO_FLAGS_PARTITION_ALIGNED_ON_DEVICE = 0x2

	/*
		STORAGE_INFO_OFFSET_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STORAGE_INFO_OFFSET_UNKNOWN = 0xffffffff

	/*
		STREAM_CONTAINS_GHOSTED_FILE_EXTENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STREAM_CONTAINS_GHOSTED_FILE_EXTENTS = 0x10

	/*
		STREAM_CONTAINS_PROPERTIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STREAM_CONTAINS_PROPERTIES = 0x4

	/*
		STREAM_CONTAINS_SECURITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STREAM_CONTAINS_SECURITY = 0x2

	/*
		STREAM_MODIFIED_WHEN_READ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STREAM_MODIFIED_WHEN_READ = 0x1

	/*
		STREAM_NORMAL_ATTRIBUTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STREAM_NORMAL_ATTRIBUTE = 0x0

	/*
		STREAM_SPARSE_ATTRIBUTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STREAM_SPARSE_ATTRIBUTE = 0x8

	/*
		SYSTEM_STATUS_FLAG_POWER_SAVING_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SYSTEM_STATUS_FLAG_POWER_SAVING_ON = 0x1

	/*
		S_ALLTHRESHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_ALLTHRESHOLD = 0x2

	/*
		S_LEGATO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_LEGATO = 0x1

	/*
		S_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_NORMAL = 0x0

	/*
		S_PERIOD1024 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_PERIOD1024 = 0x1

	/*
		S_PERIOD2048 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_PERIOD2048 = 0x2

	/*
		S_PERIOD512 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_PERIOD512 = 0x0

	/*
		S_PERIODVOICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_PERIODVOICE = 0x3

	/*
		S_QUEUEEMPTY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_QUEUEEMPTY = 0x0

	/*
		S_SERBDNT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERBDNT = -5

	/*
		S_SERDCC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDCC = -7

	/*
		S_SERDDR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDDR = -14

	/*
		S_SERDFQ from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDFQ = -13

	/*
		S_SERDLN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDLN = -6

	/*
		S_SERDMD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDMD = -10

	/*
		S_SERDPT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDPT = -12

	/*
		S_SERDSH from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDSH = -11

	/*
		S_SERDSR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDSR = -15

	/*
		S_SERDST from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDST = -16

	/*
		S_SERDTP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDTP = -8

	/*
		S_SERDVL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDVL = -9

	/*
		S_SERDVNA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERDVNA = -1

	/*
		S_SERMACT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERMACT = -3

	/*
		S_SEROFM from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SEROFM = -2

	/*
		S_SERQFUL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_SERQFUL = -4

	/*
		S_STACCATO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_STACCATO = 0x2

	/*
		S_THRESHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_THRESHOLD = 0x1

	/*
		S_WHITE1024 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_WHITE1024 = 0x5

	/*
		S_WHITE2048 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_WHITE2048 = 0x6

	/*
		S_WHITE512 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_WHITE512 = 0x4

	/*
		S_WHITEVOICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_WHITEVOICE = 0x7

	/*
		TC_GP_TRAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_GP_TRAP = 0x2

	/*
		TC_HARDERR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_HARDERR = 0x1

	/*
		TC_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_NORMAL = 0x0

	/*
		TC_SIGNAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_SIGNAL = 0x3

	/*
		THREAD_PRIORITY_ERROR_RETURN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	THREAD_PRIORITY_ERROR_RETURN = 0x7fffffff

	/*
		UMS_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UMS_VERSION = 0x100

	/*
		WINWATCHNOTIFY_CHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINWATCHNOTIFY_CHANGED = 0x4

	/*
		WINWATCHNOTIFY_CHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINWATCHNOTIFY_CHANGING = 0x3

	/*
		WINWATCHNOTIFY_DESTROY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINWATCHNOTIFY_DESTROY = 0x2

	/*
		WINWATCHNOTIFY_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINWATCHNOTIFY_START = 0x0

	/*
		WINWATCHNOTIFY_STOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINWATCHNOTIFY_STOP = 0x1

	/*
		WLDP_FLAGS_SKIPSIGNATUREVALIDATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_FLAGS_SKIPSIGNATUREVALIDATION = 0x100

	/*
		WLDP_HOST_INFORMATION_REVISION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_HOST_INFORMATION_REVISION = 0x1

	/*
		WLDP_LOCKDOWN_AUDIT_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_AUDIT_FLAG = 0x8

	/*
		WLDP_LOCKDOWN_CONFIG_CI_AUDIT_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_CONFIG_CI_AUDIT_FLAG = 0x2

	/*
		WLDP_LOCKDOWN_CONFIG_CI_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_CONFIG_CI_FLAG = 0x1

	/*
		WLDP_LOCKDOWN_DEFINED_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_DEFINED_FLAG = 0x80000000

	/*
		WLDP_LOCKDOWN_EXCLUSION_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_EXCLUSION_FLAG = 0x10

	/*
		WLDP_LOCKDOWN_OFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_OFF = 0x80000000

	/*
		WLDP_LOCKDOWN_UMCIENFORCE_FLAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_UMCIENFORCE_FLAG = 0x4

	/*
		WLDP_LOCKDOWN_UNDEFINED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WLDP_LOCKDOWN_UNDEFINED = 0x0

	/*
		WM_CONVERTREQUEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CONVERTREQUEST = 0x10a

	/*
		WM_CONVERTRESULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CONVERTRESULT = 0x10b

	/*
		WM_IMEKEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IMEKEYDOWN = 0x290

	/*
		WM_IMEKEYUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IMEKEYUP = 0x291

	/*
		WM_IME_REPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_REPORT = 0x280

	/*
		WM_INTERIM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INTERIM = 0x10c

	/*
		WM_WNT_CONVERTREQUESTEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_WNT_CONVERTREQUESTEX = 0x109
)

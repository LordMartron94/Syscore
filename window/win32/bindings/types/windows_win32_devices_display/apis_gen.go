//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_devices_display

const (
	/*
		BITMAP_ARRAY_BYTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BITMAP_ARRAY_BYTE = 0x3

	/*
		BITMAP_BITS_BYTE_ALIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BITMAP_BITS_BYTE_ALIGN = 0x8

	/*
		BITMAP_BITS_PIXEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BITMAP_BITS_PIXEL = 0x1

	/*
		BITMAP_BITS_WORD_ALIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BITMAP_BITS_WORD_ALIGN = 0x10

	/*
		BITMAP_PLANES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BITMAP_PLANES = 0x1

	/*
		BMF_16BPP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_16BPP = 0x4

	/*
		BMF_1BPP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_1BPP = 0x1

	/*
		BMF_24BPP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_24BPP = 0x5

	/*
		BMF_32BPP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_32BPP = 0x6

	/*
		BMF_4BPP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_4BPP = 0x2

	/*
		BMF_4RLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_4RLE = 0x7

	/*
		BMF_8BPP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_8BPP = 0x3

	/*
		BMF_8RLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_8RLE = 0x8

	/*
		BMF_ACC_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_ACC_NOTIFY = 0x8000

	/*
		BMF_DONTCACHE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_DONTCACHE = 0x4

	/*
		BMF_JPEG from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_JPEG = 0x9

	/*
		BMF_KMSECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_KMSECTION = 0x10

	/*
		BMF_NOTSYSMEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_NOTSYSMEM = 0x20

	/*
		BMF_NOZEROINIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_NOZEROINIT = 0x2

	/*
		BMF_PNG from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_PNG = 0xa

	/*
		BMF_RESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_RESERVED = 0x3e00

	/*
		BMF_RMT_ENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_RMT_ENTER = 0x4000

	/*
		BMF_TEMP_ALPHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_TEMP_ALPHA = 0x100

	/*
		BMF_TOPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_TOPDOWN = 0x1

	/*
		BMF_UMPDMEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_UMPDMEM = 0x80

	/*
		BMF_USERMEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_USERMEM = 0x8

	/*
		BMF_WINDOW_BLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BMF_WINDOW_BLT = 0x40

	/*
		BRIGHTNESS_MAX_LEVEL_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BRIGHTNESS_MAX_LEVEL_COUNT = 0x67

	/*
		BRIGHTNESS_MAX_NIT_RANGE_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BRIGHTNESS_MAX_NIT_RANGE_COUNT = 0x10

	/*
		BR_CMYKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BR_CMYKCOLOR = 0x4

	/*
		BR_DEVICE_ICM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BR_DEVICE_ICM = 0x1

	/*
		BR_HOST_ICM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BR_HOST_ICM = 0x2

	/*
		BR_ORIGCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BR_ORIGCOLOR = 0x8

	/*
		CDBEX_CROSSADAPTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDBEX_CROSSADAPTER = 0x8

	/*
		CDBEX_DXINTEROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDBEX_DXINTEROP = 0x2

	/*
		CDBEX_NTSHAREDSURFACEHANDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDBEX_NTSHAREDSURFACEHANDLE = 0x4

	/*
		CDBEX_REDIRECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDBEX_REDIRECTION = 0x1

	/*
		CDBEX_REUSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDBEX_REUSE = 0x10

	/*
		CD_ANY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_ANY = 0x4

	/*
		CD_LEFTDOWN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_LEFTDOWN = 0x1

	/*
		CD_LEFTUP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_LEFTUP = 0x3

	/*
		CD_LEFTWARDS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_LEFTWARDS = 0x1

	/*
		CD_RIGHTDOWN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_RIGHTDOWN = 0x0

	/*
		CD_RIGHTUP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_RIGHTUP = 0x2

	/*
		CD_UPWARDS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CD_UPWARDS = 0x2

	/*
		CHAR_TYPE_LEADING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHAR_TYPE_LEADING = 0x2

	/*
		CHAR_TYPE_SBCS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHAR_TYPE_SBCS = 0x0

	/*
		CHAR_TYPE_TRAILING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHAR_TYPE_TRAILING = 0x3

	/*
		CT_RECTANGLES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CT_RECTANGLES = 0x0

	/*
		DCR_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCR_DRIVER = 0x1

	/*
		DCR_HALFTONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCR_HALFTONE = 0x2

	/*
		DCR_SOLID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCR_SOLID = 0x0

	/*
		DC_COMPLEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_COMPLEX = 0x3

	/*
		DC_RECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_RECT = 0x1

	/*
		DC_TRIVIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_TRIVIAL = 0x0

	/*
		DDI_DRIVER_VERSION_NT4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDI_DRIVER_VERSION_NT4 = 0x20000

	/*
		DDI_DRIVER_VERSION_NT5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDI_DRIVER_VERSION_NT5 = 0x30000

	/*
		DDI_DRIVER_VERSION_NT5_01 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDI_DRIVER_VERSION_NT5_01 = 0x30100

	/*
		DDI_DRIVER_VERSION_NT5_01_SP1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDI_DRIVER_VERSION_NT5_01_SP1 = 0x30101

	/*
		DDI_DRIVER_VERSION_SP3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDI_DRIVER_VERSION_SP3 = 0x20003

	/*
		DDI_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDI_ERROR = 0xffffffff

	/*
		DEVHTADJF_ADDITIVE_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVHTADJF_ADDITIVE_DEVICE = 0x2

	/*
		DEVHTADJF_COLOR_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DEVHTADJF_COLOR_DEVICE = 0x1

	/*
		DISPLAYPOLICY_AC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYPOLICY_AC = 0x1

	/*
		DISPLAYPOLICY_DC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISPLAYPOLICY_DC = 0x2

	/*
		DM_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DM_DEFAULT = 0x1

	/*
		DM_MONOCHROME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DM_MONOCHROME = 0x2

	/*
		DN_ACCELERATION_LEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_ACCELERATION_LEVEL = 0x1

	/*
		DN_ASSOCIATE_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_ASSOCIATE_WINDOW = 0x5

	/*
		DN_COMPOSITION_CHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_COMPOSITION_CHANGED = 0x6

	/*
		DN_DEVICE_ORIGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_DEVICE_ORIGIN = 0x2

	/*
		DN_DRAWING_BEGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_DRAWING_BEGIN = 0x4

	/*
		DN_DRAWING_BEGIN_APIBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_DRAWING_BEGIN_APIBITMAP = 0x7

	/*
		DN_SLEEP_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_SLEEP_MODE = 0x3

	/*
		DN_SURFOBJ_DESTRUCTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DN_SURFOBJ_DESTRUCTION = 0x8

	/*
		DRD_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRD_ERROR = 0x1

	/*
		DRD_SUCCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRD_SUCCESS = 0x0

	/*
		DRH_APIBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRH_APIBITMAP = 0x1

	/*
		DRVQUERY_USERMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DRVQUERY_USERMODE = 0x1

	/*
		DSI_CHECKSUM_ERROR_CORRECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_CHECKSUM_ERROR_CORRECTED = 0x100

	/*
		DSI_CHECKSUM_ERROR_NOT_CORRECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_CHECKSUM_ERROR_NOT_CORRECTED = 0x200

	/*
		DSI_CONTENTION_DETECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_CONTENTION_DETECTED = 0x80

	/*
		DSI_DSI_DATA_TYPE_NOT_RECOGNIZED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_DSI_DATA_TYPE_NOT_RECOGNIZED = 0x800

	/*
		DSI_DSI_PROTOCOL_VIOLATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_DSI_PROTOCOL_VIOLATION = 0x8000

	/*
		DSI_DSI_VC_ID_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_DSI_VC_ID_INVALID = 0x1000

	/*
		DSI_EOT_SYNC_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_EOT_SYNC_ERROR = 0x4

	/*
		DSI_ESCAPE_MODE_ENTRY_COMMAND_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_ESCAPE_MODE_ENTRY_COMMAND_ERROR = 0x8

	/*
		DSI_FALSE_CONTROL_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_FALSE_CONTROL_ERROR = 0x40

	/*
		DSI_INVALID_PACKET_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_INVALID_PACKET_INDEX = 0xff

	/*
		DSI_INVALID_TRANSMISSION_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_INVALID_TRANSMISSION_LENGTH = 0x2000

	/*
		DSI_LONG_PACKET_PAYLOAD_CHECKSUM_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_LONG_PACKET_PAYLOAD_CHECKSUM_ERROR = 0x400

	/*
		DSI_LOW_POWER_TRANSMIT_SYNC_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_LOW_POWER_TRANSMIT_SYNC_ERROR = 0x10

	/*
		DSI_PACKET_EMBEDDED_PAYLOAD_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_PACKET_EMBEDDED_PAYLOAD_SIZE = 0x8

	/*
		DSI_PERIPHERAL_TIMEOUT_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_PERIPHERAL_TIMEOUT_ERROR = 0x20

	/*
		DSI_SOT_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_SOT_ERROR = 0x1

	/*
		DSI_SOT_SYNC_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSI_SOT_SYNC_ERROR = 0x2

	/*
		DSS_FLUSH_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSS_FLUSH_EVENT = 0x2

	/*
		DSS_RESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSS_RESERVED = 0x4

	/*
		DSS_RESERVED1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSS_RESERVED1 = 0x8

	/*
		DSS_RESERVED2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSS_RESERVED2 = 0x10

	/*
		DSS_TIMER_EVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSS_TIMER_EVENT = 0x1

	/*
		DXGK_WIN32K_PARAM_FLAG_DISABLEVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DXGK_WIN32K_PARAM_FLAG_DISABLEVIEW = 0x4

	/*
		DXGK_WIN32K_PARAM_FLAG_MODESWITCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DXGK_WIN32K_PARAM_FLAG_MODESWITCH = 0x2

	/*
		DXGK_WIN32K_PARAM_FLAG_UPDATEREGISTRY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DXGK_WIN32K_PARAM_FLAG_UPDATEREGISTRY = 0x1

	/*
		ECS_REDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ECS_REDRAW = 0x2

	/*
		ECS_TEARDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ECS_TEARDOWN = 0x1

	/*
		ED_ABORTDOC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ED_ABORTDOC = 0x1

	/*
		EHN_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EHN_ERROR = 0x1

	/*
		EHN_RESTORED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EHN_RESTORED = 0x0

	/*
		ENDCAP_BUTT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDCAP_BUTT = 0x2

	/*
		ENDCAP_ROUND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDCAP_ROUND = 0x0

	/*
		ENDCAP_SQUARE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDCAP_SQUARE = 0x1

	/*
		ENG_FNT_CACHE_READ_FAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENG_FNT_CACHE_READ_FAULT = 0x1

	/*
		ENG_FNT_CACHE_WRITE_FAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENG_FNT_CACHE_WRITE_FAULT = 0x2

	/*
		FC_COMPLEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FC_COMPLEX = 0x3

	/*
		FC_RECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FC_RECT = 0x1

	/*
		FC_RECT4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FC_RECT4 = 0x2

	/*
		FDM_TYPE_BM_SIDE_CONST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FDM_TYPE_BM_SIDE_CONST = 0x1

	/*
		FDM_TYPE_CHAR_INC_EQUAL_BM_BASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FDM_TYPE_CHAR_INC_EQUAL_BM_BASE = 0x4

	/*
		FDM_TYPE_CONST_BEARINGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FDM_TYPE_CONST_BEARINGS = 0x10

	/*
		FDM_TYPE_MAXEXT_EQUAL_BM_SIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FDM_TYPE_MAXEXT_EQUAL_BM_SIDE = 0x2

	/*
		FDM_TYPE_ZERO_BEARINGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FDM_TYPE_ZERO_BEARINGS = 0x8

	/*
		FD_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FD_ERROR = 0xffffffff

	/*
		FD_NEGATIVE_FONT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FD_NEGATIVE_FONT = 0x1

	/*
		FF_IGNORED_SIGNATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FF_IGNORED_SIGNATURE = 0x2

	/*
		FF_SIGNATURE_VERIFIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FF_SIGNATURE_VERIFIED = 0x1

	/*
		FL_NONPAGED_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FL_NONPAGED_MEMORY = 0x2

	/*
		FL_NON_SESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FL_NON_SESSION = 0x4

	/*
		FL_ZERO_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FL_ZERO_MEMORY = 0x1

	/*
		FM_EDITABLE_EMBED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_EDITABLE_EMBED = 0x8

	/*
		FM_INFO_16BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_16BPP = 0x100

	/*
		FM_INFO_1BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_1BPP = 0x20

	/*
		FM_INFO_24BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_24BPP = 0x200

	/*
		FM_INFO_32BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_32BPP = 0x400

	/*
		FM_INFO_4BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_4BPP = 0x40

	/*
		FM_INFO_8BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_8BPP = 0x80

	/*
		FM_INFO_90DEGREE_ROTATIONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_90DEGREE_ROTATIONS = 0x200000

	/*
		FM_INFO_ANISOTROPIC_SCALING_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_ANISOTROPIC_SCALING_ONLY = 0x2000000

	/*
		FM_INFO_ARB_XFORMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_ARB_XFORMS = 0x10

	/*
		FM_INFO_CONSTANT_WIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_CONSTANT_WIDTH = 0x1000

	/*
		FM_INFO_DBCS_FIXED_PITCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_DBCS_FIXED_PITCH = 0x10000000

	/*
		FM_INFO_DO_NOT_ENUMERATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_DO_NOT_ENUMERATE = 0x800000

	/*
		FM_INFO_DSIG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_DSIG = 0x40000

	/*
		FM_INFO_FAMILY_EQUIV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_FAMILY_EQUIV = 0x8000000

	/*
		FM_INFO_IGNORE_TC_RA_ABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_IGNORE_TC_RA_ABLE = 0x40000000

	/*
		FM_INFO_INTEGER_WIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_INTEGER_WIDTH = 0x800

	/*
		FM_INFO_INTEGRAL_SCALING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_INTEGRAL_SCALING = 0x100000

	/*
		FM_INFO_ISOTROPIC_SCALING_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_ISOTROPIC_SCALING_ONLY = 0x1000000

	/*
		FM_INFO_NONNEGATIVE_AC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_NONNEGATIVE_AC = 0x20000000

	/*
		FM_INFO_NOT_CONTIGUOUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_NOT_CONTIGUOUS = 0x2000

	/*
		FM_INFO_OPTICALLY_FIXED_PITCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_OPTICALLY_FIXED_PITCH = 0x400000

	/*
		FM_INFO_RETURNS_BITMAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_RETURNS_BITMAPS = 0x20000

	/*
		FM_INFO_RETURNS_OUTLINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_RETURNS_OUTLINES = 0x8000

	/*
		FM_INFO_RETURNS_STROKES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_RETURNS_STROKES = 0x10000

	/*
		FM_INFO_RIGHT_HANDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_RIGHT_HANDED = 0x80000

	/*
		FM_INFO_TECH_BITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_BITMAP = 0x2

	/*
		FM_INFO_TECH_CFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_CFF = 0x4000000

	/*
		FM_INFO_TECH_MM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_MM = 0x4000

	/*
		FM_INFO_TECH_OUTLINE_NOT_TRUETYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_OUTLINE_NOT_TRUETYPE = 0x8

	/*
		FM_INFO_TECH_STROKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_STROKE = 0x4

	/*
		FM_INFO_TECH_TRUETYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_TRUETYPE = 0x1

	/*
		FM_INFO_TECH_TYPE1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_INFO_TECH_TYPE1 = 0x80000000

	/*
		FM_NO_EMBEDDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_NO_EMBEDDING = 0x2

	/*
		FM_PANOSE_CULTURE_LATIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_PANOSE_CULTURE_LATIN = 0x0

	/*
		FM_READONLY_EMBED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_READONLY_EMBED = 0x4

	/*
		FM_SEL_BOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_BOLD = 0x20

	/*
		FM_SEL_ITALIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_ITALIC = 0x1

	/*
		FM_SEL_NEGATIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_NEGATIVE = 0x4

	/*
		FM_SEL_OUTLINED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_OUTLINED = 0x8

	/*
		FM_SEL_REGULAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_REGULAR = 0x40

	/*
		FM_SEL_STRIKEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_STRIKEOUT = 0x10

	/*
		FM_SEL_UNDERSCORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_SEL_UNDERSCORE = 0x2

	/*
		FM_TYPE_LICENSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_TYPE_LICENSED = 0x2

	/*
		FM_VERSION_NUMBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FM_VERSION_NUMBER = 0x0

	/*
		FO_ATTR_MODE_ROTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_ATTR_MODE_ROTATE = 0x1

	/*
		FO_CFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_CFF = 0x100000

	/*
		FO_CLEARTYPENATURAL_X from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_CLEARTYPENATURAL_X = 0x40000000

	/*
		FO_CLEARTYPE_X from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_CLEARTYPE_X = 0x10000000

	/*
		FO_CLEARTYPE_Y from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_CLEARTYPE_Y = 0x20000000

	/*
		FO_DBCS_FONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_DBCS_FONT = 0x1000000

	/*
		FO_DEVICE_FONT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_DEVICE_FONT = 0x1

	/*
		FO_EM_HEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_EM_HEIGHT = 0x8000

	/*
		FO_GLYPHBITS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_GLYPHBITS = 0x1

	/*
		FO_GRAY16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_GRAY16 = 0x10000

	/*
		FO_HGLYPHS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_HGLYPHS = 0x0

	/*
		FO_MULTIPLEMASTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_MULTIPLEMASTER = 0x400000

	/*
		FO_NOCLEARTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_NOCLEARTYPE = 0x2000000

	/*
		FO_NOGRAY16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_NOGRAY16 = 0x20000

	/*
		FO_NOHINTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_NOHINTS = 0x40000

	/*
		FO_NO_CHOICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_NO_CHOICE = 0x80000

	/*
		FO_OUTLINE_CAPABLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_OUTLINE_CAPABLE = 0x2

	/*
		FO_PATHOBJ from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_PATHOBJ = 0x2

	/*
		FO_POSTSCRIPT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_POSTSCRIPT = 0x200000

	/*
		FO_SIM_BOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_SIM_BOLD = 0x2000

	/*
		FO_SIM_ITALIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_SIM_ITALIC = 0x4000

	/*
		FO_VERT_FACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FO_VERT_FACE = 0x800000

	/*
		FP_ALTERNATEMODE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FP_ALTERNATEMODE = 0x1

	/*
		FP_WINDINGMODE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FP_WINDINGMODE = 0x2

	/*
		GCAPS2_ACC_DRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_ACC_DRIVER = 0x8000

	/*
		GCAPS2_ALPHACURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_ALPHACURSOR = 0x20

	/*
		GCAPS2_BITMAPEXREUSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_BITMAPEXREUSE = 0x10000

	/*
		GCAPS2_CHANGEGAMMARAMP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_CHANGEGAMMARAMP = 0x10

	/*
		GCAPS2_CLEARTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_CLEARTYPE = 0x4000

	/*
		GCAPS2_EXCLUDELAYERED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_EXCLUDELAYERED = 0x800

	/*
		GCAPS2_ICD_MULTIMON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_ICD_MULTIMON = 0x100

	/*
		GCAPS2_INCLUDEAPIBITMAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_INCLUDEAPIBITMAPS = 0x1000

	/*
		GCAPS2_JPEGSRC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_JPEGSRC = 0x1

	/*
		GCAPS2_MOUSETRAILS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_MOUSETRAILS = 0x200

	/*
		GCAPS2_PNGSRC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_PNGSRC = 0x8

	/*
		GCAPS2_REMOTEDRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_REMOTEDRIVER = 0x400

	/*
		GCAPS2_RESERVED1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_RESERVED1 = 0x400

	/*
		GCAPS2_SHOWHIDDENPOINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_SHOWHIDDENPOINTER = 0x2000

	/*
		GCAPS2_SYNCFLUSH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_SYNCFLUSH = 0x40

	/*
		GCAPS2_SYNCTIMER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_SYNCTIMER = 0x80

	/*
		GCAPS2_xxxx from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS2_xxxx = 0x2

	/*
		GCAPS_ALTERNATEFILL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_ALTERNATEFILL = 0x4

	/*
		GCAPS_ARBRUSHOPAQUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_ARBRUSHOPAQUE = 0x8000

	/*
		GCAPS_ARBRUSHTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_ARBRUSHTEXT = 0x10000000

	/*
		GCAPS_ASYNCCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_ASYNCCHANGE = 0x800

	/*
		GCAPS_ASYNCMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_ASYNCMOVE = 0x1000

	/*
		GCAPS_BEZIERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_BEZIERS = 0x1

	/*
		GCAPS_CMYKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_CMYKCOLOR = 0x4000000

	/*
		GCAPS_COLOR_DITHER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_COLOR_DITHER = 0x20

	/*
		GCAPS_DIRECTDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_DIRECTDRAW = 0x4000

	/*
		GCAPS_DITHERONREALIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_DITHERONREALIZE = 0x200000

	/*
		GCAPS_DONTJOURNAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_DONTJOURNAL = 0x2000

	/*
		GCAPS_FONT_RASTERIZER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_FONT_RASTERIZER = 0x40000000

	/*
		GCAPS_FORCEDITHER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_FORCEDITHER = 0x800000

	/*
		GCAPS_GEOMETRICWIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_GEOMETRICWIDE = 0x2

	/*
		GCAPS_GRAY16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_GRAY16 = 0x1000000

	/*
		GCAPS_HALFTONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_HALFTONE = 0x10

	/*
		GCAPS_HIGHRESTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_HIGHRESTEXT = 0x40000

	/*
		GCAPS_HORIZSTRIKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_HORIZSTRIKE = 0x40

	/*
		GCAPS_ICM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_ICM = 0x2000000

	/*
		GCAPS_LAYERED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_LAYERED = 0x8000000

	/*
		GCAPS_MONO_DITHER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_MONO_DITHER = 0x400

	/*
		GCAPS_NO64BITMEMACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_NO64BITMEMACCESS = 0x400000

	/*
		GCAPS_NUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_NUP = 0x80000000

	/*
		GCAPS_OPAQUERECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_OPAQUERECT = 0x100

	/*
		GCAPS_PALMANAGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_PALMANAGED = 0x80000

	/*
		GCAPS_PANNING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_PANNING = 0x10000

	/*
		GCAPS_SCREENPRECISION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_SCREENPRECISION = 0x20000000

	/*
		GCAPS_VECTORFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_VECTORFONT = 0x200

	/*
		GCAPS_VERTSTRIKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_VERTSTRIKE = 0x80

	/*
		GCAPS_WINDINGFILL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCAPS_WINDINGFILL = 0x8

	/*
		GDI_DRIVER_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDI_DRIVER_VERSION = 0x4000

	/*
		GETCONNECTEDIDS_SOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETCONNECTEDIDS_SOURCE = 0x1

	/*
		GETCONNECTEDIDS_TARGET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GETCONNECTEDIDS_TARGET = 0x0

	/*
		GS_16BIT_HANDLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GS_16BIT_HANDLES = 0x4

	/*
		GS_8BIT_HANDLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GS_8BIT_HANDLES = 0x2

	/*
		GS_UNICODE_HANDLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GS_UNICODE_HANDLES = 0x1

	/*
		GX_GENERAL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GX_GENERAL = 0x3

	/*
		GX_IDENTITY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GX_IDENTITY = 0x0

	/*
		GX_OFFSET from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GX_OFFSET = 0x1

	/*
		GX_SCALE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GX_SCALE = 0x2

	/*
		HOOK_ALPHABLEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_ALPHABLEND = 0x10000

	/*
		HOOK_BITBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_BITBLT = 0x1

	/*
		HOOK_COPYBITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_COPYBITS = 0x400

	/*
		HOOK_FILLPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_FILLPATH = 0x40

	/*
		HOOK_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_FLAGS = 0x3b5ff

	/*
		HOOK_GRADIENTFILL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_GRADIENTFILL = 0x20000

	/*
		HOOK_LINETO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_LINETO = 0x100

	/*
		HOOK_MOVEPANNING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_MOVEPANNING = 0x800

	/*
		HOOK_PAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_PAINT = 0x10

	/*
		HOOK_PLGBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_PLGBLT = 0x4

	/*
		HOOK_STRETCHBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_STRETCHBLT = 0x2

	/*
		HOOK_STRETCHBLTROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_STRETCHBLTROP = 0x2000

	/*
		HOOK_STROKEANDFILLPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_STROKEANDFILLPATH = 0x80

	/*
		HOOK_STROKEPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_STROKEPATH = 0x20

	/*
		HOOK_SYNCHRONIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_SYNCHRONIZE = 0x1000

	/*
		HOOK_SYNCHRONIZEACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_SYNCHRONIZEACCESS = 0x4000

	/*
		HOOK_TEXTOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_TEXTOUT = 0x8

	/*
		HOOK_TRANSPARENTBLT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOOK_TRANSPARENTBLT = 0x8000

	/*
		HOST_DSI_BAD_TRANSMISSION_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_BAD_TRANSMISSION_MODE = 0x1000

	/*
		HOST_DSI_DEVICE_NOT_READY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_DEVICE_NOT_READY = 0x1

	/*
		HOST_DSI_DEVICE_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_DEVICE_RESET = 0x4

	/*
		HOST_DSI_DRIVER_REJECTED_PACKET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_DRIVER_REJECTED_PACKET = 0x400

	/*
		HOST_DSI_INTERFACE_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_INTERFACE_RESET = 0x2

	/*
		HOST_DSI_INVALID_TRANSMISSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_INVALID_TRANSMISSION = 0x100

	/*
		HOST_DSI_OS_REJECTED_PACKET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_OS_REJECTED_PACKET = 0x200

	/*
		HOST_DSI_TRANSMISSION_CANCELLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_TRANSMISSION_CANCELLED = 0x10

	/*
		HOST_DSI_TRANSMISSION_DROPPED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_TRANSMISSION_DROPPED = 0x20

	/*
		HOST_DSI_TRANSMISSION_TIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOST_DSI_TRANSMISSION_TIMEOUT = 0x40

	/*
		HS_DDI_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HS_DDI_MAX = 0x6

	/*
		HT_FLAG_8BPP_CMY332_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_8BPP_CMY332_MASK = 0xff000000

	/*
		HT_FLAG_ADDITIVE_PRIMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_ADDITIVE_PRIMS = 0x4

	/*
		HT_FLAG_DO_DEVCLR_XFORM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_DO_DEVCLR_XFORM = 0x80

	/*
		HT_FLAG_HAS_BLACK_DYE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_HAS_BLACK_DYE = 0x2

	/*
		HT_FLAG_INK_ABSORPTION_IDX0 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INK_ABSORPTION_IDX0 = 0x0

	/*
		HT_FLAG_INK_ABSORPTION_IDX1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INK_ABSORPTION_IDX1 = 0x20

	/*
		HT_FLAG_INK_ABSORPTION_IDX2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INK_ABSORPTION_IDX2 = 0x40

	/*
		HT_FLAG_INK_ABSORPTION_IDX3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INK_ABSORPTION_IDX3 = 0x60

	/*
		HT_FLAG_INK_ABSORPTION_INDICES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INK_ABSORPTION_INDICES = 0x60

	/*
		HT_FLAG_INK_HIGH_ABSORPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INK_HIGH_ABSORPTION = 0x10

	/*
		HT_FLAG_INVERT_8BPP_BITMASK_IDX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_INVERT_8BPP_BITMASK_IDX = 0x400

	/*
		HT_FLAG_LOWER_INK_ABSORPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_LOWER_INK_ABSORPTION = 0x40

	/*
		HT_FLAG_LOWEST_INK_ABSORPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_LOWEST_INK_ABSORPTION = 0x60

	/*
		HT_FLAG_LOW_INK_ABSORPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_LOW_INK_ABSORPTION = 0x20

	/*
		HT_FLAG_NORMAL_INK_ABSORPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_NORMAL_INK_ABSORPTION = 0x0

	/*
		HT_FLAG_OUTPUT_CMY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_OUTPUT_CMY = 0x100

	/*
		HT_FLAG_PRINT_DRAFT_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_PRINT_DRAFT_MODE = 0x200

	/*
		HT_FLAG_SQUARE_DEVICE_PEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_SQUARE_DEVICE_PEL = 0x1

	/*
		HT_FLAG_USE_8BPP_BITMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FLAG_USE_8BPP_BITMASK = 0x8

	/*
		HT_FORMAT_16BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_16BPP = 0x5

	/*
		HT_FORMAT_1BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_1BPP = 0x0

	/*
		HT_FORMAT_24BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_24BPP = 0x6

	/*
		HT_FORMAT_32BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_32BPP = 0x7

	/*
		HT_FORMAT_4BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_4BPP = 0x2

	/*
		HT_FORMAT_4BPP_IRGB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_4BPP_IRGB = 0x3

	/*
		HT_FORMAT_8BPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_FORMAT_8BPP = 0x4

	/*
		HT_PATSIZE_10x10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_10x10 = 0x8

	/*
		HT_PATSIZE_10x10_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_10x10_M = 0x9

	/*
		HT_PATSIZE_12x12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_12x12 = 0xa

	/*
		HT_PATSIZE_12x12_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_12x12_M = 0xb

	/*
		HT_PATSIZE_14x14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_14x14 = 0xc

	/*
		HT_PATSIZE_14x14_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_14x14_M = 0xd

	/*
		HT_PATSIZE_16x16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_16x16 = 0xe

	/*
		HT_PATSIZE_16x16_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_16x16_M = 0xf

	/*
		HT_PATSIZE_2x2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_2x2 = 0x0

	/*
		HT_PATSIZE_2x2_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_2x2_M = 0x1

	/*
		HT_PATSIZE_4x4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_4x4 = 0x2

	/*
		HT_PATSIZE_4x4_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_4x4_M = 0x3

	/*
		HT_PATSIZE_6x6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_6x6 = 0x4

	/*
		HT_PATSIZE_6x6_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_6x6_M = 0x5

	/*
		HT_PATSIZE_8x8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_8x8 = 0x6

	/*
		HT_PATSIZE_8x8_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_8x8_M = 0x7

	/*
		HT_PATSIZE_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_DEFAULT = 0x11

	/*
		HT_PATSIZE_MAX_INDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_MAX_INDEX = 0x12

	/*
		HT_PATSIZE_SUPERCELL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_SUPERCELL = 0x10

	/*
		HT_PATSIZE_SUPERCELL_M from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_SUPERCELL_M = 0x11

	/*
		HT_PATSIZE_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_PATSIZE_USER = 0x12

	/*
		HT_USERPAT_CX_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_USERPAT_CX_MAX = 0x100

	/*
		HT_USERPAT_CX_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_USERPAT_CX_MIN = 0x4

	/*
		HT_USERPAT_CY_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_USERPAT_CY_MAX = 0x100

	/*
		HT_USERPAT_CY_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HT_USERPAT_CY_MIN = 0x4

	/*
		IGRF_RGB_256BYTES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IGRF_RGB_256BYTES = 0x0

	/*
		IGRF_RGB_256WORDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IGRF_RGB_256WORDS = 0x1

	/*
		INDEX_DrvAccumulateD3DDirtyRect from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvAccumulateD3DDirtyRect = 0x62

	/*
		INDEX_DrvAlphaBlend from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvAlphaBlend = 0x47

	/*
		INDEX_DrvAssertMode from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvAssertMode = 0x5

	/*
		INDEX_DrvAssociateSharedSurface from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvAssociateSharedSurface = 0x60

	/*
		INDEX_DrvBitBlt from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvBitBlt = 0x12

	/*
		INDEX_DrvCompletePDEV from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvCompletePDEV = 0x1

	/*
		INDEX_DrvCopyBits from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvCopyBits = 0x13

	/*
		INDEX_DrvCreateDeviceBitmap from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvCreateDeviceBitmap = 0xa

	/*
		INDEX_DrvCreateDeviceBitmapEx from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvCreateDeviceBitmapEx = 0x5e

	/*
		INDEX_DrvDeleteDeviceBitmap from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDeleteDeviceBitmap = 0xb

	/*
		INDEX_DrvDeleteDeviceBitmapEx from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDeleteDeviceBitmapEx = 0x5f

	/*
		INDEX_DrvDeriveSurface from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDeriveSurface = 0x55

	/*
		INDEX_DrvDescribePixelFormat from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDescribePixelFormat = 0x37

	/*
		INDEX_DrvDestroyFont from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDestroyFont = 0x2b

	/*
		INDEX_DrvDisableDirectDraw from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDisableDirectDraw = 0x3d

	/*
		INDEX_DrvDisableDriver from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDisableDriver = 0x8

	/*
		INDEX_DrvDisablePDEV from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDisablePDEV = 0x2

	/*
		INDEX_DrvDisableSurface from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDisableSurface = 0x4

	/*
		INDEX_DrvDitherColor from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDitherColor = 0xd

	/*
		INDEX_DrvDrawEscape from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvDrawEscape = 0x19

	/*
		INDEX_DrvEnableDirectDraw from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvEnableDirectDraw = 0x3c

	/*
		INDEX_DrvEnablePDEV from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvEnablePDEV = 0x0

	/*
		INDEX_DrvEnableSurface from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvEnableSurface = 0x3

	/*
		INDEX_DrvEndDoc from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvEndDoc = 0x22

	/*
		INDEX_DrvEndDxInterop from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvEndDxInterop = 0x64

	/*
		INDEX_DrvEscape from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvEscape = 0x18

	/*
		INDEX_DrvFillPath from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvFillPath = 0xf

	/*
		INDEX_DrvFontManagement from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvFontManagement = 0x2f

	/*
		INDEX_DrvFree from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvFree = 0x2a

	/*
		INDEX_DrvGetDirectDrawInfo from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvGetDirectDrawInfo = 0x3b

	/*
		INDEX_DrvGetGlyphMode from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvGetGlyphMode = 0x25

	/*
		INDEX_DrvGetModes from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvGetModes = 0x29

	/*
		INDEX_DrvGetSynthesizedFontFiles from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvGetSynthesizedFontFiles = 0x49

	/*
		INDEX_DrvGetTrueTypeFile from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvGetTrueTypeFile = 0x32

	/*
		INDEX_DrvGradientFill from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvGradientFill = 0x44

	/*
		INDEX_DrvIcmCheckBitmapBits from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvIcmCheckBitmapBits = 0x42

	/*
		INDEX_DrvIcmCreateColorTransform from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvIcmCreateColorTransform = 0x40

	/*
		INDEX_DrvIcmDeleteColorTransform from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvIcmDeleteColorTransform = 0x41

	/*
		INDEX_DrvIcmSetDeviceGammaRamp from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvIcmSetDeviceGammaRamp = 0x43

	/*
		INDEX_DrvLineTo from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvLineTo = 0x1f

	/*
		INDEX_DrvLoadFontFile from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvLoadFontFile = 0x2d

	/*
		INDEX_DrvLockDisplayArea from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvLockDisplayArea = 0x65

	/*
		INDEX_DrvMovePanning from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvMovePanning = 0x34

	/*
		INDEX_DrvMovePointer from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvMovePointer = 0x1e

	/*
		INDEX_DrvNextBand from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvNextBand = 0x3a

	/*
		INDEX_DrvNotify from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvNotify = 0x57

	/*
		INDEX_DrvOffset from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvOffset = 0x6

	/*
		INDEX_DrvPaint from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvPaint = 0x11

	/*
		INDEX_DrvPlgBlt from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvPlgBlt = 0x46

	/*
		INDEX_DrvQueryAdvanceWidths from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryAdvanceWidths = 0x35

	/*
		INDEX_DrvQueryDeviceSupport from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryDeviceSupport = 0x4c

	/*
		INDEX_DrvQueryFont from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryFont = 0x1a

	/*
		INDEX_DrvQueryFontCaps from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryFontCaps = 0x2c

	/*
		INDEX_DrvQueryFontData from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryFontData = 0x1c

	/*
		INDEX_DrvQueryFontFile from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryFontFile = 0x33

	/*
		INDEX_DrvQueryFontTree from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryFontTree = 0x1b

	/*
		INDEX_DrvQueryGlyphAttrs from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryGlyphAttrs = 0x56

	/*
		INDEX_DrvQueryPerBandInfo from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryPerBandInfo = 0x4b

	/*
		INDEX_DrvQuerySpoolType from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQuerySpoolType = 0x3e

	/*
		INDEX_DrvQueryTrueTypeOutline from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryTrueTypeOutline = 0x31

	/*
		INDEX_DrvQueryTrueTypeTable from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvQueryTrueTypeTable = 0x30

	/*
		INDEX_DrvRealizeBrush from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvRealizeBrush = 0xc

	/*
		INDEX_DrvRenderHint from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvRenderHint = 0x5d

	/*
		INDEX_DrvReserved1 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved1 = 0x4d

	/*
		INDEX_DrvReserved10 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved10 = 0x5b

	/*
		INDEX_DrvReserved11 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved11 = 0x5c

	/*
		INDEX_DrvReserved2 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved2 = 0x4e

	/*
		INDEX_DrvReserved3 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved3 = 0x4f

	/*
		INDEX_DrvReserved4 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved4 = 0x50

	/*
		INDEX_DrvReserved5 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved5 = 0x51

	/*
		INDEX_DrvReserved6 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved6 = 0x52

	/*
		INDEX_DrvReserved7 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved7 = 0x53

	/*
		INDEX_DrvReserved8 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved8 = 0x54

	/*
		INDEX_DrvReserved9 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvReserved9 = 0x5a

	/*
		INDEX_DrvResetDevice from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvResetDevice = 0x59

	/*
		INDEX_DrvResetPDEV from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvResetPDEV = 0x7

	/*
		INDEX_DrvSaveScreenBits from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSaveScreenBits = 0x28

	/*
		INDEX_DrvSendPage from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSendPage = 0x20

	/*
		INDEX_DrvSetPalette from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSetPalette = 0x16

	/*
		INDEX_DrvSetPixelFormat from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSetPixelFormat = 0x36

	/*
		INDEX_DrvSetPointerShape from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSetPointerShape = 0x1d

	/*
		INDEX_DrvStartBanding from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStartBanding = 0x39

	/*
		INDEX_DrvStartDoc from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStartDoc = 0x23

	/*
		INDEX_DrvStartDxInterop from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStartDxInterop = 0x63

	/*
		INDEX_DrvStartPage from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStartPage = 0x21

	/*
		INDEX_DrvStretchBlt from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStretchBlt = 0x14

	/*
		INDEX_DrvStretchBltROP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStretchBltROP = 0x45

	/*
		INDEX_DrvStrokeAndFillPath from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStrokeAndFillPath = 0x10

	/*
		INDEX_DrvStrokePath from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvStrokePath = 0xe

	/*
		INDEX_DrvSurfaceComplete from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSurfaceComplete = 0x67

	/*
		INDEX_DrvSwapBuffers from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSwapBuffers = 0x38

	/*
		INDEX_DrvSynchronize from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSynchronize = 0x26

	/*
		INDEX_DrvSynchronizeRedirectionBitmaps from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSynchronizeRedirectionBitmaps = 0x61

	/*
		INDEX_DrvSynchronizeSurface from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSynchronizeSurface = 0x58

	/*
		INDEX_DrvSynthesizeFont from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvSynthesizeFont = 0x48

	/*
		INDEX_DrvTextOut from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvTextOut = 0x17

	/*
		INDEX_DrvTransparentBlt from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvTransparentBlt = 0x4a

	/*
		INDEX_DrvUnloadFontFile from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvUnloadFontFile = 0x2e

	/*
		INDEX_DrvUnlockDisplayArea from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_DrvUnlockDisplayArea = 0x66

	/*
		INDEX_LAST from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEX_LAST = 0x59

	/*
		INDIRECT_DISPLAY_INFO_FLAGS_CREATED_IDDCX_ADAPTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDIRECT_DISPLAY_INFO_FLAGS_CREATED_IDDCX_ADAPTER = 0x1

	/*
		IOCTL_COLORSPACE_TRANSFORM_QUERY_TARGET_CAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_COLORSPACE_TRANSFORM_QUERY_TARGET_CAPS = 0x231000

	/*
		IOCTL_COLORSPACE_TRANSFORM_SET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_COLORSPACE_TRANSFORM_SET = 0x231004

	/*
		IOCTL_FSVIDEO_COPY_FRAME_BUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_FSVIDEO_COPY_FRAME_BUFFER = 0x340800

	/*
		IOCTL_FSVIDEO_REVERSE_MOUSE_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_FSVIDEO_REVERSE_MOUSE_POINTER = 0x340808

	/*
		IOCTL_FSVIDEO_SET_CURRENT_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_FSVIDEO_SET_CURRENT_MODE = 0x34080c

	/*
		IOCTL_FSVIDEO_SET_CURSOR_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_FSVIDEO_SET_CURSOR_POSITION = 0x340814

	/*
		IOCTL_FSVIDEO_SET_SCREEN_INFORMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_FSVIDEO_SET_SCREEN_INFORMATION = 0x340810

	/*
		IOCTL_FSVIDEO_WRITE_TO_FRAME_BUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_FSVIDEO_WRITE_TO_FRAME_BUFFER = 0x340804

	/*
		IOCTL_MIPI_DSI_QUERY_CAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_MIPI_DSI_QUERY_CAPS = 0x231400

	/*
		IOCTL_MIPI_DSI_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_MIPI_DSI_RESET = 0x231408

	/*
		IOCTL_MIPI_DSI_TRANSMISSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_MIPI_DSI_TRANSMISSION = 0x231404

	/*
		IOCTL_PANEL_GET_BACKLIGHT_REDUCTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_GET_BACKLIGHT_REDUCTION = 0x230c18

	/*
		IOCTL_PANEL_GET_BRIGHTNESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_GET_BRIGHTNESS = 0x230c08

	/*
		IOCTL_PANEL_GET_MANUFACTURING_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_GET_MANUFACTURING_MODE = 0x230c1c

	/*
		IOCTL_PANEL_QUERY_BRIGHTNESS_CAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_QUERY_BRIGHTNESS_CAPS = 0x230c00

	/*
		IOCTL_PANEL_QUERY_BRIGHTNESS_RANGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_QUERY_BRIGHTNESS_RANGES = 0x230c04

	/*
		IOCTL_PANEL_SET_BACKLIGHT_OPTIMIZATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_SET_BACKLIGHT_OPTIMIZATION = 0x230c14

	/*
		IOCTL_PANEL_SET_BRIGHTNESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_SET_BRIGHTNESS = 0x230c0c

	/*
		IOCTL_PANEL_SET_BRIGHTNESS_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_PANEL_SET_BRIGHTNESS_STATE = 0x230c10

	/*
		IOCTL_SET_ACTIVE_COLOR_PROFILE_NAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_SET_ACTIVE_COLOR_PROFILE_NAME = 0x231008

	/*
		IOCTL_VIDEO_DISABLE_CURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_DISABLE_CURSOR = 0x230424

	/*
		IOCTL_VIDEO_DISABLE_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_DISABLE_POINTER = 0x23043c

	/*
		IOCTL_VIDEO_DISABLE_VDM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_DISABLE_VDM = 0x230004

	/*
		IOCTL_VIDEO_ENABLE_CURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_ENABLE_CURSOR = 0x230420

	/*
		IOCTL_VIDEO_ENABLE_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_ENABLE_POINTER = 0x230438

	/*
		IOCTL_VIDEO_ENABLE_VDM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_ENABLE_VDM = 0x230000

	/*
		IOCTL_VIDEO_ENUM_MONITOR_PDO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_ENUM_MONITOR_PDO = 0x230018

	/*
		IOCTL_VIDEO_FREE_PUBLIC_ACCESS_RANGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_FREE_PUBLIC_ACCESS_RANGES = 0x230464

	/*
		IOCTL_VIDEO_GET_BANK_SELECT_CODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_GET_BANK_SELECT_CODE = 0x230454

	/*
		IOCTL_VIDEO_GET_CHILD_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_GET_CHILD_STATE = 0x230480

	/*
		IOCTL_VIDEO_GET_OUTPUT_DEVICE_POWER_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_GET_OUTPUT_DEVICE_POWER_STATE = 0x230010

	/*
		IOCTL_VIDEO_GET_POWER_MANAGEMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_GET_POWER_MANAGEMENT = 0x230470

	/*
		IOCTL_VIDEO_HANDLE_VIDEOPARAMETERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_HANDLE_VIDEOPARAMETERS = 0x230020

	/*
		IOCTL_VIDEO_INIT_WIN32K_CALLBACKS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_INIT_WIN32K_CALLBACKS = 0x23001c

	/*
		IOCTL_VIDEO_IS_VGA_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_IS_VGA_DEVICE = 0x230024

	/*
		IOCTL_VIDEO_LOAD_AND_SET_FONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_LOAD_AND_SET_FONT = 0x230414

	/*
		IOCTL_VIDEO_MAP_VIDEO_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_MAP_VIDEO_MEMORY = 0x230458

	/*
		IOCTL_VIDEO_MONITOR_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_MONITOR_DEVICE = 0x230014

	/*
		IOCTL_VIDEO_PREPARE_FOR_EARECOVERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_PREPARE_FOR_EARECOVERY = 0x23002c

	/*
		IOCTL_VIDEO_QUERY_AVAIL_MODES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_AVAIL_MODES = 0x230400

	/*
		IOCTL_VIDEO_QUERY_COLOR_CAPABILITIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_COLOR_CAPABILITIES = 0x230468

	/*
		IOCTL_VIDEO_QUERY_CURRENT_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_CURRENT_MODE = 0x230408

	/*
		IOCTL_VIDEO_QUERY_CURSOR_ATTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_CURSOR_ATTR = 0x23042c

	/*
		IOCTL_VIDEO_QUERY_CURSOR_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_CURSOR_POSITION = 0x230434

	/*
		IOCTL_VIDEO_QUERY_DISPLAY_BRIGHTNESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_DISPLAY_BRIGHTNESS = 0x230498

	/*
		IOCTL_VIDEO_QUERY_NUM_AVAIL_MODES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_NUM_AVAIL_MODES = 0x230404

	/*
		IOCTL_VIDEO_QUERY_POINTER_ATTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_POINTER_ATTR = 0x230444

	/*
		IOCTL_VIDEO_QUERY_POINTER_CAPABILITIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_POINTER_CAPABILITIES = 0x230450

	/*
		IOCTL_VIDEO_QUERY_POINTER_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_POINTER_POSITION = 0x23044c

	/*
		IOCTL_VIDEO_QUERY_PUBLIC_ACCESS_RANGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_PUBLIC_ACCESS_RANGES = 0x230460

	/*
		IOCTL_VIDEO_QUERY_SUPPORTED_BRIGHTNESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_QUERY_SUPPORTED_BRIGHTNESS = 0x230494

	/*
		IOCTL_VIDEO_REGISTER_VDM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_REGISTER_VDM = 0x230008

	/*
		IOCTL_VIDEO_RESET_DEVICE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_RESET_DEVICE = 0x230410

	/*
		IOCTL_VIDEO_RESTORE_HARDWARE_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_RESTORE_HARDWARE_STATE = 0x230204

	/*
		IOCTL_VIDEO_SAVE_HARDWARE_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SAVE_HARDWARE_STATE = 0x230200

	/*
		IOCTL_VIDEO_SET_BANK_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_BANK_POSITION = 0x230490

	/*
		IOCTL_VIDEO_SET_CHILD_STATE_CONFIGURATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_CHILD_STATE_CONFIGURATION = 0x230488

	/*
		IOCTL_VIDEO_SET_COLOR_LUT_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_COLOR_LUT_DATA = 0x23047c

	/*
		IOCTL_VIDEO_SET_COLOR_REGISTERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_COLOR_REGISTERS = 0x23041c

	/*
		IOCTL_VIDEO_SET_CURRENT_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_CURRENT_MODE = 0x23040c

	/*
		IOCTL_VIDEO_SET_CURSOR_ATTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_CURSOR_ATTR = 0x230428

	/*
		IOCTL_VIDEO_SET_CURSOR_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_CURSOR_POSITION = 0x230430

	/*
		IOCTL_VIDEO_SET_DISPLAY_BRIGHTNESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_DISPLAY_BRIGHTNESS = 0x23049c

	/*
		IOCTL_VIDEO_SET_OUTPUT_DEVICE_POWER_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_OUTPUT_DEVICE_POWER_STATE = 0x23000c

	/*
		IOCTL_VIDEO_SET_PALETTE_REGISTERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_PALETTE_REGISTERS = 0x230418

	/*
		IOCTL_VIDEO_SET_POINTER_ATTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_POINTER_ATTR = 0x230440

	/*
		IOCTL_VIDEO_SET_POINTER_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_POINTER_POSITION = 0x230448

	/*
		IOCTL_VIDEO_SET_POWER_MANAGEMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SET_POWER_MANAGEMENT = 0x23046c

	/*
		IOCTL_VIDEO_SHARE_VIDEO_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SHARE_VIDEO_MEMORY = 0x230474

	/*
		IOCTL_VIDEO_SWITCH_DUALVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_SWITCH_DUALVIEW = 0x23048c

	/*
		IOCTL_VIDEO_UNMAP_VIDEO_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_UNMAP_VIDEO_MEMORY = 0x23045c

	/*
		IOCTL_VIDEO_UNSHARE_VIDEO_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_UNSHARE_VIDEO_MEMORY = 0x230478

	/*
		IOCTL_VIDEO_USE_DEVICE_IN_SESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_USE_DEVICE_IN_SESSION = 0x230028

	/*
		IOCTL_VIDEO_VALIDATE_CHILD_STATE_CONFIGURATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VIDEO_VALIDATE_CHILD_STATE_CONFIGURATION = 0x230484

	/*
		JOIN_BEVEL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	JOIN_BEVEL = 0x1

	/*
		JOIN_MITER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	JOIN_MITER = 0x2

	/*
		JOIN_ROUND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	JOIN_ROUND = 0x0

	/*
		LA_ALTERNATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LA_ALTERNATE = 0x2

	/*
		LA_GEOMETRIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LA_GEOMETRIC = 0x1

	/*
		LA_STARTGAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LA_STARTGAP = 0x4

	/*
		LA_STYLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LA_STYLED = 0x8

	/*
		MAXCHARSETS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXCHARSETS = 0x10

	/*
		MAX_PACKET_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_PACKET_COUNT = 0x80

	/*
		MC_CAPS_BRIGHTNESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_BRIGHTNESS = 0x2

	/*
		MC_CAPS_COLOR_TEMPERATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_COLOR_TEMPERATURE = 0x8

	/*
		MC_CAPS_CONTRAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_CONTRAST = 0x4

	/*
		MC_CAPS_DEGAUSS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_DEGAUSS = 0x40

	/*
		MC_CAPS_DISPLAY_AREA_POSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_DISPLAY_AREA_POSITION = 0x80

	/*
		MC_CAPS_DISPLAY_AREA_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_DISPLAY_AREA_SIZE = 0x100

	/*
		MC_CAPS_MONITOR_TECHNOLOGY_TYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_MONITOR_TECHNOLOGY_TYPE = 0x1

	/*
		MC_CAPS_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_NONE = 0x0

	/*
		MC_CAPS_RED_GREEN_BLUE_DRIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_RED_GREEN_BLUE_DRIVE = 0x20

	/*
		MC_CAPS_RED_GREEN_BLUE_GAIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_RED_GREEN_BLUE_GAIN = 0x10

	/*
		MC_CAPS_RESTORE_FACTORY_COLOR_DEFAULTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_RESTORE_FACTORY_COLOR_DEFAULTS = 0x800

	/*
		MC_CAPS_RESTORE_FACTORY_DEFAULTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_CAPS_RESTORE_FACTORY_DEFAULTS = 0x400

	/*
		MC_RESTORE_FACTORY_DEFAULTS_ENABLES_MONITOR_SETTINGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_RESTORE_FACTORY_DEFAULTS_ENABLES_MONITOR_SETTINGS = 0x1000

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_10000K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_10000K = 0x40

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_11500K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_11500K = 0x80

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_4000K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_4000K = 0x1

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_5000K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_5000K = 0x2

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_6500K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_6500K = 0x4

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_7500K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_7500K = 0x8

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_8200K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_8200K = 0x10

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_9300K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_9300K = 0x20

	/*
		MC_SUPPORTED_COLOR_TEMPERATURE_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MC_SUPPORTED_COLOR_TEMPERATURE_NONE = 0x0

	/*
		MS_CDDDEVICEBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MS_CDDDEVICEBITMAP = 0x4

	/*
		MS_NOTSYSTEMMEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MS_NOTSYSTEMMEMORY = 0x1

	/*
		MS_REUSEDDEVICEBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MS_REUSEDDEVICEBITMAP = 0x8

	/*
		MS_SHAREDACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MS_SHAREDACCESS = 0x2

	/*
		OC_BANK_CLIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OC_BANK_CLIP = 0x1

	/*
		OPENGL_CMD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OPENGL_CMD = 0x1100

	/*
		OPENGL_GETINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OPENGL_GETINFO = 0x1101

	/*
		PAL_BGR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAL_BGR = 0x8

	/*
		PAL_BITFIELDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAL_BITFIELDS = 0x2

	/*
		PAL_CMYK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAL_CMYK = 0x10

	/*
		PAL_INDEXED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAL_INDEXED = 0x1

	/*
		PAL_RGB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAL_RGB = 0x4

	/*
		PD_BEGINSUBPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PD_BEGINSUBPATH = 0x1

	/*
		PD_BEZIERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PD_BEZIERS = 0x10

	/*
		PD_CLOSEFIGURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PD_CLOSEFIGURE = 0x8

	/*
		PD_ENDSUBPATH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PD_ENDSUBPATH = 0x2

	/*
		PD_RESETSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PD_RESETSTYLE = 0x4

	/*
		PHYSICAL_MONITOR_DESCRIPTION_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PHYSICAL_MONITOR_DESCRIPTION_SIZE = 0x80

	/*
		PLANAR_HC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PLANAR_HC = 0x1

	/*
		PO_ALL_INTEGERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PO_ALL_INTEGERS = 0x4

	/*
		PO_BEZIERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PO_BEZIERS = 0x1

	/*
		PO_ELLIPSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PO_ELLIPSE = 0x2

	/*
		PO_ENUM_AS_INTEGERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PO_ENUM_AS_INTEGERS = 0x8

	/*
		PO_WIDENED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PO_WIDENED = 0x10

	/*
		PPC_BGR_ORDER_HORIZONTAL_STRIPES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPC_BGR_ORDER_HORIZONTAL_STRIPES = 0x5

	/*
		PPC_BGR_ORDER_VERTICAL_STRIPES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPC_BGR_ORDER_VERTICAL_STRIPES = 0x3

	/*
		PPC_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPC_DEFAULT = 0x0

	/*
		PPC_RGB_ORDER_HORIZONTAL_STRIPES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPC_RGB_ORDER_HORIZONTAL_STRIPES = 0x4

	/*
		PPC_RGB_ORDER_VERTICAL_STRIPES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPC_RGB_ORDER_VERTICAL_STRIPES = 0x2

	/*
		PPC_UNDEFINED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPC_UNDEFINED = 0x1

	/*
		PPG_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPG_DEFAULT = 0x0

	/*
		PPG_SRGB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PPG_SRGB = 0x1

	/*
		PRIMARY_ORDER_ABC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIMARY_ORDER_ABC = 0x0

	/*
		PRIMARY_ORDER_ACB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIMARY_ORDER_ACB = 0x1

	/*
		PRIMARY_ORDER_BAC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIMARY_ORDER_BAC = 0x2

	/*
		PRIMARY_ORDER_BCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIMARY_ORDER_BCA = 0x3

	/*
		PRIMARY_ORDER_CAB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIMARY_ORDER_CAB = 0x5

	/*
		PRIMARY_ORDER_CBA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRIMARY_ORDER_CBA = 0x4

	/*
		QAW_GETEASYWIDTHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QAW_GETEASYWIDTHS = 0x1

	/*
		QAW_GETWIDTHS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QAW_GETWIDTHS = 0x0

	/*
		QC_1BIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QC_1BIT = 0x2

	/*
		QC_4BIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QC_4BIT = 0x4

	/*
		QC_OUTLINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QC_OUTLINES = 0x1

	/*
		QDS_CHECKJPEGFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QDS_CHECKJPEGFORMAT = 0x0

	/*
		QDS_CHECKPNGFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QDS_CHECKPNGFORMAT = 0x1

	/*
		QFD_GLYPHANDBITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_GLYPHANDBITMAP = 0x1

	/*
		QFD_GLYPHANDOUTLINE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_GLYPHANDOUTLINE = 0x2

	/*
		QFD_MAXEXTENTS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_MAXEXTENTS = 0x3

	/*
		QFD_TT_GLYPHANDBITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_TT_GLYPHANDBITMAP = 0x4

	/*
		QFD_TT_GRAY1_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_TT_GRAY1_BITMAP = 0x5

	/*
		QFD_TT_GRAY2_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_TT_GRAY2_BITMAP = 0x6

	/*
		QFD_TT_GRAY4_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_TT_GRAY4_BITMAP = 0x8

	/*
		QFD_TT_GRAY8_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_TT_GRAY8_BITMAP = 0x9

	/*
		QFD_TT_MONO_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFD_TT_MONO_BITMAP = 0x5

	/*
		QFF_DESCRIPTION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFF_DESCRIPTION = 0x1

	/*
		QFF_NUMFACES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFF_NUMFACES = 0x2

	/*
		QFT_GLYPHSET from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFT_GLYPHSET = 0x3

	/*
		QFT_KERNPAIRS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFT_KERNPAIRS = 0x2

	/*
		QFT_LIGATURES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QFT_LIGATURES = 0x1

	/*
		QSA_3DNOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QSA_3DNOW = 0x4000

	/*
		QSA_MMX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QSA_MMX = 0x100

	/*
		QSA_SSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QSA_SSE = 0x2000

	/*
		QSA_SSE1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QSA_SSE1 = 0x2000

	/*
		QSA_SSE2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QSA_SSE2 = 0x10000

	/*
		QSA_SSE3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QSA_SSE3 = 0x80000

	/*
		RB_DITHERCOLOR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_DITHERCOLOR = -2147483648

	/*
		SETCONFIGURATION_STATUS_ADDITIONAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETCONFIGURATION_STATUS_ADDITIONAL = 0x1

	/*
		SETCONFIGURATION_STATUS_APPLIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETCONFIGURATION_STATUS_APPLIED = 0x0

	/*
		SETCONFIGURATION_STATUS_OVERRIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SETCONFIGURATION_STATUS_OVERRIDDEN = 0x2

	/*
		SGI_EXTRASPACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SGI_EXTRASPACE = 0x0

	/*
		SO_BREAK_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_BREAK_EXTRA = 0x1000

	/*
		SO_CHARACTER_EXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_CHARACTER_EXTRA = 0x800

	/*
		SO_CHAR_INC_EQUAL_BM_BASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_CHAR_INC_EQUAL_BM_BASE = 0x20

	/*
		SO_DO_NOT_SUBSTITUTE_DEVICE_FONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_DO_NOT_SUBSTITUTE_DEVICE_FONT = 0x80

	/*
		SO_DXDY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_DXDY = 0x400

	/*
		SO_ESC_NOT_ORIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_ESC_NOT_ORIENT = 0x200

	/*
		SO_FLAG_DEFAULT_PLACEMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_FLAG_DEFAULT_PLACEMENT = 0x1

	/*
		SO_GLYPHINDEX_TEXTOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_GLYPHINDEX_TEXTOUT = 0x100

	/*
		SO_HORIZONTAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_HORIZONTAL = 0x2

	/*
		SO_MAXEXT_EQUAL_BM_SIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_MAXEXT_EQUAL_BM_SIDE = 0x40

	/*
		SO_REVERSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_REVERSED = 0x8

	/*
		SO_VERTICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_VERTICAL = 0x4

	/*
		SO_ZERO_BEARINGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SO_ZERO_BEARINGS = 0x10

	/*
		SPS_ACCEPT_EXCLUDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ACCEPT_EXCLUDE = 0x3

	/*
		SPS_ACCEPT_NOEXCLUDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ACCEPT_NOEXCLUDE = 0x2

	/*
		SPS_ACCEPT_SYNCHRONOUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ACCEPT_SYNCHRONOUS = 0x4

	/*
		SPS_ALPHA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ALPHA = 0x10

	/*
		SPS_ANIMATESTART from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ANIMATESTART = 0x4

	/*
		SPS_ANIMATEUPDATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ANIMATEUPDATE = 0x8

	/*
		SPS_ASYNCCHANGE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ASYNCCHANGE = 0x2

	/*
		SPS_CHANGE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_CHANGE = 0x1

	/*
		SPS_DECLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_DECLINE = 0x1

	/*
		SPS_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_ERROR = 0x0

	/*
		SPS_FLAGSMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_FLAGSMASK = 0xff

	/*
		SPS_FREQMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_FREQMASK = 0xff000

	/*
		SPS_LENGTHMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_LENGTHMASK = 0xf00

	/*
		SPS_RESERVED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_RESERVED = 0x20

	/*
		SPS_RESERVED1 from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SPS_RESERVED1 = 0x40

	/*
		SS_FREE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SS_FREE = 0x2

	/*
		SS_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SS_RESTORE = 0x1

	/*
		SS_SAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SS_SAVE = 0x0

	/*
		STYPE_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_BITMAP = 0x0

	/*
		STYPE_DEVBITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_DEVBITMAP = 0x3

	/*
		S_INIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	S_INIT = 0x2

	/*
		TC_PATHOBJ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_PATHOBJ = 0x2

	/*
		TC_RECTANGLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TC_RECTANGLES = 0x0

	/*
		TTO_METRICS_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTO_METRICS_ONLY = 0x1

	/*
		TTO_QUBICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTO_QUBICS = 0x2

	/*
		TTO_UNHINTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTO_UNHINTED = 0x4

	/*
		VIDEO_COLOR_LUT_DATA_FORMAT_PRIVATEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_COLOR_LUT_DATA_FORMAT_PRIVATEFORMAT = 0x80000000

	/*
		VIDEO_COLOR_LUT_DATA_FORMAT_RGB256WORDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_COLOR_LUT_DATA_FORMAT_RGB256WORDS = 0x1

	/*
		VIDEO_DEVICE_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_DEVICE_COLOR = 0x1

	/*
		VIDEO_DUALVIEW_PRIMARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_DUALVIEW_PRIMARY = 0x80000000

	/*
		VIDEO_DUALVIEW_REMOVABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_DUALVIEW_REMOVABLE = 0x1

	/*
		VIDEO_DUALVIEW_SECONDARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_DUALVIEW_SECONDARY = 0x40000000

	/*
		VIDEO_DUALVIEW_WDDM_VGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_DUALVIEW_WDDM_VGA = 0x20000000

	/*
		VIDEO_MAX_REASON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MAX_REASON = 0x9

	/*
		VIDEO_MODE_ANIMATE_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_ANIMATE_START = 0x8

	/*
		VIDEO_MODE_ANIMATE_UPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_ANIMATE_UPDATE = 0x10

	/*
		VIDEO_MODE_ASYNC_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_ASYNC_POINTER = 0x1

	/*
		VIDEO_MODE_BANKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_BANKED = 0x80

	/*
		VIDEO_MODE_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_COLOR = 0x1

	/*
		VIDEO_MODE_COLOR_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_COLOR_POINTER = 0x4

	/*
		VIDEO_MODE_GRAPHICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_GRAPHICS = 0x2

	/*
		VIDEO_MODE_INTERLACED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_INTERLACED = 0x10

	/*
		VIDEO_MODE_LINEAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_LINEAR = 0x100

	/*
		VIDEO_MODE_MANAGED_PALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_MANAGED_PALETTE = 0x8

	/*
		VIDEO_MODE_MAP_MEM_LINEAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_MAP_MEM_LINEAR = 0x40000000

	/*
		VIDEO_MODE_MONO_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_MONO_POINTER = 0x2

	/*
		VIDEO_MODE_NO_64_BIT_ACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_NO_64_BIT_ACCESS = 0x40

	/*
		VIDEO_MODE_NO_OFF_SCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_NO_OFF_SCREEN = 0x20

	/*
		VIDEO_MODE_NO_ZERO_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_NO_ZERO_MEMORY = 0x80000000

	/*
		VIDEO_MODE_PALETTE_DRIVEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_MODE_PALETTE_DRIVEN = 0x4

	/*
		VIDEO_OPTIONAL_GAMMET_TABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_OPTIONAL_GAMMET_TABLE = 0x2

	/*
		VIDEO_REASON_ALLOCATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_ALLOCATION = 0x6

	/*
		VIDEO_REASON_CONFIGURATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_CONFIGURATION = 0x9

	/*
		VIDEO_REASON_FAILED_ROTATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_FAILED_ROTATION = 0x5

	/*
		VIDEO_REASON_LOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_LOCK = 0x5

	/*
		VIDEO_REASON_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_NONE = 0x0

	/*
		VIDEO_REASON_POLICY1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_POLICY1 = 0x1

	/*
		VIDEO_REASON_POLICY2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_POLICY2 = 0x2

	/*
		VIDEO_REASON_POLICY3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_POLICY3 = 0x3

	/*
		VIDEO_REASON_POLICY4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_POLICY4 = 0x4

	/*
		VIDEO_REASON_SCRATCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_REASON_SCRATCH = 0x8

	/*
		VIDEO_STATE_NON_STANDARD_VGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_STATE_NON_STANDARD_VGA = 0x1

	/*
		VIDEO_STATE_PACKED_CHAIN4_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_STATE_PACKED_CHAIN4_MODE = 0x4

	/*
		VIDEO_STATE_UNEMULATED_VGA_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIDEO_STATE_UNEMULATED_VGA_STATE = 0x2

	/*
		WINDDI_MAXSETPALETTECOLORINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINDDI_MAXSETPALETTECOLORINDEX = 0xff

	/*
		WINDDI_MAXSETPALETTECOLORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINDDI_MAXSETPALETTECOLORS = 0x100

	/*
		WINDDI_MAX_BROADCAST_CONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINDDI_MAX_BROADCAST_CONTEXT = 0x40

	/*
		WNDOBJ_SETUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WNDOBJ_SETUP = 0x1102

	/*
		WOC_CHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_CHANGED = 0x10

	/*
		WOC_DELETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_DELETE = 0x20

	/*
		WOC_DRAWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_DRAWN = 0x40

	/*
		WOC_RGN_CLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_RGN_CLIENT = 0x2

	/*
		WOC_RGN_CLIENT_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_RGN_CLIENT_DELTA = 0x1

	/*
		WOC_RGN_SPRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_RGN_SPRITE = 0x200

	/*
		WOC_RGN_SURFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_RGN_SURFACE = 0x8

	/*
		WOC_RGN_SURFACE_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_RGN_SURFACE_DELTA = 0x4

	/*
		WOC_SPRITE_NO_OVERLAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_SPRITE_NO_OVERLAP = 0x100

	/*
		WOC_SPRITE_OVERLAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOC_SPRITE_OVERLAP = 0x80

	/*
		WO_DRAW_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_DRAW_NOTIFY = 0x40

	/*
		WO_RGN_CLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_CLIENT = 0x2

	/*
		WO_RGN_CLIENT_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_CLIENT_DELTA = 0x1

	/*
		WO_RGN_DESKTOP_COORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_DESKTOP_COORD = 0x100

	/*
		WO_RGN_SPRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_SPRITE = 0x200

	/*
		WO_RGN_SURFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_SURFACE = 0x8

	/*
		WO_RGN_SURFACE_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_SURFACE_DELTA = 0x4

	/*
		WO_RGN_UPDATE_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_UPDATE_ALL = 0x10

	/*
		WO_RGN_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_RGN_WINDOW = 0x20

	/*
		WO_SPRITE_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WO_SPRITE_NOTIFY = 0x80

	/*
		XF_INV_FXTOL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XF_INV_FXTOL = 0x3

	/*
		XF_INV_LTOL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XF_INV_LTOL = 0x1

	/*
		XF_LTOFX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XF_LTOFX = 0x2

	/*
		XF_LTOL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XF_LTOL = 0x0

	/*
		XO_DESTBITFIELDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_DESTBITFIELDS = 0x5

	/*
		XO_DESTDCPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_DESTDCPALETTE = 0x3

	/*
		XO_DESTPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_DESTPALETTE = 0x2

	/*
		XO_DEVICE_ICM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_DEVICE_ICM = 0x10

	/*
		XO_FROM_CMYK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_FROM_CMYK = 0x8

	/*
		XO_HOST_ICM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_HOST_ICM = 0x20

	/*
		XO_SRCBITFIELDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_SRCBITFIELDS = 0x4

	/*
		XO_SRCPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_SRCPALETTE = 0x1

	/*
		XO_TABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_TABLE = 0x2

	/*
		XO_TO_MONO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_TO_MONO = 0x4

	/*
		XO_TRIVIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XO_TRIVIAL = 0x1
)

//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_storage_filesystem

const (
	/*
		CLFS_FLAG_FILTER_INTERMEDIATE_LEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_FILTER_INTERMEDIATE_LEVEL = 0x10

	/*
		CLFS_FLAG_FILTER_TOP_LEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_FILTER_TOP_LEVEL = 0x20

	/*
		CLFS_FLAG_HIDDEN_SYSTEM_LOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_HIDDEN_SYSTEM_LOG = 0x200

	/*
		CLFS_FLAG_IGNORE_SHARE_ACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_IGNORE_SHARE_ACCESS = 0x40

	/*
		CLFS_FLAG_MINIFILTER_LEVEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_MINIFILTER_LEVEL = 0x100

	/*
		CLFS_FLAG_NON_REENTRANT_FILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_NON_REENTRANT_FILTER = 0x10

	/*
		CLFS_FLAG_READ_IN_PROGRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_READ_IN_PROGRESS = 0x80

	/*
		CLFS_FLAG_REENTRANT_FILE_SYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_REENTRANT_FILE_SYSTEM = 0x8

	/*
		CLFS_FLAG_REENTRANT_FILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_FLAG_REENTRANT_FILTER = 0x20

	/*
		CLFS_MARSHALLING_FLAG_DISABLE_BUFF_INIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_MARSHALLING_FLAG_DISABLE_BUFF_INIT = 0x1

	/*
		CLFS_MARSHALLING_FLAG_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_MARSHALLING_FLAG_NONE = 0x0

	/*
		CLFS_MAX_CONTAINER_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_MAX_CONTAINER_INFO = 0x100

	/*
		CLFS_MGMT_CLIENT_REGISTRATION_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_MGMT_CLIENT_REGISTRATION_VERSION = 0x1

	/*
		CLFS_MGMT_POLICY_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_MGMT_POLICY_VERSION = 0x1

	/*
		CLFS_SCAN_BACKWARD from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_SCAN_BACKWARD = 0x4

	/*
		CLFS_SCAN_BUFFERED from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_SCAN_BUFFERED = 0x20

	/*
		CLFS_SCAN_CLOSE from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_SCAN_CLOSE = 0x8

	/*
		CLFS_SCAN_FORWARD from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_SCAN_FORWARD = 0x2

	/*
		CLFS_SCAN_INIT from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_SCAN_INIT = 0x1

	/*
		CLFS_SCAN_INITIALIZED from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLFS_SCAN_INITIALIZED = 0x10

	/*
		CRM_PROTOCOL_DYNAMIC_MARSHAL_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CRM_PROTOCOL_DYNAMIC_MARSHAL_INFO = 0x2

	/*
		CRM_PROTOCOL_EXPLICIT_MARSHAL_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CRM_PROTOCOL_EXPLICIT_MARSHAL_ONLY = 0x1

	/*
		CRM_PROTOCOL_MAXIMUM_OPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CRM_PROTOCOL_MAXIMUM_OPTION = 0x3

	/*
		CSC_CACHE_AUTO_REINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSC_CACHE_AUTO_REINT = 0x10

	/*
		CSC_CACHE_MANUAL_REINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSC_CACHE_MANUAL_REINT = 0x0

	/*
		CSC_CACHE_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSC_CACHE_NONE = 0x30

	/*
		CSC_CACHE_VDO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSC_CACHE_VDO = 0x20

	/*
		CSC_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSC_MASK = 0x30

	/*
		CSC_MASK_EXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSC_MASK_EXT = 0x2030

	/*
		CSV_BLOCK_AND_FILE_CACHE_CALLBACK_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSV_BLOCK_AND_FILE_CACHE_CALLBACK_VERSION = 0x2

	/*
		CSV_BLOCK_CACHE_CALLBACK_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSV_BLOCK_CACHE_CALLBACK_VERSION = 0x1

	/*
		ClfsClientRecord from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsClientRecord = 0x3

	/*
		ClfsContainerActive from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsContainerActive = 0x4

	/*
		ClfsContainerActivePendingDelete from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsContainerActivePendingDelete = 0x8

	/*
		ClfsContainerInactive from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsContainerInactive = 0x2

	/*
		ClfsContainerInitializing from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsContainerInitializing = 0x1

	/*
		ClfsContainerPendingArchive from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsContainerPendingArchive = 0x10

	/*
		ClfsContainerPendingArchiveAndDelete from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsContainerPendingArchiveAndDelete = 0x20

	/*
		ClfsDataRecord from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsDataRecord = 0x1

	/*
		ClfsNullRecord from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsNullRecord = 0x0

	/*
		ClfsRestartRecord from Windows.Win32 metadata (originally typed uint8). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClfsRestartRecord = 0x2

	/*
		ClsContainerActive from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClsContainerActive = 0x4

	/*
		ClsContainerActivePendingDelete from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClsContainerActivePendingDelete = 0x8

	/*
		ClsContainerInactive from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClsContainerInactive = 0x2

	/*
		ClsContainerInitializing from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClsContainerInitializing = 0x1

	/*
		ClsContainerPendingArchive from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClsContainerPendingArchive = 0x10

	/*
		ClsContainerPendingArchiveAndDelete from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ClsContainerPendingArchiveAndDelete = 0x20

	/*
		DISKQUOTA_FILESTATE_INCOMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_FILESTATE_INCOMPLETE = 0x100

	/*
		DISKQUOTA_FILESTATE_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_FILESTATE_MASK = 0x300

	/*
		DISKQUOTA_FILESTATE_REBUILDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_FILESTATE_REBUILDING = 0x200

	/*
		DISKQUOTA_LOGFLAG_USER_LIMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_LOGFLAG_USER_LIMIT = 0x2

	/*
		DISKQUOTA_LOGFLAG_USER_THRESHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_LOGFLAG_USER_THRESHOLD = 0x1

	/*
		DISKQUOTA_STATE_DISABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_STATE_DISABLED = 0x0

	/*
		DISKQUOTA_STATE_ENFORCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_STATE_ENFORCE = 0x2

	/*
		DISKQUOTA_STATE_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_STATE_MASK = 0x3

	/*
		DISKQUOTA_STATE_TRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_STATE_TRACK = 0x1

	/*
		DISKQUOTA_USER_ACCOUNT_DELETED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_USER_ACCOUNT_DELETED = 0x2

	/*
		DISKQUOTA_USER_ACCOUNT_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_USER_ACCOUNT_INVALID = 0x3

	/*
		DISKQUOTA_USER_ACCOUNT_RESOLVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_USER_ACCOUNT_RESOLVED = 0x0

	/*
		DISKQUOTA_USER_ACCOUNT_UNAVAILABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_USER_ACCOUNT_UNAVAILABLE = 0x1

	/*
		DISKQUOTA_USER_ACCOUNT_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_USER_ACCOUNT_UNKNOWN = 0x4

	/*
		DISKQUOTA_USER_ACCOUNT_UNRESOLVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DISKQUOTA_USER_ACCOUNT_UNRESOLVED = 0x5

	/*
		EFS_COMPATIBILITY_VERSION_NCRYPT_PROTECTOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_COMPATIBILITY_VERSION_NCRYPT_PROTECTOR = 0x5

	/*
		EFS_COMPATIBILITY_VERSION_PFILE_PROTECTOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_COMPATIBILITY_VERSION_PFILE_PROTECTOR = 0x6

	/*
		EFS_EFS_SUBVER_EFS_CERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_EFS_SUBVER_EFS_CERT = 0x1

	/*
		EFS_METADATA_ADD_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_METADATA_ADD_USER = 0x1

	/*
		EFS_METADATA_GENERAL_OP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_METADATA_GENERAL_OP = 0x8

	/*
		EFS_METADATA_REMOVE_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_METADATA_REMOVE_USER = 0x2

	/*
		EFS_METADATA_REPLACE_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_METADATA_REPLACE_USER = 0x4

	/*
		EFS_PFILE_SUBVER_APPX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_PFILE_SUBVER_APPX = 0x3

	/*
		EFS_PFILE_SUBVER_RMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_PFILE_SUBVER_RMS = 0x2

	/*
		EFS_SUBVER_UNKNOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EFS_SUBVER_UNKNOWN = 0x0

	/*
		ENLISTMENT_MAXIMUM_OPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENLISTMENT_MAXIMUM_OPTION = 0x1

	/*
		ENLISTMENT_SUPERIOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENLISTMENT_SUPERIOR = 0x1

	/*
		FILE_PROVIDER_COMPRESSION_LZX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_PROVIDER_COMPRESSION_LZX = 0x1

	/*
		FILE_PROVIDER_COMPRESSION_XPRESS16K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_PROVIDER_COMPRESSION_XPRESS16K = 0x3

	/*
		FILE_PROVIDER_COMPRESSION_XPRESS4K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_PROVIDER_COMPRESSION_XPRESS4K = 0x0

	/*
		FILE_PROVIDER_COMPRESSION_XPRESS8K from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILE_PROVIDER_COMPRESSION_XPRESS8K = 0x2

	/*
		FT_TYPES_DEFINITION_ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FT_TYPES_DEFINITION_ = 0x1

	/*
		INVALID_FILE_ATTRIBUTES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INVALID_FILE_ATTRIBUTES = 0xffffffff

	/*
		INVALID_FILE_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INVALID_FILE_SIZE = 0xffffffff

	/*
		INVALID_SET_FILE_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INVALID_SET_FILE_POINTER = 0xffffffff

	/*
		IOCTL_VOLUME_ALLOCATE_BC_STREAM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_ALLOCATE_BC_STREAM = 0x56c040

	/*
		IOCTL_VOLUME_BASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_BASE = 0x56

	/*
		IOCTL_VOLUME_BC_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_BC_VERSION = 0x1

	/*
		IOCTL_VOLUME_FREE_BC_STREAM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_FREE_BC_STREAM = 0x56c044

	/*
		IOCTL_VOLUME_GET_BC_PROPERTIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_GET_BC_PROPERTIES = 0x56403c

	/*
		IOCTL_VOLUME_GET_CSVBLOCKCACHE_CALLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_GET_CSVBLOCKCACHE_CALLBACK = 0x56c068

	/*
		IOCTL_VOLUME_GET_GPT_ATTRIBUTES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_GET_GPT_ATTRIBUTES = 0x560038

	/*
		IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS = 0x560000

	/*
		IOCTL_VOLUME_IS_CLUSTERED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_IS_CLUSTERED = 0x560030

	/*
		IOCTL_VOLUME_IS_CSV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_IS_CSV = 0x560060

	/*
		IOCTL_VOLUME_IS_DYNAMIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_IS_DYNAMIC = 0x560048

	/*
		IOCTL_VOLUME_IS_IO_CAPABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_IS_IO_CAPABLE = 0x560014

	/*
		IOCTL_VOLUME_IS_OFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_IS_OFFLINE = 0x560010

	/*
		IOCTL_VOLUME_IS_PARTITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_IS_PARTITION = 0x560028

	/*
		IOCTL_VOLUME_LOGICAL_TO_PHYSICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_LOGICAL_TO_PHYSICAL = 0x560020

	/*
		IOCTL_VOLUME_OFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_OFFLINE = 0x56c00c

	/*
		IOCTL_VOLUME_ONLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_ONLINE = 0x56c008

	/*
		IOCTL_VOLUME_PHYSICAL_TO_LOGICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_PHYSICAL_TO_LOGICAL = 0x560024

	/*
		IOCTL_VOLUME_POST_ONLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_POST_ONLINE = 0x56c064

	/*
		IOCTL_VOLUME_PREPARE_FOR_CRITICAL_IO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_PREPARE_FOR_CRITICAL_IO = 0x56c04c

	/*
		IOCTL_VOLUME_PREPARE_FOR_SHRINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_PREPARE_FOR_SHRINK = 0x56c05c

	/*
		IOCTL_VOLUME_QUERY_ALLOCATION_HINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_QUERY_ALLOCATION_HINT = 0x564052

	/*
		IOCTL_VOLUME_QUERY_FAILOVER_SET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_QUERY_FAILOVER_SET = 0x560018

	/*
		IOCTL_VOLUME_QUERY_MINIMUM_SHRINK_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_QUERY_MINIMUM_SHRINK_SIZE = 0x564058

	/*
		IOCTL_VOLUME_QUERY_VOLUME_NUMBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_QUERY_VOLUME_NUMBER = 0x56001c

	/*
		IOCTL_VOLUME_READ_PLEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_READ_PLEX = 0x56402e

	/*
		IOCTL_VOLUME_SET_GPT_ATTRIBUTES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_SET_GPT_ATTRIBUTES = 0x560034

	/*
		IOCTL_VOLUME_SUPPORTS_ONLINE_OFFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_SUPPORTS_ONLINE_OFFLINE = 0x560004

	/*
		IOCTL_VOLUME_UPDATE_PROPERTIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IOCTL_VOLUME_UPDATE_PROPERTIES = 0x560054

	/*
		KTM_MARSHAL_BLOB_VERSION_MAJOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KTM_MARSHAL_BLOB_VERSION_MAJOR = 0x1

	/*
		KTM_MARSHAL_BLOB_VERSION_MINOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KTM_MARSHAL_BLOB_VERSION_MINOR = 0x1

	/*
		LOG_POLICY_OVERWRITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOG_POLICY_OVERWRITE = 0x1

	/*
		LOG_POLICY_PERSIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOG_POLICY_PERSIST = 0x2

	/*
		LZERROR_BADINHANDLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_BADINHANDLE = -1

	/*
		LZERROR_BADOUTHANDLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_BADOUTHANDLE = -2

	/*
		LZERROR_BADVALUE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_BADVALUE = -7

	/*
		LZERROR_GLOBALLOC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_GLOBALLOC = -5

	/*
		LZERROR_GLOBLOCK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_GLOBLOCK = -6

	/*
		LZERROR_READ from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_READ = -3

	/*
		LZERROR_UNKNOWNALG from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_UNKNOWNALG = -8

	/*
		LZERROR_WRITE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LZERROR_WRITE = -4

	/*
		MAXIMUM_REPARSE_DATA_BUFFER_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXIMUM_REPARSE_DATA_BUFFER_SIZE = 0x4000

	/*
		MAX_RESOURCEMANAGER_DESCRIPTION_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_RESOURCEMANAGER_DESCRIPTION_LENGTH = 0x40

	/*
		MAX_SID_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_SID_SIZE = 0x100

	/*
		MAX_TRANSACTION_DESCRIPTION_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_TRANSACTION_DESCRIPTION_LENGTH = 0x40

	/*
		NTMSMLI_MAXAPPDESCR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMSMLI_MAXAPPDESCR = 0x100

	/*
		NTMSMLI_MAXIDSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMSMLI_MAXIDSIZE = 0x100

	/*
		NTMSMLI_MAXTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMSMLI_MAXTYPE = 0x40

	/*
		NTMS_APPLICATIONNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_APPLICATIONNAME_LENGTH = 0x40

	/*
		NTMS_BARCODE_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_BARCODE_LENGTH = 0x40

	/*
		NTMS_COMPUTERNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_COMPUTERNAME_LENGTH = 0x40

	/*
		NTMS_DESCRIPTION_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_DESCRIPTION_LENGTH = 0x7f

	/*
		NTMS_DEVICENAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_DEVICENAME_LENGTH = 0x40

	/*
		NTMS_I1_MESSAGE_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_I1_MESSAGE_LENGTH = 0x7f

	/*
		NTMS_MAXATTR_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_MAXATTR_LENGTH = 0x10000

	/*
		NTMS_MAXATTR_NAMELEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_MAXATTR_NAMELEN = 0x20

	/*
		NTMS_MESSAGE_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_MESSAGE_LENGTH = 0x100

	/*
		NTMS_OBJECTNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_OBJECTNAME_LENGTH = 0x40

	/*
		NTMS_OMIDLABELID_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_OMIDLABELID_LENGTH = 0xff

	/*
		NTMS_OMIDLABELINFO_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_OMIDLABELINFO_LENGTH = 0x100

	/*
		NTMS_OMIDLABELTYPE_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_OMIDLABELTYPE_LENGTH = 0x40

	/*
		NTMS_POOLHIERARCHY_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_POOLHIERARCHY_LENGTH = 0x200

	/*
		NTMS_PRODUCTNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_PRODUCTNAME_LENGTH = 0x80

	/*
		NTMS_REVISION_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_REVISION_LENGTH = 0x20

	/*
		NTMS_SEQUENCE_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_SEQUENCE_LENGTH = 0x20

	/*
		NTMS_SERIALNUMBER_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_SERIALNUMBER_LENGTH = 0x20

	/*
		NTMS_USERNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_USERNAME_LENGTH = 0x40

	/*
		NTMS_VENDORNAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NTMS_VENDORNAME_LENGTH = 0x80

	/*
		RESOURCE_MANAGER_COMMUNICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RESOURCE_MANAGER_COMMUNICATION = 0x2

	/*
		RESOURCE_MANAGER_MAXIMUM_OPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RESOURCE_MANAGER_MAXIMUM_OPTION = 0x3

	/*
		RESOURCE_MANAGER_VOLATILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RESOURCE_MANAGER_VOLATILE = 0x1

	/*
		SESI1_NUM_ELEMENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SESI1_NUM_ELEMENTS = 0x8

	/*
		SESI2_NUM_ELEMENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SESI2_NUM_ELEMENTS = 0x9

	/*
		SHARE_CURRENT_USES_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_CURRENT_USES_PARMNUM = 0x7

	/*
		SHARE_FILE_SD_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_FILE_SD_PARMNUM = 0x1f5

	/*
		SHARE_MAX_USES_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_MAX_USES_PARMNUM = 0x6

	/*
		SHARE_NETNAME_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_NETNAME_PARMNUM = 0x1

	/*
		SHARE_PASSWD_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_PASSWD_PARMNUM = 0x9

	/*
		SHARE_PATH_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_PATH_PARMNUM = 0x8

	/*
		SHARE_PERMISSIONS_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_PERMISSIONS_PARMNUM = 0x5

	/*
		SHARE_QOS_POLICY_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_QOS_POLICY_PARMNUM = 0x1f8

	/*
		SHARE_REMARK_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_REMARK_PARMNUM = 0x4

	/*
		SHARE_SERVER_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_SERVER_PARMNUM = 0x1f7

	/*
		SHARE_TYPE_PARMNUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHARE_TYPE_PARMNUM = 0x3

	/*
		SHI1005_FLAGS_ACCESS_BASED_DIRECTORY_ENUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_ACCESS_BASED_DIRECTORY_ENUM = 0x800

	/*
		SHI1005_FLAGS_ALLOW_NAMESPACE_CACHING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_ALLOW_NAMESPACE_CACHING = 0x400

	/*
		SHI1005_FLAGS_CLUSTER_MANAGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_CLUSTER_MANAGED = 0x80000

	/*
		SHI1005_FLAGS_COMPRESS_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_COMPRESS_DATA = 0x100000

	/*
		SHI1005_FLAGS_DFS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_DFS = 0x1

	/*
		SHI1005_FLAGS_DFS_ROOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_DFS_ROOT = 0x2

	/*
		SHI1005_FLAGS_DISABLE_CLIENT_BUFFERING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_DISABLE_CLIENT_BUFFERING = 0x20000

	/*
		SHI1005_FLAGS_DISABLE_DIRECTORY_HANDLE_LEASING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_DISABLE_DIRECTORY_HANDLE_LEASING = 0x400000

	/*
		SHI1005_FLAGS_ENABLE_CA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_ENABLE_CA = 0x4000

	/*
		SHI1005_FLAGS_ENABLE_HASH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_ENABLE_HASH = 0x2000

	/*
		SHI1005_FLAGS_ENCRYPT_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_ENCRYPT_DATA = 0x8000

	/*
		SHI1005_FLAGS_FORCE_LEVELII_OPLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_FORCE_LEVELII_OPLOCK = 0x1000

	/*
		SHI1005_FLAGS_FORCE_SHARED_DELETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_FORCE_SHARED_DELETE = 0x200

	/*
		SHI1005_FLAGS_IDENTITY_REMOTING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_IDENTITY_REMOTING = 0x40000

	/*
		SHI1005_FLAGS_ISOLATED_TRANSPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_ISOLATED_TRANSPORT = 0x200000

	/*
		SHI1005_FLAGS_RESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_RESERVED = 0x10000

	/*
		SHI1005_FLAGS_RESTRICT_EXCLUSIVE_OPENS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1005_FLAGS_RESTRICT_EXCLUSIVE_OPENS = 0x100

	/*
		SHI1_NUM_ELEMENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI1_NUM_ELEMENTS = 0x4

	/*
		SHI2_NUM_ELEMENTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI2_NUM_ELEMENTS = 0xa

	/*
		SHI_USES_UNLIMITED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHI_USES_UNLIMITED = 0xffffffff

	/*
		STATSOPT_CLR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATSOPT_CLR = 0x1

	/*
		STYPE_RESERVED1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_RESERVED1 = 0x1000000

	/*
		STYPE_RESERVED2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_RESERVED2 = 0x2000000

	/*
		STYPE_RESERVED3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_RESERVED3 = 0x4000000

	/*
		STYPE_RESERVED4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_RESERVED4 = 0x8000000

	/*
		STYPE_RESERVED5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_RESERVED5 = 0x100000

	/*
		STYPE_RESERVED_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STYPE_RESERVED_ALL = 0x3fffff00

	/*
		TRANSACTION_DO_NOT_PROMOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_DO_NOT_PROMOTE = 0x1

	/*
		TRANSACTION_MANAGER_COMMIT_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_COMMIT_DEFAULT = 0x0

	/*
		TRANSACTION_MANAGER_COMMIT_LOWEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_COMMIT_LOWEST = 0x8

	/*
		TRANSACTION_MANAGER_COMMIT_SYSTEM_HIVES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_COMMIT_SYSTEM_HIVES = 0x4

	/*
		TRANSACTION_MANAGER_COMMIT_SYSTEM_VOLUME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_COMMIT_SYSTEM_VOLUME = 0x2

	/*
		TRANSACTION_MANAGER_CORRUPT_FOR_PROGRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_CORRUPT_FOR_PROGRESS = 0x20

	/*
		TRANSACTION_MANAGER_CORRUPT_FOR_RECOVERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_CORRUPT_FOR_RECOVERY = 0x10

	/*
		TRANSACTION_MANAGER_MAXIMUM_OPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_MAXIMUM_OPTION = 0x3f

	/*
		TRANSACTION_MANAGER_VOLATILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MANAGER_VOLATILE = 0x1

	/*
		TRANSACTION_MAXIMUM_OPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_MAXIMUM_OPTION = 0x1

	/*
		TRANSACTION_NOTIFICATION_TM_ONLINE_FLAG_IS_CLUSTERED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFICATION_TM_ONLINE_FLAG_IS_CLUSTERED = 0x1

	/*
		TRANSACTION_NOTIFY_COMMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_COMMIT = 0x4

	/*
		TRANSACTION_NOTIFY_COMMIT_COMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_COMMIT_COMPLETE = 0x40

	/*
		TRANSACTION_NOTIFY_COMMIT_FINALIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_COMMIT_FINALIZE = 0x40000000

	/*
		TRANSACTION_NOTIFY_COMMIT_REQUEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_COMMIT_REQUEST = 0x4000000

	/*
		TRANSACTION_NOTIFY_DELEGATE_COMMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_DELEGATE_COMMIT = 0x400

	/*
		TRANSACTION_NOTIFY_ENLIST_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_ENLIST_MASK = 0x40000

	/*
		TRANSACTION_NOTIFY_ENLIST_PREPREPARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_ENLIST_PREPREPARE = 0x1000

	/*
		TRANSACTION_NOTIFY_INDOUBT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_INDOUBT = 0x4000

	/*
		TRANSACTION_NOTIFY_LAST_RECOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_LAST_RECOVER = 0x2000

	/*
		TRANSACTION_NOTIFY_MARSHAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_MARSHAL = 0x20000

	/*
		TRANSACTION_NOTIFY_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_MASK = 0x3fffffff

	/*
		TRANSACTION_NOTIFY_PREPARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PREPARE = 0x2

	/*
		TRANSACTION_NOTIFY_PREPARE_COMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PREPARE_COMPLETE = 0x20

	/*
		TRANSACTION_NOTIFY_PREPREPARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PREPREPARE = 0x1

	/*
		TRANSACTION_NOTIFY_PREPREPARE_COMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PREPREPARE_COMPLETE = 0x10

	/*
		TRANSACTION_NOTIFY_PROMOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PROMOTE = 0x8000000

	/*
		TRANSACTION_NOTIFY_PROMOTE_NEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PROMOTE_NEW = 0x10000000

	/*
		TRANSACTION_NOTIFY_PROPAGATE_PULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PROPAGATE_PULL = 0x8000

	/*
		TRANSACTION_NOTIFY_PROPAGATE_PUSH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_PROPAGATE_PUSH = 0x10000

	/*
		TRANSACTION_NOTIFY_RECOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_RECOVER = 0x100

	/*
		TRANSACTION_NOTIFY_RECOVER_QUERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_RECOVER_QUERY = 0x800

	/*
		TRANSACTION_NOTIFY_REQUEST_OUTCOME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_REQUEST_OUTCOME = 0x20000000

	/*
		TRANSACTION_NOTIFY_RM_DISCONNECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_RM_DISCONNECTED = 0x1000000

	/*
		TRANSACTION_NOTIFY_ROLLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_ROLLBACK = 0x8

	/*
		TRANSACTION_NOTIFY_ROLLBACK_COMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_ROLLBACK_COMPLETE = 0x80

	/*
		TRANSACTION_NOTIFY_SINGLE_PHASE_COMMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_SINGLE_PHASE_COMMIT = 0x200

	/*
		TRANSACTION_NOTIFY_TM_ONLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRANSACTION_NOTIFY_TM_ONLINE = 0x2000000

	/*
		TXF_LOG_RECORD_GENERIC_TYPE_ABORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TXF_LOG_RECORD_GENERIC_TYPE_ABORT = 0x2

	/*
		TXF_LOG_RECORD_GENERIC_TYPE_COMMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TXF_LOG_RECORD_GENERIC_TYPE_COMMIT = 0x1

	/*
		TXF_LOG_RECORD_GENERIC_TYPE_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TXF_LOG_RECORD_GENERIC_TYPE_DATA = 0x8

	/*
		TXF_LOG_RECORD_GENERIC_TYPE_PREPARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TXF_LOG_RECORD_GENERIC_TYPE_PREPARE = 0x4

	/*
		VS_FFI_FILEFLAGSMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VS_FFI_FILEFLAGSMASK = 0x3f

	/*
		VS_FFI_SIGNATURE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VS_FFI_SIGNATURE = -17890115

	/*
		VS_FFI_STRUCVERSION from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VS_FFI_STRUCVERSION = 0x10000

	/*
		VS_USER_DEFINED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VS_USER_DEFINED = 0x64

	/*
		VS_VERSION_INFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VS_VERSION_INFO = 0x1

	/*
		WIM_BOOT_NOT_OS_WIM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_BOOT_NOT_OS_WIM = 0x0

	/*
		WIM_BOOT_OS_WIM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_BOOT_OS_WIM = 0x1

	/*
		WIM_ENTRY_FLAG_NOT_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_ENTRY_FLAG_NOT_ACTIVE = 0x1

	/*
		WIM_ENTRY_FLAG_SUSPENDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_ENTRY_FLAG_SUSPENDED = 0x2

	/*
		WIM_EXTERNAL_FILE_INFO_FLAG_NOT_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_EXTERNAL_FILE_INFO_FLAG_NOT_ACTIVE = 0x1

	/*
		WIM_EXTERNAL_FILE_INFO_FLAG_SUSPENDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_EXTERNAL_FILE_INFO_FLAG_SUSPENDED = 0x2

	/*
		WIM_PROVIDER_HASH_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIM_PROVIDER_HASH_SIZE = 0x14

	/*
		WINEFS_SETUSERKEY_SET_CAPABILITIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINEFS_SETUSERKEY_SET_CAPABILITIES = 0x1

	/*
		WOF_PROVIDER_FILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOF_PROVIDER_FILE = 0x2

	/*
		WOF_PROVIDER_WIM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WOF_PROVIDER_WIM = 0x1
)

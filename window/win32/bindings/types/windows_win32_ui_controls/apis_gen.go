//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_ui_controls

const (
	/*
		ACM_ISPLAYING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACM_ISPLAYING = 0x468

	/*
		ACM_OPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACM_OPEN = 0x467

	/*
		ACM_OPENA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACM_OPENA = 0x464

	/*
		ACM_OPENW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACM_OPENW = 0x467

	/*
		ACM_PLAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACM_PLAY = 0x465

	/*
		ACM_STOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACM_STOP = 0x466

	/*
		ACN_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACN_START = 0x1

	/*
		ACN_STOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACN_STOP = 0x2

	/*
		ACS_AUTOPLAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACS_AUTOPLAY = 0x4

	/*
		ACS_CENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACS_CENTER = 0x1

	/*
		ACS_TIMER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACS_TIMER = 0x8

	/*
		ACS_TRANSPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ACS_TRANSPARENT = 0x2

	/*
		BCM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_FIRST = 0x1600

	/*
		BCM_GETIDEALSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_GETIDEALSIZE = 0x1601

	/*
		BCM_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_GETIMAGELIST = 0x1603

	/*
		BCM_GETNOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_GETNOTE = 0x160a

	/*
		BCM_GETNOTELENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_GETNOTELENGTH = 0x160b

	/*
		BCM_GETSPLITINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_GETSPLITINFO = 0x1608

	/*
		BCM_GETTEXTMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_GETTEXTMARGIN = 0x1605

	/*
		BCM_SETDROPDOWNSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_SETDROPDOWNSTATE = 0x1606

	/*
		BCM_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_SETIMAGELIST = 0x1602

	/*
		BCM_SETNOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_SETNOTE = 0x1609

	/*
		BCM_SETSHIELD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_SETSHIELD = 0x160c

	/*
		BCM_SETSPLITINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_SETSPLITINFO = 0x1607

	/*
		BCM_SETTEXTMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCM_SETTEXTMARGIN = 0x1604

	/*
		BCN_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCN_DROPDOWN = 0xfffffb20

	/*
		BCN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCN_FIRST = 0xfffffb1e

	/*
		BCN_HOTITEMCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCN_HOTITEMCHANGE = 0xfffffb1f

	/*
		BCN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCN_LAST = 0xfffffaba

	/*
		BCSIF_GLYPH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSIF_GLYPH = 0x1

	/*
		BCSIF_IMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSIF_IMAGE = 0x2

	/*
		BCSIF_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSIF_SIZE = 0x8

	/*
		BCSIF_STYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSIF_STYLE = 0x4

	/*
		BCSS_ALIGNLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSS_ALIGNLEFT = 0x4

	/*
		BCSS_IMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSS_IMAGE = 0x8

	/*
		BCSS_NOSPLIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSS_NOSPLIT = 0x1

	/*
		BCSS_STRETCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BCSS_STRETCH = 0x2

	/*
		BST_DROPDOWNPUSHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BST_DROPDOWNPUSHED = 0x400

	/*
		BST_HOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BST_HOT = 0x200

	/*
		BS_COMMANDLINK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_COMMANDLINK = 0xe

	/*
		BS_DEFCOMMANDLINK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_DEFCOMMANDLINK = 0xf

	/*
		BS_DEFSPLITBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_DEFSPLITBUTTON = 0xd

	/*
		BS_SPLITBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_SPLITBUTTON = 0xc

	/*
		BTNS_AUTOSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_AUTOSIZE = 0x10

	/*
		BTNS_BUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_BUTTON = 0x0

	/*
		BTNS_CHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_CHECK = 0x2

	/*
		BTNS_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_DROPDOWN = 0x8

	/*
		BTNS_GROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_GROUP = 0x4

	/*
		BTNS_NOPREFIX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_NOPREFIX = 0x20

	/*
		BTNS_SEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_SEP = 0x1

	/*
		BTNS_SHOWTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_SHOWTEXT = 0x40

	/*
		BTNS_WHOLEDROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BTNS_WHOLEDROPDOWN = 0x80

	/*
		CBEMAXSTRLEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEMAXSTRLEN = 0x104

	/*
		CBEM_GETCOMBOCONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETCOMBOCONTROL = 0x406

	/*
		CBEM_GETEDITCONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETEDITCONTROL = 0x407

	/*
		CBEM_GETEXSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETEXSTYLE = 0x409

	/*
		CBEM_GETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETEXTENDEDSTYLE = 0x409

	/*
		CBEM_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETIMAGELIST = 0x403

	/*
		CBEM_GETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETITEM = 0x40d

	/*
		CBEM_GETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETITEMA = 0x404

	/*
		CBEM_GETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETITEMW = 0x40d

	/*
		CBEM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_GETUNICODEFORMAT = 0x2006

	/*
		CBEM_HASEDITCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_HASEDITCHANGED = 0x40a

	/*
		CBEM_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_INSERTITEM = 0x40b

	/*
		CBEM_INSERTITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_INSERTITEMA = 0x401

	/*
		CBEM_INSERTITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_INSERTITEMW = 0x40b

	/*
		CBEM_SETEXSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETEXSTYLE = 0x408

	/*
		CBEM_SETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETEXTENDEDSTYLE = 0x40e

	/*
		CBEM_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETIMAGELIST = 0x402

	/*
		CBEM_SETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETITEM = 0x40c

	/*
		CBEM_SETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETITEMA = 0x405

	/*
		CBEM_SETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETITEMW = 0x40c

	/*
		CBEM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETUNICODEFORMAT = 0x2005

	/*
		CBEM_SETWINDOWTHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEM_SETWINDOWTHEME = 0x200b

	/*
		CBENF_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBENF_DROPDOWN = 0x4

	/*
		CBENF_ESCAPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBENF_ESCAPE = 0x3

	/*
		CBENF_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBENF_KILLFOCUS = 0x1

	/*
		CBENF_RETURN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBENF_RETURN = 0x2

	/*
		CBEN_BEGINEDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_BEGINEDIT = 0xfffffcdc

	/*
		CBEN_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_DELETEITEM = 0xfffffcde

	/*
		CBEN_DRAGBEGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_DRAGBEGIN = 0xfffffcd7

	/*
		CBEN_DRAGBEGINA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_DRAGBEGINA = 0xfffffcd8

	/*
		CBEN_DRAGBEGINW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_DRAGBEGINW = 0xfffffcd7

	/*
		CBEN_ENDEDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_ENDEDIT = 0xfffffcda

	/*
		CBEN_ENDEDITA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_ENDEDITA = 0xfffffcdb

	/*
		CBEN_ENDEDITW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_ENDEDITW = 0xfffffcda

	/*
		CBEN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_FIRST = 0xfffffce0

	/*
		CBEN_GETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_GETDISPINFOA = 0xfffffce0

	/*
		CBEN_GETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_GETDISPINFOW = 0xfffffcd9

	/*
		CBEN_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_INSERTITEM = 0xfffffcdf

	/*
		CBEN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBEN_LAST = 0xfffffcc2

	/*
		CBES_EX_CASESENSITIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBES_EX_CASESENSITIVE = 0x10

	/*
		CBES_EX_NOEDITIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBES_EX_NOEDITIMAGE = 0x1

	/*
		CBES_EX_NOEDITIMAGEINDENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBES_EX_NOEDITIMAGEINDENT = 0x2

	/*
		CBES_EX_NOSIZELIMIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBES_EX_NOSIZELIMIT = 0x8

	/*
		CBES_EX_PATHWORDBREAKPROC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBES_EX_PATHWORDBREAKPROC = 0x4

	/*
		CBES_EX_TEXTENDELLIPSIS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBES_EX_TEXTENDELLIPSIS = 0x20

	/*
		CBM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBM_FIRST = 0x1700

	/*
		CB_GETCUEBANNER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETCUEBANNER = 0x1704

	/*
		CB_GETMINVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETMINVISIBLE = 0x1702

	/*
		CB_SETCUEBANNER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETCUEBANNER = 0x1703

	/*
		CB_SETMINVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETMINVISIBLE = 0x1701

	/*
		CCF_NOTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCF_NOTEXT = 0x1

	/*
		CCHCCCLASS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCHCCCLASS = 0x20

	/*
		CCHCCDESC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCHCCDESC = 0x20

	/*
		CCHCCTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCHCCTEXT = 0x100

	/*
		CCM_DPISCALE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_DPISCALE = 0x200c

	/*
		CCM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_FIRST = 0x2000

	/*
		CCM_GETCOLORSCHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_GETCOLORSCHEME = 0x2003

	/*
		CCM_GETDROPTARGET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_GETDROPTARGET = 0x2004

	/*
		CCM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_GETUNICODEFORMAT = 0x2006

	/*
		CCM_GETVERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_GETVERSION = 0x2008

	/*
		CCM_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_LAST = 0x2200

	/*
		CCM_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_SETBKCOLOR = 0x2001

	/*
		CCM_SETCOLORSCHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_SETCOLORSCHEME = 0x2002

	/*
		CCM_SETNOTIFYWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_SETNOTIFYWINDOW = 0x2009

	/*
		CCM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_SETUNICODEFORMAT = 0x2005

	/*
		CCM_SETVERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_SETVERSION = 0x2007

	/*
		CCM_SETWINDOWTHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCM_SETWINDOWTHEME = 0x200b

	/*
		CCS_ADJUSTABLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_ADJUSTABLE = 0x20

	/*
		CCS_BOTTOM from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_BOTTOM = 0x3

	/*
		CCS_NODIVIDER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_NODIVIDER = 0x40

	/*
		CCS_NOMOVEY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_NOMOVEY = 0x2

	/*
		CCS_NOPARENTALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_NOPARENTALIGN = 0x8

	/*
		CCS_NORESIZE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_NORESIZE = 0x4

	/*
		CCS_TOP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_TOP = 0x1

	/*
		CCS_VERT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCS_VERT = 0x80

	/*
		CDDS_ITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDDS_ITEM = 0x10000

	/*
		CDDS_POSTERASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDDS_POSTERASE = 0x4

	/*
		CDN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDN_FIRST = 0xfffffda7

	/*
		CDN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDN_LAST = 0xfffffd45

	/*
		CDRF_DODEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_DODEFAULT = 0x0

	/*
		CDRF_DOERASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_DOERASE = 0x8

	/*
		CDRF_NEWFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_NEWFONT = 0x2

	/*
		CDRF_NOTIFYITEMDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_NOTIFYITEMDRAW = 0x20

	/*
		CDRF_NOTIFYPOSTERASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_NOTIFYPOSTERASE = 0x40

	/*
		CDRF_NOTIFYPOSTPAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_NOTIFYPOSTPAINT = 0x10

	/*
		CDRF_NOTIFYSUBITEMDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_NOTIFYSUBITEMDRAW = 0x20

	/*
		CDRF_SKIPDEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_SKIPDEFAULT = 0x4

	/*
		CDRF_SKIPPOSTPAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CDRF_SKIPPOSTPAINT = 0x100

	/*
		CLR_DEFAULT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLR_DEFAULT = -16777216

	/*
		CLR_HILIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLR_HILIGHT = -16777216

	/*
		CLR_NONE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CLR_NONE = -1

	/*
		CMB_MASKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CMB_MASKED = 0x2

	/*
		COLORMGMTDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COLORMGMTDLGORD = 0x60f

	/*
		COMCTL32_VERSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	COMCTL32_VERSION = 0x6

	/*
		Chx1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx1 = 0x410

	/*
		Chx10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx10 = 0x419

	/*
		Chx11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx11 = 0x41a

	/*
		Chx12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx12 = 0x41b

	/*
		Chx13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx13 = 0x41c

	/*
		Chx14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx14 = 0x41d

	/*
		Chx15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx15 = 0x41e

	/*
		Chx16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx16 = 0x41f

	/*
		Chx2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx2 = 0x411

	/*
		Chx3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx3 = 0x412

	/*
		Chx4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx4 = 0x413

	/*
		Chx5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx5 = 0x414

	/*
		Chx6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx6 = 0x415

	/*
		Chx7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx7 = 0x416

	/*
		Chx8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx8 = 0x417

	/*
		Chx9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Chx9 = 0x418

	/*
		Cmb1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb1 = 0x470

	/*
		Cmb10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb10 = 0x479

	/*
		Cmb11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb11 = 0x47a

	/*
		Cmb12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb12 = 0x47b

	/*
		Cmb13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb13 = 0x47c

	/*
		Cmb14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb14 = 0x47d

	/*
		Cmb15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb15 = 0x47e

	/*
		Cmb16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb16 = 0x47f

	/*
		Cmb2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb2 = 0x471

	/*
		Cmb3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb3 = 0x472

	/*
		Cmb4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb4 = 0x473

	/*
		Cmb5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb5 = 0x474

	/*
		Cmb6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb6 = 0x475

	/*
		Cmb7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb7 = 0x476

	/*
		Cmb8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb8 = 0x477

	/*
		Cmb9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Cmb9 = 0x478

	/*
		Ctl1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Ctl1 = 0x4a0

	/*
		CtlFirst from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CtlFirst = 0x400

	/*
		CtlLast from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CtlLast = 0x4ff

	/*
		DA_ERR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DA_ERR = -1

	/*
		DA_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DA_LAST = 0x7fffffff

	/*
		DL_COPYCURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DL_COPYCURSOR = 0x2

	/*
		DL_CURSORSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DL_CURSORSET = 0x0

	/*
		DL_MOVECURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DL_MOVECURSOR = 0x3

	/*
		DL_STOPCURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DL_STOPCURSOR = 0x1

	/*
		DPAM_INTERSECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAM_INTERSECT = 0x8

	/*
		DPAM_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAM_NORMAL = 0x2

	/*
		DPAM_SORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAM_SORTED = 0x1

	/*
		DPAM_UNION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAM_UNION = 0x4

	/*
		DPAS_INSERTAFTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAS_INSERTAFTER = 0x4

	/*
		DPAS_INSERTBEFORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAS_INSERTBEFORE = 0x2

	/*
		DPAS_SORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPAS_SORTED = 0x1

	/*
		DPA_APPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPA_APPEND = 0x7fffffff

	/*
		DPA_ERR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DPA_ERR = -1

	/*
		DSA_APPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSA_APPEND = 0x7fffffff

	/*
		DSA_ERR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DSA_ERR = -1

	/*
		DTBG_CLIPRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_CLIPRECT = 0x1

	/*
		DTBG_COMPUTINGREGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_COMPUTINGREGION = 0x10

	/*
		DTBG_DRAWSOLID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_DRAWSOLID = 0x2

	/*
		DTBG_MIRRORDC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_MIRRORDC = 0x20

	/*
		DTBG_NOMIRROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_NOMIRROR = 0x40

	/*
		DTBG_OMITBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_OMITBORDER = 0x4

	/*
		DTBG_OMITCONTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTBG_OMITCONTENT = 0x8

	/*
		DTM_CLOSEMONTHCAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_CLOSEMONTHCAL = 0x100d

	/*
		DTM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_FIRST = 0x1000

	/*
		DTM_GETDATETIMEPICKERINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETDATETIMEPICKERINFO = 0x100e

	/*
		DTM_GETIDEALSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETIDEALSIZE = 0x100f

	/*
		DTM_GETMCCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETMCCOLOR = 0x1007

	/*
		DTM_GETMCFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETMCFONT = 0x100a

	/*
		DTM_GETMCSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETMCSTYLE = 0x100c

	/*
		DTM_GETMONTHCAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETMONTHCAL = 0x1008

	/*
		DTM_GETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETRANGE = 0x1003

	/*
		DTM_GETSYSTEMTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_GETSYSTEMTIME = 0x1001

	/*
		DTM_SETFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETFORMAT = 0x1032

	/*
		DTM_SETFORMATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETFORMATA = 0x1005

	/*
		DTM_SETFORMATW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETFORMATW = 0x1032

	/*
		DTM_SETMCCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETMCCOLOR = 0x1006

	/*
		DTM_SETMCFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETMCFONT = 0x1009

	/*
		DTM_SETMCSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETMCSTYLE = 0x100b

	/*
		DTM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETRANGE = 0x1004

	/*
		DTM_SETSYSTEMTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTM_SETSYSTEMTIME = 0x1002

	/*
		DTN_CLOSEUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_CLOSEUP = 0xfffffd0f

	/*
		DTN_DATETIMECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_DATETIMECHANGE = 0xfffffd09

	/*
		DTN_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_DROPDOWN = 0xfffffd0e

	/*
		DTN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FIRST = 0xfffffd1c

	/*
		DTN_FIRST2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FIRST2 = 0xfffffd0f

	/*
		DTN_FORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FORMAT = 0xfffffd19

	/*
		DTN_FORMATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FORMATA = 0xfffffd0c

	/*
		DTN_FORMATQUERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FORMATQUERY = 0xfffffd1a

	/*
		DTN_FORMATQUERYA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FORMATQUERYA = 0xfffffd0d

	/*
		DTN_FORMATQUERYW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FORMATQUERYW = 0xfffffd1a

	/*
		DTN_FORMATW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_FORMATW = 0xfffffd19

	/*
		DTN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_LAST = 0xfffffd17

	/*
		DTN_LAST2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_LAST2 = 0xfffffce1

	/*
		DTN_USERSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_USERSTRING = 0xfffffd17

	/*
		DTN_USERSTRINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_USERSTRINGA = 0xfffffd0a

	/*
		DTN_USERSTRINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_USERSTRINGW = 0xfffffd17

	/*
		DTN_WMKEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_WMKEYDOWN = 0xfffffd18

	/*
		DTN_WMKEYDOWNA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_WMKEYDOWNA = 0xfffffd0b

	/*
		DTN_WMKEYDOWNW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTN_WMKEYDOWNW = 0xfffffd18

	/*
		DTS_APPCANPARSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_APPCANPARSE = 0x10

	/*
		DTS_LONGDATEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_LONGDATEFORMAT = 0x4

	/*
		DTS_RIGHTALIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_RIGHTALIGN = 0x20

	/*
		DTS_SHORTDATECENTURYFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_SHORTDATECENTURYFORMAT = 0xc

	/*
		DTS_SHORTDATEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_SHORTDATEFORMAT = 0x0

	/*
		DTS_SHOWNONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_SHOWNONE = 0x2

	/*
		DTS_TIMEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_TIMEFORMAT = 0x9

	/*
		DTS_UPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTS_UPDOWN = 0x1

	/*
		DTT_FLAGS2VALIDBITS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTT_FLAGS2VALIDBITS = 0x1

	/*
		DTT_GRAYED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DTT_GRAYED = 0x1

	/*
		ECM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ECM_FIRST = 0x1500

	/*
		EM_CANUNDO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_CANUNDO = 0xc6

	/*
		EM_CHARFROMPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_CHARFROMPOS = 0xd7

	/*
		EM_EMPTYUNDOBUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_EMPTYUNDOBUFFER = 0xcd

	/*
		EM_ENABLEFEATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_ENABLEFEATURE = 0xda

	/*
		EM_ENABLESEARCHWEB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_ENABLESEARCHWEB = 0x150e

	/*
		EM_FILELINEFROMCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_FILELINEFROMCHAR = 0x1513

	/*
		EM_FILELINEINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_FILELINEINDEX = 0x1514

	/*
		EM_FILELINELENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_FILELINELENGTH = 0x1515

	/*
		EM_FMTLINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_FMTLINES = 0xc8

	/*
		EM_GETCARETINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETCARETINDEX = 0x1512

	/*
		EM_GETCUEBANNER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETCUEBANNER = 0x1502

	/*
		EM_GETENDOFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETENDOFLINE = 0x150d

	/*
		EM_GETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETEXTENDEDSTYLE = 0x150b

	/*
		EM_GETFILELINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETFILELINE = 0x1516

	/*
		EM_GETFILELINECOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETFILELINECOUNT = 0x1517

	/*
		EM_GETFIRSTVISIBLELINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETFIRSTVISIBLELINE = 0xce

	/*
		EM_GETHANDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETHANDLE = 0xbd

	/*
		EM_GETHILITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETHILITE = 0x1506

	/*
		EM_GETIMESTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETIMESTATUS = 0xd9

	/*
		EM_GETLIMITTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETLIMITTEXT = 0xd5

	/*
		EM_GETLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETLINE = 0xc4

	/*
		EM_GETLINECOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETLINECOUNT = 0xba

	/*
		EM_GETMARGINS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETMARGINS = 0xd4

	/*
		EM_GETMODIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETMODIFY = 0xb8

	/*
		EM_GETPASSWORDCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETPASSWORDCHAR = 0xd2

	/*
		EM_GETRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETRECT = 0xb2

	/*
		EM_GETSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETSEL = 0xb0

	/*
		EM_GETTHUMB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETTHUMB = 0xbe

	/*
		EM_GETWORDBREAKPROC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_GETWORDBREAKPROC = 0xd1

	/*
		EM_HIDEBALLOONTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_HIDEBALLOONTIP = 0x1504

	/*
		EM_LIMITTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_LIMITTEXT = 0xc5

	/*
		EM_LINEFROMCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_LINEFROMCHAR = 0xc9

	/*
		EM_LINEINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_LINEINDEX = 0xbb

	/*
		EM_LINELENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_LINELENGTH = 0xc1

	/*
		EM_LINESCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_LINESCROLL = 0xb6

	/*
		EM_NOSETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_NOSETFOCUS = 0x1507

	/*
		EM_POSFROMCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_POSFROMCHAR = 0xd6

	/*
		EM_REPLACESEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_REPLACESEL = 0xc2

	/*
		EM_SCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SCROLL = 0xb5

	/*
		EM_SCROLLCARET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SCROLLCARET = 0xb7

	/*
		EM_SEARCHWEB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SEARCHWEB = 0x150f

	/*
		EM_SETCARETINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETCARETINDEX = 0x1511

	/*
		EM_SETCUEBANNER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETCUEBANNER = 0x1501

	/*
		EM_SETENDOFLINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETENDOFLINE = 0x150c

	/*
		EM_SETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETEXTENDEDSTYLE = 0x150a

	/*
		EM_SETHANDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETHANDLE = 0xbc

	/*
		EM_SETHILITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETHILITE = 0x1505

	/*
		EM_SETIMESTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETIMESTATUS = 0xd8

	/*
		EM_SETLIMITTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETLIMITTEXT = 0xc5

	/*
		EM_SETMARGINS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETMARGINS = 0xd3

	/*
		EM_SETMODIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETMODIFY = 0xb9

	/*
		EM_SETPASSWORDCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETPASSWORDCHAR = 0xcc

	/*
		EM_SETREADONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETREADONLY = 0xcf

	/*
		EM_SETRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETRECT = 0xb3

	/*
		EM_SETRECTNP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETRECTNP = 0xb4

	/*
		EM_SETSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETSEL = 0xb1

	/*
		EM_SETTABSTOPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETTABSTOPS = 0xcb

	/*
		EM_SETWORDBREAKPROC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SETWORDBREAKPROC = 0xd0

	/*
		EM_SHOWBALLOONTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_SHOWBALLOONTIP = 0x1503

	/*
		EM_TAKEFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_TAKEFOCUS = 0x1508

	/*
		EM_UNDO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EM_UNDO = 0xc7

	/*
		EN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_FIRST = 0xfffffa10

	/*
		EN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_LAST = 0xfffff9fc

	/*
		EN_SEARCHWEB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_SEARCHWEB = 0xfffffa10

	/*
		ES_EX_ALLOWEOL_CR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_EX_ALLOWEOL_CR = 0x1

	/*
		ES_EX_ALLOWEOL_LF from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_EX_ALLOWEOL_LF = 0x2

	/*
		ES_EX_CONVERT_EOL_ON_PASTE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_EX_CONVERT_EOL_ON_PASTE = 0x4

	/*
		ES_EX_ZOOMABLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_EX_ZOOMABLE = 0x10

	/*
		ETDT_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ETDT_DISABLE = 0x1

	/*
		ETDT_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ETDT_ENABLE = 0x2

	/*
		ETDT_USEAEROWIZARDTABTEXTURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ETDT_USEAEROWIZARDTABTEXTURE = 0x8

	/*
		ETDT_USETABTEXTURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ETDT_USETABTEXTURE = 0x4

	/*
		Edt1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt1 = 0x480

	/*
		Edt10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt10 = 0x489

	/*
		Edt11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt11 = 0x48a

	/*
		Edt12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt12 = 0x48b

	/*
		Edt13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt13 = 0x48c

	/*
		Edt14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt14 = 0x48d

	/*
		Edt15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt15 = 0x48e

	/*
		Edt16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt16 = 0x48f

	/*
		Edt2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt2 = 0x481

	/*
		Edt3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt3 = 0x482

	/*
		Edt4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt4 = 0x483

	/*
		Edt5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt5 = 0x484

	/*
		Edt6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt6 = 0x485

	/*
		Edt7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt7 = 0x486

	/*
		Edt8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt8 = 0x487

	/*
		Edt9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Edt9 = 0x488

	/*
		FILEOPENORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FILEOPENORD = 0x600

	/*
		FINDDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FINDDLGORD = 0x604

	/*
		FONTDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FONTDLGORD = 0x606

	/*
		FORMATDLGORD30 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FORMATDLGORD30 = 0x608

	/*
		FORMATDLGORD31 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FORMATDLGORD31 = 0x607

	/*
		FSB_ENCARTA_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FSB_ENCARTA_MODE = 0x1

	/*
		FSB_FLAT_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FSB_FLAT_MODE = 0x2

	/*
		FSB_REGULAR_MODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FSB_REGULAR_MODE = 0x0

	/*
		Frm1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Frm1 = 0x434

	/*
		Frm2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Frm2 = 0x435

	/*
		Frm3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Frm3 = 0x436

	/*
		Frm4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Frm4 = 0x437

	/*
		GDTR_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDTR_MAX = 0x2

	/*
		GDTR_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDTR_MIN = 0x1

	/*
		GDT_ERROR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GDT_ERROR = -1

	/*
		GMR_DAYSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMR_DAYSTATE = 0x1

	/*
		GMR_VISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GMR_VISIBLE = 0x0

	/*
		Grp1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Grp1 = 0x430

	/*
		Grp2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Grp2 = 0x431

	/*
		Grp3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Grp3 = 0x432

	/*
		Grp4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Grp4 = 0x433

	/*
		HDM_CLEARFILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_CLEARFILTER = 0x1218

	/*
		HDM_CREATEDRAGIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_CREATEDRAGIMAGE = 0x1210

	/*
		HDM_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_DELETEITEM = 0x1202

	/*
		HDM_EDITFILTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_EDITFILTER = 0x1217

	/*
		HDM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_FIRST = 0x1200

	/*
		HDM_GETBITMAPMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETBITMAPMARGIN = 0x1215

	/*
		HDM_GETFOCUSEDITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETFOCUSEDITEM = 0x121b

	/*
		HDM_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETIMAGELIST = 0x1209

	/*
		HDM_GETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETITEM = 0x120b

	/*
		HDM_GETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETITEMA = 0x1203

	/*
		HDM_GETITEMCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETITEMCOUNT = 0x1200

	/*
		HDM_GETITEMDROPDOWNRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETITEMDROPDOWNRECT = 0x1219

	/*
		HDM_GETITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETITEMRECT = 0x1207

	/*
		HDM_GETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETITEMW = 0x120b

	/*
		HDM_GETORDERARRAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETORDERARRAY = 0x1211

	/*
		HDM_GETOVERFLOWRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETOVERFLOWRECT = 0x121a

	/*
		HDM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_GETUNICODEFORMAT = 0x2006

	/*
		HDM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_HITTEST = 0x1206

	/*
		HDM_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_INSERTITEM = 0x120a

	/*
		HDM_INSERTITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_INSERTITEMA = 0x1201

	/*
		HDM_INSERTITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_INSERTITEMW = 0x120a

	/*
		HDM_LAYOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_LAYOUT = 0x1205

	/*
		HDM_ORDERTOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_ORDERTOINDEX = 0x120f

	/*
		HDM_SETBITMAPMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETBITMAPMARGIN = 0x1214

	/*
		HDM_SETFILTERCHANGETIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETFILTERCHANGETIMEOUT = 0x1216

	/*
		HDM_SETFOCUSEDITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETFOCUSEDITEM = 0x121c

	/*
		HDM_SETHOTDIVIDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETHOTDIVIDER = 0x1213

	/*
		HDM_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETIMAGELIST = 0x1208

	/*
		HDM_SETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETITEM = 0x120c

	/*
		HDM_SETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETITEMA = 0x1204

	/*
		HDM_SETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETITEMW = 0x120c

	/*
		HDM_SETORDERARRAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETORDERARRAY = 0x1212

	/*
		HDM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDM_SETUNICODEFORMAT = 0x2005

	/*
		HDN_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_BEGINDRAG = 0xfffffeca

	/*
		HDN_BEGINFILTEREDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_BEGINFILTEREDIT = 0xfffffec6

	/*
		HDN_BEGINTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_BEGINTRACK = 0xfffffeba

	/*
		HDN_BEGINTRACKA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_BEGINTRACKA = 0xfffffece

	/*
		HDN_BEGINTRACKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_BEGINTRACKW = 0xfffffeba

	/*
		HDN_DIVIDERDBLCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_DIVIDERDBLCLICK = 0xfffffebb

	/*
		HDN_DIVIDERDBLCLICKA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_DIVIDERDBLCLICKA = 0xfffffecf

	/*
		HDN_DIVIDERDBLCLICKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_DIVIDERDBLCLICKW = 0xfffffebb

	/*
		HDN_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_DROPDOWN = 0xfffffec2

	/*
		HDN_ENDDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ENDDRAG = 0xfffffec9

	/*
		HDN_ENDFILTEREDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ENDFILTEREDIT = 0xfffffec5

	/*
		HDN_ENDTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ENDTRACK = 0xfffffeb9

	/*
		HDN_ENDTRACKA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ENDTRACKA = 0xfffffecd

	/*
		HDN_ENDTRACKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ENDTRACKW = 0xfffffeb9

	/*
		HDN_FILTERBTNCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_FILTERBTNCLICK = 0xfffffec7

	/*
		HDN_FILTERCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_FILTERCHANGE = 0xfffffec8

	/*
		HDN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_FIRST = 0xfffffed4

	/*
		HDN_GETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_GETDISPINFO = 0xfffffeb7

	/*
		HDN_GETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_GETDISPINFOA = 0xfffffecb

	/*
		HDN_GETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_GETDISPINFOW = 0xfffffeb7

	/*
		HDN_ITEMCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCHANGED = 0xfffffebf

	/*
		HDN_ITEMCHANGEDA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCHANGEDA = 0xfffffed3

	/*
		HDN_ITEMCHANGEDW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCHANGEDW = 0xfffffebf

	/*
		HDN_ITEMCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCHANGING = 0xfffffec0

	/*
		HDN_ITEMCHANGINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCHANGINGA = 0xfffffed4

	/*
		HDN_ITEMCHANGINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCHANGINGW = 0xfffffec0

	/*
		HDN_ITEMCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCLICK = 0xfffffebe

	/*
		HDN_ITEMCLICKA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCLICKA = 0xfffffed2

	/*
		HDN_ITEMCLICKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMCLICKW = 0xfffffebe

	/*
		HDN_ITEMDBLCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMDBLCLICK = 0xfffffebd

	/*
		HDN_ITEMDBLCLICKA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMDBLCLICKA = 0xfffffed1

	/*
		HDN_ITEMDBLCLICKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMDBLCLICKW = 0xfffffebd

	/*
		HDN_ITEMKEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMKEYDOWN = 0xfffffec3

	/*
		HDN_ITEMSTATEICONCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_ITEMSTATEICONCLICK = 0xfffffec4

	/*
		HDN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_LAST = 0xfffffe71

	/*
		HDN_OVERFLOWCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_OVERFLOWCLICK = 0xfffffec1

	/*
		HDN_TRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_TRACK = 0xfffffeb8

	/*
		HDN_TRACKA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_TRACKA = 0xfffffecc

	/*
		HDN_TRACKW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDN_TRACKW = 0xfffffeb8

	/*
		HDSIL_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDSIL_NORMAL = 0x0

	/*
		HDSIL_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDSIL_STATE = 0x1

	/*
		HDS_BUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_BUTTONS = 0x2

	/*
		HDS_CHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_CHECKBOXES = 0x400

	/*
		HDS_DRAGDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_DRAGDROP = 0x40

	/*
		HDS_FILTERBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_FILTERBAR = 0x100

	/*
		HDS_FLAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_FLAT = 0x200

	/*
		HDS_FULLDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_FULLDRAG = 0x80

	/*
		HDS_HIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_HIDDEN = 0x8

	/*
		HDS_HORZ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_HORZ = 0x0

	/*
		HDS_HOTTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_HOTTRACK = 0x4

	/*
		HDS_NOSIZING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_NOSIZING = 0x800

	/*
		HDS_OVERFLOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDS_OVERFLOW = 0x1000

	/*
		HIST_ADDTOFAVORITES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HIST_ADDTOFAVORITES = 0x3

	/*
		HIST_BACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HIST_BACK = 0x0

	/*
		HIST_FAVORITES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HIST_FAVORITES = 0x2

	/*
		HIST_FORWARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HIST_FORWARD = 0x1

	/*
		HIST_VIEWTREE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HIST_VIEWTREE = 0x4

	/*
		HKCOMB_A from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_A = 0x8

	/*
		HKCOMB_C from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_C = 0x4

	/*
		HKCOMB_CA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_CA = 0x40

	/*
		HKCOMB_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_NONE = 0x1

	/*
		HKCOMB_S from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_S = 0x2

	/*
		HKCOMB_SA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_SA = 0x20

	/*
		HKCOMB_SC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_SC = 0x10

	/*
		HKCOMB_SCA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKCOMB_SCA = 0x80

	/*
		HKM_GETHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKM_GETHOTKEY = 0x402

	/*
		HKM_SETHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKM_SETHOTKEY = 0x401

	/*
		HKM_SETRULES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKM_SETRULES = 0x403

	/*
		HOTKEYF_ALT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOTKEYF_ALT = 0x4

	/*
		HOTKEYF_CONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOTKEYF_CONTROL = 0x2

	/*
		HOTKEYF_EXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOTKEYF_EXT = 0x8

	/*
		HOTKEYF_SHIFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOTKEYF_SHIFT = 0x1

	/*
		HOVER_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HOVER_DEFAULT = 0xffffffff

	/*
		IDB_HIST_DISABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_HIST_DISABLED = 0xe

	/*
		IDB_HIST_HOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_HIST_HOT = 0xd

	/*
		IDB_HIST_LARGE_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_HIST_LARGE_COLOR = 0x9

	/*
		IDB_HIST_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_HIST_NORMAL = 0xc

	/*
		IDB_HIST_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_HIST_PRESSED = 0xf

	/*
		IDB_HIST_SMALL_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_HIST_SMALL_COLOR = 0x8

	/*
		IDB_STD_LARGE_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_STD_LARGE_COLOR = 0x1

	/*
		IDB_STD_SMALL_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_STD_SMALL_COLOR = 0x0

	/*
		IDB_VIEW_LARGE_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_VIEW_LARGE_COLOR = 0x5

	/*
		IDB_VIEW_SMALL_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDB_VIEW_SMALL_COLOR = 0x4

	/*
		IDC_MANAGE_LINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_MANAGE_LINK = 0x638

	/*
		ID_PSRESTARTWINDOWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ID_PSRESTARTWINDOWS = 0x2

	/*
		ILDI_PURGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILDI_PURGE = 0x1

	/*
		ILDI_QUERYACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILDI_QUERYACCESS = 0x8

	/*
		ILDI_RESETACCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILDI_RESETACCESS = 0x4

	/*
		ILDI_STANDBY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILDI_STANDBY = 0x2

	/*
		ILDRF_IMAGELOWQUALITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILDRF_IMAGELOWQUALITY = 0x1

	/*
		ILDRF_OVERLAYLOWQUALITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILDRF_OVERLAYLOWQUALITY = 0x10

	/*
		ILFIP_ALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILFIP_ALWAYS = 0x0

	/*
		ILFIP_FROMSTANDBY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILFIP_FROMSTANDBY = 0x1

	/*
		ILGOS_ALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILGOS_ALWAYS = 0x0

	/*
		ILGOS_FROMSTANDBY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILGOS_FROMSTANDBY = 0x1

	/*
		ILGT_ASYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILGT_ASYNC = 0x1

	/*
		ILGT_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILGT_NORMAL = 0x0

	/*
		ILR_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_DEFAULT = 0x0

	/*
		ILR_HORIZONTAL_CENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_HORIZONTAL_CENTER = 0x1

	/*
		ILR_HORIZONTAL_LEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_HORIZONTAL_LEFT = 0x0

	/*
		ILR_HORIZONTAL_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_HORIZONTAL_RIGHT = 0x2

	/*
		ILR_SCALE_ASPECTRATIO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_SCALE_ASPECTRATIO = 0x100

	/*
		ILR_SCALE_CLIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_SCALE_CLIP = 0x0

	/*
		ILR_VERTICAL_BOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_VERTICAL_BOTTOM = 0x20

	/*
		ILR_VERTICAL_CENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_VERTICAL_CENTER = 0x10

	/*
		ILR_VERTICAL_TOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILR_VERTICAL_TOP = 0x0

	/*
		ILS_ALPHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILS_ALPHA = 0x8

	/*
		ILS_GLOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILS_GLOW = 0x1

	/*
		ILS_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILS_NORMAL = 0x0

	/*
		ILS_SATURATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILS_SATURATE = 0x4

	/*
		ILS_SHADOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ILS_SHADOW = 0x2

	/*
		INFOTIPSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INFOTIPSIZE = 0x400

	/*
		INVALID_LINK_INDEX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INVALID_LINK_INDEX = -1

	/*
		IPM_CLEARADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPM_CLEARADDRESS = 0x464

	/*
		IPM_GETADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPM_GETADDRESS = 0x466

	/*
		IPM_ISBLANK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPM_ISBLANK = 0x469

	/*
		IPM_SETADDRESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPM_SETADDRESS = 0x465

	/*
		IPM_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPM_SETFOCUS = 0x468

	/*
		IPM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPM_SETRANGE = 0x467

	/*
		IPN_FIELDCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPN_FIELDCHANGED = 0xfffffca4

	/*
		IPN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPN_FIRST = 0xfffffca4

	/*
		IPN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IPN_LAST = 0xfffffc91

	/*
		I_IMAGECALLBACK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	I_IMAGECALLBACK = -1

	/*
		I_IMAGENONE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	I_IMAGENONE = -2

	/*
		I_INDENTCALLBACK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	I_INDENTCALLBACK = -1

	/*
		Ico1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Ico1 = 0x43c

	/*
		Ico2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Ico2 = 0x43d

	/*
		Ico3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Ico3 = 0x43e

	/*
		Ico4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Ico4 = 0x43f

	/*
		LM_GETIDEALHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LM_GETIDEALHEIGHT = 0x701

	/*
		LM_GETIDEALSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LM_GETIDEALSIZE = 0x701

	/*
		LM_GETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LM_GETITEM = 0x703

	/*
		LM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LM_HITTEST = 0x700

	/*
		LM_SETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LM_SETITEM = 0x702

	/*
		LVA_ALIGNLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVA_ALIGNLEFT = 0x1

	/*
		LVA_ALIGNTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVA_ALIGNTOP = 0x2

	/*
		LVA_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVA_DEFAULT = 0x0

	/*
		LVA_SNAPTOGRID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVA_SNAPTOGRID = 0x5

	/*
		LVCDRF_NOGROUPFRAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVCDRF_NOGROUPFRAME = 0x20000

	/*
		LVCDRF_NOSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVCDRF_NOSELECT = 0x10000

	/*
		LVFF_ITEMCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVFF_ITEMCOUNT = 0x1

	/*
		LVFIS_FOCUSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVFIS_FOCUSED = 0x1

	/*
		LVGGR_GROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGGR_GROUP = 0x0

	/*
		LVGGR_HEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGGR_HEADER = 0x1

	/*
		LVGGR_LABEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGGR_LABEL = 0x2

	/*
		LVGGR_SUBSETLINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGGR_SUBSETLINK = 0x3

	/*
		LVGMF_BORDERCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGMF_BORDERCOLOR = 0x2

	/*
		LVGMF_BORDERSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGMF_BORDERSIZE = 0x1

	/*
		LVGMF_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGMF_NONE = 0x0

	/*
		LVGMF_TEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVGMF_TEXTCOLOR = 0x4

	/*
		LVIM_AFTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVIM_AFTER = 0x1

	/*
		LVIR_BOUNDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVIR_BOUNDS = 0x0

	/*
		LVIR_ICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVIR_ICON = 0x1

	/*
		LVIR_LABEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVIR_LABEL = 0x2

	/*
		LVIR_SELECTBOUNDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVIR_SELECTBOUNDS = 0x3

	/*
		LVKF_ALT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVKF_ALT = 0x1

	/*
		LVKF_CONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVKF_CONTROL = 0x2

	/*
		LVKF_SHIFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVKF_SHIFT = 0x4

	/*
		LVM_APPROXIMATEVIEWRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_APPROXIMATEVIEWRECT = 0x1040

	/*
		LVM_ARRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_ARRANGE = 0x1016

	/*
		LVM_CANCELEDITLABEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_CANCELEDITLABEL = 0x10b3

	/*
		LVM_CREATEDRAGIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_CREATEDRAGIMAGE = 0x1021

	/*
		LVM_DELETEALLITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_DELETEALLITEMS = 0x1009

	/*
		LVM_DELETECOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_DELETECOLUMN = 0x101c

	/*
		LVM_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_DELETEITEM = 0x1008

	/*
		LVM_EDITLABEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_EDITLABEL = 0x1076

	/*
		LVM_EDITLABELA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_EDITLABELA = 0x1017

	/*
		LVM_EDITLABELW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_EDITLABELW = 0x1076

	/*
		LVM_ENABLEGROUPVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_ENABLEGROUPVIEW = 0x109d

	/*
		LVM_ENSUREVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_ENSUREVISIBLE = 0x1013

	/*
		LVM_FINDITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_FINDITEM = 0x1053

	/*
		LVM_FINDITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_FINDITEMA = 0x100d

	/*
		LVM_FINDITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_FINDITEMW = 0x1053

	/*
		LVM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_FIRST = 0x1000

	/*
		LVM_GETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETBKCOLOR = 0x1000

	/*
		LVM_GETBKIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETBKIMAGE = 0x108b

	/*
		LVM_GETBKIMAGEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETBKIMAGEA = 0x1045

	/*
		LVM_GETBKIMAGEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETBKIMAGEW = 0x108b

	/*
		LVM_GETCALLBACKMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCALLBACKMASK = 0x100a

	/*
		LVM_GETCOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCOLUMN = 0x105f

	/*
		LVM_GETCOLUMNA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCOLUMNA = 0x1019

	/*
		LVM_GETCOLUMNORDERARRAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCOLUMNORDERARRAY = 0x103b

	/*
		LVM_GETCOLUMNW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCOLUMNW = 0x105f

	/*
		LVM_GETCOLUMNWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCOLUMNWIDTH = 0x101d

	/*
		LVM_GETCOUNTPERPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETCOUNTPERPAGE = 0x1028

	/*
		LVM_GETEDITCONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETEDITCONTROL = 0x1018

	/*
		LVM_GETEMPTYTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETEMPTYTEXT = 0x10cc

	/*
		LVM_GETEXTENDEDLISTVIEWSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETEXTENDEDLISTVIEWSTYLE = 0x1037

	/*
		LVM_GETFOCUSEDGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETFOCUSEDGROUP = 0x105d

	/*
		LVM_GETFOOTERINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETFOOTERINFO = 0x10ce

	/*
		LVM_GETFOOTERITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETFOOTERITEM = 0x10d0

	/*
		LVM_GETFOOTERITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETFOOTERITEMRECT = 0x10cf

	/*
		LVM_GETFOOTERRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETFOOTERRECT = 0x10cd

	/*
		LVM_GETGROUPCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETGROUPCOUNT = 0x1098

	/*
		LVM_GETGROUPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETGROUPINFO = 0x1095

	/*
		LVM_GETGROUPINFOBYINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETGROUPINFOBYINDEX = 0x1099

	/*
		LVM_GETGROUPMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETGROUPMETRICS = 0x109c

	/*
		LVM_GETGROUPRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETGROUPRECT = 0x1062

	/*
		LVM_GETGROUPSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETGROUPSTATE = 0x105c

	/*
		LVM_GETHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETHEADER = 0x101f

	/*
		LVM_GETHOTCURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETHOTCURSOR = 0x103f

	/*
		LVM_GETHOTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETHOTITEM = 0x103d

	/*
		LVM_GETHOVERTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETHOVERTIME = 0x1048

	/*
		LVM_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETIMAGELIST = 0x1002

	/*
		LVM_GETINSERTMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETINSERTMARK = 0x10a7

	/*
		LVM_GETINSERTMARKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETINSERTMARKCOLOR = 0x10ab

	/*
		LVM_GETINSERTMARKRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETINSERTMARKRECT = 0x10a9

	/*
		LVM_GETISEARCHSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETISEARCHSTRING = 0x1075

	/*
		LVM_GETISEARCHSTRINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETISEARCHSTRINGA = 0x1034

	/*
		LVM_GETISEARCHSTRINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETISEARCHSTRINGW = 0x1075

	/*
		LVM_GETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEM = 0x104b

	/*
		LVM_GETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMA = 0x1005

	/*
		LVM_GETITEMCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMCOUNT = 0x1004

	/*
		LVM_GETITEMINDEXRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMINDEXRECT = 0x10d1

	/*
		LVM_GETITEMPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMPOSITION = 0x1010

	/*
		LVM_GETITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMRECT = 0x100e

	/*
		LVM_GETITEMSPACING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMSPACING = 0x1033

	/*
		LVM_GETITEMSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMSTATE = 0x102c

	/*
		LVM_GETITEMTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMTEXT = 0x1073

	/*
		LVM_GETITEMTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMTEXTA = 0x102d

	/*
		LVM_GETITEMTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMTEXTW = 0x1073

	/*
		LVM_GETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETITEMW = 0x104b

	/*
		LVM_GETNEXTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETNEXTITEM = 0x100c

	/*
		LVM_GETNEXTITEMINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETNEXTITEMINDEX = 0x10d3

	/*
		LVM_GETNUMBEROFWORKAREAS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETNUMBEROFWORKAREAS = 0x1049

	/*
		LVM_GETORIGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETORIGIN = 0x1029

	/*
		LVM_GETOUTLINECOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETOUTLINECOLOR = 0x10b0

	/*
		LVM_GETSELECTEDCOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSELECTEDCOLUMN = 0x10ae

	/*
		LVM_GETSELECTEDCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSELECTEDCOUNT = 0x1032

	/*
		LVM_GETSELECTIONMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSELECTIONMARK = 0x1042

	/*
		LVM_GETSTRINGWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSTRINGWIDTH = 0x1057

	/*
		LVM_GETSTRINGWIDTHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSTRINGWIDTHA = 0x1011

	/*
		LVM_GETSTRINGWIDTHW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSTRINGWIDTHW = 0x1057

	/*
		LVM_GETSUBITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETSUBITEMRECT = 0x1038

	/*
		LVM_GETTEXTBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETTEXTBKCOLOR = 0x1025

	/*
		LVM_GETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETTEXTCOLOR = 0x1023

	/*
		LVM_GETTILEINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETTILEINFO = 0x10a5

	/*
		LVM_GETTILEVIEWINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETTILEVIEWINFO = 0x10a3

	/*
		LVM_GETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETTOOLTIPS = 0x104e

	/*
		LVM_GETTOPINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETTOPINDEX = 0x1027

	/*
		LVM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETUNICODEFORMAT = 0x2006

	/*
		LVM_GETVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETVIEW = 0x108f

	/*
		LVM_GETVIEWRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETVIEWRECT = 0x1022

	/*
		LVM_GETWORKAREAS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_GETWORKAREAS = 0x1046

	/*
		LVM_HASGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_HASGROUP = 0x10a1

	/*
		LVM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_HITTEST = 0x1012

	/*
		LVM_INSERTCOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTCOLUMN = 0x1061

	/*
		LVM_INSERTCOLUMNA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTCOLUMNA = 0x101b

	/*
		LVM_INSERTCOLUMNW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTCOLUMNW = 0x1061

	/*
		LVM_INSERTGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTGROUP = 0x1091

	/*
		LVM_INSERTGROUPSORTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTGROUPSORTED = 0x109f

	/*
		LVM_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTITEM = 0x104d

	/*
		LVM_INSERTITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTITEMA = 0x1007

	/*
		LVM_INSERTITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTITEMW = 0x104d

	/*
		LVM_INSERTMARKHITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_INSERTMARKHITTEST = 0x10a8

	/*
		LVM_ISGROUPVIEWENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_ISGROUPVIEWENABLED = 0x10af

	/*
		LVM_ISITEMVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_ISITEMVISIBLE = 0x10b6

	/*
		LVM_MAPIDTOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_MAPIDTOINDEX = 0x10b5

	/*
		LVM_MAPINDEXTOID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_MAPINDEXTOID = 0x10b4

	/*
		LVM_MOVEGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_MOVEGROUP = 0x1097

	/*
		LVM_MOVEITEMTOGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_MOVEITEMTOGROUP = 0x109a

	/*
		LVM_REDRAWITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_REDRAWITEMS = 0x1015

	/*
		LVM_REMOVEALLGROUPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_REMOVEALLGROUPS = 0x10a0

	/*
		LVM_REMOVEGROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_REMOVEGROUP = 0x1096

	/*
		LVM_SCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SCROLL = 0x1014

	/*
		LVM_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETBKCOLOR = 0x1001

	/*
		LVM_SETBKIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETBKIMAGE = 0x108a

	/*
		LVM_SETBKIMAGEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETBKIMAGEA = 0x1044

	/*
		LVM_SETBKIMAGEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETBKIMAGEW = 0x108a

	/*
		LVM_SETCALLBACKMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETCALLBACKMASK = 0x100b

	/*
		LVM_SETCOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETCOLUMN = 0x1060

	/*
		LVM_SETCOLUMNA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETCOLUMNA = 0x101a

	/*
		LVM_SETCOLUMNORDERARRAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETCOLUMNORDERARRAY = 0x103a

	/*
		LVM_SETCOLUMNW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETCOLUMNW = 0x1060

	/*
		LVM_SETCOLUMNWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETCOLUMNWIDTH = 0x101e

	/*
		LVM_SETEXTENDEDLISTVIEWSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETEXTENDEDLISTVIEWSTYLE = 0x1036

	/*
		LVM_SETGROUPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETGROUPINFO = 0x1093

	/*
		LVM_SETGROUPMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETGROUPMETRICS = 0x109b

	/*
		LVM_SETHOTCURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETHOTCURSOR = 0x103e

	/*
		LVM_SETHOTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETHOTITEM = 0x103c

	/*
		LVM_SETHOVERTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETHOVERTIME = 0x1047

	/*
		LVM_SETICONSPACING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETICONSPACING = 0x1035

	/*
		LVM_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETIMAGELIST = 0x1003

	/*
		LVM_SETINFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETINFOTIP = 0x10ad

	/*
		LVM_SETINSERTMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETINSERTMARK = 0x10a6

	/*
		LVM_SETINSERTMARKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETINSERTMARKCOLOR = 0x10aa

	/*
		LVM_SETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEM = 0x104c

	/*
		LVM_SETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMA = 0x1006

	/*
		LVM_SETITEMCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMCOUNT = 0x102f

	/*
		LVM_SETITEMINDEXSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMINDEXSTATE = 0x10d2

	/*
		LVM_SETITEMPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMPOSITION = 0x100f

	/*
		LVM_SETITEMPOSITION32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMPOSITION32 = 0x1031

	/*
		LVM_SETITEMSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMSTATE = 0x102b

	/*
		LVM_SETITEMTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMTEXT = 0x1074

	/*
		LVM_SETITEMTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMTEXTA = 0x102e

	/*
		LVM_SETITEMTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMTEXTW = 0x1074

	/*
		LVM_SETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETITEMW = 0x104c

	/*
		LVM_SETOUTLINECOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETOUTLINECOLOR = 0x10b1

	/*
		LVM_SETSELECTEDCOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETSELECTEDCOLUMN = 0x108c

	/*
		LVM_SETSELECTIONMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETSELECTIONMARK = 0x1043

	/*
		LVM_SETTEXTBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETTEXTBKCOLOR = 0x1026

	/*
		LVM_SETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETTEXTCOLOR = 0x1024

	/*
		LVM_SETTILEINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETTILEINFO = 0x10a4

	/*
		LVM_SETTILEVIEWINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETTILEVIEWINFO = 0x10a2

	/*
		LVM_SETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETTOOLTIPS = 0x104a

	/*
		LVM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETUNICODEFORMAT = 0x2005

	/*
		LVM_SETVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETVIEW = 0x108e

	/*
		LVM_SETWORKAREAS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SETWORKAREAS = 0x1041

	/*
		LVM_SORTGROUPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SORTGROUPS = 0x109e

	/*
		LVM_SORTITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SORTITEMS = 0x1030

	/*
		LVM_SORTITEMSEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SORTITEMSEX = 0x1051

	/*
		LVM_SUBITEMHITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_SUBITEMHITTEST = 0x1039

	/*
		LVM_UPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVM_UPDATE = 0x102a

	/*
		LVNI_ABOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_ABOVE = 0x100

	/*
		LVNI_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_ALL = 0x0

	/*
		LVNI_BELOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_BELOW = 0x200

	/*
		LVNI_CUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_CUT = 0x4

	/*
		LVNI_DROPHILITED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_DROPHILITED = 0x8

	/*
		LVNI_FOCUSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_FOCUSED = 0x1

	/*
		LVNI_PREVIOUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_PREVIOUS = 0x20

	/*
		LVNI_SAMEGROUPONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_SAMEGROUPONLY = 0x80

	/*
		LVNI_SELECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_SELECTED = 0x2

	/*
		LVNI_TOLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_TOLEFT = 0x400

	/*
		LVNI_TORIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_TORIGHT = 0x800

	/*
		LVNI_VISIBLEONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_VISIBLEONLY = 0x40

	/*
		LVNI_VISIBLEORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNI_VISIBLEORDER = 0x10

	/*
		LVNSCH_DEFAULT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNSCH_DEFAULT = -1

	/*
		LVNSCH_ERROR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNSCH_ERROR = -2

	/*
		LVNSCH_IGNORE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVNSCH_IGNORE = -3

	/*
		LVN_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_BEGINDRAG = 0xffffff93

	/*
		LVN_BEGINLABELEDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_BEGINLABELEDIT = 0xffffff51

	/*
		LVN_BEGINLABELEDITA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_BEGINLABELEDITA = 0xffffff97

	/*
		LVN_BEGINLABELEDITW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_BEGINLABELEDITW = 0xffffff51

	/*
		LVN_BEGINRDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_BEGINRDRAG = 0xffffff91

	/*
		LVN_BEGINSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_BEGINSCROLL = 0xffffff4c

	/*
		LVN_COLUMNCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_COLUMNCLICK = 0xffffff94

	/*
		LVN_COLUMNDROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_COLUMNDROPDOWN = 0xffffff5c

	/*
		LVN_COLUMNOVERFLOWCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_COLUMNOVERFLOWCLICK = 0xffffff5a

	/*
		LVN_DELETEALLITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_DELETEALLITEMS = 0xffffff98

	/*
		LVN_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_DELETEITEM = 0xffffff99

	/*
		LVN_ENDLABELEDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ENDLABELEDIT = 0xffffff50

	/*
		LVN_ENDLABELEDITA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ENDLABELEDITA = 0xffffff96

	/*
		LVN_ENDLABELEDITW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ENDLABELEDITW = 0xffffff50

	/*
		LVN_ENDSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ENDSCROLL = 0xffffff4b

	/*
		LVN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_FIRST = 0xffffff9c

	/*
		LVN_GETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETDISPINFO = 0xffffff4f

	/*
		LVN_GETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETDISPINFOA = 0xffffff6a

	/*
		LVN_GETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETDISPINFOW = 0xffffff4f

	/*
		LVN_GETEMPTYMARKUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETEMPTYMARKUP = 0xffffff45

	/*
		LVN_GETINFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETINFOTIP = 0xffffff62

	/*
		LVN_GETINFOTIPA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETINFOTIPA = 0xffffff63

	/*
		LVN_GETINFOTIPW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_GETINFOTIPW = 0xffffff62

	/*
		LVN_HOTTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_HOTTRACK = 0xffffff87

	/*
		LVN_INCREMENTALSEARCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_INCREMENTALSEARCH = 0xffffff5d

	/*
		LVN_INCREMENTALSEARCHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_INCREMENTALSEARCHA = 0xffffff5e

	/*
		LVN_INCREMENTALSEARCHW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_INCREMENTALSEARCHW = 0xffffff5d

	/*
		LVN_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_INSERTITEM = 0xffffff9a

	/*
		LVN_ITEMACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ITEMACTIVATE = 0xffffff8e

	/*
		LVN_ITEMCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ITEMCHANGED = 0xffffff9b

	/*
		LVN_ITEMCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ITEMCHANGING = 0xffffff9c

	/*
		LVN_KEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_KEYDOWN = 0xffffff65

	/*
		LVN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_LAST = 0xffffff39

	/*
		LVN_LINKCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_LINKCLICK = 0xffffff48

	/*
		LVN_MARQUEEBEGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_MARQUEEBEGIN = 0xffffff64

	/*
		LVN_ODCACHEHINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ODCACHEHINT = 0xffffff8f

	/*
		LVN_ODFINDITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ODFINDITEM = 0xffffff4d

	/*
		LVN_ODFINDITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ODFINDITEMA = 0xffffff68

	/*
		LVN_ODFINDITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ODFINDITEMW = 0xffffff4d

	/*
		LVN_ODSTATECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_ODSTATECHANGED = 0xffffff8d

	/*
		LVN_SETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_SETDISPINFO = 0xffffff4e

	/*
		LVN_SETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_SETDISPINFOA = 0xffffff69

	/*
		LVN_SETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVN_SETDISPINFOW = 0xffffff4e

	/*
		LVSCW_AUTOSIZE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSCW_AUTOSIZE = -1

	/*
		LVSCW_AUTOSIZE_USEHEADER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSCW_AUTOSIZE_USEHEADER = -2

	/*
		LVSICF_NOINVALIDATEALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSICF_NOINVALIDATEALL = 0x1

	/*
		LVSICF_NOSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSICF_NOSCROLL = 0x2

	/*
		LVSIL_GROUPHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSIL_GROUPHEADER = 0x3

	/*
		LVSIL_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSIL_NORMAL = 0x0

	/*
		LVSIL_SMALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSIL_SMALL = 0x1

	/*
		LVSIL_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVSIL_STATE = 0x2

	/*
		LVS_ALIGNLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_ALIGNLEFT = 0x800

	/*
		LVS_ALIGNMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_ALIGNMASK = 0xc00

	/*
		LVS_ALIGNTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_ALIGNTOP = 0x0

	/*
		LVS_AUTOARRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_AUTOARRANGE = 0x100

	/*
		LVS_EDITLABELS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EDITLABELS = 0x200

	/*
		LVS_EX_AUTOAUTOARRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_AUTOAUTOARRANGE = 0x1000000

	/*
		LVS_EX_AUTOCHECKSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_AUTOCHECKSELECT = 0x8000000

	/*
		LVS_EX_AUTOSIZECOLUMNS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_AUTOSIZECOLUMNS = 0x10000000

	/*
		LVS_EX_BORDERSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_BORDERSELECT = 0x8000

	/*
		LVS_EX_CHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_CHECKBOXES = 0x4

	/*
		LVS_EX_COLUMNOVERFLOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_COLUMNOVERFLOW = 0x80000000

	/*
		LVS_EX_COLUMNSNAPPOINTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_COLUMNSNAPPOINTS = 0x40000000

	/*
		LVS_EX_DOUBLEBUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_DOUBLEBUFFER = 0x10000

	/*
		LVS_EX_FLATSB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_FLATSB = 0x100

	/*
		LVS_EX_FULLROWSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_FULLROWSELECT = 0x20

	/*
		LVS_EX_GRIDLINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_GRIDLINES = 0x1

	/*
		LVS_EX_HEADERDRAGDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_HEADERDRAGDROP = 0x10

	/*
		LVS_EX_HEADERINALLVIEWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_HEADERINALLVIEWS = 0x2000000

	/*
		LVS_EX_HIDELABELS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_HIDELABELS = 0x20000

	/*
		LVS_EX_INFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_INFOTIP = 0x400

	/*
		LVS_EX_JUSTIFYCOLUMNS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_JUSTIFYCOLUMNS = 0x200000

	/*
		LVS_EX_LABELTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_LABELTIP = 0x4000

	/*
		LVS_EX_MULTIWORKAREAS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_MULTIWORKAREAS = 0x2000

	/*
		LVS_EX_ONECLICKACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_ONECLICKACTIVATE = 0x40

	/*
		LVS_EX_REGIONAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_REGIONAL = 0x200

	/*
		LVS_EX_SIMPLESELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_SIMPLESELECT = 0x100000

	/*
		LVS_EX_SINGLEROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_SINGLEROW = 0x40000

	/*
		LVS_EX_SNAPTOGRID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_SNAPTOGRID = 0x80000

	/*
		LVS_EX_SUBITEMIMAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_SUBITEMIMAGES = 0x2

	/*
		LVS_EX_TRACKSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_TRACKSELECT = 0x8

	/*
		LVS_EX_TRANSPARENTBKGND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_TRANSPARENTBKGND = 0x400000

	/*
		LVS_EX_TRANSPARENTSHADOWTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_TRANSPARENTSHADOWTEXT = 0x800000

	/*
		LVS_EX_TWOCLICKACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_TWOCLICKACTIVATE = 0x80

	/*
		LVS_EX_UNDERLINECOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_UNDERLINECOLD = 0x1000

	/*
		LVS_EX_UNDERLINEHOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_EX_UNDERLINEHOT = 0x800

	/*
		LVS_ICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_ICON = 0x0

	/*
		LVS_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_LIST = 0x3

	/*
		LVS_NOCOLUMNHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_NOCOLUMNHEADER = 0x4000

	/*
		LVS_NOLABELWRAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_NOLABELWRAP = 0x80

	/*
		LVS_NOSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_NOSCROLL = 0x2000

	/*
		LVS_NOSORTHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_NOSORTHEADER = 0x8000

	/*
		LVS_OWNERDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_OWNERDATA = 0x1000

	/*
		LVS_OWNERDRAWFIXED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_OWNERDRAWFIXED = 0x400

	/*
		LVS_REPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_REPORT = 0x1

	/*
		LVS_SHAREIMAGELISTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_SHAREIMAGELISTS = 0x40

	/*
		LVS_SHOWSELALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_SHOWSELALWAYS = 0x8

	/*
		LVS_SINGLESEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_SINGLESEL = 0x4

	/*
		LVS_SMALLICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_SMALLICON = 0x2

	/*
		LVS_SORTASCENDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_SORTASCENDING = 0x10

	/*
		LVS_SORTDESCENDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_SORTDESCENDING = 0x20

	/*
		LVS_TYPEMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_TYPEMASK = 0x3

	/*
		LVS_TYPESTYLEMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVS_TYPESTYLEMASK = 0xfc00

	/*
		LVTVIF_EXTENDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LVTVIF_EXTENDED = 0x4

	/*
		LV_MAX_WORKAREAS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_MAX_WORKAREAS = 0x10

	/*
		LV_VIEW_DETAILS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_VIEW_DETAILS = 0x1

	/*
		LV_VIEW_ICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_VIEW_ICON = 0x0

	/*
		LV_VIEW_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_VIEW_LIST = 0x3

	/*
		LV_VIEW_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_VIEW_MAX = 0x4

	/*
		LV_VIEW_SMALLICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_VIEW_SMALLICON = 0x2

	/*
		LV_VIEW_TILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LV_VIEW_TILE = 0x4

	/*
		LWS_IGNORERETURN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LWS_IGNORERETURN = 0x2

	/*
		LWS_NOPREFIX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LWS_NOPREFIX = 0x4

	/*
		LWS_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LWS_RIGHT = 0x20

	/*
		LWS_TRANSPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LWS_TRANSPARENT = 0x1

	/*
		LWS_USECUSTOMTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LWS_USECUSTOMTEXT = 0x10

	/*
		LWS_USEVISUALSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LWS_USEVISUALSTYLE = 0x8

	/*
		Lst1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst1 = 0x460

	/*
		Lst10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst10 = 0x469

	/*
		Lst11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst11 = 0x46a

	/*
		Lst12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst12 = 0x46b

	/*
		Lst13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst13 = 0x46c

	/*
		Lst14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst14 = 0x46d

	/*
		Lst15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst15 = 0x46e

	/*
		Lst16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst16 = 0x46f

	/*
		Lst2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst2 = 0x461

	/*
		Lst3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst3 = 0x462

	/*
		Lst4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst4 = 0x463

	/*
		Lst5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst5 = 0x464

	/*
		Lst6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst6 = 0x465

	/*
		Lst7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst7 = 0x466

	/*
		Lst8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst8 = 0x467

	/*
		Lst9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Lst9 = 0x468

	/*
		MAXPROPPAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXPROPPAGES = 0x64

	/*
		MAX_INTLIST_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_INTLIST_COUNT = 0x192

	/*
		MAX_LINKID_TEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_LINKID_TEXT = 0x30

	/*
		MAX_THEMECOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_THEMECOLOR = 0x40

	/*
		MAX_THEMESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_THEMESIZE = 0x40

	/*
		MCM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_FIRST = 0x1000

	/*
		MCM_GETCALENDARBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCALENDARBORDER = 0x101f

	/*
		MCM_GETCALENDARCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCALENDARCOUNT = 0x1017

	/*
		MCM_GETCALENDARGRIDINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCALENDARGRIDINFO = 0x1018

	/*
		MCM_GETCALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCALID = 0x101b

	/*
		MCM_GETCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCOLOR = 0x100b

	/*
		MCM_GETCURRENTVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCURRENTVIEW = 0x1016

	/*
		MCM_GETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETCURSEL = 0x1001

	/*
		MCM_GETFIRSTDAYOFWEEK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETFIRSTDAYOFWEEK = 0x1010

	/*
		MCM_GETMAXSELCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETMAXSELCOUNT = 0x1003

	/*
		MCM_GETMAXTODAYWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETMAXTODAYWIDTH = 0x1015

	/*
		MCM_GETMINREQRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETMINREQRECT = 0x1009

	/*
		MCM_GETMONTHDELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETMONTHDELTA = 0x1013

	/*
		MCM_GETMONTHRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETMONTHRANGE = 0x1007

	/*
		MCM_GETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETRANGE = 0x1011

	/*
		MCM_GETSELRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETSELRANGE = 0x1005

	/*
		MCM_GETTODAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETTODAY = 0x100d

	/*
		MCM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_GETUNICODEFORMAT = 0x2006

	/*
		MCM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_HITTEST = 0x100e

	/*
		MCM_SETCALENDARBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETCALENDARBORDER = 0x101e

	/*
		MCM_SETCALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETCALID = 0x101c

	/*
		MCM_SETCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETCOLOR = 0x100a

	/*
		MCM_SETCURRENTVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETCURRENTVIEW = 0x1020

	/*
		MCM_SETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETCURSEL = 0x1002

	/*
		MCM_SETDAYSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETDAYSTATE = 0x1008

	/*
		MCM_SETFIRSTDAYOFWEEK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETFIRSTDAYOFWEEK = 0x100f

	/*
		MCM_SETMAXSELCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETMAXSELCOUNT = 0x1004

	/*
		MCM_SETMONTHDELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETMONTHDELTA = 0x1014

	/*
		MCM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETRANGE = 0x1012

	/*
		MCM_SETSELRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETSELRANGE = 0x1006

	/*
		MCM_SETTODAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETTODAY = 0x100c

	/*
		MCM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SETUNICODEFORMAT = 0x2005

	/*
		MCM_SIZERECTTOMIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCM_SIZERECTTOMIN = 0x101d

	/*
		MCN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCN_FIRST = 0xfffffd16

	/*
		MCN_GETDAYSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCN_GETDAYSTATE = 0xfffffd15

	/*
		MCN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCN_LAST = 0xfffffd10

	/*
		MCN_SELCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCN_SELCHANGE = 0xfffffd13

	/*
		MCN_SELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCN_SELECT = 0xfffffd16

	/*
		MCN_VIEWCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCN_VIEWCHANGE = 0xfffffd12

	/*
		MCSC_BACKGROUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCSC_BACKGROUND = 0x0

	/*
		MCSC_MONTHBK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCSC_MONTHBK = 0x4

	/*
		MCSC_TEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCSC_TEXT = 0x1

	/*
		MCSC_TITLEBK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCSC_TITLEBK = 0x2

	/*
		MCSC_TITLETEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCSC_TITLETEXT = 0x3

	/*
		MCSC_TRAILINGTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCSC_TRAILINGTEXT = 0x5

	/*
		MCS_DAYSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_DAYSTATE = 0x1

	/*
		MCS_MULTISELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_MULTISELECT = 0x2

	/*
		MCS_NOSELCHANGEONNAV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_NOSELCHANGEONNAV = 0x100

	/*
		MCS_NOTODAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_NOTODAY = 0x10

	/*
		MCS_NOTODAYCIRCLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_NOTODAYCIRCLE = 0x8

	/*
		MCS_NOTRAILINGDATES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_NOTRAILINGDATES = 0x40

	/*
		MCS_SHORTDAYSOFWEEK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_SHORTDAYSOFWEEK = 0x80

	/*
		MCS_WEEKNUMBERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MCS_WEEKNUMBERS = 0x4

	/*
		MSGF_COMMCTRL_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_COMMCTRL_BEGINDRAG = 0x4200

	/*
		MSGF_COMMCTRL_DRAGSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_COMMCTRL_DRAGSELECT = 0x4202

	/*
		MSGF_COMMCTRL_SIZEHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_COMMCTRL_SIZEHEADER = 0x4201

	/*
		MSGF_COMMCTRL_TOOLBARCUST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_COMMCTRL_TOOLBARCUST = 0x4203

	/*
		MULTIFILEOPENORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MULTIFILEOPENORD = 0x601

	/*
		NEWFILEOPENORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEWFILEOPENORD = 0x60b

	/*
		NEWFILEOPENV2ORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEWFILEOPENV2ORD = 0x610

	/*
		NEWFILEOPENV3ORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEWFILEOPENV3ORD = 0x611

	/*
		NEWFORMATDLGWITHLINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NEWFORMATDLGWITHLINK = 0x637

	/*
		NFS_ALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFS_ALL = 0x10

	/*
		NFS_BUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFS_BUTTON = 0x8

	/*
		NFS_EDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFS_EDIT = 0x1

	/*
		NFS_LISTCOMBO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFS_LISTCOMBO = 0x4

	/*
		NFS_STATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFS_STATIC = 0x2

	/*
		NFS_USEFONTASSOC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFS_USEFONTASSOC = 0x20

	/*
		NM_CHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_CHAR = 0xffffffee

	/*
		NM_CLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_CLICK = 0xfffffffe

	/*
		NM_CUSTOMDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_CUSTOMDRAW = 0xfffffff4

	/*
		NM_CUSTOMTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_CUSTOMTEXT = 0xffffffe8

	/*
		NM_DBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_DBLCLK = 0xfffffffd

	/*
		NM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_FIRST = 0x0

	/*
		NM_FONTCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_FONTCHANGED = 0xffffffe9

	/*
		NM_GETCUSTOMSPLITRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_GETCUSTOMSPLITRECT = 0xfffffb21

	/*
		NM_HOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_HOVER = 0xfffffff3

	/*
		NM_KEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_KEYDOWN = 0xfffffff1

	/*
		NM_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_KILLFOCUS = 0xfffffff8

	/*
		NM_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_LAST = 0xffffff9d

	/*
		NM_LDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_LDOWN = 0xffffffec

	/*
		NM_NCHITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_NCHITTEST = 0xfffffff2

	/*
		NM_OUTOFMEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_OUTOFMEMORY = 0xffffffff

	/*
		NM_RCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_RCLICK = 0xfffffffb

	/*
		NM_RDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_RDBLCLK = 0xfffffffa

	/*
		NM_RDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_RDOWN = 0xffffffeb

	/*
		NM_RELEASEDCAPTURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_RELEASEDCAPTURE = 0xfffffff0

	/*
		NM_RETURN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_RETURN = 0xfffffffc

	/*
		NM_SETCURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_SETCURSOR = 0xffffffef

	/*
		NM_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_SETFOCUS = 0xfffffff9

	/*
		NM_THEMECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_THEMECHANGED = 0xffffffea

	/*
		NM_TOOLTIPSCREATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_TOOLTIPSCREATED = 0xffffffed

	/*
		NM_TVSTATEIMAGECHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NM_TVSTATEIMAGECHANGING = 0xffffffe8

	/*
		ODT_HEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ODT_HEADER = 0x64

	/*
		PAGESETUPDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAGESETUPDLGORD = 0x60a

	/*
		PAGESETUPDLGORDMOTIF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PAGESETUPDLGORDMOTIF = 0x60e

	/*
		PBM_DELTAPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_DELTAPOS = 0x403

	/*
		PBM_GETBARCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_GETBARCOLOR = 0x40f

	/*
		PBM_GETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_GETBKCOLOR = 0x40e

	/*
		PBM_GETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_GETPOS = 0x408

	/*
		PBM_GETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_GETRANGE = 0x407

	/*
		PBM_GETSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_GETSTATE = 0x411

	/*
		PBM_GETSTEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_GETSTEP = 0x40d

	/*
		PBM_SETBARCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETBARCOLOR = 0x409

	/*
		PBM_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETBKCOLOR = 0x2001

	/*
		PBM_SETMARQUEE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETMARQUEE = 0x40a

	/*
		PBM_SETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETPOS = 0x402

	/*
		PBM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETRANGE = 0x401

	/*
		PBM_SETRANGE32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETRANGE32 = 0x406

	/*
		PBM_SETSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETSTATE = 0x410

	/*
		PBM_SETSTEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_SETSTEP = 0x404

	/*
		PBM_STEPIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBM_STEPIT = 0x405

	/*
		PBST_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBST_ERROR = 0x2

	/*
		PBST_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBST_NORMAL = 0x1

	/*
		PBST_PAUSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBST_PAUSED = 0x3

	/*
		PBS_MARQUEE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBS_MARQUEE = 0x8

	/*
		PBS_SMOOTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBS_SMOOTH = 0x1

	/*
		PBS_SMOOTHREVERSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBS_SMOOTHREVERSE = 0x10

	/*
		PBS_VERTICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBS_VERTICAL = 0x4

	/*
		PGB_BOTTOMORRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGB_BOTTOMORRIGHT = 0x1

	/*
		PGB_TOPORLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGB_TOPORLEFT = 0x0

	/*
		PGF_DEPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGF_DEPRESSED = 0x4

	/*
		PGF_GRAYED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGF_GRAYED = 0x2

	/*
		PGF_HOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGF_HOT = 0x8

	/*
		PGF_INVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGF_INVISIBLE = 0x0

	/*
		PGF_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGF_NORMAL = 0x1

	/*
		PGM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_FIRST = 0x1400

	/*
		PGM_FORWARDMOUSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_FORWARDMOUSE = 0x1403

	/*
		PGM_GETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_GETBKCOLOR = 0x1405

	/*
		PGM_GETBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_GETBORDER = 0x1407

	/*
		PGM_GETBUTTONSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_GETBUTTONSIZE = 0x140b

	/*
		PGM_GETBUTTONSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_GETBUTTONSTATE = 0x140c

	/*
		PGM_GETDROPTARGET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_GETDROPTARGET = 0x2004

	/*
		PGM_GETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_GETPOS = 0x1409

	/*
		PGM_RECALCSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_RECALCSIZE = 0x1402

	/*
		PGM_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_SETBKCOLOR = 0x1404

	/*
		PGM_SETBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_SETBORDER = 0x1406

	/*
		PGM_SETBUTTONSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_SETBUTTONSIZE = 0x140a

	/*
		PGM_SETCHILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_SETCHILD = 0x1401

	/*
		PGM_SETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_SETPOS = 0x1408

	/*
		PGM_SETSCROLLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGM_SETSCROLLINFO = 0x140d

	/*
		PGN_CALCSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGN_CALCSIZE = 0xfffffc7a

	/*
		PGN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGN_FIRST = 0xfffffc7c

	/*
		PGN_HOTITEMCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGN_HOTITEMCHANGE = 0xfffffc79

	/*
		PGN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGN_LAST = 0xfffffc4a

	/*
		PGN_SCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGN_SCROLL = 0xfffffc7b

	/*
		PGS_AUTOSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGS_AUTOSCROLL = 0x2

	/*
		PGS_DRAGNDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGS_DRAGNDROP = 0x4

	/*
		PGS_HORZ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGS_HORZ = 0x1

	/*
		PGS_VERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PGS_VERT = 0x0

	/*
		PRINTDLGEXORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRINTDLGEXORD = 0x60d

	/*
		PRINTDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRINTDLGORD = 0x602

	/*
		PRNSETUPDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRNSETUPDLGORD = 0x603

	/*
		PROP_LG_CXDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROP_LG_CXDLG = 0xfc

	/*
		PROP_LG_CYDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROP_LG_CYDLG = 0xda

	/*
		PROP_MED_CXDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROP_MED_CXDLG = 0xe3

	/*
		PROP_MED_CYDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROP_MED_CYDLG = 0xd7

	/*
		PROP_SM_CXDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROP_SM_CXDLG = 0xd4

	/*
		PROP_SM_CYDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PROP_SM_CYDLG = 0xbc

	/*
		PSBTN_APPLYNOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_APPLYNOW = 0x4

	/*
		PSBTN_BACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_BACK = 0x0

	/*
		PSBTN_CANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_CANCEL = 0x5

	/*
		PSBTN_FINISH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_FINISH = 0x2

	/*
		PSBTN_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_HELP = 0x6

	/*
		PSBTN_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_MAX = 0x6

	/*
		PSBTN_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_NEXT = 0x1

	/*
		PSBTN_OK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSBTN_OK = 0x3

	/*
		PSCB_BUTTONPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSCB_BUTTONPRESSED = 0x3

	/*
		PSCB_INITIALIZED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSCB_INITIALIZED = 0x1

	/*
		PSCB_PRECREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSCB_PRECREATE = 0x2

	/*
		PSH_AEROWIZARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_AEROWIZARD = 0x4000

	/*
		PSH_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_DEFAULT = 0x0

	/*
		PSH_HASHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_HASHELP = 0x200

	/*
		PSH_HEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_HEADER = 0x80000

	/*
		PSH_HEADERBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_HEADERBITMAP = 0x8000000

	/*
		PSH_MODELESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_MODELESS = 0x400

	/*
		PSH_NOAPPLYNOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_NOAPPLYNOW = 0x80

	/*
		PSH_NOCONTEXTHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_NOCONTEXTHELP = 0x2000000

	/*
		PSH_NOMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_NOMARGIN = 0x10000000

	/*
		PSH_PROPSHEETPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_PROPSHEETPAGE = 0x8

	/*
		PSH_PROPTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_PROPTITLE = 0x1

	/*
		PSH_RESIZABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_RESIZABLE = 0x4000000

	/*
		PSH_RTLREADING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_RTLREADING = 0x800

	/*
		PSH_STRETCHWATERMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_STRETCHWATERMARK = 0x40000

	/*
		PSH_USECALLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USECALLBACK = 0x100

	/*
		PSH_USEHBMHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEHBMHEADER = 0x100000

	/*
		PSH_USEHBMWATERMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEHBMWATERMARK = 0x10000

	/*
		PSH_USEHICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEHICON = 0x2

	/*
		PSH_USEHPLWATERMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEHPLWATERMARK = 0x20000

	/*
		PSH_USEICONID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEICONID = 0x4

	/*
		PSH_USEPAGELANG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEPAGELANG = 0x200000

	/*
		PSH_USEPSTARTPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_USEPSTARTPAGE = 0x40

	/*
		PSH_WATERMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_WATERMARK = 0x8000

	/*
		PSH_WIZARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_WIZARD = 0x20

	/*
		PSH_WIZARD97 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_WIZARD97 = 0x2000

	/*
		PSH_WIZARDCONTEXTHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_WIZARDCONTEXTHELP = 0x1000

	/*
		PSH_WIZARDHASFINISH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_WIZARDHASFINISH = 0x10

	/*
		PSH_WIZARD_LITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSH_WIZARD_LITE = 0x400000

	/*
		PSM_ADDPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_ADDPAGE = 0x467

	/*
		PSM_APPLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_APPLY = 0x46e

	/*
		PSM_CANCELTOCLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_CANCELTOCLOSE = 0x46b

	/*
		PSM_CHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_CHANGED = 0x468

	/*
		PSM_ENABLEWIZBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_ENABLEWIZBUTTONS = 0x48b

	/*
		PSM_GETCURRENTPAGEHWND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_GETCURRENTPAGEHWND = 0x476

	/*
		PSM_GETRESULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_GETRESULT = 0x487

	/*
		PSM_GETTABCONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_GETTABCONTROL = 0x474

	/*
		PSM_HWNDTOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_HWNDTOINDEX = 0x481

	/*
		PSM_IDTOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_IDTOINDEX = 0x485

	/*
		PSM_INDEXTOHWND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_INDEXTOHWND = 0x482

	/*
		PSM_INDEXTOID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_INDEXTOID = 0x486

	/*
		PSM_INDEXTOPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_INDEXTOPAGE = 0x484

	/*
		PSM_INSERTPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_INSERTPAGE = 0x477

	/*
		PSM_ISDIALOGMESSAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_ISDIALOGMESSAGE = 0x475

	/*
		PSM_PAGETOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_PAGETOINDEX = 0x483

	/*
		PSM_PRESSBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_PRESSBUTTON = 0x471

	/*
		PSM_QUERYSIBLINGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_QUERYSIBLINGS = 0x46c

	/*
		PSM_REBOOTSYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_REBOOTSYSTEM = 0x46a

	/*
		PSM_RECALCPAGESIZES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_RECALCPAGESIZES = 0x488

	/*
		PSM_REMOVEPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_REMOVEPAGE = 0x466

	/*
		PSM_RESTARTWINDOWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_RESTARTWINDOWS = 0x469

	/*
		PSM_SETBUTTONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETBUTTONTEXT = 0x48c

	/*
		PSM_SETBUTTONTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETBUTTONTEXTW = 0x48c

	/*
		PSM_SETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETCURSEL = 0x465

	/*
		PSM_SETCURSELID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETCURSELID = 0x472

	/*
		PSM_SETFINISHTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETFINISHTEXT = 0x479

	/*
		PSM_SETFINISHTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETFINISHTEXTA = 0x473

	/*
		PSM_SETFINISHTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETFINISHTEXTW = 0x479

	/*
		PSM_SETHEADERSUBTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETHEADERSUBTITLE = 0x480

	/*
		PSM_SETHEADERSUBTITLEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETHEADERSUBTITLEA = 0x47f

	/*
		PSM_SETHEADERSUBTITLEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETHEADERSUBTITLEW = 0x480

	/*
		PSM_SETHEADERTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETHEADERTITLE = 0x47e

	/*
		PSM_SETHEADERTITLEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETHEADERTITLEA = 0x47d

	/*
		PSM_SETHEADERTITLEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETHEADERTITLEW = 0x47e

	/*
		PSM_SETNEXTTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETNEXTTEXT = 0x489

	/*
		PSM_SETNEXTTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETNEXTTEXTW = 0x489

	/*
		PSM_SETTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETTITLE = 0x478

	/*
		PSM_SETTITLEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETTITLEA = 0x46f

	/*
		PSM_SETTITLEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETTITLEW = 0x478

	/*
		PSM_SETWIZBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SETWIZBUTTONS = 0x470

	/*
		PSM_SHOWWIZBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_SHOWWIZBUTTONS = 0x48a

	/*
		PSM_UNCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSM_UNCHANGED = 0x46d

	/*
		PSNRET_INVALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSNRET_INVALID = 0x1

	/*
		PSNRET_INVALID_NOCHANGEPAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSNRET_INVALID_NOCHANGEPAGE = 0x2

	/*
		PSNRET_MESSAGEHANDLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSNRET_MESSAGEHANDLED = 0x3

	/*
		PSNRET_NOERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSNRET_NOERROR = 0x0

	/*
		PSN_APPLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_APPLY = 0xffffff36

	/*
		PSN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_FIRST = 0xffffff38

	/*
		PSN_GETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_GETOBJECT = 0xffffff2e

	/*
		PSN_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_HELP = 0xffffff33

	/*
		PSN_KILLACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_KILLACTIVE = 0xffffff37

	/*
		PSN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_LAST = 0xfffffed5

	/*
		PSN_QUERYCANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_QUERYCANCEL = 0xffffff2f

	/*
		PSN_QUERYINITIALFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_QUERYINITIALFOCUS = 0xffffff2b

	/*
		PSN_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_RESET = 0xffffff35

	/*
		PSN_SETACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_SETACTIVE = 0xffffff38

	/*
		PSN_TRANSLATEACCELERATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_TRANSLATEACCELERATOR = 0xffffff2c

	/*
		PSN_WIZBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_WIZBACK = 0xffffff32

	/*
		PSN_WIZFINISH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_WIZFINISH = 0xffffff30

	/*
		PSN_WIZNEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSN_WIZNEXT = 0xffffff31

	/*
		PSP_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_DEFAULT = 0x0

	/*
		PSP_DLGINDIRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_DLGINDIRECT = 0x1

	/*
		PSP_HASHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_HASHELP = 0x20

	/*
		PSP_HIDEHEADER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_HIDEHEADER = 0x800

	/*
		PSP_PREMATURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_PREMATURE = 0x400

	/*
		PSP_RTLREADING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_RTLREADING = 0x10

	/*
		PSP_USECALLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USECALLBACK = 0x80

	/*
		PSP_USEFUSIONCONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USEFUSIONCONTEXT = 0x4000

	/*
		PSP_USEHEADERSUBTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USEHEADERSUBTITLE = 0x2000

	/*
		PSP_USEHEADERTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USEHEADERTITLE = 0x1000

	/*
		PSP_USEHICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USEHICON = 0x2

	/*
		PSP_USEICONID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USEICONID = 0x4

	/*
		PSP_USEREFPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USEREFPARENT = 0x40

	/*
		PSP_USETITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSP_USETITLE = 0x8

	/*
		PSWIZBF_ELEVATIONREQUIRED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZBF_ELEVATIONREQUIRED = 0x1

	/*
		PSWIZB_BACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_BACK = 0x1

	/*
		PSWIZB_CANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_CANCEL = 0x10

	/*
		PSWIZB_DISABLEDFINISH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_DISABLEDFINISH = 0x8

	/*
		PSWIZB_FINISH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_FINISH = 0x4

	/*
		PSWIZB_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_NEXT = 0x2

	/*
		PSWIZB_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_RESTORE = 0x1

	/*
		PSWIZB_SHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PSWIZB_SHOW = 0x0

	/*
		Psh1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh1 = 0x400

	/*
		Psh10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh10 = 0x409

	/*
		Psh11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh11 = 0x40a

	/*
		Psh12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh12 = 0x40b

	/*
		Psh13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh13 = 0x40c

	/*
		Psh14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh14 = 0x40d

	/*
		Psh15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh15 = 0x40e

	/*
		Psh16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh16 = 0x40f

	/*
		Psh2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh2 = 0x401

	/*
		Psh3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh3 = 0x402

	/*
		Psh4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh4 = 0x403

	/*
		Psh5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh5 = 0x404

	/*
		Psh6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh6 = 0x405

	/*
		Psh7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh7 = 0x406

	/*
		Psh8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh8 = 0x407

	/*
		Psh9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Psh9 = 0x408

	/*
		PshHelp from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PshHelp = 0x40e

	/*
		RBAB_ADDBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBAB_ADDBAND = 0x2

	/*
		RBAB_AUTOSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBAB_AUTOSIZE = 0x1

	/*
		RBBIM_BACKGROUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_BACKGROUND = 0x80

	/*
		RBBIM_CHEVRONLOCATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_CHEVRONLOCATION = 0x1000

	/*
		RBBIM_CHEVRONSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_CHEVRONSTATE = 0x2000

	/*
		RBBIM_CHILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_CHILD = 0x10

	/*
		RBBIM_CHILDSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_CHILDSIZE = 0x20

	/*
		RBBIM_COLORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_COLORS = 0x2

	/*
		RBBIM_HEADERSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_HEADERSIZE = 0x800

	/*
		RBBIM_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_ID = 0x100

	/*
		RBBIM_IDEALSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_IDEALSIZE = 0x200

	/*
		RBBIM_IMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_IMAGE = 0x8

	/*
		RBBIM_LPARAM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_LPARAM = 0x400

	/*
		RBBIM_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_SIZE = 0x40

	/*
		RBBIM_STYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_STYLE = 0x1

	/*
		RBBIM_TEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBIM_TEXT = 0x4

	/*
		RBBS_BREAK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_BREAK = 0x1

	/*
		RBBS_CHILDEDGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_CHILDEDGE = 0x4

	/*
		RBBS_FIXEDBMP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_FIXEDBMP = 0x20

	/*
		RBBS_FIXEDSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_FIXEDSIZE = 0x2

	/*
		RBBS_GRIPPERALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_GRIPPERALWAYS = 0x80

	/*
		RBBS_HIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_HIDDEN = 0x8

	/*
		RBBS_HIDETITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_HIDETITLE = 0x400

	/*
		RBBS_NOGRIPPER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_NOGRIPPER = 0x100

	/*
		RBBS_NOVERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_NOVERT = 0x10

	/*
		RBBS_TOPALIGN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_TOPALIGN = 0x800

	/*
		RBBS_USECHEVRON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_USECHEVRON = 0x200

	/*
		RBBS_VARIABLEHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBBS_VARIABLEHEIGHT = 0x40

	/*
		RBHT_CAPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBHT_CAPTION = 0x2

	/*
		RBHT_CHEVRON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBHT_CHEVRON = 0x8

	/*
		RBHT_CLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBHT_CLIENT = 0x3

	/*
		RBHT_GRABBER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBHT_GRABBER = 0x4

	/*
		RBHT_NOWHERE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBHT_NOWHERE = 0x1

	/*
		RBHT_SPLITTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBHT_SPLITTER = 0x10

	/*
		RBIM_IMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBIM_IMAGELIST = 0x1

	/*
		RBN_AUTOBREAK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_AUTOBREAK = 0xfffffcab

	/*
		RBN_AUTOSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_AUTOSIZE = 0xfffffcbe

	/*
		RBN_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_BEGINDRAG = 0xfffffcbd

	/*
		RBN_CHEVRONPUSHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_CHEVRONPUSHED = 0xfffffcb7

	/*
		RBN_CHILDSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_CHILDSIZE = 0xfffffcb9

	/*
		RBN_DELETEDBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_DELETEDBAND = 0xfffffcba

	/*
		RBN_DELETINGBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_DELETINGBAND = 0xfffffcbb

	/*
		RBN_ENDDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_ENDDRAG = 0xfffffcbc

	/*
		RBN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_FIRST = 0xfffffcc1

	/*
		RBN_GETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_GETOBJECT = 0xfffffcc0

	/*
		RBN_HEIGHTCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_HEIGHTCHANGE = 0xfffffcc1

	/*
		RBN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_LAST = 0xfffffca5

	/*
		RBN_LAYOUTCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_LAYOUTCHANGED = 0xfffffcbf

	/*
		RBN_MINMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_MINMAX = 0xfffffcac

	/*
		RBN_SPLITTERDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBN_SPLITTERDRAG = 0xfffffcb6

	/*
		RBSTR_CHANGERECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBSTR_CHANGERECT = 0x1

	/*
		RBS_AUTOSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_AUTOSIZE = 0x2000

	/*
		RBS_BANDBORDERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_BANDBORDERS = 0x400

	/*
		RBS_DBLCLKTOGGLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_DBLCLKTOGGLE = 0x8000

	/*
		RBS_FIXEDORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_FIXEDORDER = 0x800

	/*
		RBS_REGISTERDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_REGISTERDROP = 0x1000

	/*
		RBS_TOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_TOOLTIPS = 0x100

	/*
		RBS_VARHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_VARHEIGHT = 0x200

	/*
		RBS_VERTICALGRIPPER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RBS_VERTICALGRIPPER = 0x4000

	/*
		RB_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_BEGINDRAG = 0x418

	/*
		RB_DELETEBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_DELETEBAND = 0x402

	/*
		RB_DRAGMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_DRAGMOVE = 0x41a

	/*
		RB_ENDDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_ENDDRAG = 0x419

	/*
		RB_GETBANDBORDERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBANDBORDERS = 0x422

	/*
		RB_GETBANDCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBANDCOUNT = 0x40c

	/*
		RB_GETBANDINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBANDINFO = 0x41c

	/*
		RB_GETBANDINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBANDINFOA = 0x41d

	/*
		RB_GETBANDINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBANDINFOW = 0x41c

	/*
		RB_GETBANDMARGINS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBANDMARGINS = 0x428

	/*
		RB_GETBARHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBARHEIGHT = 0x41b

	/*
		RB_GETBARINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBARINFO = 0x403

	/*
		RB_GETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETBKCOLOR = 0x414

	/*
		RB_GETCOLORSCHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETCOLORSCHEME = 0x2003

	/*
		RB_GETDROPTARGET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETDROPTARGET = 0x2004

	/*
		RB_GETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETEXTENDEDSTYLE = 0x42a

	/*
		RB_GETPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETPALETTE = 0x426

	/*
		RB_GETRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETRECT = 0x409

	/*
		RB_GETROWCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETROWCOUNT = 0x40d

	/*
		RB_GETROWHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETROWHEIGHT = 0x40e

	/*
		RB_GETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETTEXTCOLOR = 0x416

	/*
		RB_GETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETTOOLTIPS = 0x411

	/*
		RB_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_GETUNICODEFORMAT = 0x2006

	/*
		RB_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_HITTEST = 0x408

	/*
		RB_IDTOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_IDTOINDEX = 0x410

	/*
		RB_INSERTBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_INSERTBAND = 0x40a

	/*
		RB_INSERTBANDA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_INSERTBANDA = 0x401

	/*
		RB_INSERTBANDW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_INSERTBANDW = 0x40a

	/*
		RB_MAXIMIZEBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_MAXIMIZEBAND = 0x41f

	/*
		RB_MINIMIZEBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_MINIMIZEBAND = 0x41e

	/*
		RB_MOVEBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_MOVEBAND = 0x427

	/*
		RB_PUSHCHEVRON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_PUSHCHEVRON = 0x42b

	/*
		RB_SETBANDINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETBANDINFO = 0x40b

	/*
		RB_SETBANDINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETBANDINFOA = 0x406

	/*
		RB_SETBANDINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETBANDINFOW = 0x40b

	/*
		RB_SETBANDWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETBANDWIDTH = 0x42c

	/*
		RB_SETBARINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETBARINFO = 0x404

	/*
		RB_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETBKCOLOR = 0x413

	/*
		RB_SETCOLORSCHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETCOLORSCHEME = 0x2002

	/*
		RB_SETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETEXTENDEDSTYLE = 0x429

	/*
		RB_SETPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETPALETTE = 0x425

	/*
		RB_SETPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETPARENT = 0x407

	/*
		RB_SETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETTEXTCOLOR = 0x415

	/*
		RB_SETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETTOOLTIPS = 0x412

	/*
		RB_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETUNICODEFORMAT = 0x2005

	/*
		RB_SETWINDOWTHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SETWINDOWTHEME = 0x200b

	/*
		RB_SHOWBAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SHOWBAND = 0x423

	/*
		RB_SIZETORECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RB_SIZETORECT = 0x417

	/*
		REPLACEDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	REPLACEDLGORD = 0x605

	/*
		RUNDLGORD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RUNDLGORD = 0x609

	/*
		Rad1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad1 = 0x420

	/*
		Rad10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad10 = 0x429

	/*
		Rad11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad11 = 0x42a

	/*
		Rad12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad12 = 0x42b

	/*
		Rad13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad13 = 0x42c

	/*
		Rad14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad14 = 0x42d

	/*
		Rad15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad15 = 0x42e

	/*
		Rad16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad16 = 0x42f

	/*
		Rad2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad2 = 0x421

	/*
		Rad3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad3 = 0x422

	/*
		Rad4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad4 = 0x423

	/*
		Rad5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad5 = 0x424

	/*
		Rad6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad6 = 0x425

	/*
		Rad7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad7 = 0x426

	/*
		Rad8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad8 = 0x427

	/*
		Rad9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rad9 = 0x428

	/*
		Rct1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rct1 = 0x438

	/*
		Rct2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rct2 = 0x439

	/*
		Rct3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rct3 = 0x43a

	/*
		Rct4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Rct4 = 0x43b

	/*
		SBARS_SIZEGRIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBARS_SIZEGRIP = 0x100

	/*
		SBARS_TOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBARS_TOOLTIPS = 0x800

	/*
		SBN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBN_FIRST = 0xfffffc90

	/*
		SBN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBN_LAST = 0xfffffc7d

	/*
		SBN_SIMPLEMODECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBN_SIMPLEMODECHANGE = 0xfffffc90

	/*
		SBT_NOBORDERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBT_NOBORDERS = 0x100

	/*
		SBT_NOTABPARSING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBT_NOTABPARSING = 0x800

	/*
		SBT_OWNERDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBT_OWNERDRAW = 0x1000

	/*
		SBT_POPOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBT_POPOUT = 0x200

	/*
		SBT_RTLREADING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBT_RTLREADING = 0x400

	/*
		SBT_TOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBT_TOOLTIPS = 0x800

	/*
		SB_GETBORDERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETBORDERS = 0x407

	/*
		SB_GETICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETICON = 0x414

	/*
		SB_GETPARTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETPARTS = 0x406

	/*
		SB_GETRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETRECT = 0x40a

	/*
		SB_GETTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTEXT = 0x40d

	/*
		SB_GETTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTEXTA = 0x402

	/*
		SB_GETTEXTLENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTEXTLENGTH = 0x40c

	/*
		SB_GETTEXTLENGTHA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTEXTLENGTHA = 0x403

	/*
		SB_GETTEXTLENGTHW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTEXTLENGTHW = 0x40c

	/*
		SB_GETTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTEXTW = 0x40d

	/*
		SB_GETTIPTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTIPTEXTA = 0x412

	/*
		SB_GETTIPTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETTIPTEXTW = 0x413

	/*
		SB_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_GETUNICODEFORMAT = 0x2006

	/*
		SB_ISSIMPLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_ISSIMPLE = 0x40e

	/*
		SB_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETBKCOLOR = 0x2001

	/*
		SB_SETICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETICON = 0x40f

	/*
		SB_SETMINHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETMINHEIGHT = 0x408

	/*
		SB_SETPARTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETPARTS = 0x404

	/*
		SB_SETTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETTEXT = 0x40b

	/*
		SB_SETTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETTEXTA = 0x401

	/*
		SB_SETTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETTEXTW = 0x40b

	/*
		SB_SETTIPTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETTIPTEXTA = 0x410

	/*
		SB_SETTIPTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETTIPTEXTW = 0x411

	/*
		SB_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SETUNICODEFORMAT = 0x2005

	/*
		SB_SIMPLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SIMPLE = 0x409

	/*
		SB_SIMPLEID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SB_SIMPLEID = 0xff

	/*
		STD_COPY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_COPY = 0x1

	/*
		STD_CUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_CUT = 0x0

	/*
		STD_DELETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_DELETE = 0x5

	/*
		STD_FILENEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_FILENEW = 0x6

	/*
		STD_FILEOPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_FILEOPEN = 0x7

	/*
		STD_FILESAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_FILESAVE = 0x8

	/*
		STD_FIND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_FIND = 0xc

	/*
		STD_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_HELP = 0xb

	/*
		STD_PASTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_PASTE = 0x2

	/*
		STD_PRINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_PRINT = 0xe

	/*
		STD_PRINTPRE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_PRINTPRE = 0x9

	/*
		STD_PROPERTIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_PROPERTIES = 0xa

	/*
		STD_REDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_REDOW = 0x4

	/*
		STD_REPLACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_REPLACE = 0xd

	/*
		STD_UNDO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STD_UNDO = 0x3

	/*
		Scr1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr1 = 0x490

	/*
		Scr2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr2 = 0x491

	/*
		Scr3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr3 = 0x492

	/*
		Scr4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr4 = 0x493

	/*
		Scr5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr5 = 0x494

	/*
		Scr6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr6 = 0x495

	/*
		Scr7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr7 = 0x496

	/*
		Scr8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Scr8 = 0x497

	/*
		Stc1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc1 = 0x440

	/*
		Stc10 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc10 = 0x449

	/*
		Stc11 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc11 = 0x44a

	/*
		Stc12 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc12 = 0x44b

	/*
		Stc13 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc13 = 0x44c

	/*
		Stc14 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc14 = 0x44d

	/*
		Stc15 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc15 = 0x44e

	/*
		Stc16 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc16 = 0x44f

	/*
		Stc17 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc17 = 0x450

	/*
		Stc18 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc18 = 0x451

	/*
		Stc19 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc19 = 0x452

	/*
		Stc2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc2 = 0x441

	/*
		Stc20 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc20 = 0x453

	/*
		Stc21 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc21 = 0x454

	/*
		Stc22 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc22 = 0x455

	/*
		Stc23 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc23 = 0x456

	/*
		Stc24 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc24 = 0x457

	/*
		Stc25 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc25 = 0x458

	/*
		Stc26 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc26 = 0x459

	/*
		Stc27 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc27 = 0x45a

	/*
		Stc28 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc28 = 0x45b

	/*
		Stc29 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc29 = 0x45c

	/*
		Stc3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc3 = 0x442

	/*
		Stc30 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc30 = 0x45d

	/*
		Stc31 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc31 = 0x45e

	/*
		Stc32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc32 = 0x45f

	/*
		Stc4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc4 = 0x443

	/*
		Stc5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc5 = 0x444

	/*
		Stc6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc6 = 0x445

	/*
		Stc7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc7 = 0x446

	/*
		Stc8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc8 = 0x447

	/*
		Stc9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	Stc9 = 0x448

	/*
		TBBF_LARGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBBF_LARGE = 0x1

	/*
		TBCDRF_BLENDICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_BLENDICON = 0x200000

	/*
		TBCDRF_HILITEHOTTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_HILITEHOTTRACK = 0x20000

	/*
		TBCDRF_NOBACKGROUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_NOBACKGROUND = 0x400000

	/*
		TBCDRF_NOEDGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_NOEDGES = 0x10000

	/*
		TBCDRF_NOETCHEDEFFECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_NOETCHEDEFFECT = 0x100000

	/*
		TBCDRF_NOMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_NOMARK = 0x80000

	/*
		TBCDRF_NOOFFSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_NOOFFSET = 0x40000

	/*
		TBCDRF_USECDCOLORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCDRF_USECDCOLORS = 0x800000

	/*
		TBCD_CHANNEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCD_CHANNEL = 0x3

	/*
		TBCD_THUMB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCD_THUMB = 0x2

	/*
		TBCD_TICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBCD_TICS = 0x1

	/*
		TBDDRET_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBDDRET_DEFAULT = 0x0

	/*
		TBDDRET_NODEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBDDRET_NODEFAULT = 0x1

	/*
		TBDDRET_TREATPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBDDRET_TREATPRESSED = 0x2

	/*
		TBMF_BARPAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBMF_BARPAD = 0x2

	/*
		TBMF_BUTTONSPACING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBMF_BUTTONSPACING = 0x4

	/*
		TBMF_PAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBMF_PAD = 0x1

	/*
		TBM_CLEARSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_CLEARSEL = 0x413

	/*
		TBM_CLEARTICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_CLEARTICS = 0x409

	/*
		TBM_GETBUDDY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETBUDDY = 0x421

	/*
		TBM_GETCHANNELRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETCHANNELRECT = 0x41a

	/*
		TBM_GETLINESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETLINESIZE = 0x418

	/*
		TBM_GETNUMTICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETNUMTICS = 0x410

	/*
		TBM_GETPAGESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETPAGESIZE = 0x416

	/*
		TBM_GETPTICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETPTICS = 0x40e

	/*
		TBM_GETRANGEMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETRANGEMAX = 0x402

	/*
		TBM_GETRANGEMIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETRANGEMIN = 0x401

	/*
		TBM_GETSELEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETSELEND = 0x412

	/*
		TBM_GETSELSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETSELSTART = 0x411

	/*
		TBM_GETTHUMBLENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETTHUMBLENGTH = 0x41c

	/*
		TBM_GETTHUMBRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETTHUMBRECT = 0x419

	/*
		TBM_GETTIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETTIC = 0x403

	/*
		TBM_GETTICPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETTICPOS = 0x40f

	/*
		TBM_GETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETTOOLTIPS = 0x41e

	/*
		TBM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_GETUNICODEFORMAT = 0x2006

	/*
		TBM_SETBUDDY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETBUDDY = 0x420

	/*
		TBM_SETLINESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETLINESIZE = 0x417

	/*
		TBM_SETPAGESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETPAGESIZE = 0x415

	/*
		TBM_SETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETPOS = 0x405

	/*
		TBM_SETPOSNOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETPOSNOTIFY = 0x422

	/*
		TBM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETRANGE = 0x406

	/*
		TBM_SETRANGEMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETRANGEMAX = 0x408

	/*
		TBM_SETRANGEMIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETRANGEMIN = 0x407

	/*
		TBM_SETSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETSEL = 0x40a

	/*
		TBM_SETSELEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETSELEND = 0x40c

	/*
		TBM_SETSELSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETSELSTART = 0x40b

	/*
		TBM_SETTHUMBLENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETTHUMBLENGTH = 0x41b

	/*
		TBM_SETTIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETTIC = 0x404

	/*
		TBM_SETTICFREQ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETTICFREQ = 0x414

	/*
		TBM_SETTIPSIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETTIPSIDE = 0x41f

	/*
		TBM_SETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETTOOLTIPS = 0x41d

	/*
		TBM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBM_SETUNICODEFORMAT = 0x2005

	/*
		TBNRF_ENDCUSTOMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBNRF_ENDCUSTOMIZE = 0x2

	/*
		TBNRF_HIDEHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBNRF_HIDEHELP = 0x1

	/*
		TBN_BEGINADJUST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_BEGINADJUST = 0xfffffd41

	/*
		TBN_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_BEGINDRAG = 0xfffffd43

	/*
		TBN_CUSTHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_CUSTHELP = 0xfffffd3b

	/*
		TBN_DELETINGBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_DELETINGBUTTON = 0xfffffd35

	/*
		TBN_DRAGOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_DRAGOUT = 0xfffffd36

	/*
		TBN_DRAGOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_DRAGOVER = 0xfffffd29

	/*
		TBN_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_DROPDOWN = 0xfffffd3a

	/*
		TBN_DUPACCELERATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_DUPACCELERATOR = 0xfffffd2b

	/*
		TBN_ENDADJUST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_ENDADJUST = 0xfffffd40

	/*
		TBN_ENDDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_ENDDRAG = 0xfffffd42

	/*
		TBN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_FIRST = 0xfffffd44

	/*
		TBN_GETBUTTONINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETBUTTONINFO = 0xfffffd30

	/*
		TBN_GETBUTTONINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETBUTTONINFOA = 0xfffffd44

	/*
		TBN_GETBUTTONINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETBUTTONINFOW = 0xfffffd30

	/*
		TBN_GETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETDISPINFO = 0xfffffd33

	/*
		TBN_GETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETDISPINFOA = 0xfffffd34

	/*
		TBN_GETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETDISPINFOW = 0xfffffd33

	/*
		TBN_GETINFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETINFOTIP = 0xfffffd31

	/*
		TBN_GETINFOTIPA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETINFOTIPA = 0xfffffd32

	/*
		TBN_GETINFOTIPW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETINFOTIPW = 0xfffffd31

	/*
		TBN_GETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_GETOBJECT = 0xfffffd38

	/*
		TBN_HOTITEMCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_HOTITEMCHANGE = 0xfffffd37

	/*
		TBN_INITCUSTOMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_INITCUSTOMIZE = 0xfffffd2d

	/*
		TBN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_LAST = 0xfffffd30

	/*
		TBN_MAPACCELERATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_MAPACCELERATOR = 0xfffffd28

	/*
		TBN_QUERYDELETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_QUERYDELETE = 0xfffffd3d

	/*
		TBN_QUERYINSERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_QUERYINSERT = 0xfffffd3e

	/*
		TBN_RESET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_RESET = 0xfffffd3f

	/*
		TBN_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_RESTORE = 0xfffffd2f

	/*
		TBN_SAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_SAVE = 0xfffffd2e

	/*
		TBN_TOOLBARCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_TOOLBARCHANGE = 0xfffffd3c

	/*
		TBN_WRAPACCELERATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_WRAPACCELERATOR = 0xfffffd2a

	/*
		TBN_WRAPHOTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBN_WRAPHOTITEM = 0xfffffd2c

	/*
		TBSTATE_CHECKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_CHECKED = 0x1

	/*
		TBSTATE_ELLIPSES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_ELLIPSES = 0x40

	/*
		TBSTATE_ENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_ENABLED = 0x4

	/*
		TBSTATE_HIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_HIDDEN = 0x8

	/*
		TBSTATE_INDETERMINATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_INDETERMINATE = 0x10

	/*
		TBSTATE_MARKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_MARKED = 0x80

	/*
		TBSTATE_PRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_PRESSED = 0x2

	/*
		TBSTATE_WRAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTATE_WRAP = 0x20

	/*
		TBSTYLE_ALTDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_ALTDRAG = 0x400

	/*
		TBSTYLE_AUTOSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_AUTOSIZE = 0x10

	/*
		TBSTYLE_BUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_BUTTON = 0x0

	/*
		TBSTYLE_CHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_CHECK = 0x2

	/*
		TBSTYLE_CUSTOMERASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_CUSTOMERASE = 0x2000

	/*
		TBSTYLE_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_DROPDOWN = 0x8

	/*
		TBSTYLE_EX_DOUBLEBUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_EX_DOUBLEBUFFER = 0x80

	/*
		TBSTYLE_EX_DRAWDDARROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_EX_DRAWDDARROWS = 0x1

	/*
		TBSTYLE_EX_HIDECLIPPEDBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_EX_HIDECLIPPEDBUTTONS = 0x10

	/*
		TBSTYLE_EX_MIXEDBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_EX_MIXEDBUTTONS = 0x8

	/*
		TBSTYLE_EX_MULTICOLUMN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_EX_MULTICOLUMN = 0x2

	/*
		TBSTYLE_EX_VERTICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_EX_VERTICAL = 0x4

	/*
		TBSTYLE_FLAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_FLAT = 0x800

	/*
		TBSTYLE_GROUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_GROUP = 0x4

	/*
		TBSTYLE_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_LIST = 0x1000

	/*
		TBSTYLE_NOPREFIX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_NOPREFIX = 0x20

	/*
		TBSTYLE_REGISTERDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_REGISTERDROP = 0x4000

	/*
		TBSTYLE_SEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_SEP = 0x1

	/*
		TBSTYLE_TOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_TOOLTIPS = 0x100

	/*
		TBSTYLE_TRANSPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_TRANSPARENT = 0x8000

	/*
		TBSTYLE_WRAPABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBSTYLE_WRAPABLE = 0x200

	/*
		TBS_AUTOTICKS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_AUTOTICKS = 0x1

	/*
		TBS_BOTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_BOTH = 0x8

	/*
		TBS_BOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_BOTTOM = 0x0

	/*
		TBS_DOWNISLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_DOWNISLEFT = 0x400

	/*
		TBS_ENABLESELRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_ENABLESELRANGE = 0x20

	/*
		TBS_FIXEDLENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_FIXEDLENGTH = 0x40

	/*
		TBS_HORZ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_HORZ = 0x0

	/*
		TBS_LEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_LEFT = 0x4

	/*
		TBS_NOTHUMB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_NOTHUMB = 0x80

	/*
		TBS_NOTICKS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_NOTICKS = 0x10

	/*
		TBS_NOTIFYBEFOREMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_NOTIFYBEFOREMOVE = 0x800

	/*
		TBS_REVERSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_REVERSED = 0x200

	/*
		TBS_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_RIGHT = 0x0

	/*
		TBS_TOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_TOOLTIPS = 0x100

	/*
		TBS_TOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_TOP = 0x4

	/*
		TBS_TRANSPARENTBKGND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_TRANSPARENTBKGND = 0x1000

	/*
		TBS_VERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBS_VERT = 0x2

	/*
		TBTS_BOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBTS_BOTTOM = 0x2

	/*
		TBTS_LEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBTS_LEFT = 0x1

	/*
		TBTS_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBTS_RIGHT = 0x3

	/*
		TBTS_TOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TBTS_TOP = 0x0

	/*
		TB_ADDBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDBITMAP = 0x413

	/*
		TB_ADDBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDBUTTONS = 0x444

	/*
		TB_ADDBUTTONSA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDBUTTONSA = 0x414

	/*
		TB_ADDBUTTONSW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDBUTTONSW = 0x444

	/*
		TB_ADDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDSTRING = 0x44d

	/*
		TB_ADDSTRINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDSTRINGA = 0x41c

	/*
		TB_ADDSTRINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ADDSTRINGW = 0x44d

	/*
		TB_AUTOSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_AUTOSIZE = 0x421

	/*
		TB_BOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_BOTTOM = 0x7

	/*
		TB_BUTTONCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_BUTTONCOUNT = 0x418

	/*
		TB_BUTTONSTRUCTSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_BUTTONSTRUCTSIZE = 0x41e

	/*
		TB_CHANGEBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_CHANGEBITMAP = 0x42b

	/*
		TB_CHECKBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_CHECKBUTTON = 0x402

	/*
		TB_COMMANDTOINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_COMMANDTOINDEX = 0x419

	/*
		TB_CUSTOMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_CUSTOMIZE = 0x41b

	/*
		TB_DELETEBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_DELETEBUTTON = 0x416

	/*
		TB_ENABLEBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ENABLEBUTTON = 0x401

	/*
		TB_ENDTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ENDTRACK = 0x8

	/*
		TB_GETANCHORHIGHLIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETANCHORHIGHLIGHT = 0x44a

	/*
		TB_GETBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBITMAP = 0x42c

	/*
		TB_GETBITMAPFLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBITMAPFLAGS = 0x429

	/*
		TB_GETBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTON = 0x417

	/*
		TB_GETBUTTONINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONINFO = 0x43f

	/*
		TB_GETBUTTONINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONINFOA = 0x441

	/*
		TB_GETBUTTONINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONINFOW = 0x43f

	/*
		TB_GETBUTTONSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONSIZE = 0x43a

	/*
		TB_GETBUTTONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONTEXT = 0x44b

	/*
		TB_GETBUTTONTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONTEXTA = 0x42d

	/*
		TB_GETBUTTONTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETBUTTONTEXTW = 0x44b

	/*
		TB_GETCOLORSCHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETCOLORSCHEME = 0x2003

	/*
		TB_GETDISABLEDIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETDISABLEDIMAGELIST = 0x437

	/*
		TB_GETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETEXTENDEDSTYLE = 0x455

	/*
		TB_GETHOTIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETHOTIMAGELIST = 0x435

	/*
		TB_GETHOTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETHOTITEM = 0x447

	/*
		TB_GETIDEALSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETIDEALSIZE = 0x463

	/*
		TB_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETIMAGELIST = 0x431

	/*
		TB_GETIMAGELISTCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETIMAGELISTCOUNT = 0x462

	/*
		TB_GETINSERTMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETINSERTMARK = 0x44f

	/*
		TB_GETINSERTMARKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETINSERTMARKCOLOR = 0x459

	/*
		TB_GETITEMDROPDOWNRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETITEMDROPDOWNRECT = 0x467

	/*
		TB_GETITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETITEMRECT = 0x41d

	/*
		TB_GETMAXSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETMAXSIZE = 0x453

	/*
		TB_GETMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETMETRICS = 0x465

	/*
		TB_GETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETOBJECT = 0x43e

	/*
		TB_GETPADDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETPADDING = 0x456

	/*
		TB_GETPRESSEDIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETPRESSEDIMAGELIST = 0x469

	/*
		TB_GETRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETRECT = 0x433

	/*
		TB_GETROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETROWS = 0x428

	/*
		TB_GETSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETSTATE = 0x412

	/*
		TB_GETSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETSTRING = 0x45b

	/*
		TB_GETSTRINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETSTRINGA = 0x45c

	/*
		TB_GETSTRINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETSTRINGW = 0x45b

	/*
		TB_GETSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETSTYLE = 0x439

	/*
		TB_GETTEXTROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETTEXTROWS = 0x43d

	/*
		TB_GETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETTOOLTIPS = 0x423

	/*
		TB_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_GETUNICODEFORMAT = 0x2006

	/*
		TB_HASACCELERATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_HASACCELERATOR = 0x45f

	/*
		TB_HIDEBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_HIDEBUTTON = 0x404

	/*
		TB_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_HITTEST = 0x445

	/*
		TB_INDETERMINATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_INDETERMINATE = 0x405

	/*
		TB_INSERTBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_INSERTBUTTON = 0x443

	/*
		TB_INSERTBUTTONA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_INSERTBUTTONA = 0x415

	/*
		TB_INSERTBUTTONW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_INSERTBUTTONW = 0x443

	/*
		TB_INSERTMARKHITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_INSERTMARKHITTEST = 0x451

	/*
		TB_ISBUTTONCHECKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ISBUTTONCHECKED = 0x40a

	/*
		TB_ISBUTTONENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ISBUTTONENABLED = 0x409

	/*
		TB_ISBUTTONHIDDEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ISBUTTONHIDDEN = 0x40c

	/*
		TB_ISBUTTONHIGHLIGHTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ISBUTTONHIGHLIGHTED = 0x40e

	/*
		TB_ISBUTTONINDETERMINATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ISBUTTONINDETERMINATE = 0x40d

	/*
		TB_ISBUTTONPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_ISBUTTONPRESSED = 0x40b

	/*
		TB_LINEDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_LINEDOWN = 0x1

	/*
		TB_LINEUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_LINEUP = 0x0

	/*
		TB_LOADIMAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_LOADIMAGES = 0x432

	/*
		TB_MAPACCELERATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_MAPACCELERATOR = 0x45a

	/*
		TB_MAPACCELERATORA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_MAPACCELERATORA = 0x44e

	/*
		TB_MAPACCELERATORW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_MAPACCELERATORW = 0x45a

	/*
		TB_MARKBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_MARKBUTTON = 0x406

	/*
		TB_MOVEBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_MOVEBUTTON = 0x452

	/*
		TB_PAGEDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_PAGEDOWN = 0x3

	/*
		TB_PAGEUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_PAGEUP = 0x2

	/*
		TB_PRESSBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_PRESSBUTTON = 0x403

	/*
		TB_REPLACEBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_REPLACEBITMAP = 0x42e

	/*
		TB_SAVERESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SAVERESTORE = 0x44c

	/*
		TB_SAVERESTOREA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SAVERESTOREA = 0x41a

	/*
		TB_SAVERESTOREW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SAVERESTOREW = 0x44c

	/*
		TB_SETANCHORHIGHLIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETANCHORHIGHLIGHT = 0x449

	/*
		TB_SETBITMAPSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBITMAPSIZE = 0x420

	/*
		TB_SETBOUNDINGSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBOUNDINGSIZE = 0x45d

	/*
		TB_SETBUTTONINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBUTTONINFO = 0x440

	/*
		TB_SETBUTTONINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBUTTONINFOA = 0x442

	/*
		TB_SETBUTTONINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBUTTONINFOW = 0x440

	/*
		TB_SETBUTTONSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBUTTONSIZE = 0x41f

	/*
		TB_SETBUTTONWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETBUTTONWIDTH = 0x43b

	/*
		TB_SETCMDID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETCMDID = 0x42a

	/*
		TB_SETCOLORSCHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETCOLORSCHEME = 0x2002

	/*
		TB_SETDISABLEDIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETDISABLEDIMAGELIST = 0x436

	/*
		TB_SETDRAWTEXTFLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETDRAWTEXTFLAGS = 0x446

	/*
		TB_SETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETEXTENDEDSTYLE = 0x454

	/*
		TB_SETHOTIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETHOTIMAGELIST = 0x434

	/*
		TB_SETHOTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETHOTITEM = 0x448

	/*
		TB_SETHOTITEM2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETHOTITEM2 = 0x45e

	/*
		TB_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETIMAGELIST = 0x430

	/*
		TB_SETINDENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETINDENT = 0x42f

	/*
		TB_SETINSERTMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETINSERTMARK = 0x450

	/*
		TB_SETINSERTMARKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETINSERTMARKCOLOR = 0x458

	/*
		TB_SETLISTGAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETLISTGAP = 0x460

	/*
		TB_SETMAXTEXTROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETMAXTEXTROWS = 0x43c

	/*
		TB_SETMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETMETRICS = 0x466

	/*
		TB_SETPADDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETPADDING = 0x457

	/*
		TB_SETPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETPARENT = 0x425

	/*
		TB_SETPRESSEDIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETPRESSEDIMAGELIST = 0x468

	/*
		TB_SETROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETROWS = 0x427

	/*
		TB_SETSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETSTATE = 0x411

	/*
		TB_SETSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETSTYLE = 0x438

	/*
		TB_SETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETTOOLTIPS = 0x424

	/*
		TB_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETUNICODEFORMAT = 0x2005

	/*
		TB_SETWINDOWTHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_SETWINDOWTHEME = 0x200b

	/*
		TB_THUMBPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_THUMBPOSITION = 0x4

	/*
		TB_THUMBTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_THUMBTRACK = 0x5

	/*
		TB_TOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TB_TOP = 0x6

	/*
		TCM_ADJUSTRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_ADJUSTRECT = 0x1328

	/*
		TCM_DELETEALLITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_DELETEALLITEMS = 0x1309

	/*
		TCM_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_DELETEITEM = 0x1308

	/*
		TCM_DESELECTALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_DESELECTALL = 0x1332

	/*
		TCM_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_FIRST = 0x1300

	/*
		TCM_GETCURFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETCURFOCUS = 0x132f

	/*
		TCM_GETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETCURSEL = 0x130b

	/*
		TCM_GETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETEXTENDEDSTYLE = 0x1335

	/*
		TCM_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETIMAGELIST = 0x1302

	/*
		TCM_GETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETITEM = 0x133c

	/*
		TCM_GETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETITEMA = 0x1305

	/*
		TCM_GETITEMCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETITEMCOUNT = 0x1304

	/*
		TCM_GETITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETITEMRECT = 0x130a

	/*
		TCM_GETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETITEMW = 0x133c

	/*
		TCM_GETROWCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETROWCOUNT = 0x132c

	/*
		TCM_GETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETTOOLTIPS = 0x132d

	/*
		TCM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_GETUNICODEFORMAT = 0x2006

	/*
		TCM_HIGHLIGHTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_HIGHLIGHTITEM = 0x1333

	/*
		TCM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_HITTEST = 0x130d

	/*
		TCM_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_INSERTITEM = 0x133e

	/*
		TCM_INSERTITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_INSERTITEMA = 0x1307

	/*
		TCM_INSERTITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_INSERTITEMW = 0x133e

	/*
		TCM_REMOVEIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_REMOVEIMAGE = 0x132a

	/*
		TCM_SETCURFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETCURFOCUS = 0x1330

	/*
		TCM_SETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETCURSEL = 0x130c

	/*
		TCM_SETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETEXTENDEDSTYLE = 0x1334

	/*
		TCM_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETIMAGELIST = 0x1303

	/*
		TCM_SETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETITEM = 0x133d

	/*
		TCM_SETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETITEMA = 0x1306

	/*
		TCM_SETITEMEXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETITEMEXTRA = 0x130e

	/*
		TCM_SETITEMSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETITEMSIZE = 0x1329

	/*
		TCM_SETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETITEMW = 0x133d

	/*
		TCM_SETMINTABWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETMINTABWIDTH = 0x1331

	/*
		TCM_SETPADDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETPADDING = 0x132b

	/*
		TCM_SETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETTOOLTIPS = 0x132e

	/*
		TCM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCM_SETUNICODEFORMAT = 0x2005

	/*
		TCN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_FIRST = 0xfffffdda

	/*
		TCN_FOCUSCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_FOCUSCHANGE = 0xfffffdd6

	/*
		TCN_GETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_GETOBJECT = 0xfffffdd7

	/*
		TCN_KEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_KEYDOWN = 0xfffffdda

	/*
		TCN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_LAST = 0xfffffdbc

	/*
		TCN_SELCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_SELCHANGE = 0xfffffdd9

	/*
		TCN_SELCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCN_SELCHANGING = 0xfffffdd8

	/*
		TCS_BOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_BOTTOM = 0x2

	/*
		TCS_BUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_BUTTONS = 0x100

	/*
		TCS_EX_FLATSEPARATORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_EX_FLATSEPARATORS = 0x1

	/*
		TCS_EX_REGISTERDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_EX_REGISTERDROP = 0x2

	/*
		TCS_FIXEDWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_FIXEDWIDTH = 0x400

	/*
		TCS_FLATBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_FLATBUTTONS = 0x8

	/*
		TCS_FOCUSNEVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_FOCUSNEVER = 0x8000

	/*
		TCS_FOCUSONBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_FOCUSONBUTTONDOWN = 0x1000

	/*
		TCS_FORCEICONLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_FORCEICONLEFT = 0x10

	/*
		TCS_FORCELABELLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_FORCELABELLEFT = 0x20

	/*
		TCS_HOTTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_HOTTRACK = 0x40

	/*
		TCS_MULTILINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_MULTILINE = 0x200

	/*
		TCS_MULTISELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_MULTISELECT = 0x4

	/*
		TCS_OWNERDRAWFIXED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_OWNERDRAWFIXED = 0x2000

	/*
		TCS_RAGGEDRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_RAGGEDRIGHT = 0x800

	/*
		TCS_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_RIGHT = 0x2

	/*
		TCS_RIGHTJUSTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_RIGHTJUSTIFY = 0x0

	/*
		TCS_SCROLLOPPOSITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_SCROLLOPPOSITE = 0x1

	/*
		TCS_SINGLELINE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_SINGLELINE = 0x0

	/*
		TCS_TABS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_TABS = 0x0

	/*
		TCS_TOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_TOOLTIPS = 0x4000

	/*
		TCS_VERTICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TCS_VERTICAL = 0x80

	/*
		TD_ERROR_ICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TD_ERROR_ICON = 0xfffe

	/*
		TD_INFORMATION_ICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TD_INFORMATION_ICON = 0xfffd

	/*
		TD_SHIELD_ICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TD_SHIELD_ICON = 0xfffc

	/*
		TD_WARNING_ICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TD_WARNING_ICON = 0xffff

	/*
		TMTVS_RESERVEDHIGH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TMTVS_RESERVEDHIGH = 0x4e1f

	/*
		TMTVS_RESERVEDLOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TMTVS_RESERVEDLOW = 0x186a0

	/*
		TRBN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRBN_FIRST = 0xfffffa23

	/*
		TRBN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRBN_LAST = 0xfffffa11

	/*
		TRBN_THUMBPOSCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TRBN_THUMBPOSCHANGING = 0xfffffa22

	/*
		TTDT_AUTOMATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTDT_AUTOMATIC = 0x0

	/*
		TTDT_AUTOPOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTDT_AUTOPOP = 0x2

	/*
		TTDT_INITIAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTDT_INITIAL = 0x3

	/*
		TTDT_RESHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTDT_RESHOW = 0x1

	/*
		TTM_ACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ACTIVATE = 0x401

	/*
		TTM_ADDTOOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ADDTOOL = 0x432

	/*
		TTM_ADDTOOLA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ADDTOOLA = 0x404

	/*
		TTM_ADDTOOLW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ADDTOOLW = 0x432

	/*
		TTM_ADJUSTRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ADJUSTRECT = 0x41f

	/*
		TTM_DELTOOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_DELTOOL = 0x433

	/*
		TTM_DELTOOLA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_DELTOOLA = 0x405

	/*
		TTM_DELTOOLW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_DELTOOLW = 0x433

	/*
		TTM_ENUMTOOLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ENUMTOOLS = 0x43a

	/*
		TTM_ENUMTOOLSA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ENUMTOOLSA = 0x40e

	/*
		TTM_ENUMTOOLSW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_ENUMTOOLSW = 0x43a

	/*
		TTM_GETBUBBLESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETBUBBLESIZE = 0x41e

	/*
		TTM_GETCURRENTTOOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETCURRENTTOOL = 0x43b

	/*
		TTM_GETCURRENTTOOLA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETCURRENTTOOLA = 0x40f

	/*
		TTM_GETCURRENTTOOLW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETCURRENTTOOLW = 0x43b

	/*
		TTM_GETDELAYTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETDELAYTIME = 0x415

	/*
		TTM_GETMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETMARGIN = 0x41b

	/*
		TTM_GETMAXTIPWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETMAXTIPWIDTH = 0x419

	/*
		TTM_GETTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTEXT = 0x438

	/*
		TTM_GETTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTEXTA = 0x40b

	/*
		TTM_GETTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTEXTW = 0x438

	/*
		TTM_GETTIPBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTIPBKCOLOR = 0x416

	/*
		TTM_GETTIPTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTIPTEXTCOLOR = 0x417

	/*
		TTM_GETTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTITLE = 0x423

	/*
		TTM_GETTOOLCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTOOLCOUNT = 0x40d

	/*
		TTM_GETTOOLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTOOLINFO = 0x435

	/*
		TTM_GETTOOLINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTOOLINFOA = 0x408

	/*
		TTM_GETTOOLINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_GETTOOLINFOW = 0x435

	/*
		TTM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_HITTEST = 0x437

	/*
		TTM_HITTESTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_HITTESTA = 0x40a

	/*
		TTM_HITTESTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_HITTESTW = 0x437

	/*
		TTM_NEWTOOLRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_NEWTOOLRECT = 0x434

	/*
		TTM_NEWTOOLRECTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_NEWTOOLRECTA = 0x406

	/*
		TTM_NEWTOOLRECTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_NEWTOOLRECTW = 0x434

	/*
		TTM_POP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_POP = 0x41c

	/*
		TTM_POPUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_POPUP = 0x422

	/*
		TTM_RELAYEVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_RELAYEVENT = 0x407

	/*
		TTM_SETDELAYTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETDELAYTIME = 0x403

	/*
		TTM_SETMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETMARGIN = 0x41a

	/*
		TTM_SETMAXTIPWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETMAXTIPWIDTH = 0x418

	/*
		TTM_SETTIPBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTIPBKCOLOR = 0x413

	/*
		TTM_SETTIPTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTIPTEXTCOLOR = 0x414

	/*
		TTM_SETTITLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTITLE = 0x421

	/*
		TTM_SETTITLEA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTITLEA = 0x420

	/*
		TTM_SETTITLEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTITLEW = 0x421

	/*
		TTM_SETTOOLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTOOLINFO = 0x436

	/*
		TTM_SETTOOLINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTOOLINFOA = 0x409

	/*
		TTM_SETTOOLINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETTOOLINFOW = 0x436

	/*
		TTM_SETWINDOWTHEME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_SETWINDOWTHEME = 0x200b

	/*
		TTM_TRACKACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_TRACKACTIVATE = 0x411

	/*
		TTM_TRACKPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_TRACKPOSITION = 0x412

	/*
		TTM_UPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_UPDATE = 0x41d

	/*
		TTM_UPDATETIPTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_UPDATETIPTEXT = 0x439

	/*
		TTM_UPDATETIPTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_UPDATETIPTEXTA = 0x40c

	/*
		TTM_UPDATETIPTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_UPDATETIPTEXTW = 0x439

	/*
		TTM_WINDOWFROMPOINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTM_WINDOWFROMPOINT = 0x410

	/*
		TTN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_FIRST = 0xfffffdf8

	/*
		TTN_GETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_GETDISPINFO = 0xfffffdee

	/*
		TTN_GETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_GETDISPINFOA = 0xfffffdf8

	/*
		TTN_GETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_GETDISPINFOW = 0xfffffdee

	/*
		TTN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_LAST = 0xfffffddb

	/*
		TTN_LINKCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_LINKCLICK = 0xfffffdf5

	/*
		TTN_NEEDTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_NEEDTEXT = 0xfffffdee

	/*
		TTN_NEEDTEXTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_NEEDTEXTA = 0xfffffdf8

	/*
		TTN_NEEDTEXTW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_NEEDTEXTW = 0xfffffdee

	/*
		TTN_POP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_POP = 0xfffffdf6

	/*
		TTN_SHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTN_SHOW = 0xfffffdf7

	/*
		TTS_ALWAYSTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_ALWAYSTIP = 0x1

	/*
		TTS_BALLOON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_BALLOON = 0x40

	/*
		TTS_CLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_CLOSE = 0x80

	/*
		TTS_NOANIMATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_NOANIMATE = 0x10

	/*
		TTS_NOFADE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_NOFADE = 0x20

	/*
		TTS_NOPREFIX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_NOPREFIX = 0x2

	/*
		TTS_USEVISUALSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TTS_USEVISUALSTYLE = 0x100

	/*
		TVCDRF_NOIMAGES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVCDRF_NOIMAGES = 0x10000

	/*
		TVGN_CARET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_CARET = 0x9

	/*
		TVGN_CHILD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_CHILD = 0x4

	/*
		TVGN_DROPHILITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_DROPHILITE = 0x8

	/*
		TVGN_FIRSTVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_FIRSTVISIBLE = 0x5

	/*
		TVGN_LASTVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_LASTVISIBLE = 0xa

	/*
		TVGN_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_NEXT = 0x1

	/*
		TVGN_NEXTSELECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_NEXTSELECTED = 0xb

	/*
		TVGN_NEXTVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_NEXTVISIBLE = 0x6

	/*
		TVGN_PARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_PARENT = 0x3

	/*
		TVGN_PREVIOUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_PREVIOUS = 0x2

	/*
		TVGN_PREVIOUSVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_PREVIOUSVISIBLE = 0x7

	/*
		TVGN_ROOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVGN_ROOT = 0x0

	/*
		TVI_FIRST from Windows.Win32 metadata (originally typed HTREEITEM). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVI_FIRST = -65535

	/*
		TVI_LAST from Windows.Win32 metadata (originally typed HTREEITEM). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVI_LAST = -65534

	/*
		TVI_ROOT from Windows.Win32 metadata (originally typed HTREEITEM). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVI_ROOT = -65536

	/*
		TVI_SORT from Windows.Win32 metadata (originally typed HTREEITEM). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVI_SORT = -65533

	/*
		TVM_CREATEDRAGIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_CREATEDRAGIMAGE = 0x1112

	/*
		TVM_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_DELETEITEM = 0x1101

	/*
		TVM_EDITLABEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_EDITLABEL = 0x1141

	/*
		TVM_EDITLABELA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_EDITLABELA = 0x110e

	/*
		TVM_EDITLABELW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_EDITLABELW = 0x1141

	/*
		TVM_ENDEDITLABELNOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_ENDEDITLABELNOW = 0x1116

	/*
		TVM_ENSUREVISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_ENSUREVISIBLE = 0x1114

	/*
		TVM_EXPAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_EXPAND = 0x1102

	/*
		TVM_GETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETBKCOLOR = 0x111f

	/*
		TVM_GETCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETCOUNT = 0x1105

	/*
		TVM_GETEDITCONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETEDITCONTROL = 0x110f

	/*
		TVM_GETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETEXTENDEDSTYLE = 0x112d

	/*
		TVM_GETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETIMAGELIST = 0x1108

	/*
		TVM_GETINDENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETINDENT = 0x1106

	/*
		TVM_GETINSERTMARKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETINSERTMARKCOLOR = 0x1126

	/*
		TVM_GETISEARCHSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETISEARCHSTRING = 0x1140

	/*
		TVM_GETISEARCHSTRINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETISEARCHSTRINGA = 0x1117

	/*
		TVM_GETISEARCHSTRINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETISEARCHSTRINGW = 0x1140

	/*
		TVM_GETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEM = 0x113e

	/*
		TVM_GETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEMA = 0x110c

	/*
		TVM_GETITEMHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEMHEIGHT = 0x111c

	/*
		TVM_GETITEMPARTRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEMPARTRECT = 0x1148

	/*
		TVM_GETITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEMRECT = 0x1104

	/*
		TVM_GETITEMSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEMSTATE = 0x1127

	/*
		TVM_GETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETITEMW = 0x113e

	/*
		TVM_GETLINECOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETLINECOLOR = 0x1129

	/*
		TVM_GETNEXTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETNEXTITEM = 0x110a

	/*
		TVM_GETSCROLLTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETSCROLLTIME = 0x1122

	/*
		TVM_GETSELECTEDCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETSELECTEDCOUNT = 0x1146

	/*
		TVM_GETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETTEXTCOLOR = 0x1120

	/*
		TVM_GETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETTOOLTIPS = 0x1119

	/*
		TVM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETUNICODEFORMAT = 0x2006

	/*
		TVM_GETVISIBLECOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_GETVISIBLECOUNT = 0x1110

	/*
		TVM_HITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_HITTEST = 0x1111

	/*
		TVM_INSERTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_INSERTITEM = 0x1132

	/*
		TVM_INSERTITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_INSERTITEMA = 0x1100

	/*
		TVM_INSERTITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_INSERTITEMW = 0x1132

	/*
		TVM_MAPACCIDTOHTREEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_MAPACCIDTOHTREEITEM = 0x112a

	/*
		TVM_MAPHTREEITEMTOACCID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_MAPHTREEITEMTOACCID = 0x112b

	/*
		TVM_SELECTITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SELECTITEM = 0x110b

	/*
		TVM_SETAUTOSCROLLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETAUTOSCROLLINFO = 0x113b

	/*
		TVM_SETBKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETBKCOLOR = 0x111d

	/*
		TVM_SETBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETBORDER = 0x1123

	/*
		TVM_SETEXTENDEDSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETEXTENDEDSTYLE = 0x112c

	/*
		TVM_SETHOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETHOT = 0x113a

	/*
		TVM_SETIMAGELIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETIMAGELIST = 0x1109

	/*
		TVM_SETINDENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETINDENT = 0x1107

	/*
		TVM_SETINSERTMARK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETINSERTMARK = 0x111a

	/*
		TVM_SETINSERTMARKCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETINSERTMARKCOLOR = 0x1125

	/*
		TVM_SETITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETITEM = 0x113f

	/*
		TVM_SETITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETITEMA = 0x110d

	/*
		TVM_SETITEMHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETITEMHEIGHT = 0x111b

	/*
		TVM_SETITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETITEMW = 0x113f

	/*
		TVM_SETLINECOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETLINECOLOR = 0x1128

	/*
		TVM_SETSCROLLTIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETSCROLLTIME = 0x1121

	/*
		TVM_SETTEXTCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETTEXTCOLOR = 0x111e

	/*
		TVM_SETTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETTOOLTIPS = 0x1118

	/*
		TVM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SETUNICODEFORMAT = 0x2005

	/*
		TVM_SHOWINFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SHOWINFOTIP = 0x1147

	/*
		TVM_SORTCHILDREN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SORTCHILDREN = 0x1113

	/*
		TVM_SORTCHILDRENCB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVM_SORTCHILDRENCB = 0x1115

	/*
		TVNRET_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVNRET_DEFAULT = 0x0

	/*
		TVNRET_SKIPNEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVNRET_SKIPNEW = 0x2

	/*
		TVNRET_SKIPOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVNRET_SKIPOLD = 0x1

	/*
		TVN_ASYNCDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ASYNCDRAW = 0xfffffe5c

	/*
		TVN_BEGINDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINDRAG = 0xfffffe38

	/*
		TVN_BEGINDRAGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINDRAGA = 0xfffffe69

	/*
		TVN_BEGINDRAGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINDRAGW = 0xfffffe38

	/*
		TVN_BEGINLABELEDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINLABELEDIT = 0xfffffe35

	/*
		TVN_BEGINLABELEDITA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINLABELEDITA = 0xfffffe66

	/*
		TVN_BEGINLABELEDITW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINLABELEDITW = 0xfffffe35

	/*
		TVN_BEGINRDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINRDRAG = 0xfffffe37

	/*
		TVN_BEGINRDRAGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINRDRAGA = 0xfffffe68

	/*
		TVN_BEGINRDRAGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_BEGINRDRAGW = 0xfffffe37

	/*
		TVN_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_DELETEITEM = 0xfffffe36

	/*
		TVN_DELETEITEMA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_DELETEITEMA = 0xfffffe67

	/*
		TVN_DELETEITEMW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_DELETEITEMW = 0xfffffe36

	/*
		TVN_ENDLABELEDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ENDLABELEDIT = 0xfffffe34

	/*
		TVN_ENDLABELEDITA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ENDLABELEDITA = 0xfffffe65

	/*
		TVN_ENDLABELEDITW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ENDLABELEDITW = 0xfffffe34

	/*
		TVN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_FIRST = 0xfffffe70

	/*
		TVN_GETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_GETDISPINFO = 0xfffffe3c

	/*
		TVN_GETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_GETDISPINFOA = 0xfffffe6d

	/*
		TVN_GETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_GETDISPINFOW = 0xfffffe3c

	/*
		TVN_GETINFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_GETINFOTIP = 0xfffffe62

	/*
		TVN_GETINFOTIPA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_GETINFOTIPA = 0xfffffe63

	/*
		TVN_GETINFOTIPW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_GETINFOTIPW = 0xfffffe62

	/*
		TVN_ITEMCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMCHANGED = 0xfffffe5d

	/*
		TVN_ITEMCHANGEDA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMCHANGEDA = 0xfffffe5e

	/*
		TVN_ITEMCHANGEDW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMCHANGEDW = 0xfffffe5d

	/*
		TVN_ITEMCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMCHANGING = 0xfffffe5f

	/*
		TVN_ITEMCHANGINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMCHANGINGA = 0xfffffe60

	/*
		TVN_ITEMCHANGINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMCHANGINGW = 0xfffffe5f

	/*
		TVN_ITEMEXPANDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMEXPANDED = 0xfffffe39

	/*
		TVN_ITEMEXPANDEDA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMEXPANDEDA = 0xfffffe6a

	/*
		TVN_ITEMEXPANDEDW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMEXPANDEDW = 0xfffffe39

	/*
		TVN_ITEMEXPANDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMEXPANDING = 0xfffffe3a

	/*
		TVN_ITEMEXPANDINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMEXPANDINGA = 0xfffffe6b

	/*
		TVN_ITEMEXPANDINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_ITEMEXPANDINGW = 0xfffffe3a

	/*
		TVN_KEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_KEYDOWN = 0xfffffe64

	/*
		TVN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_LAST = 0xfffffe0d

	/*
		TVN_SELCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SELCHANGED = 0xfffffe3d

	/*
		TVN_SELCHANGEDA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SELCHANGEDA = 0xfffffe6e

	/*
		TVN_SELCHANGEDW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SELCHANGEDW = 0xfffffe3d

	/*
		TVN_SELCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SELCHANGING = 0xfffffe3e

	/*
		TVN_SELCHANGINGA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SELCHANGINGA = 0xfffffe6f

	/*
		TVN_SELCHANGINGW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SELCHANGINGW = 0xfffffe3e

	/*
		TVN_SETDISPINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SETDISPINFO = 0xfffffe3b

	/*
		TVN_SETDISPINFOA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SETDISPINFOA = 0xfffffe6c

	/*
		TVN_SETDISPINFOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SETDISPINFOW = 0xfffffe3b

	/*
		TVN_SINGLEEXPAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVN_SINGLEEXPAND = 0xfffffe61

	/*
		TVSBF_XBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVSBF_XBORDER = 0x1

	/*
		TVSBF_YBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVSBF_YBORDER = 0x2

	/*
		TVSIL_NORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVSIL_NORMAL = 0x0

	/*
		TVSIL_STATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVSIL_STATE = 0x2

	/*
		TVSI_NOSINGLEEXPAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVSI_NOSINGLEEXPAND = 0x8000

	/*
		TVS_CHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_CHECKBOXES = 0x100

	/*
		TVS_DISABLEDRAGDROP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_DISABLEDRAGDROP = 0x10

	/*
		TVS_EDITLABELS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EDITLABELS = 0x8

	/*
		TVS_EX_AUTOHSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_AUTOHSCROLL = 0x20

	/*
		TVS_EX_DIMMEDCHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_DIMMEDCHECKBOXES = 0x200

	/*
		TVS_EX_DOUBLEBUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_DOUBLEBUFFER = 0x4

	/*
		TVS_EX_DRAWIMAGEASYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_DRAWIMAGEASYNC = 0x400

	/*
		TVS_EX_EXCLUSIONCHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_EXCLUSIONCHECKBOXES = 0x100

	/*
		TVS_EX_FADEINOUTEXPANDOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_FADEINOUTEXPANDOS = 0x40

	/*
		TVS_EX_MULTISELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_MULTISELECT = 0x2

	/*
		TVS_EX_NOINDENTSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_NOINDENTSTATE = 0x8

	/*
		TVS_EX_NOSINGLECOLLAPSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_NOSINGLECOLLAPSE = 0x1

	/*
		TVS_EX_PARTIALCHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_PARTIALCHECKBOXES = 0x80

	/*
		TVS_EX_RICHTOOLTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_EX_RICHTOOLTIP = 0x10

	/*
		TVS_FULLROWSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_FULLROWSELECT = 0x1000

	/*
		TVS_HASBUTTONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_HASBUTTONS = 0x1

	/*
		TVS_HASLINES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_HASLINES = 0x2

	/*
		TVS_INFOTIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_INFOTIP = 0x800

	/*
		TVS_LINESATROOT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_LINESATROOT = 0x4

	/*
		TVS_NOHSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_NOHSCROLL = 0x8000

	/*
		TVS_NONEVENHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_NONEVENHEIGHT = 0x4000

	/*
		TVS_NOSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_NOSCROLL = 0x2000

	/*
		TVS_NOTOOLTIPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_NOTOOLTIPS = 0x80

	/*
		TVS_RTLREADING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_RTLREADING = 0x40

	/*
		TVS_SHOWSELALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_SHOWSELALWAYS = 0x20

	/*
		TVS_SINGLEEXPAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_SINGLEEXPAND = 0x400

	/*
		TVS_TRACKSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TVS_TRACKSELECT = 0x200

	/*
		TV_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TV_FIRST = 0x1100

	/*
		UDM_GETACCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETACCEL = 0x46c

	/*
		UDM_GETBASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETBASE = 0x46e

	/*
		UDM_GETBUDDY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETBUDDY = 0x46a

	/*
		UDM_GETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETPOS = 0x468

	/*
		UDM_GETPOS32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETPOS32 = 0x472

	/*
		UDM_GETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETRANGE = 0x466

	/*
		UDM_GETRANGE32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETRANGE32 = 0x470

	/*
		UDM_GETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_GETUNICODEFORMAT = 0x2006

	/*
		UDM_SETACCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETACCEL = 0x46b

	/*
		UDM_SETBASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETBASE = 0x46d

	/*
		UDM_SETBUDDY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETBUDDY = 0x469

	/*
		UDM_SETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETPOS = 0x467

	/*
		UDM_SETPOS32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETPOS32 = 0x471

	/*
		UDM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETRANGE = 0x465

	/*
		UDM_SETRANGE32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETRANGE32 = 0x46f

	/*
		UDM_SETUNICODEFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDM_SETUNICODEFORMAT = 0x2005

	/*
		UDN_DELTAPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDN_DELTAPOS = 0xfffffd2e

	/*
		UDN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDN_FIRST = 0xfffffd2f

	/*
		UDN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDN_LAST = 0xfffffd27

	/*
		UDS_ALIGNLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_ALIGNLEFT = 0x8

	/*
		UDS_ALIGNRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_ALIGNRIGHT = 0x4

	/*
		UDS_ARROWKEYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_ARROWKEYS = 0x20

	/*
		UDS_AUTOBUDDY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_AUTOBUDDY = 0x10

	/*
		UDS_HORZ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_HORZ = 0x40

	/*
		UDS_HOTTRACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_HOTTRACK = 0x100

	/*
		UDS_NOTHOUSANDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_NOTHOUSANDS = 0x80

	/*
		UDS_SETBUDDYINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_SETBUDDYINT = 0x2

	/*
		UDS_WRAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UDS_WRAP = 0x1

	/*
		UD_MAXVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UD_MAXVAL = 0x7fff

	/*
		VIEW_DETAILS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_DETAILS = 0x3

	/*
		VIEW_LARGEICONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_LARGEICONS = 0x0

	/*
		VIEW_LIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_LIST = 0x2

	/*
		VIEW_NETCONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_NETCONNECT = 0x9

	/*
		VIEW_NETDISCONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_NETDISCONNECT = 0xa

	/*
		VIEW_NEWFOLDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_NEWFOLDER = 0xb

	/*
		VIEW_PARENTFOLDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_PARENTFOLDER = 0x8

	/*
		VIEW_SMALLICONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_SMALLICONS = 0x1

	/*
		VIEW_SORTDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_SORTDATE = 0x6

	/*
		VIEW_SORTNAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_SORTNAME = 0x4

	/*
		VIEW_SORTSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_SORTSIZE = 0x5

	/*
		VIEW_SORTTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_SORTTYPE = 0x7

	/*
		VIEW_VIEWMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	VIEW_VIEWMENU = 0xc

	/*
		WIZ_BODYCX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIZ_BODYCX = 0xb8

	/*
		WIZ_BODYX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIZ_BODYX = 0x5c

	/*
		WIZ_CXBMP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIZ_CXBMP = 0x50

	/*
		WIZ_CXDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIZ_CXDLG = 0x114

	/*
		WIZ_CYDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WIZ_CYDLG = 0x8c

	/*
		WMN_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMN_FIRST = 0xfffffc18

	/*
		WMN_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMN_LAST = 0xfffffb50

	/*
		WM_CTLCOLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLOR = 0x19

	/*
		WM_MOUSEHOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSEHOVER = 0x2a1

	/*
		WM_MOUSELEAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSELEAVE = 0x2a3

	/*
		WSB_PROP_MASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WSB_PROP_MASK = 0xfff

	/*
		WTNCA_NODRAWCAPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTNCA_NODRAWCAPTION = 0x1

	/*
		WTNCA_NODRAWICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTNCA_NODRAWICON = 0x2

	/*
		WTNCA_NOMIRRORHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTNCA_NOMIRRORHELP = 0x8

	/*
		WTNCA_NOSYSMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTNCA_NOSYSMENU = 0x4
)

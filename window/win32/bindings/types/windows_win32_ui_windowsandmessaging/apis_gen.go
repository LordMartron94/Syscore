//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-05-27 08:35:22 CEST

package windows_win32_ui_windowsandmessaging

const (
	/*
		ARW_DOWN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_DOWN = 0x4

	/*
		ARW_HIDE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_HIDE = 0x8

	/*
		ARW_LEFT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_LEFT = 0x0

	/*
		ARW_RIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_RIGHT = 0x0

	/*
		ARW_STARTMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_STARTMASK = 0x3

	/*
		ARW_STARTRIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_STARTRIGHT = 0x1

	/*
		ARW_STARTTOP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_STARTTOP = 0x2

	/*
		ARW_UP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ARW_UP = 0x4

	/*
		ASFW_ANY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ASFW_ANY = 0xffffffff

	/*
		BM_CLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_CLICK = 0xf5

	/*
		BM_GETCHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_GETCHECK = 0xf0

	/*
		BM_GETIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_GETIMAGE = 0xf6

	/*
		BM_GETSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_GETSTATE = 0xf2

	/*
		BM_SETCHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_SETCHECK = 0xf1

	/*
		BM_SETDONTCLICK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_SETDONTCLICK = 0xf8

	/*
		BM_SETIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_SETIMAGE = 0xf7

	/*
		BM_SETSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_SETSTATE = 0xf3

	/*
		BM_SETSTYLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BM_SETSTYLE = 0xf4

	/*
		BN_CLICKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_CLICKED = 0x0

	/*
		BN_DBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_DBLCLK = 0x5

	/*
		BN_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_DISABLE = 0x4

	/*
		BN_DOUBLECLICKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_DOUBLECLICKED = 0x5

	/*
		BN_HILITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_HILITE = 0x2

	/*
		BN_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_KILLFOCUS = 0x7

	/*
		BN_PAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_PAINT = 0x1

	/*
		BN_PUSHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_PUSHED = 0x2

	/*
		BN_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_SETFOCUS = 0x6

	/*
		BN_UNHILITE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_UNHILITE = 0x3

	/*
		BN_UNPUSHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BN_UNPUSHED = 0x3

	/*
		BROADCAST_QUERY_DENY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BROADCAST_QUERY_DENY = 0x424d5144

	/*
		BSF_MSGSRV32ISOK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BSF_MSGSRV32ISOK = 0x80000000

	/*
		BSF_MSGSRV32ISOK_BIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BSF_MSGSRV32ISOK_BIT = 0x1f

	/*
		BSM_INSTALLABLEDRIVERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BSM_INSTALLABLEDRIVERS = 0x4

	/*
		BSM_NETDRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BSM_NETDRIVER = 0x2

	/*
		BSM_VXDS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BSM_VXDS = 0x1

	/*
		BST_FOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BST_FOCUS = 0x8

	/*
		BST_PUSHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BST_PUSHED = 0x4

	/*
		BS_3STATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_3STATE = 0x5

	/*
		BS_AUTO3STATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_AUTO3STATE = 0x6

	/*
		BS_AUTOCHECKBOX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_AUTOCHECKBOX = 0x3

	/*
		BS_AUTORADIOBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_AUTORADIOBUTTON = 0x9

	/*
		BS_BITMAP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_BITMAP = 0x80

	/*
		BS_BOTTOM from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_BOTTOM = 0x800

	/*
		BS_CENTER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_CENTER = 0x300

	/*
		BS_CHECKBOX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_CHECKBOX = 0x2

	/*
		BS_DEFPUSHBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_DEFPUSHBUTTON = 0x1

	/*
		BS_FLAT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_FLAT = 0x8000

	/*
		BS_GROUPBOX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_GROUPBOX = 0x7

	/*
		BS_ICON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_ICON = 0x40

	/*
		BS_LEFT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_LEFT = 0x100

	/*
		BS_LEFTTEXT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_LEFTTEXT = 0x20

	/*
		BS_MULTILINE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_MULTILINE = 0x2000

	/*
		BS_NOTIFY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_NOTIFY = 0x4000

	/*
		BS_OWNERDRAW from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_OWNERDRAW = 0xb

	/*
		BS_PUSHBOX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_PUSHBOX = 0xa

	/*
		BS_PUSHBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_PUSHBUTTON = 0x0

	/*
		BS_PUSHLIKE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_PUSHLIKE = 0x1000

	/*
		BS_RADIOBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_RADIOBUTTON = 0x4

	/*
		BS_RIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_RIGHT = 0x200

	/*
		BS_RIGHTBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_RIGHTBUTTON = 0x20

	/*
		BS_TEXT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_TEXT = 0x0

	/*
		BS_TOP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_TOP = 0x400

	/*
		BS_TYPEMASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_TYPEMASK = 0xf

	/*
		BS_USERBUTTON from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_USERBUTTON = 0x8

	/*
		BS_VCENTER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	BS_VCENTER = 0xc00

	/*
		CALERT_SYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CALERT_SYSTEM = 0x6

	/*
		CBN_CLOSEUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_CLOSEUP = 0x8

	/*
		CBN_DBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_DBLCLK = 0x2

	/*
		CBN_DROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_DROPDOWN = 0x7

	/*
		CBN_EDITCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_EDITCHANGE = 0x5

	/*
		CBN_EDITUPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_EDITUPDATE = 0x6

	/*
		CBN_ERRSPACE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_ERRSPACE = -1

	/*
		CBN_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_KILLFOCUS = 0x4

	/*
		CBN_SELCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_SELCHANGE = 0x1

	/*
		CBN_SELENDCANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_SELENDCANCEL = 0xa

	/*
		CBN_SELENDOK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_SELENDOK = 0x9

	/*
		CBN_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBN_SETFOCUS = 0x3

	/*
		CBS_AUTOHSCROLL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_AUTOHSCROLL = 0x40

	/*
		CBS_DISABLENOSCROLL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_DISABLENOSCROLL = 0x800

	/*
		CBS_DROPDOWN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_DROPDOWN = 0x2

	/*
		CBS_DROPDOWNLIST from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_DROPDOWNLIST = 0x3

	/*
		CBS_HASSTRINGS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_HASSTRINGS = 0x200

	/*
		CBS_LOWERCASE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_LOWERCASE = 0x4000

	/*
		CBS_NOINTEGRALHEIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_NOINTEGRALHEIGHT = 0x400

	/*
		CBS_OEMCONVERT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_OEMCONVERT = 0x80

	/*
		CBS_OWNERDRAWFIXED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_OWNERDRAWFIXED = 0x10

	/*
		CBS_OWNERDRAWVARIABLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_OWNERDRAWVARIABLE = 0x20

	/*
		CBS_SIMPLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_SIMPLE = 0x1

	/*
		CBS_SORT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_SORT = 0x100

	/*
		CBS_UPPERCASE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CBS_UPPERCASE = 0x2000

	/*
		CB_ADDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_ADDSTRING = 0x143

	/*
		CB_DELETESTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_DELETESTRING = 0x144

	/*
		CB_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_DIR = 0x145

	/*
		CB_ERR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_ERR = -1

	/*
		CB_ERRSPACE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_ERRSPACE = -2

	/*
		CB_FINDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_FINDSTRING = 0x14c

	/*
		CB_FINDSTRINGEXACT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_FINDSTRINGEXACT = 0x158

	/*
		CB_GETCOMBOBOXINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETCOMBOBOXINFO = 0x164

	/*
		CB_GETCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETCOUNT = 0x146

	/*
		CB_GETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETCURSEL = 0x147

	/*
		CB_GETDROPPEDCONTROLRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETDROPPEDCONTROLRECT = 0x152

	/*
		CB_GETDROPPEDSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETDROPPEDSTATE = 0x157

	/*
		CB_GETDROPPEDWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETDROPPEDWIDTH = 0x15f

	/*
		CB_GETEDITSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETEDITSEL = 0x140

	/*
		CB_GETEXTENDEDUI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETEXTENDEDUI = 0x156

	/*
		CB_GETHORIZONTALEXTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETHORIZONTALEXTENT = 0x15d

	/*
		CB_GETITEMDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETITEMDATA = 0x150

	/*
		CB_GETITEMHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETITEMHEIGHT = 0x154

	/*
		CB_GETLBTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETLBTEXT = 0x148

	/*
		CB_GETLBTEXTLEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETLBTEXTLEN = 0x149

	/*
		CB_GETLOCALE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETLOCALE = 0x15a

	/*
		CB_GETTOPINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_GETTOPINDEX = 0x15b

	/*
		CB_INITSTORAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_INITSTORAGE = 0x161

	/*
		CB_INSERTSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_INSERTSTRING = 0x14a

	/*
		CB_LIMITTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_LIMITTEXT = 0x141

	/*
		CB_MSGMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_MSGMAX = 0x165

	/*
		CB_MULTIPLEADDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_MULTIPLEADDSTRING = 0x163

	/*
		CB_OKAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_OKAY = 0x0

	/*
		CB_RESETCONTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_RESETCONTENT = 0x14b

	/*
		CB_SELECTSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SELECTSTRING = 0x14d

	/*
		CB_SETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETCURSEL = 0x14e

	/*
		CB_SETDROPPEDWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETDROPPEDWIDTH = 0x160

	/*
		CB_SETEDITSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETEDITSEL = 0x142

	/*
		CB_SETEXTENDEDUI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETEXTENDEDUI = 0x155

	/*
		CB_SETHORIZONTALEXTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETHORIZONTALEXTENT = 0x15e

	/*
		CB_SETITEMDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETITEMDATA = 0x151

	/*
		CB_SETITEMHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETITEMHEIGHT = 0x153

	/*
		CB_SETLOCALE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETLOCALE = 0x159

	/*
		CB_SETTOPINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SETTOPINDEX = 0x15c

	/*
		CB_SHOWDROPDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CB_SHOWDROPDOWN = 0x14f

	/*
		CCHILDREN_SCROLLBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCHILDREN_SCROLLBAR = 0x5

	/*
		CCHILDREN_TITLEBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CCHILDREN_TITLEBAR = 0x5

	/*
		CHILDID_SELF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CHILDID_SELF = 0x0

	/*
		CONSOLE_APPLICATION_16BIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_APPLICATION_16BIT = 0x0

	/*
		CONSOLE_CARET_SELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_CARET_SELECTION = 0x1

	/*
		CONSOLE_CARET_VISIBLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONSOLE_CARET_VISIBLE = 0x2

	/*
		CONTACTVISUALIZATION_OFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONTACTVISUALIZATION_OFF = 0x0

	/*
		CONTACTVISUALIZATION_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONTACTVISUALIZATION_ON = 0x1

	/*
		CONTACTVISUALIZATION_PRESENTATIONMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CONTACTVISUALIZATION_PRESENTATIONMODE = 0x2

	/*
		CREATEPROCESS_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CREATEPROCESS_MANIFEST_RESOURCE_ID = 0x1

	/*
		CSOUND_SYSTEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CSOUND_SYSTEM = 0x10

	/*
		CTLCOLOR_BTN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_BTN = 0x3

	/*
		CTLCOLOR_DLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_DLG = 0x4

	/*
		CTLCOLOR_EDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_EDIT = 0x1

	/*
		CTLCOLOR_LISTBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_LISTBOX = 0x2

	/*
		CTLCOLOR_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_MAX = 0x7

	/*
		CTLCOLOR_MSGBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_MSGBOX = 0x0

	/*
		CTLCOLOR_SCROLLBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_SCROLLBAR = 0x5

	/*
		CTLCOLOR_STATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CTLCOLOR_STATIC = 0x6

	/*
		CURSOR_CREATION_SCALING_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CURSOR_CREATION_SCALING_DEFAULT = 0x2

	/*
		CURSOR_CREATION_SCALING_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CURSOR_CREATION_SCALING_NONE = 0x1

	/*
		CWF_CREATE_ONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CWF_CREATE_ONLY = 0x1

	/*
		CW_USEDEFAULT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CW_USEDEFAULT = -2147483648

	/*
		DBTF_RESOURCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBTF_RESOURCE = 0x1

	/*
		DBTF_SLOWNET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBTF_SLOWNET = 0x4

	/*
		DBTF_XPORT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBTF_XPORT = 0x2

	/*
		DBT_APPYBEGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_APPYBEGIN = 0x0

	/*
		DBT_APPYEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_APPYEND = 0x1

	/*
		DBT_CONFIGCHANGECANCELED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_CONFIGCHANGECANCELED = 0x19

	/*
		DBT_CONFIGCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_CONFIGCHANGED = 0x18

	/*
		DBT_CONFIGMGAPI32 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_CONFIGMGAPI32 = 0x22

	/*
		DBT_CONFIGMGPRIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_CONFIGMGPRIVATE = 0x7fff

	/*
		DBT_CUSTOMEVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_CUSTOMEVENT = 0x8006

	/*
		DBT_DEVICEARRIVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVICEARRIVAL = 0x8000

	/*
		DBT_DEVICEQUERYREMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVICEQUERYREMOVE = 0x8001

	/*
		DBT_DEVICEQUERYREMOVEFAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVICEQUERYREMOVEFAILED = 0x8002

	/*
		DBT_DEVICEREMOVECOMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVICEREMOVECOMPLETE = 0x8004

	/*
		DBT_DEVICEREMOVEPENDING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVICEREMOVEPENDING = 0x8003

	/*
		DBT_DEVICETYPESPECIFIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVICETYPESPECIFIC = 0x8005

	/*
		DBT_DEVNODES_CHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVNODES_CHANGED = 0x7

	/*
		DBT_DEVTYP_DEVNODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVTYP_DEVNODE = 0x1

	/*
		DBT_DEVTYP_NET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_DEVTYP_NET = 0x4

	/*
		DBT_LOW_DISK_SPACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_LOW_DISK_SPACE = 0x48

	/*
		DBT_MONITORCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_MONITORCHANGE = 0x1b

	/*
		DBT_NO_DISK_SPACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_NO_DISK_SPACE = 0x47

	/*
		DBT_QUERYCHANGECONFIG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_QUERYCHANGECONFIG = 0x17

	/*
		DBT_SHELLLOGGEDON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_SHELLLOGGEDON = 0x20

	/*
		DBT_USERDEFINED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_USERDEFINED = 0xffff

	/*
		DBT_VOLLOCKLOCKFAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VOLLOCKLOCKFAILED = 0x8043

	/*
		DBT_VOLLOCKLOCKRELEASED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VOLLOCKLOCKRELEASED = 0x8045

	/*
		DBT_VOLLOCKLOCKTAKEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VOLLOCKLOCKTAKEN = 0x8042

	/*
		DBT_VOLLOCKQUERYLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VOLLOCKQUERYLOCK = 0x8041

	/*
		DBT_VOLLOCKQUERYUNLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VOLLOCKQUERYUNLOCK = 0x8044

	/*
		DBT_VOLLOCKUNLOCKFAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VOLLOCKUNLOCKFAILED = 0x8046

	/*
		DBT_VPOWERDAPI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VPOWERDAPI = 0x8100

	/*
		DBT_VXDINITCOMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DBT_VXDINITCOMPLETE = 0x23

	/*
		DCX_EXCLUDEUPDATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DCX_EXCLUDEUPDATE = 0x100

	/*
		DC_HASDEFID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DC_HASDEFID = 0x534b

	/*
		DIFFERENCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DIFFERENCE = 0xb

	/*
		DLGC_BUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_BUTTON = 0x2000

	/*
		DLGC_DEFPUSHBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_DEFPUSHBUTTON = 0x10

	/*
		DLGC_HASSETSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_HASSETSEL = 0x8

	/*
		DLGC_RADIOBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_RADIOBUTTON = 0x40

	/*
		DLGC_STATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_STATIC = 0x100

	/*
		DLGC_UNDEFPUSHBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_UNDEFPUSHBUTTON = 0x20

	/*
		DLGC_WANTALLKEYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_WANTALLKEYS = 0x4

	/*
		DLGC_WANTARROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_WANTARROWS = 0x1

	/*
		DLGC_WANTCHARS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_WANTCHARS = 0x80

	/*
		DLGC_WANTMESSAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_WANTMESSAGE = 0x4

	/*
		DLGC_WANTTAB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGC_WANTTAB = 0x2

	/*
		DLGWINDOWEXTRA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DLGWINDOWEXTRA = 0x1e

	/*
		DM_GETDEFID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DM_GETDEFID = 0x400

	/*
		DM_POINTERHITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DM_POINTERHITTEST = 0x250

	/*
		DM_REPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DM_REPOSITION = 0x402

	/*
		DM_SETDEFID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DM_SETDEFID = 0x401

	/*
		DOF_DIRECTORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOF_DIRECTORY = 0x8003

	/*
		DOF_DOCUMENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOF_DOCUMENT = 0x8002

	/*
		DOF_EXECUTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOF_EXECUTABLE = 0x8001

	/*
		DOF_MULTIPLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOF_MULTIPLE = 0x8004

	/*
		DOF_PROGMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOF_PROGMAN = 0x1

	/*
		DOF_SHELLDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DOF_SHELLDATA = 0x2

	/*
		DO_DROPFILE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DO_DROPFILE = 0x454c4946

	/*
		DO_PRINTFILE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DO_PRINTFILE = 0x544e5250

	/*
		DS_3DLOOK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_3DLOOK = 0x4

	/*
		DS_ABSALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_ABSALIGN = 0x1

	/*
		DS_CENTER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_CENTER = 0x800

	/*
		DS_CENTERMOUSE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_CENTERMOUSE = 0x1000

	/*
		DS_CONTEXTHELP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_CONTEXTHELP = 0x2000

	/*
		DS_CONTROL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_CONTROL = 0x400

	/*
		DS_FIXEDSYS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_FIXEDSYS = 0x8

	/*
		DS_LOCALEDIT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_LOCALEDIT = 0x20

	/*
		DS_MODALFRAME from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_MODALFRAME = 0x80

	/*
		DS_NOFAILCREATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_NOFAILCREATE = 0x10

	/*
		DS_NOIDLEMSG from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_NOIDLEMSG = 0x100

	/*
		DS_SETFONT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_SETFONT = 0x40

	/*
		DS_SETFOREGROUND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_SETFOREGROUND = 0x200

	/*
		DS_SYSMODAL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_SYSMODAL = 0x2

	/*
		DS_USEPIXELS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DS_USEPIXELS = 0x8000

	/*
		DWLP_MSGRESULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DWLP_MSGRESULT = 0x0

	/*
		DWL_DLGPROC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DWL_DLGPROC = 0x4

	/*
		DWL_MSGRESULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DWL_MSGRESULT = 0x0

	/*
		DWL_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DWL_USER = 0x8

	/*
		EC_LEFTMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EC_LEFTMARGIN = 0x1

	/*
		EC_RIGHTMARGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EC_RIGHTMARGIN = 0x2

	/*
		EC_USEFONTINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EC_USEFONTINFO = 0xffff

	/*
		EDD_GET_DEVICE_INTERFACE_NAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EDD_GET_DEVICE_INTERFACE_NAME = 0x1

	/*
		EIMES_CANCELCOMPSTRINFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EIMES_CANCELCOMPSTRINFOCUS = 0x2

	/*
		EIMES_COMPLETECOMPSTRKILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EIMES_COMPLETECOMPSTRKILLFOCUS = 0x4

	/*
		EIMES_GETCOMPSTRATONCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EIMES_GETCOMPSTRATONCE = 0x1

	/*
		EMSIS_COMPOSITIONSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EMSIS_COMPOSITIONSTRING = 0x1

	/*
		ENDSESSION_CLOSEAPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDSESSION_CLOSEAPP = 0x1

	/*
		ENDSESSION_CRITICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDSESSION_CRITICAL = 0x40000000

	/*
		ENDSESSION_LOGOFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ENDSESSION_LOGOFF = 0x80000000

	/*
		EN_AFTER_PASTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_AFTER_PASTE = 0x801

	/*
		EN_ALIGN_LTR_EC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_ALIGN_LTR_EC = 0x700

	/*
		EN_ALIGN_RTL_EC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_ALIGN_RTL_EC = 0x701

	/*
		EN_BEFORE_PASTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_BEFORE_PASTE = 0x800

	/*
		EN_CHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_CHANGE = 0x300

	/*
		EN_ERRSPACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_ERRSPACE = 0x500

	/*
		EN_HSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_HSCROLL = 0x601

	/*
		EN_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_KILLFOCUS = 0x200

	/*
		EN_MAXTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_MAXTEXT = 0x501

	/*
		EN_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_SETFOCUS = 0x100

	/*
		EN_UPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_UPDATE = 0x400

	/*
		EN_VSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EN_VSCROLL = 0x602

	/*
		ES_AUTOHSCROLL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_AUTOHSCROLL = 0x80

	/*
		ES_AUTOVSCROLL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_AUTOVSCROLL = 0x40

	/*
		ES_CENTER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_CENTER = 0x1

	/*
		ES_LEFT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_LEFT = 0x0

	/*
		ES_LOWERCASE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_LOWERCASE = 0x10

	/*
		ES_MULTILINE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_MULTILINE = 0x4

	/*
		ES_NOHIDESEL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_NOHIDESEL = 0x100

	/*
		ES_NUMBER from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_NUMBER = 0x2000

	/*
		ES_OEMCONVERT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_OEMCONVERT = 0x400

	/*
		ES_PASSWORD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_PASSWORD = 0x20

	/*
		ES_READONLY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_READONLY = 0x800

	/*
		ES_RIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_RIGHT = 0x2

	/*
		ES_UPPERCASE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_UPPERCASE = 0x8

	/*
		ES_WANTRETURN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ES_WANTRETURN = 0x1000

	/*
		EVENT_AIA_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_AIA_END = 0xafff

	/*
		EVENT_AIA_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_AIA_START = 0xa000

	/*
		EVENT_CONSOLE_CARET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_CARET = 0x4001

	/*
		EVENT_CONSOLE_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_END = 0x40ff

	/*
		EVENT_CONSOLE_END_APPLICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_END_APPLICATION = 0x4007

	/*
		EVENT_CONSOLE_LAYOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_LAYOUT = 0x4005

	/*
		EVENT_CONSOLE_START_APPLICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_START_APPLICATION = 0x4006

	/*
		EVENT_CONSOLE_UPDATE_REGION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_UPDATE_REGION = 0x4002

	/*
		EVENT_CONSOLE_UPDATE_SCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_UPDATE_SCROLL = 0x4004

	/*
		EVENT_CONSOLE_UPDATE_SIMPLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_CONSOLE_UPDATE_SIMPLE = 0x4003

	/*
		EVENT_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_MAX = 0x7fffffff

	/*
		EVENT_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_MIN = 0x1

	/*
		EVENT_OBJECT_ACCELERATORCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_ACCELERATORCHANGE = 0x8012

	/*
		EVENT_OBJECT_CLOAKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_CLOAKED = 0x8017

	/*
		EVENT_OBJECT_CONTENTSCROLLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_CONTENTSCROLLED = 0x8015

	/*
		EVENT_OBJECT_CREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_CREATE = 0x8000

	/*
		EVENT_OBJECT_DEFACTIONCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DEFACTIONCHANGE = 0x8011

	/*
		EVENT_OBJECT_DESCRIPTIONCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DESCRIPTIONCHANGE = 0x800d

	/*
		EVENT_OBJECT_DESTROY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DESTROY = 0x8001

	/*
		EVENT_OBJECT_DRAGCANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DRAGCANCEL = 0x8022

	/*
		EVENT_OBJECT_DRAGCOMPLETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DRAGCOMPLETE = 0x8023

	/*
		EVENT_OBJECT_DRAGDROPPED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DRAGDROPPED = 0x8026

	/*
		EVENT_OBJECT_DRAGENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DRAGENTER = 0x8024

	/*
		EVENT_OBJECT_DRAGLEAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DRAGLEAVE = 0x8025

	/*
		EVENT_OBJECT_DRAGSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_DRAGSTART = 0x8021

	/*
		EVENT_OBJECT_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_END = 0x80ff

	/*
		EVENT_OBJECT_FOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_FOCUS = 0x8005

	/*
		EVENT_OBJECT_HELPCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_HELPCHANGE = 0x8010

	/*
		EVENT_OBJECT_HIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_HIDE = 0x8003

	/*
		EVENT_OBJECT_HOSTEDOBJECTSINVALIDATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_HOSTEDOBJECTSINVALIDATED = 0x8020

	/*
		EVENT_OBJECT_IME_CHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_IME_CHANGE = 0x8029

	/*
		EVENT_OBJECT_IME_HIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_IME_HIDE = 0x8028

	/*
		EVENT_OBJECT_IME_SHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_IME_SHOW = 0x8027

	/*
		EVENT_OBJECT_INVOKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_INVOKED = 0x8013

	/*
		EVENT_OBJECT_LIVEREGIONCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_LIVEREGIONCHANGED = 0x8019

	/*
		EVENT_OBJECT_LOCATIONCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_LOCATIONCHANGE = 0x800b

	/*
		EVENT_OBJECT_NAMECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_NAMECHANGE = 0x800c

	/*
		EVENT_OBJECT_PARENTCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_PARENTCHANGE = 0x800f

	/*
		EVENT_OBJECT_REORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_REORDER = 0x8004

	/*
		EVENT_OBJECT_SELECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_SELECTION = 0x8006

	/*
		EVENT_OBJECT_SELECTIONADD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_SELECTIONADD = 0x8007

	/*
		EVENT_OBJECT_SELECTIONREMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_SELECTIONREMOVE = 0x8008

	/*
		EVENT_OBJECT_SELECTIONWITHIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_SELECTIONWITHIN = 0x8009

	/*
		EVENT_OBJECT_SHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_SHOW = 0x8002

	/*
		EVENT_OBJECT_STATECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_STATECHANGE = 0x800a

	/*
		EVENT_OBJECT_TEXTEDIT_CONVERSIONTARGETCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_TEXTEDIT_CONVERSIONTARGETCHANGED = 0x8030

	/*
		EVENT_OBJECT_TEXTSELECTIONCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_TEXTSELECTIONCHANGED = 0x8014

	/*
		EVENT_OBJECT_UNCLOAKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_UNCLOAKED = 0x8018

	/*
		EVENT_OBJECT_VALUECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OBJECT_VALUECHANGE = 0x800e

	/*
		EVENT_OEM_DEFINED_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OEM_DEFINED_END = 0x1ff

	/*
		EVENT_OEM_DEFINED_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_OEM_DEFINED_START = 0x101

	/*
		EVENT_SYSTEM_ALERT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_ALERT = 0x2

	/*
		EVENT_SYSTEM_ARRANGMENTPREVIEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_ARRANGMENTPREVIEW = 0x8016

	/*
		EVENT_SYSTEM_CAPTUREEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_CAPTUREEND = 0x9

	/*
		EVENT_SYSTEM_CAPTURESTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_CAPTURESTART = 0x8

	/*
		EVENT_SYSTEM_CONTEXTHELPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_CONTEXTHELPEND = 0xd

	/*
		EVENT_SYSTEM_CONTEXTHELPSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_CONTEXTHELPSTART = 0xc

	/*
		EVENT_SYSTEM_DESKTOPSWITCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_DESKTOPSWITCH = 0x20

	/*
		EVENT_SYSTEM_DIALOGEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_DIALOGEND = 0x11

	/*
		EVENT_SYSTEM_DIALOGSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_DIALOGSTART = 0x10

	/*
		EVENT_SYSTEM_DRAGDROPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_DRAGDROPEND = 0xf

	/*
		EVENT_SYSTEM_DRAGDROPSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_DRAGDROPSTART = 0xe

	/*
		EVENT_SYSTEM_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_END = 0xff

	/*
		EVENT_SYSTEM_FOREGROUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_FOREGROUND = 0x3

	/*
		EVENT_SYSTEM_IME_KEY_NOTIFICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_IME_KEY_NOTIFICATION = 0x29

	/*
		EVENT_SYSTEM_MENUEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MENUEND = 0x5

	/*
		EVENT_SYSTEM_MENUPOPUPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MENUPOPUPEND = 0x7

	/*
		EVENT_SYSTEM_MENUPOPUPSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MENUPOPUPSTART = 0x6

	/*
		EVENT_SYSTEM_MENUSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MENUSTART = 0x4

	/*
		EVENT_SYSTEM_MINIMIZEEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MINIMIZEEND = 0x17

	/*
		EVENT_SYSTEM_MINIMIZESTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MINIMIZESTART = 0x16

	/*
		EVENT_SYSTEM_MOVESIZEEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MOVESIZEEND = 0xb

	/*
		EVENT_SYSTEM_MOVESIZESTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_MOVESIZESTART = 0xa

	/*
		EVENT_SYSTEM_SCROLLINGEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SCROLLINGEND = 0x13

	/*
		EVENT_SYSTEM_SCROLLINGSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SCROLLINGSTART = 0x12

	/*
		EVENT_SYSTEM_SOUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SOUND = 0x1

	/*
		EVENT_SYSTEM_SWITCHEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SWITCHEND = 0x15

	/*
		EVENT_SYSTEM_SWITCHER_APPDROPPED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SWITCHER_APPDROPPED = 0x26

	/*
		EVENT_SYSTEM_SWITCHER_APPGRABBED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SWITCHER_APPGRABBED = 0x24

	/*
		EVENT_SYSTEM_SWITCHER_APPOVERTARGET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SWITCHER_APPOVERTARGET = 0x25

	/*
		EVENT_SYSTEM_SWITCHER_CANCELLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SWITCHER_CANCELLED = 0x27

	/*
		EVENT_SYSTEM_SWITCHSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_SYSTEM_SWITCHSTART = 0x14

	/*
		EVENT_UIA_EVENTID_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_UIA_EVENTID_END = 0x4eff

	/*
		EVENT_UIA_EVENTID_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_UIA_EVENTID_START = 0x4e00

	/*
		EVENT_UIA_PROPID_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_UIA_PROPID_END = 0x75ff

	/*
		EVENT_UIA_PROPID_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	EVENT_UIA_PROPID_START = 0x7500

	/*
		FAPPCOMMAND_KEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FAPPCOMMAND_KEY = 0x0

	/*
		FAPPCOMMAND_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FAPPCOMMAND_MASK = 0xf000

	/*
		FAPPCOMMAND_MOUSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FAPPCOMMAND_MOUSE = 0x8000

	/*
		FAPPCOMMAND_OEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FAPPCOMMAND_OEM = 0x1000

	/*
		FE_FONTSMOOTHINGCLEARTYPE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FE_FONTSMOOTHINGCLEARTYPE = 0x2

	/*
		FE_FONTSMOOTHINGORIENTATIONBGR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FE_FONTSMOOTHINGORIENTATIONBGR = 0x0

	/*
		FE_FONTSMOOTHINGORIENTATIONRGB from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FE_FONTSMOOTHINGORIENTATIONRGB = 0x1

	/*
		FE_FONTSMOOTHINGSTANDARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FE_FONTSMOOTHINGSTANDARD = 0x1

	/*
		FKF_AVAILABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_AVAILABLE = 0x2

	/*
		FKF_CLICKON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_CLICKON = 0x40

	/*
		FKF_CONFIRMHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_CONFIRMHOTKEY = 0x8

	/*
		FKF_FILTERKEYSON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_FILTERKEYSON = 0x1

	/*
		FKF_HOTKEYACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_HOTKEYACTIVE = 0x4

	/*
		FKF_HOTKEYSOUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_HOTKEYSOUND = 0x10

	/*
		FKF_INDICATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	FKF_INDICATOR = 0x20

	/*
		GCF_INCLUDE_ANCESTORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GCF_INCLUDE_ANCESTORS = 0x1

	/*
		GESTURECONFIGMAXCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTURECONFIGMAXCOUNT = 0x100

	/*
		GESTUREVISUALIZATION_DOUBLETAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_DOUBLETAP = 0x2

	/*
		GESTUREVISUALIZATION_OFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_OFF = 0x0

	/*
		GESTUREVISUALIZATION_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_ON = 0x1f

	/*
		GESTUREVISUALIZATION_PRESSANDHOLD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_PRESSANDHOLD = 0x8

	/*
		GESTUREVISUALIZATION_PRESSANDTAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_PRESSANDTAP = 0x4

	/*
		GESTUREVISUALIZATION_RIGHTTAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_RIGHTTAP = 0x10

	/*
		GESTUREVISUALIZATION_TAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GESTUREVISUALIZATION_TAP = 0x1

	/*
		GF_BEGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GF_BEGIN = 0x1

	/*
		GF_END from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GF_END = 0x4

	/*
		GF_INERTIA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GF_INERTIA = 0x2

	/*
		GIDC_ARRIVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GIDC_ARRIVAL = 0x1

	/*
		GIDC_REMOVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GIDC_REMOVAL = 0x2

	/*
		GUI_16BITTASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GUI_16BITTASK = 0x0

	/*
		GWFS_INCLUDE_ANCESTORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GWFS_INCLUDE_ANCESTORS = 0x1

	/*
		GW_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	GW_MAX = 0x5

	/*
		HBMMENU_CALLBACK from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_CALLBACK = -1

	/*
		HBMMENU_MBAR_CLOSE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_MBAR_CLOSE = 0x5

	/*
		HBMMENU_MBAR_CLOSE_D from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_MBAR_CLOSE_D = 0x6

	/*
		HBMMENU_MBAR_MINIMIZE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_MBAR_MINIMIZE = 0x3

	/*
		HBMMENU_MBAR_MINIMIZE_D from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_MBAR_MINIMIZE_D = 0x7

	/*
		HBMMENU_MBAR_RESTORE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_MBAR_RESTORE = 0x2

	/*
		HBMMENU_POPUP_CLOSE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_POPUP_CLOSE = 0x8

	/*
		HBMMENU_POPUP_MAXIMIZE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_POPUP_MAXIMIZE = 0xa

	/*
		HBMMENU_POPUP_MINIMIZE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_POPUP_MINIMIZE = 0xb

	/*
		HBMMENU_POPUP_RESTORE from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_POPUP_RESTORE = 0x9

	/*
		HBMMENU_SYSTEM from Windows.Win32 metadata (originally typed HBITMAP). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HBMMENU_SYSTEM = 0x1

	/*
		HCBT_ACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_ACTIVATE = 0x5

	/*
		HCBT_CLICKSKIPPED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_CLICKSKIPPED = 0x6

	/*
		HCBT_CREATEWND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_CREATEWND = 0x3

	/*
		HCBT_DESTROYWND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_DESTROYWND = 0x4

	/*
		HCBT_KEYSKIPPED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_KEYSKIPPED = 0x7

	/*
		HCBT_MINMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_MINMAX = 0x1

	/*
		HCBT_MOVESIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_MOVESIZE = 0x0

	/*
		HCBT_QS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_QS = 0x2

	/*
		HCBT_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_SETFOCUS = 0x9

	/*
		HCBT_SYSCOMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCBT_SYSCOMMAND = 0x8

	/*
		HCF_DEFAULTDESKTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCF_DEFAULTDESKTOP = 0x200

	/*
		HCF_LOGONDESKTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HCF_LOGONDESKTOP = 0x100

	/*
		HC_ACTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_ACTION = 0x0

	/*
		HC_GETNEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_GETNEXT = 0x1

	/*
		HC_NOREM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_NOREM = 0x3

	/*
		HC_NOREMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_NOREMOVE = 0x3

	/*
		HC_SKIP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_SKIP = 0x2

	/*
		HC_SYSMODALOFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_SYSMODALOFF = 0x5

	/*
		HC_SYSMODALON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HC_SYSMODALON = 0x4

	/*
		HELP_COMMAND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_COMMAND = 0x102

	/*
		HELP_CONTENTS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_CONTENTS = 0x3

	/*
		HELP_CONTEXT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_CONTEXT = 0x1

	/*
		HELP_CONTEXTMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_CONTEXTMENU = 0xa

	/*
		HELP_CONTEXTPOPUP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_CONTEXTPOPUP = 0x8

	/*
		HELP_FINDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_FINDER = 0xb

	/*
		HELP_FORCEFILE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_FORCEFILE = 0x9

	/*
		HELP_HELPONHELP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_HELPONHELP = 0x4

	/*
		HELP_INDEX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_INDEX = 0x3

	/*
		HELP_KEY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_KEY = 0x101

	/*
		HELP_MULTIKEY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_MULTIKEY = 0x201

	/*
		HELP_PARTIALKEY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_PARTIALKEY = 0x105

	/*
		HELP_QUIT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_QUIT = 0x2

	/*
		HELP_SETCONTENTS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_SETCONTENTS = 0x5

	/*
		HELP_SETINDEX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_SETINDEX = 0x5

	/*
		HELP_SETPOPUP_POS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_SETPOPUP_POS = 0xd

	/*
		HELP_SETWINPOS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_SETWINPOS = 0x203

	/*
		HELP_TCARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_TCARD = 0x8000

	/*
		HELP_TCARD_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_TCARD_DATA = 0x10

	/*
		HELP_TCARD_OTHER_CALLER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_TCARD_OTHER_CALLER = 0x11

	/*
		HELP_WM_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HELP_WM_HELP = 0xc

	/*
		HIDE_WINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HIDE_WINDOW = 0x0

	/*
		HKL_NEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKL_NEXT = 0x1

	/*
		HKL_PREV from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HKL_PREV = 0x0

	/*
		HSHELL_ACCESSIBILITYSTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_ACCESSIBILITYSTATE = 0xb

	/*
		HSHELL_ACTIVATESHELLWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_ACTIVATESHELLWINDOW = 0x3

	/*
		HSHELL_APPCOMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_APPCOMMAND = 0xc

	/*
		HSHELL_ENDTASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_ENDTASK = 0xa

	/*
		HSHELL_GETMINRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_GETMINRECT = 0x5

	/*
		HSHELL_HIGHBIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_HIGHBIT = 0x8000

	/*
		HSHELL_LANGUAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_LANGUAGE = 0x8

	/*
		HSHELL_MONITORCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_MONITORCHANGED = 0x10

	/*
		HSHELL_REDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_REDRAW = 0x6

	/*
		HSHELL_SYSMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_SYSMENU = 0x9

	/*
		HSHELL_TASKMAN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_TASKMAN = 0x7

	/*
		HSHELL_WINDOWACTIVATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_WINDOWACTIVATED = 0x4

	/*
		HSHELL_WINDOWCREATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_WINDOWCREATED = 0x1

	/*
		HSHELL_WINDOWDESTROYED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_WINDOWDESTROYED = 0x2

	/*
		HSHELL_WINDOWREPLACED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_WINDOWREPLACED = 0xd

	/*
		HSHELL_WINDOWREPLACING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HSHELL_WINDOWREPLACING = 0xe

	/*
		HTBORDER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTBORDER = 0x12

	/*
		HTBOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTBOTTOM = 0xf

	/*
		HTBOTTOMLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTBOTTOMLEFT = 0x10

	/*
		HTBOTTOMRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTBOTTOMRIGHT = 0x11

	/*
		HTCAPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTCAPTION = 0x2

	/*
		HTCLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTCLIENT = 0x1

	/*
		HTCLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTCLOSE = 0x14

	/*
		HTERROR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTERROR = -2

	/*
		HTGROWBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTGROWBOX = 0x4

	/*
		HTHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTHELP = 0x15

	/*
		HTHSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTHSCROLL = 0x6

	/*
		HTLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTLEFT = 0xa

	/*
		HTMAXBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTMAXBUTTON = 0x9

	/*
		HTMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTMENU = 0x5

	/*
		HTMINBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTMINBUTTON = 0x8

	/*
		HTNOWHERE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTNOWHERE = 0x0

	/*
		HTOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTOBJECT = 0x13

	/*
		HTREDUCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTREDUCE = 0x8

	/*
		HTRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTRIGHT = 0xb

	/*
		HTSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTSIZE = 0x4

	/*
		HTSIZEFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTSIZEFIRST = 0xa

	/*
		HTSIZELAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTSIZELAST = 0x11

	/*
		HTSYSMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTSYSMENU = 0x3

	/*
		HTTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTTOP = 0xc

	/*
		HTTOPLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTTOPLEFT = 0xd

	/*
		HTTOPRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTTOPRIGHT = 0xe

	/*
		HTTRANSPARENT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTTRANSPARENT = -1

	/*
		HTVSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTVSCROLL = 0x7

	/*
		HTZOOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HTZOOM = 0x9

	/*
		HWND_BOTTOM from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_BOTTOM = 0x1

	/*
		HWND_BROADCAST from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_BROADCAST = 0xffff

	/*
		HWND_DESKTOP from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_DESKTOP = 0x0

	/*
		HWND_MESSAGE from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_MESSAGE = -3

	/*
		HWND_NOTOPMOST from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_NOTOPMOST = -2

	/*
		HWND_TOP from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_TOP = 0x0

	/*
		HWND_TOPMOST from Windows.Win32 metadata (originally typed HWND). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HWND_TOPMOST = -1

	/*
		ICON_BIG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ICON_BIG = 0x1

	/*
		ICON_SMALL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ICON_SMALL = 0x0

	/*
		ICON_SMALL2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ICON_SMALL2 = 0x2

	/*
		IDANI_CAPTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDANI_CAPTION = 0x3

	/*
		IDANI_OPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDANI_OPEN = 0x1

	/*
		IDC_APPSTARTING from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_APPSTARTING = 0x7f8a

	/*
		IDC_ARROW from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_ARROW = 0x7f00

	/*
		IDC_CROSS from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_CROSS = 0x7f03

	/*
		IDC_HAND from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_HAND = 0x7f89

	/*
		IDC_HELP from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_HELP = 0x7f8b

	/*
		IDC_IBEAM from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_IBEAM = 0x7f01

	/*
		IDC_ICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_ICON = 0x7f81

	/*
		IDC_NO from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_NO = 0x7f88

	/*
		IDC_PERSON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_PERSON = 0x7fa0

	/*
		IDC_PIN from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_PIN = 0x7f9f

	/*
		IDC_SIZE from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_SIZE = 0x7f80

	/*
		IDC_SIZEALL from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_SIZEALL = 0x7f86

	/*
		IDC_SIZENESW from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_SIZENESW = 0x7f83

	/*
		IDC_SIZENS from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_SIZENS = 0x7f85

	/*
		IDC_SIZENWSE from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_SIZENWSE = 0x7f82

	/*
		IDC_SIZEWE from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_SIZEWE = 0x7f84

	/*
		IDC_STATIC from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_STATIC = -1

	/*
		IDC_UPARROW from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_UPARROW = 0x7f04

	/*
		IDC_WAIT from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDC_WAIT = 0x7f02

	/*
		IDHOT_SNAPDESKTOP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDHOT_SNAPDESKTOP = -2

	/*
		IDHOT_SNAPWINDOW from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDHOT_SNAPWINDOW = -1

	/*
		IDH_CANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDH_CANCEL = 0x6f1c

	/*
		IDH_GENERIC_HELP_BUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDH_GENERIC_HELP_BUTTON = 0x6f1a

	/*
		IDH_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDH_HELP = 0x6f1d

	/*
		IDH_MISSING_CONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDH_MISSING_CONTEXT = 0x6f19

	/*
		IDH_NO_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDH_NO_HELP = 0x6f18

	/*
		IDH_OK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDH_OK = 0x6f1b

	/*
		IDI_APPLICATION from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_APPLICATION = 0x7f00

	/*
		IDI_ASTERISK from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_ASTERISK = 0x7f04

	/*
		IDI_ERROR from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_ERROR = 0x7f01

	/*
		IDI_EXCLAMATION from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_EXCLAMATION = 0x7f03

	/*
		IDI_HAND from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_HAND = 0x7f01

	/*
		IDI_INFORMATION from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_INFORMATION = 0x7f04

	/*
		IDI_QUESTION from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_QUESTION = 0x7f02

	/*
		IDI_SHIELD from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_SHIELD = 0x7f06

	/*
		IDI_WARNING from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_WARNING = 0x7f03

	/*
		IDI_WINLOGO from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IDI_WINLOGO = 0x7f05

	/*
		IMAGE_ENHMETAFILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	IMAGE_ENHMETAFILE = 0x3

	/*
		INDEXID_CONTAINER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEXID_CONTAINER = 0x0

	/*
		INDEXID_OBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INDEXID_OBJECT = 0x0

	/*
		INPUTLANGCHANGE_BACKWARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INPUTLANGCHANGE_BACKWARD = 0x4

	/*
		INPUTLANGCHANGE_FORWARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INPUTLANGCHANGE_FORWARD = 0x2

	/*
		INPUTLANGCHANGE_SYSCHARSET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	INPUTLANGCHANGE_SYSCHARSET = 0x1

	/*
		ISMEX_CALLBACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISMEX_CALLBACK = 0x4

	/*
		ISMEX_NOSEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISMEX_NOSEND = 0x0

	/*
		ISMEX_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISMEX_NOTIFY = 0x2

	/*
		ISMEX_REPLIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISMEX_REPLIED = 0x8

	/*
		ISMEX_SEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISMEX_SEND = 0x1

	/*
		ISOLATIONAWARE_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISOLATIONAWARE_MANIFEST_RESOURCE_ID = 0x2

	/*
		ISOLATIONAWARE_NOSTATICIMPORT_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISOLATIONAWARE_NOSTATICIMPORT_MANIFEST_RESOURCE_ID = 0x3

	/*
		ISOLATIONPOLICY_BROWSER_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISOLATIONPOLICY_BROWSER_MANIFEST_RESOURCE_ID = 0x5

	/*
		ISOLATIONPOLICY_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ISOLATIONPOLICY_MANIFEST_RESOURCE_ID = 0x4

	/*
		KF_ALTDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KF_ALTDOWN = 0x2000

	/*
		KF_DLGMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KF_DLGMODE = 0x800

	/*
		KF_EXTENDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KF_EXTENDED = 0x100

	/*
		KF_MENUMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KF_MENUMODE = 0x1000

	/*
		KF_REPEAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KF_REPEAT = 0x4000

	/*
		KF_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KF_UP = 0x8000

	/*
		KL_NAMELENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	KL_NAMELENGTH = 0x9

	/*
		LBN_DBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBN_DBLCLK = 0x2

	/*
		LBN_ERRSPACE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBN_ERRSPACE = -2

	/*
		LBN_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBN_KILLFOCUS = 0x5

	/*
		LBN_SELCANCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBN_SELCANCEL = 0x3

	/*
		LBN_SELCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBN_SELCHANGE = 0x1

	/*
		LBN_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBN_SETFOCUS = 0x4

	/*
		LBS_COMBOBOX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_COMBOBOX = 0x8000

	/*
		LBS_DISABLENOSCROLL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_DISABLENOSCROLL = 0x1000

	/*
		LBS_EXTENDEDSEL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_EXTENDEDSEL = 0x800

	/*
		LBS_HASSTRINGS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_HASSTRINGS = 0x40

	/*
		LBS_MULTICOLUMN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_MULTICOLUMN = 0x200

	/*
		LBS_MULTIPLESEL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_MULTIPLESEL = 0x8

	/*
		LBS_NODATA from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_NODATA = 0x2000

	/*
		LBS_NOINTEGRALHEIGHT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_NOINTEGRALHEIGHT = 0x100

	/*
		LBS_NOREDRAW from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_NOREDRAW = 0x4

	/*
		LBS_NOSEL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_NOSEL = 0x4000

	/*
		LBS_NOTIFY from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_NOTIFY = 0x1

	/*
		LBS_OWNERDRAWFIXED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_OWNERDRAWFIXED = 0x10

	/*
		LBS_OWNERDRAWVARIABLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_OWNERDRAWVARIABLE = 0x20

	/*
		LBS_SORT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_SORT = 0x2

	/*
		LBS_STANDARD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_STANDARD = 0xa00003

	/*
		LBS_USETABSTOPS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_USETABSTOPS = 0x80

	/*
		LBS_WANTKEYBOARDINPUT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LBS_WANTKEYBOARDINPUT = 0x400

	/*
		LB_ADDFILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_ADDFILE = 0x196

	/*
		LB_ADDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_ADDSTRING = 0x180

	/*
		LB_CTLCODE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_CTLCODE = 0x0

	/*
		LB_DELETESTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_DELETESTRING = 0x182

	/*
		LB_DIR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_DIR = 0x18d

	/*
		LB_ERR from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_ERR = -1

	/*
		LB_ERRSPACE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_ERRSPACE = -2

	/*
		LB_FINDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_FINDSTRING = 0x18f

	/*
		LB_FINDSTRINGEXACT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_FINDSTRINGEXACT = 0x1a2

	/*
		LB_GETANCHORINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETANCHORINDEX = 0x19d

	/*
		LB_GETCARETINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETCARETINDEX = 0x19f

	/*
		LB_GETCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETCOUNT = 0x18b

	/*
		LB_GETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETCURSEL = 0x188

	/*
		LB_GETHORIZONTALEXTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETHORIZONTALEXTENT = 0x193

	/*
		LB_GETITEMDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETITEMDATA = 0x199

	/*
		LB_GETITEMHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETITEMHEIGHT = 0x1a1

	/*
		LB_GETITEMRECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETITEMRECT = 0x198

	/*
		LB_GETLISTBOXINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETLISTBOXINFO = 0x1b2

	/*
		LB_GETLOCALE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETLOCALE = 0x1a6

	/*
		LB_GETSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETSEL = 0x187

	/*
		LB_GETSELCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETSELCOUNT = 0x190

	/*
		LB_GETSELITEMS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETSELITEMS = 0x191

	/*
		LB_GETTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETTEXT = 0x189

	/*
		LB_GETTEXTLEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETTEXTLEN = 0x18a

	/*
		LB_GETTOPINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_GETTOPINDEX = 0x18e

	/*
		LB_INITSTORAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_INITSTORAGE = 0x1a8

	/*
		LB_INSERTSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_INSERTSTRING = 0x181

	/*
		LB_ITEMFROMPOINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_ITEMFROMPOINT = 0x1a9

	/*
		LB_MSGMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_MSGMAX = 0x1b3

	/*
		LB_MULTIPLEADDSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_MULTIPLEADDSTRING = 0x1b1

	/*
		LB_OKAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_OKAY = 0x0

	/*
		LB_RESETCONTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_RESETCONTENT = 0x184

	/*
		LB_SELECTSTRING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SELECTSTRING = 0x18c

	/*
		LB_SELITEMRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SELITEMRANGE = 0x19b

	/*
		LB_SELITEMRANGEEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SELITEMRANGEEX = 0x183

	/*
		LB_SETANCHORINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETANCHORINDEX = 0x19c

	/*
		LB_SETCARETINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETCARETINDEX = 0x19e

	/*
		LB_SETCOLUMNWIDTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETCOLUMNWIDTH = 0x195

	/*
		LB_SETCOUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETCOUNT = 0x1a7

	/*
		LB_SETCURSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETCURSEL = 0x186

	/*
		LB_SETHORIZONTALEXTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETHORIZONTALEXTENT = 0x194

	/*
		LB_SETITEMDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETITEMDATA = 0x19a

	/*
		LB_SETITEMHEIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETITEMHEIGHT = 0x1a0

	/*
		LB_SETLOCALE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETLOCALE = 0x1a5

	/*
		LB_SETSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETSEL = 0x185

	/*
		LB_SETTABSTOPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETTABSTOPS = 0x192

	/*
		LB_SETTOPINDEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LB_SETTOPINDEX = 0x197

	/*
		LLMHF_INJECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LLMHF_INJECTED = 0x1

	/*
		LLMHF_LOWER_IL_INJECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LLMHF_LOWER_IL_INJECTED = 0x2

	/*
		LOCKF_LOGICAL_LOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKF_LOGICAL_LOCK = 0x0

	/*
		LOCKF_PHYSICAL_LOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKF_PHYSICAL_LOCK = 0x1

	/*
		LOCKP_ALLOW_MEM_MAPPING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKP_ALLOW_MEM_MAPPING = 0x0

	/*
		LOCKP_ALLOW_WRITES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKP_ALLOW_WRITES = 0x1

	/*
		LOCKP_FAIL_MEM_MAPPING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKP_FAIL_MEM_MAPPING = 0x2

	/*
		LOCKP_FAIL_WRITES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKP_FAIL_WRITES = 0x0

	/*
		LOCKP_LOCK_FOR_FORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKP_LOCK_FOR_FORMAT = 0x4

	/*
		LOCKP_USER_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LOCKP_USER_MASK = 0x3

	/*
		LR_COLOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	LR_COLOR = 0x2

	/*
		MAXIMUM_RESERVED_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAXIMUM_RESERVED_MANIFEST_RESOURCE_ID = 0x10

	/*
		MAX_LOGICALDPIOVERRIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_LOGICALDPIOVERRIDE = 0x2

	/*
		MAX_STR_BLOCKREASON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_STR_BLOCKREASON = 0x100

	/*
		MAX_TOUCH_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_TOUCH_COUNT = 0x100

	/*
		MAX_TOUCH_PREDICTION_FILTER_TAPS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_TOUCH_PREDICTION_FILTER_TAPS = 0x3

	/*
		MA_ACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MA_ACTIVATE = 0x1

	/*
		MA_ACTIVATEANDEAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MA_ACTIVATEANDEAT = 0x2

	/*
		MA_NOACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MA_NOACTIVATE = 0x3

	/*
		MA_NOACTIVATEANDEAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MA_NOACTIVATEANDEAT = 0x4

	/*
		MDIS_ALLCHILDSTYLES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MDIS_ALLCHILDSTYLES = 0x1

	/*
		METRICS_USEDEFAULT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	METRICS_USEDEFAULT = -1

	/*
		MINIMUM_RESERVED_MANIFEST_RESOURCE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MINIMUM_RESERVED_MANIFEST_RESOURCE_ID = 0x1

	/*
		MIN_LOGICALDPIOVERRIDE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MIN_LOGICALDPIOVERRIDE = -2

	/*
		MKF_AVAILABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_AVAILABLE = 0x2

	/*
		MKF_CONFIRMHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_CONFIRMHOTKEY = 0x8

	/*
		MKF_HOTKEYACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_HOTKEYACTIVE = 0x4

	/*
		MKF_HOTKEYSOUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_HOTKEYSOUND = 0x10

	/*
		MKF_INDICATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_INDICATOR = 0x20

	/*
		MKF_LEFTBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_LEFTBUTTONDOWN = 0x1000000

	/*
		MKF_LEFTBUTTONSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_LEFTBUTTONSEL = 0x10000000

	/*
		MKF_MODIFIERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_MODIFIERS = 0x40

	/*
		MKF_MOUSEKEYSON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_MOUSEKEYSON = 0x1

	/*
		MKF_MOUSEMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_MOUSEMODE = 0x80000000

	/*
		MKF_REPLACENUMBERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_REPLACENUMBERS = 0x80

	/*
		MKF_RIGHTBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_RIGHTBUTTONDOWN = 0x2000000

	/*
		MKF_RIGHTBUTTONSEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MKF_RIGHTBUTTONSEL = 0x20000000

	/*
		MNC_CLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MNC_CLOSE = 0x1

	/*
		MNC_EXECUTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MNC_EXECUTE = 0x2

	/*
		MNC_IGNORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MNC_IGNORE = 0x0

	/*
		MNC_SELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MNC_SELECT = 0x3

	/*
		MND_CONTINUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MND_CONTINUE = 0x0

	/*
		MND_ENDMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MND_ENDMENU = 0x1

	/*
		MNGO_NOERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MNGO_NOERROR = 0x1

	/*
		MNGO_NOINTERFACE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MNGO_NOINTERFACE = 0x0

	/*
		MN_GETHMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MN_GETHMENU = 0x1e1

	/*
		MONITORINFOF_PRIMARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MONITORINFOF_PRIMARY = 0x1

	/*
		MOUSEWHEEL_ROUTING_FOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSEWHEEL_ROUTING_FOCUS = 0x0

	/*
		MOUSEWHEEL_ROUTING_HYBRID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSEWHEEL_ROUTING_HYBRID = 0x1

	/*
		MOUSEWHEEL_ROUTING_MOUSE_POS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MOUSEWHEEL_ROUTING_MOUSE_POS = 0x2

	/*
		MSGF_DIALOGBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_DIALOGBOX = 0x0

	/*
		MSGF_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_MAX = 0x8

	/*
		MSGF_MENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_MENU = 0x2

	/*
		MSGF_MESSAGEBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_MESSAGEBOX = 0x1

	/*
		MSGF_NEXTWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_NEXTWINDOW = 0x6

	/*
		MSGF_SCROLLBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_SCROLLBAR = 0x5

	/*
		MSGF_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_USER = 0x1000

	/*
		NFR_ANSI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFR_ANSI = 0x1

	/*
		NFR_UNICODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NFR_UNICODE = 0x2

	/*
		NF_QUERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NF_QUERY = 0x3

	/*
		NF_REQUERY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NF_REQUERY = 0x4

	/*
		NID_EXTERNAL_PEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NID_EXTERNAL_PEN = 0x8

	/*
		NID_EXTERNAL_TOUCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NID_EXTERNAL_TOUCH = 0x2

	/*
		NID_INTEGRATED_PEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NID_INTEGRATED_PEN = 0x4

	/*
		NID_INTEGRATED_TOUCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NID_INTEGRATED_TOUCH = 0x1

	/*
		NID_MULTI_INPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NID_MULTI_INPUT = 0x40

	/*
		NID_READY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	NID_READY = 0x80

	/*
		OBM_BTNCORNERS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_BTNCORNERS = 0x7ff6

	/*
		OBM_BTSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_BTSIZE = 0x7ff9

	/*
		OBM_CHECK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_CHECK = 0x7ff8

	/*
		OBM_CHECKBOXES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_CHECKBOXES = 0x7ff7

	/*
		OBM_CLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_CLOSE = 0x7ff2

	/*
		OBM_COMBO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_COMBO = 0x7fe2

	/*
		OBM_DNARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_DNARROW = 0x7ff0

	/*
		OBM_DNARROWD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_DNARROWD = 0x7fe6

	/*
		OBM_DNARROWI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_DNARROWI = 0x7fe0

	/*
		OBM_LFARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_LFARROW = 0x7fee

	/*
		OBM_LFARROWD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_LFARROWD = 0x7fe4

	/*
		OBM_LFARROWI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_LFARROWI = 0x7fde

	/*
		OBM_MNARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_MNARROW = 0x7fe3

	/*
		OBM_OLD_CLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_CLOSE = 0x7fff

	/*
		OBM_OLD_DNARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_DNARROW = 0x7ffc

	/*
		OBM_OLD_LFARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_LFARROW = 0x7ffa

	/*
		OBM_OLD_REDUCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_REDUCE = 0x7ff5

	/*
		OBM_OLD_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_RESTORE = 0x7ff3

	/*
		OBM_OLD_RGARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_RGARROW = 0x7ffb

	/*
		OBM_OLD_UPARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_UPARROW = 0x7ffd

	/*
		OBM_OLD_ZOOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_OLD_ZOOM = 0x7ff4

	/*
		OBM_REDUCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_REDUCE = 0x7fed

	/*
		OBM_REDUCED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_REDUCED = 0x7fea

	/*
		OBM_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_RESTORE = 0x7feb

	/*
		OBM_RESTORED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_RESTORED = 0x7fe8

	/*
		OBM_RGARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_RGARROW = 0x7fef

	/*
		OBM_RGARROWD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_RGARROWD = 0x7fe5

	/*
		OBM_RGARROWI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_RGARROWI = 0x7fdf

	/*
		OBM_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_SIZE = 0x7ffe

	/*
		OBM_UPARROW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_UPARROW = 0x7ff1

	/*
		OBM_UPARROWD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_UPARROWD = 0x7fe7

	/*
		OBM_UPARROWI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_UPARROWI = 0x7fe1

	/*
		OBM_ZOOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_ZOOM = 0x7fec

	/*
		OBM_ZOOMD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OBM_ZOOMD = 0x7fe9

	/*
		OCR_ICOCUR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OCR_ICOCUR = 0x7f87

	/*
		OCR_ICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OCR_ICON = 0x7f81

	/*
		OCR_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OCR_SIZE = 0x7f80

	/*
		OIC_BANG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_BANG = 0x7f03

	/*
		OIC_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_ERROR = 0x7f01

	/*
		OIC_HAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_HAND = 0x7f01

	/*
		OIC_INFORMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_INFORMATION = 0x7f04

	/*
		OIC_NOTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_NOTE = 0x7f04

	/*
		OIC_QUES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_QUES = 0x7f02

	/*
		OIC_SAMPLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_SAMPLE = 0x7f00

	/*
		OIC_SHIELD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_SHIELD = 0x7f06

	/*
		OIC_WARNING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_WARNING = 0x7f03

	/*
		OIC_WINLOGO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	OIC_WINLOGO = 0x7f05

	/*
		ORD_LANGDRIVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	ORD_LANGDRIVER = 0x1

	/*
		PA_ACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PA_ACTIVATE = 0x1

	/*
		PA_NOACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PA_NOACTIVATE = 0x3

	/*
		PBTF_APMRESUMEFROMFAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBTF_APMRESUMEFROMFAILURE = 0x1

	/*
		PBT_APMBATTERYLOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMBATTERYLOW = 0x9

	/*
		PBT_APMOEMEVENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMOEMEVENT = 0xb

	/*
		PBT_APMPOWERSTATUSCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMPOWERSTATUSCHANGE = 0xa

	/*
		PBT_APMQUERYSTANDBY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMQUERYSTANDBY = 0x1

	/*
		PBT_APMQUERYSTANDBYFAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMQUERYSTANDBYFAILED = 0x3

	/*
		PBT_APMQUERYSUSPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMQUERYSUSPEND = 0x0

	/*
		PBT_APMQUERYSUSPENDFAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMQUERYSUSPENDFAILED = 0x2

	/*
		PBT_APMRESUMEAUTOMATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMRESUMEAUTOMATIC = 0x12

	/*
		PBT_APMRESUMECRITICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMRESUMECRITICAL = 0x6

	/*
		PBT_APMRESUMESTANDBY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMRESUMESTANDBY = 0x8

	/*
		PBT_APMRESUMESUSPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMRESUMESUSPEND = 0x7

	/*
		PBT_APMSTANDBY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMSTANDBY = 0x5

	/*
		PBT_APMSUSPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_APMSUSPEND = 0x4

	/*
		PBT_POWERSETTINGCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PBT_POWERSETTINGCHANGE = 0x8013

	/*
		PDC_ARRIVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_ARRIVAL = 0x1

	/*
		PDC_MAPPING_CHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_MAPPING_CHANGE = 0x100

	/*
		PDC_MODE_ASPECTRATIOPRESERVED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_MODE_ASPECTRATIOPRESERVED = 0x800

	/*
		PDC_MODE_CENTERED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_MODE_CENTERED = 0x80

	/*
		PDC_MODE_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_MODE_DEFAULT = 0x40

	/*
		PDC_ORIENTATION_0 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_ORIENTATION_0 = 0x4

	/*
		PDC_ORIENTATION_180 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_ORIENTATION_180 = 0x10

	/*
		PDC_ORIENTATION_270 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_ORIENTATION_270 = 0x20

	/*
		PDC_ORIENTATION_90 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_ORIENTATION_90 = 0x8

	/*
		PDC_ORIGIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_ORIGIN = 0x400

	/*
		PDC_REMOVAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_REMOVAL = 0x2

	/*
		PDC_RESOLUTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PDC_RESOLUTION = 0x200

	/*
		PENARBITRATIONTYPE_FIS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENARBITRATIONTYPE_FIS = 0x2

	/*
		PENARBITRATIONTYPE_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENARBITRATIONTYPE_MAX = 0x4

	/*
		PENARBITRATIONTYPE_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENARBITRATIONTYPE_NONE = 0x0

	/*
		PENARBITRATIONTYPE_SPT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENARBITRATIONTYPE_SPT = 0x3

	/*
		PENARBITRATIONTYPE_WIN8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENARBITRATIONTYPE_WIN8 = 0x1

	/*
		PENVISUALIZATION_CURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENVISUALIZATION_CURSOR = 0x20

	/*
		PENVISUALIZATION_DOUBLETAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENVISUALIZATION_DOUBLETAP = 0x2

	/*
		PENVISUALIZATION_OFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENVISUALIZATION_OFF = 0x0

	/*
		PENVISUALIZATION_ON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENVISUALIZATION_ON = 0x23

	/*
		PENVISUALIZATION_TAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PENVISUALIZATION_TAP = 0x1

	/*
		PEN_FLAG_BARREL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_FLAG_BARREL = 0x1

	/*
		PEN_FLAG_ERASER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_FLAG_ERASER = 0x4

	/*
		PEN_FLAG_INVERTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_FLAG_INVERTED = 0x2

	/*
		PEN_FLAG_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_FLAG_NONE = 0x0

	/*
		PEN_MASK_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_MASK_NONE = 0x0

	/*
		PEN_MASK_PRESSURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_MASK_PRESSURE = 0x1

	/*
		PEN_MASK_ROTATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_MASK_ROTATION = 0x2

	/*
		PEN_MASK_TILT_X from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_MASK_TILT_X = 0x4

	/*
		PEN_MASK_TILT_Y from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PEN_MASK_TILT_Y = 0x8

	/*
		PMB_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PMB_ACTIVE = 0x1

	/*
		POINTER_DEVICE_PRODUCT_STRING_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_DEVICE_PRODUCT_STRING_MAX = 0x208

	/*
		POINTER_MESSAGE_FLAG_CANCELED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_CANCELED = 0x8000

	/*
		POINTER_MESSAGE_FLAG_CONFIDENCE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_CONFIDENCE = 0x4000

	/*
		POINTER_MESSAGE_FLAG_FIFTHBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_FIFTHBUTTON = 0x100

	/*
		POINTER_MESSAGE_FLAG_FIRSTBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_FIRSTBUTTON = 0x10

	/*
		POINTER_MESSAGE_FLAG_FOURTHBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_FOURTHBUTTON = 0x80

	/*
		POINTER_MESSAGE_FLAG_INCONTACT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_INCONTACT = 0x4

	/*
		POINTER_MESSAGE_FLAG_INRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_INRANGE = 0x2

	/*
		POINTER_MESSAGE_FLAG_NEW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_NEW = 0x1

	/*
		POINTER_MESSAGE_FLAG_PRIMARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_PRIMARY = 0x2000

	/*
		POINTER_MESSAGE_FLAG_SECONDBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_SECONDBUTTON = 0x20

	/*
		POINTER_MESSAGE_FLAG_THIRDBUTTON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MESSAGE_FLAG_THIRDBUTTON = 0x40

	/*
		POINTER_MOD_CTRL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MOD_CTRL = 0x8

	/*
		POINTER_MOD_SHIFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POINTER_MOD_SHIFT = 0x4

	/*
		PRF_CHECKVISIBLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRF_CHECKVISIBLE = 0x1

	/*
		PRF_CHILDREN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRF_CHILDREN = 0x10

	/*
		PRF_CLIENT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRF_CLIENT = 0x4

	/*
		PRF_ERASEBKGND from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRF_ERASEBKGND = 0x8

	/*
		PRF_NONCLIENT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRF_NONCLIENT = 0x2

	/*
		PRF_OWNED from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PRF_OWNED = 0x20

	/*
		PWR_CRITICALRESUME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PWR_CRITICALRESUME = 0x3

	/*
		PWR_FAIL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PWR_FAIL = -1

	/*
		PWR_OK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PWR_OK = 0x1

	/*
		PWR_SUSPENDREQUEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PWR_SUSPENDREQUEST = 0x1

	/*
		PWR_SUSPENDRESUME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PWR_SUSPENDRESUME = 0x2

	/*
		PW_RENDERFULLCONTENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	PW_RENDERFULLCONTENT = 0x2

	/*
		QS_POINTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QS_POINTER = 0x1000

	/*
		QS_TOUCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QS_TOUCH = 0x800

	/*
		RES_CURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RES_CURSOR = 0x2

	/*
		RES_ICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RES_ICON = 0x1

	/*
		RIDEV_EXMODEMASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIDEV_EXMODEMASK = 0xf0

	/*
		RIM_INPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIM_INPUT = 0x0

	/*
		RIM_INPUTSINK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIM_INPUTSINK = 0x1

	/*
		RIM_TYPEMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RIM_TYPEMAX = 0x2

	/*
		RI_KEY_BREAK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_KEY_BREAK = 0x1

	/*
		RI_KEY_E0 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_KEY_E0 = 0x2

	/*
		RI_KEY_E1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_KEY_E1 = 0x4

	/*
		RI_KEY_MAKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_KEY_MAKE = 0x0

	/*
		RI_KEY_TERMSRV_SET_LED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_KEY_TERMSRV_SET_LED = 0x8

	/*
		RI_KEY_TERMSRV_SHADOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_KEY_TERMSRV_SHADOW = 0x10

	/*
		RI_MOUSE_BUTTON_1_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_1_DOWN = 0x1

	/*
		RI_MOUSE_BUTTON_1_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_1_UP = 0x2

	/*
		RI_MOUSE_BUTTON_2_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_2_DOWN = 0x4

	/*
		RI_MOUSE_BUTTON_2_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_2_UP = 0x8

	/*
		RI_MOUSE_BUTTON_3_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_3_DOWN = 0x10

	/*
		RI_MOUSE_BUTTON_3_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_3_UP = 0x20

	/*
		RI_MOUSE_BUTTON_4_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_4_DOWN = 0x40

	/*
		RI_MOUSE_BUTTON_4_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_4_UP = 0x80

	/*
		RI_MOUSE_BUTTON_5_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_5_DOWN = 0x100

	/*
		RI_MOUSE_BUTTON_5_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_BUTTON_5_UP = 0x200

	/*
		RI_MOUSE_HWHEEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_HWHEEL = 0x800

	/*
		RI_MOUSE_LEFT_BUTTON_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_LEFT_BUTTON_DOWN = 0x1

	/*
		RI_MOUSE_LEFT_BUTTON_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_LEFT_BUTTON_UP = 0x2

	/*
		RI_MOUSE_MIDDLE_BUTTON_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_MIDDLE_BUTTON_DOWN = 0x10

	/*
		RI_MOUSE_MIDDLE_BUTTON_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_MIDDLE_BUTTON_UP = 0x20

	/*
		RI_MOUSE_RIGHT_BUTTON_DOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_RIGHT_BUTTON_DOWN = 0x4

	/*
		RI_MOUSE_RIGHT_BUTTON_UP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_RIGHT_BUTTON_UP = 0x8

	/*
		RI_MOUSE_WHEEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RI_MOUSE_WHEEL = 0x400

	/*
		RT_ACCELERATOR from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_ACCELERATOR = 0x9

	/*
		RT_ANICURSOR from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_ANICURSOR = 0x15

	/*
		RT_ANIICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_ANIICON = 0x16

	/*
		RT_BITMAP from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_BITMAP = 0x2

	/*
		RT_CURSOR from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_CURSOR = 0x1

	/*
		RT_DIALOG from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_DIALOG = 0x5

	/*
		RT_DLGINCLUDE from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_DLGINCLUDE = 0x11

	/*
		RT_FONT from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_FONT = 0x8

	/*
		RT_FONTDIR from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_FONTDIR = 0x7

	/*
		RT_HTML from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_HTML = 0x17

	/*
		RT_ICON from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_ICON = 0x3

	/*
		RT_MANIFEST from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_MANIFEST = 0x18

	/*
		RT_MENU from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_MENU = 0x4

	/*
		RT_MESSAGETABLE from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_MESSAGETABLE = 0xb

	/*
		RT_PLUGPLAY from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_PLUGPLAY = 0x13

	/*
		RT_VERSION from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_VERSION = 0x10

	/*
		RT_VXD from Windows.Win32 metadata (originally typed *PWSTRElement). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	RT_VXD = 0x14

	/*
		SBM_ENABLE_ARROWS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_ENABLE_ARROWS = 0xe4

	/*
		SBM_GETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_GETPOS = 0xe1

	/*
		SBM_GETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_GETRANGE = 0xe3

	/*
		SBM_GETSCROLLBARINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_GETSCROLLBARINFO = 0xeb

	/*
		SBM_GETSCROLLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_GETSCROLLINFO = 0xea

	/*
		SBM_SETPOS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_SETPOS = 0xe0

	/*
		SBM_SETRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_SETRANGE = 0xe2

	/*
		SBM_SETRANGEREDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_SETRANGEREDRAW = 0xe6

	/*
		SBM_SETSCROLLINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBM_SETSCROLLINFO = 0xe9

	/*
		SBS_BOTTOMALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_BOTTOMALIGN = 0x4

	/*
		SBS_HORZ from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_HORZ = 0x0

	/*
		SBS_LEFTALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_LEFTALIGN = 0x2

	/*
		SBS_RIGHTALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_RIGHTALIGN = 0x4

	/*
		SBS_SIZEBOX from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_SIZEBOX = 0x8

	/*
		SBS_SIZEBOXBOTTOMRIGHTALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_SIZEBOXBOTTOMRIGHTALIGN = 0x4

	/*
		SBS_SIZEBOXTOPLEFTALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_SIZEBOXTOPLEFTALIGN = 0x2

	/*
		SBS_SIZEGRIP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_SIZEGRIP = 0x10

	/*
		SBS_TOPALIGN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_TOPALIGN = 0x2

	/*
		SBS_VERT from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SBS_VERT = 0x1

	/*
		SCF_ISSECURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SCF_ISSECURE = 0x1

	/*
		SC_ARRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_ARRANGE = 0xf110

	/*
		SC_CLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_CLOSE = 0xf060

	/*
		SC_CONTEXTHELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_CONTEXTHELP = 0xf180

	/*
		SC_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_DEFAULT = 0xf160

	/*
		SC_HOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_HOTKEY = 0xf150

	/*
		SC_HSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_HSCROLL = 0xf080

	/*
		SC_ICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_ICON = 0xf020

	/*
		SC_KEYMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_KEYMENU = 0xf100

	/*
		SC_MAXIMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_MAXIMIZE = 0xf030

	/*
		SC_MINIMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_MINIMIZE = 0xf020

	/*
		SC_MONITORPOWER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_MONITORPOWER = 0xf170

	/*
		SC_MOUSEMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_MOUSEMENU = 0xf090

	/*
		SC_MOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_MOVE = 0xf010

	/*
		SC_NEXTWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_NEXTWINDOW = 0xf040

	/*
		SC_PREVWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_PREVWINDOW = 0xf050

	/*
		SC_RESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_RESTORE = 0xf120

	/*
		SC_SEPARATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_SEPARATOR = 0xf00f

	/*
		SC_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_SIZE = 0xf000

	/*
		SC_TASKLIST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_TASKLIST = 0xf130

	/*
		SC_VSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_VSCROLL = 0xf070

	/*
		SC_ZOOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SC_ZOOM = 0xf030

	/*
		SHOW_FULLSCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHOW_FULLSCREEN = 0x3

	/*
		SHOW_ICONWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHOW_ICONWINDOW = 0x2

	/*
		SHOW_OPENNOACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHOW_OPENNOACTIVATE = 0x4

	/*
		SHOW_OPENWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHOW_OPENWINDOW = 0x1

	/*
		SIZEFULLSCREEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZEFULLSCREEN = 0x2

	/*
		SIZEICONIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZEICONIC = 0x1

	/*
		SIZENORMAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZENORMAL = 0x0

	/*
		SIZEZOOMHIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZEZOOMHIDE = 0x4

	/*
		SIZEZOOMSHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZEZOOMSHOW = 0x3

	/*
		SIZE_MAXHIDE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZE_MAXHIDE = 0x4

	/*
		SIZE_MAXIMIZED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZE_MAXIMIZED = 0x2

	/*
		SIZE_MAXSHOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZE_MAXSHOW = 0x3

	/*
		SIZE_MINIMIZED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZE_MINIMIZED = 0x1

	/*
		SIZE_RESTORED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SIZE_RESTORED = 0x0

	/*
		SM_CARETBLINKINGENABLED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SM_CARETBLINKINGENABLED = 0x2002

	/*
		SM_CMETRICS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SM_CMETRICS = 0x4c

	/*
		SM_RESERVED1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SM_RESERVED1 = 0x18

	/*
		SM_RESERVED2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SM_RESERVED2 = 0x19

	/*
		SM_RESERVED3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SM_RESERVED3 = 0x1a

	/*
		SM_RESERVED4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SM_RESERVED4 = 0x1b

	/*
		SOUND_SYSTEM_APPEND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_APPEND = 0xe

	/*
		SOUND_SYSTEM_APPSTART from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_APPSTART = 0xc

	/*
		SOUND_SYSTEM_BEEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_BEEP = 0x3

	/*
		SOUND_SYSTEM_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_ERROR = 0x4

	/*
		SOUND_SYSTEM_FAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_FAULT = 0xd

	/*
		SOUND_SYSTEM_INFORMATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_INFORMATION = 0x7

	/*
		SOUND_SYSTEM_MAXIMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_MAXIMIZE = 0x8

	/*
		SOUND_SYSTEM_MENUCOMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_MENUCOMMAND = 0xf

	/*
		SOUND_SYSTEM_MENUPOPUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_MENUPOPUP = 0x10

	/*
		SOUND_SYSTEM_MINIMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_MINIMIZE = 0x9

	/*
		SOUND_SYSTEM_QUESTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_QUESTION = 0x5

	/*
		SOUND_SYSTEM_RESTOREDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_RESTOREDOWN = 0xb

	/*
		SOUND_SYSTEM_RESTOREUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_RESTOREUP = 0xa

	/*
		SOUND_SYSTEM_SHUTDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_SHUTDOWN = 0x2

	/*
		SOUND_SYSTEM_STARTUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_STARTUP = 0x1

	/*
		SOUND_SYSTEM_WARNING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SOUND_SYSTEM_WARNING = 0x6

	/*
		STATE_SYSTEM_ALERT_HIGH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_ALERT_HIGH = 0x10000000

	/*
		STATE_SYSTEM_ALERT_LOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_ALERT_LOW = 0x4000000

	/*
		STATE_SYSTEM_ALERT_MEDIUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_ALERT_MEDIUM = 0x8000000

	/*
		STATE_SYSTEM_ANIMATED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_ANIMATED = 0x4000

	/*
		STATE_SYSTEM_BUSY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_BUSY = 0x800

	/*
		STATE_SYSTEM_CHECKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_CHECKED = 0x10

	/*
		STATE_SYSTEM_COLLAPSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_COLLAPSED = 0x400

	/*
		STATE_SYSTEM_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_DEFAULT = 0x100

	/*
		STATE_SYSTEM_EXPANDED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_EXPANDED = 0x200

	/*
		STATE_SYSTEM_EXTSELECTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_EXTSELECTABLE = 0x2000000

	/*
		STATE_SYSTEM_FLOATING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_FLOATING = 0x1000

	/*
		STATE_SYSTEM_FOCUSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_FOCUSED = 0x4

	/*
		STATE_SYSTEM_HOTTRACKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_HOTTRACKED = 0x80

	/*
		STATE_SYSTEM_INDETERMINATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_INDETERMINATE = 0x20

	/*
		STATE_SYSTEM_LINKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_LINKED = 0x400000

	/*
		STATE_SYSTEM_MARQUEED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_MARQUEED = 0x2000

	/*
		STATE_SYSTEM_MIXED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_MIXED = 0x20

	/*
		STATE_SYSTEM_MOVEABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_MOVEABLE = 0x40000

	/*
		STATE_SYSTEM_MULTISELECTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_MULTISELECTABLE = 0x1000000

	/*
		STATE_SYSTEM_PROTECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_PROTECTED = 0x20000000

	/*
		STATE_SYSTEM_READONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_READONLY = 0x40

	/*
		STATE_SYSTEM_SELECTABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_SELECTABLE = 0x200000

	/*
		STATE_SYSTEM_SELECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_SELECTED = 0x2

	/*
		STATE_SYSTEM_SELFVOICING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_SELFVOICING = 0x80000

	/*
		STATE_SYSTEM_SIZEABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_SIZEABLE = 0x20000

	/*
		STATE_SYSTEM_TRAVERSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_TRAVERSED = 0x800000

	/*
		STATE_SYSTEM_VALID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STATE_SYSTEM_VALID = 0x3fffffff

	/*
		STM_GETICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STM_GETICON = 0x171

	/*
		STM_GETIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STM_GETIMAGE = 0x173

	/*
		STM_MSGMAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STM_MSGMAX = 0x174

	/*
		STM_SETICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STM_SETICON = 0x170

	/*
		STM_SETIMAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STM_SETIMAGE = 0x172

	/*
		STN_CLICKED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STN_CLICKED = 0x0

	/*
		STN_DBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STN_DBLCLK = 0x1

	/*
		STN_DISABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STN_DISABLE = 0x3

	/*
		STN_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STN_ENABLE = 0x2

	/*
		STRSAFE_E_END_OF_FILE from Windows.Win32 metadata (originally typed HRESULT). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_E_END_OF_FILE = -2147024858

	/*
		STRSAFE_E_INSUFFICIENT_BUFFER from Windows.Win32 metadata (originally typed HRESULT). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_E_INSUFFICIENT_BUFFER = -2147024774

	/*
		STRSAFE_E_INVALID_PARAMETER from Windows.Win32 metadata (originally typed HRESULT). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_E_INVALID_PARAMETER = -2147024809

	/*
		STRSAFE_FILL_BEHIND_NULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_FILL_BEHIND_NULL = 0x200

	/*
		STRSAFE_FILL_ON_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_FILL_ON_FAILURE = 0x400

	/*
		STRSAFE_IGNORE_NULLS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_IGNORE_NULLS = 0x100

	/*
		STRSAFE_MAX_CCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_MAX_CCH = 0x7fffffff

	/*
		STRSAFE_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_MAX_LENGTH = 0x7ffffffe

	/*
		STRSAFE_NO_TRUNCATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_NO_TRUNCATION = 0x1000

	/*
		STRSAFE_NULL_ON_FAILURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_NULL_ON_FAILURE = 0x800

	/*
		STRSAFE_USE_SECURE_CRT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	STRSAFE_USE_SECURE_CRT = 0x0

	/*
		TIMERV_COALESCING_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TIMERV_COALESCING_MAX = 0x7ffffff5

	/*
		TIMERV_COALESCING_MIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TIMERV_COALESCING_MIN = 0x1

	/*
		TIMERV_DEFAULT_COALESCING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TIMERV_DEFAULT_COALESCING = 0x0

	/*
		TIMERV_NO_COALESCING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TIMERV_NO_COALESCING = 0xffffffff

	/*
		TKF_AVAILABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TKF_AVAILABLE = 0x2

	/*
		TKF_CONFIRMHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TKF_CONFIRMHOTKEY = 0x8

	/*
		TKF_HOTKEYACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TKF_HOTKEYACTIVE = 0x4

	/*
		TKF_HOTKEYSOUND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TKF_HOTKEYSOUND = 0x10

	/*
		TKF_INDICATOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TKF_INDICATOR = 0x20

	/*
		TKF_TOGGLEKEYSON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TKF_TOGGLEKEYSON = 0x1

	/*
		TOUCHPREDICTIONPARAMETERS_DEFAULT_LATENCY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCHPREDICTIONPARAMETERS_DEFAULT_LATENCY = 0x8

	/*
		TOUCHPREDICTIONPARAMETERS_DEFAULT_SAMPLETIME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCHPREDICTIONPARAMETERS_DEFAULT_SAMPLETIME = 0x8

	/*
		TOUCHPREDICTIONPARAMETERS_DEFAULT_USE_HW_TIMESTAMP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCHPREDICTIONPARAMETERS_DEFAULT_USE_HW_TIMESTAMP = 0x1

	/*
		TOUCH_FLAG_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_FLAG_NONE = 0x0

	/*
		TOUCH_HIT_TESTING_CLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_HIT_TESTING_CLIENT = 0x1

	/*
		TOUCH_HIT_TESTING_DEFAULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_HIT_TESTING_DEFAULT = 0x0

	/*
		TOUCH_HIT_TESTING_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_HIT_TESTING_NONE = 0x2

	/*
		TOUCH_HIT_TESTING_PROXIMITY_CLOSEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_HIT_TESTING_PROXIMITY_CLOSEST = 0x0

	/*
		TOUCH_HIT_TESTING_PROXIMITY_FARTHEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_HIT_TESTING_PROXIMITY_FARTHEST = 0xfff

	/*
		TOUCH_MASK_CONTACTAREA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_MASK_CONTACTAREA = 0x1

	/*
		TOUCH_MASK_NONE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_MASK_NONE = 0x0

	/*
		TOUCH_MASK_ORIENTATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_MASK_ORIENTATION = 0x2

	/*
		TOUCH_MASK_PRESSURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TOUCH_MASK_PRESSURE = 0x4

	/*
		UISF_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UISF_ACTIVE = 0x4

	/*
		UISF_HIDEACCEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UISF_HIDEACCEL = 0x2

	/*
		UISF_HIDEFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UISF_HIDEFOCUS = 0x1

	/*
		UIS_CLEAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIS_CLEAR = 0x2

	/*
		UIS_INITIALIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIS_INITIALIZE = 0x3

	/*
		UIS_SET from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UIS_SET = 0x1

	/*
		UNICODE_NOCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UNICODE_NOCHAR = 0xffff

	/*
		UOI_TIMERPROC_EXCEPTION_SUPPRESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	UOI_TIMERPROC_EXCEPTION_SUPPRESSION = 0x7

	/*
		USER_DEFAULT_SCREEN_DPI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	USER_DEFAULT_SCREEN_DPI = 0x60

	/*
		USER_TIMER_MAXIMUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	USER_TIMER_MAXIMUM = 0x7fffffff

	/*
		USER_TIMER_MINIMUM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	USER_TIMER_MINIMUM = 0xa

	/*
		WARNING_BANNED_API_USAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_BANNED_API_USAGE = 0x702f

	/*
		WARNING_CYCLOMATIC_COMPLEXITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_CYCLOMATIC_COMPLEXITY = 0x703e

	/*
		WARNING_DEREF_NULL_PTR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_DEREF_NULL_PTR = 0x177b

	/*
		WARNING_HIGH_PRIORITY_OVERFLOW_POSTCONDITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_HIGH_PRIORITY_OVERFLOW_POSTCONDITION = 0x65bd

	/*
		WARNING_INCORRECT_ANNOTATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_INCORRECT_ANNOTATION = 0x6597

	/*
		WARNING_INVALID_PARAM_VALUE_1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_INVALID_PARAM_VALUE_1 = 0x18f3

	/*
		WARNING_INVALID_PARAM_VALUE_3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_INVALID_PARAM_VALUE_3 = 0x6e17

	/*
		WARNING_MISSING_ZERO_TERMINATION2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_MISSING_ZERO_TERMINATION2 = 0x17a6

	/*
		WARNING_POSTCONDITION_NULLTERMINATION_VIOLATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_POSTCONDITION_NULLTERMINATION_VIOLATION = 0x65b4

	/*
		WARNING_POST_EXPECTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_POST_EXPECTED = 0x6e32

	/*
		WARNING_POTENTIAL_BUFFER_OVERFLOW_HIGH_PRIORITY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_POTENTIAL_BUFFER_OVERFLOW_HIGH_PRIORITY = 0x659f

	/*
		WARNING_POTENTIAL_RANGE_POSTCONDITION_VIOLATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_POTENTIAL_RANGE_POSTCONDITION_VIOLATION = 0x65d7

	/*
		WARNING_PRECONDITION_NULLTERMINATION_VIOLATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_PRECONDITION_NULLTERMINATION_VIOLATION = 0x65b3

	/*
		WARNING_RANGE_POSTCONDITION_VIOLATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_RANGE_POSTCONDITION_VIOLATION = 0x65cd

	/*
		WARNING_RETURNING_BAD_RESULT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_RETURNING_BAD_RESULT = 0x6e24

	/*
		WARNING_RETURN_UNINIT_VAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_RETURN_UNINIT_VAR = 0x17d5

	/*
		WARNING_USING_UNINIT_VAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WARNING_USING_UNINIT_VAR = 0x1771

	/*
		WA_ACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WA_ACTIVE = 0x1

	/*
		WA_CLICKACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WA_CLICKACTIVE = 0x2

	/*
		WA_INACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WA_INACTIVE = 0x0

	/*
		WHEEL_DELTA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WHEEL_DELTA = 0x78

	/*
		WH_HARDWARE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WH_HARDWARE = 0x8

	/*
		WH_MAX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WH_MAX = 0xe

	/*
		WH_MAXHOOK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WH_MAXHOOK = 0xe

	/*
		WH_MIN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WH_MIN = -1

	/*
		WH_MINHOOK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WH_MINHOOK = -1

	/*
		WINEVENT_INCONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINEVENT_INCONTEXT = 0x4

	/*
		WINEVENT_OUTOFCONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINEVENT_OUTOFCONTEXT = 0x0

	/*
		WINEVENT_SKIPOWNPROCESS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINEVENT_SKIPOWNPROCESS = 0x2

	/*
		WINEVENT_SKIPOWNTHREAD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINEVENT_SKIPOWNTHREAD = 0x1

	/*
		WINSTA_ACCESSCLIPBOARD from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_ACCESSCLIPBOARD = 0x4

	/*
		WINSTA_ACCESSGLOBALATOMS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_ACCESSGLOBALATOMS = 0x20

	/*
		WINSTA_ALL_ACCESS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_ALL_ACCESS = 0x37f

	/*
		WINSTA_CREATEDESKTOP from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_CREATEDESKTOP = 0x8

	/*
		WINSTA_ENUMDESKTOPS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_ENUMDESKTOPS = 0x1

	/*
		WINSTA_ENUMERATE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_ENUMERATE = 0x100

	/*
		WINSTA_EXITWINDOWS from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_EXITWINDOWS = 0x40

	/*
		WINSTA_READATTRIBUTES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_READATTRIBUTES = 0x2

	/*
		WINSTA_READSCREEN from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_READSCREEN = 0x200

	/*
		WINSTA_WRITEATTRIBUTES from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WINSTA_WRITEATTRIBUTES = 0x10

	/*
		WMSZ_BOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_BOTTOM = 0x6

	/*
		WMSZ_BOTTOMLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_BOTTOMLEFT = 0x7

	/*
		WMSZ_BOTTOMRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_BOTTOMRIGHT = 0x8

	/*
		WMSZ_LEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_LEFT = 0x1

	/*
		WMSZ_RIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_RIGHT = 0x2

	/*
		WMSZ_TOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_TOP = 0x3

	/*
		WMSZ_TOPLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_TOPLEFT = 0x4

	/*
		WMSZ_TOPRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WMSZ_TOPRIGHT = 0x5

	/*
		WM_ACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ACTIVATE = 0x6

	/*
		WM_ACTIVATEAPP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ACTIVATEAPP = 0x1c

	/*
		WM_AFXFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_AFXFIRST = 0x360

	/*
		WM_AFXLAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_AFXLAST = 0x37f

	/*
		WM_APP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_APP = 0x8000

	/*
		WM_APPCOMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_APPCOMMAND = 0x319

	/*
		WM_ASKCBFORMATNAME from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ASKCBFORMATNAME = 0x30c

	/*
		WM_CANCELJOURNAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CANCELJOURNAL = 0x4b

	/*
		WM_CANCELMODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CANCELMODE = 0x1f

	/*
		WM_CAPTURECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CAPTURECHANGED = 0x215

	/*
		WM_CHANGECBCHAIN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CHANGECBCHAIN = 0x30d

	/*
		WM_CHANGEUISTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CHANGEUISTATE = 0x127

	/*
		WM_CHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CHAR = 0x102

	/*
		WM_CHARTOITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CHARTOITEM = 0x2f

	/*
		WM_CHILDACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CHILDACTIVATE = 0x22

	/*
		WM_CLEAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CLEAR = 0x303

	/*
		WM_CLIPBOARDUPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CLIPBOARDUPDATE = 0x31d

	/*
		WM_CLOSE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CLOSE = 0x10

	/*
		WM_COMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_COMMAND = 0x111

	/*
		WM_COMMNOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_COMMNOTIFY = 0x44

	/*
		WM_COMPACTING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_COMPACTING = 0x41

	/*
		WM_COMPAREITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_COMPAREITEM = 0x39

	/*
		WM_CONTEXTMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CONTEXTMENU = 0x7b

	/*
		WM_COPY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_COPY = 0x301

	/*
		WM_COPYDATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_COPYDATA = 0x4a

	/*
		WM_CREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CREATE = 0x1

	/*
		WM_CTLCOLORBTN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLORBTN = 0x135

	/*
		WM_CTLCOLORDLG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLORDLG = 0x136

	/*
		WM_CTLCOLOREDIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLOREDIT = 0x133

	/*
		WM_CTLCOLORLISTBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLORLISTBOX = 0x134

	/*
		WM_CTLCOLORMSGBOX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLORMSGBOX = 0x132

	/*
		WM_CTLCOLORSCROLLBAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLORSCROLLBAR = 0x137

	/*
		WM_CTLCOLORSTATIC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CTLCOLORSTATIC = 0x138

	/*
		WM_CUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_CUT = 0x300

	/*
		WM_DEADCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DEADCHAR = 0x103

	/*
		WM_DELETEITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DELETEITEM = 0x2d

	/*
		WM_DESTROY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DESTROY = 0x2

	/*
		WM_DESTROYCLIPBOARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DESTROYCLIPBOARD = 0x307

	/*
		WM_DEVICECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DEVICECHANGE = 0x219

	/*
		WM_DEVMODECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DEVMODECHANGE = 0x1b

	/*
		WM_DISPLAYCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DISPLAYCHANGE = 0x7e

	/*
		WM_DPICHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DPICHANGED = 0x2e0

	/*
		WM_DPICHANGED_AFTERPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DPICHANGED_AFTERPARENT = 0x2e3

	/*
		WM_DPICHANGED_BEFOREPARENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DPICHANGED_BEFOREPARENT = 0x2e2

	/*
		WM_DRAWCLIPBOARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DRAWCLIPBOARD = 0x308

	/*
		WM_DRAWITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DRAWITEM = 0x2b

	/*
		WM_DROPFILES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DROPFILES = 0x233

	/*
		WM_DWMCOLORIZATIONCOLORCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DWMCOLORIZATIONCOLORCHANGED = 0x320

	/*
		WM_DWMCOMPOSITIONCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DWMCOMPOSITIONCHANGED = 0x31e

	/*
		WM_DWMNCRENDERINGCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DWMNCRENDERINGCHANGED = 0x31f

	/*
		WM_DWMSENDICONICLIVEPREVIEWBITMAP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DWMSENDICONICLIVEPREVIEWBITMAP = 0x326

	/*
		WM_DWMSENDICONICTHUMBNAIL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DWMSENDICONICTHUMBNAIL = 0x323

	/*
		WM_DWMWINDOWMAXIMIZEDCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DWMWINDOWMAXIMIZEDCHANGE = 0x321

	/*
		WM_ENABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ENABLE = 0xa

	/*
		WM_ENDSESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ENDSESSION = 0x16

	/*
		WM_ENTERIDLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ENTERIDLE = 0x121

	/*
		WM_ENTERMENULOOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ENTERMENULOOP = 0x211

	/*
		WM_ENTERSIZEMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ENTERSIZEMOVE = 0x231

	/*
		WM_ERASEBKGND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ERASEBKGND = 0x14

	/*
		WM_EXITMENULOOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_EXITMENULOOP = 0x212

	/*
		WM_EXITSIZEMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_EXITSIZEMOVE = 0x232

	/*
		WM_FONTCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_FONTCHANGE = 0x1d

	/*
		WM_GESTURE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GESTURE = 0x119

	/*
		WM_GESTURENOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GESTURENOTIFY = 0x11a

	/*
		WM_GETDLGCODE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETDLGCODE = 0x87

	/*
		WM_GETDPISCALEDSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETDPISCALEDSIZE = 0x2e4

	/*
		WM_GETFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETFONT = 0x31

	/*
		WM_GETHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETHOTKEY = 0x33

	/*
		WM_GETICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETICON = 0x7f

	/*
		WM_GETMINMAXINFO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETMINMAXINFO = 0x24

	/*
		WM_GETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETOBJECT = 0x3d

	/*
		WM_GETTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETTEXT = 0xd

	/*
		WM_GETTEXTLENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETTEXTLENGTH = 0xe

	/*
		WM_GETTITLEBARINFOEX from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_GETTITLEBARINFOEX = 0x33f

	/*
		WM_HANDHELDFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_HANDHELDFIRST = 0x358

	/*
		WM_HANDHELDLAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_HANDHELDLAST = 0x35f

	/*
		WM_HELP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_HELP = 0x53

	/*
		WM_HOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_HOTKEY = 0x312

	/*
		WM_HSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_HSCROLL = 0x114

	/*
		WM_HSCROLLCLIPBOARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_HSCROLLCLIPBOARD = 0x30e

	/*
		WM_ICONERASEBKGND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_ICONERASEBKGND = 0x27

	/*
		WM_IME_CHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_CHAR = 0x286

	/*
		WM_IME_COMPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_COMPOSITION = 0x10f

	/*
		WM_IME_COMPOSITIONFULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_COMPOSITIONFULL = 0x284

	/*
		WM_IME_CONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_CONTROL = 0x283

	/*
		WM_IME_ENDCOMPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_ENDCOMPOSITION = 0x10e

	/*
		WM_IME_KEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_KEYDOWN = 0x290

	/*
		WM_IME_KEYLAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_KEYLAST = 0x10f

	/*
		WM_IME_KEYUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_KEYUP = 0x291

	/*
		WM_IME_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_NOTIFY = 0x282

	/*
		WM_IME_REQUEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_REQUEST = 0x288

	/*
		WM_IME_SELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_SELECT = 0x285

	/*
		WM_IME_SETCONTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_SETCONTEXT = 0x281

	/*
		WM_IME_STARTCOMPOSITION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_IME_STARTCOMPOSITION = 0x10d

	/*
		WM_INITDIALOG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INITDIALOG = 0x110

	/*
		WM_INITMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INITMENU = 0x116

	/*
		WM_INITMENUPOPUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INITMENUPOPUP = 0x117

	/*
		WM_INPUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INPUT = 0xff

	/*
		WM_INPUTLANGCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INPUTLANGCHANGE = 0x51

	/*
		WM_INPUTLANGCHANGEREQUEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INPUTLANGCHANGEREQUEST = 0x50

	/*
		WM_INPUT_DEVICE_CHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_INPUT_DEVICE_CHANGE = 0xfe

	/*
		WM_KEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_KEYDOWN = 0x100

	/*
		WM_KEYFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_KEYFIRST = 0x100

	/*
		WM_KEYLAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_KEYLAST = 0x109

	/*
		WM_KEYUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_KEYUP = 0x101

	/*
		WM_KILLFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_KILLFOCUS = 0x8

	/*
		WM_LBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_LBUTTONDBLCLK = 0x203

	/*
		WM_LBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_LBUTTONDOWN = 0x201

	/*
		WM_LBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_LBUTTONUP = 0x202

	/*
		WM_MBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MBUTTONDBLCLK = 0x209

	/*
		WM_MBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MBUTTONDOWN = 0x207

	/*
		WM_MBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MBUTTONUP = 0x208

	/*
		WM_MDIACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIACTIVATE = 0x222

	/*
		WM_MDICASCADE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDICASCADE = 0x227

	/*
		WM_MDICREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDICREATE = 0x220

	/*
		WM_MDIDESTROY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIDESTROY = 0x221

	/*
		WM_MDIGETACTIVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIGETACTIVE = 0x229

	/*
		WM_MDIICONARRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIICONARRANGE = 0x228

	/*
		WM_MDIMAXIMIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIMAXIMIZE = 0x225

	/*
		WM_MDINEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDINEXT = 0x224

	/*
		WM_MDIREFRESHMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIREFRESHMENU = 0x234

	/*
		WM_MDIRESTORE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDIRESTORE = 0x223

	/*
		WM_MDISETMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDISETMENU = 0x230

	/*
		WM_MDITILE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MDITILE = 0x226

	/*
		WM_MEASUREITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MEASUREITEM = 0x2c

	/*
		WM_MENUCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MENUCHAR = 0x120

	/*
		WM_MENUCOMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MENUCOMMAND = 0x126

	/*
		WM_MENUDRAG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MENUDRAG = 0x123

	/*
		WM_MENUGETOBJECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MENUGETOBJECT = 0x124

	/*
		WM_MENURBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MENURBUTTONUP = 0x122

	/*
		WM_MENUSELECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MENUSELECT = 0x11f

	/*
		WM_MOUSEACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSEACTIVATE = 0x21

	/*
		WM_MOUSEFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSEFIRST = 0x200

	/*
		WM_MOUSEHWHEEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSEHWHEEL = 0x20e

	/*
		WM_MOUSELAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSELAST = 0x20e

	/*
		WM_MOUSEMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSEMOVE = 0x200

	/*
		WM_MOUSEWHEEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOUSEWHEEL = 0x20a

	/*
		WM_MOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOVE = 0x3

	/*
		WM_MOVING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_MOVING = 0x216

	/*
		WM_NCACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCACTIVATE = 0x86

	/*
		WM_NCCALCSIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCCALCSIZE = 0x83

	/*
		WM_NCCREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCCREATE = 0x81

	/*
		WM_NCDESTROY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCDESTROY = 0x82

	/*
		WM_NCHITTEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCHITTEST = 0x84

	/*
		WM_NCLBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCLBUTTONDBLCLK = 0xa3

	/*
		WM_NCLBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCLBUTTONDOWN = 0xa1

	/*
		WM_NCLBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCLBUTTONUP = 0xa2

	/*
		WM_NCMBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCMBUTTONDBLCLK = 0xa9

	/*
		WM_NCMBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCMBUTTONDOWN = 0xa7

	/*
		WM_NCMBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCMBUTTONUP = 0xa8

	/*
		WM_NCMOUSEHOVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCMOUSEHOVER = 0x2a0

	/*
		WM_NCMOUSELEAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCMOUSELEAVE = 0x2a2

	/*
		WM_NCMOUSEMOVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCMOUSEMOVE = 0xa0

	/*
		WM_NCPAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCPAINT = 0x85

	/*
		WM_NCPOINTERDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCPOINTERDOWN = 0x242

	/*
		WM_NCPOINTERUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCPOINTERUP = 0x243

	/*
		WM_NCPOINTERUPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCPOINTERUPDATE = 0x241

	/*
		WM_NCRBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCRBUTTONDBLCLK = 0xa6

	/*
		WM_NCRBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCRBUTTONDOWN = 0xa4

	/*
		WM_NCRBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCRBUTTONUP = 0xa5

	/*
		WM_NCXBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCXBUTTONDBLCLK = 0xad

	/*
		WM_NCXBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCXBUTTONDOWN = 0xab

	/*
		WM_NCXBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NCXBUTTONUP = 0xac

	/*
		WM_NEXTDLGCTL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NEXTDLGCTL = 0x28

	/*
		WM_NEXTMENU from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NEXTMENU = 0x213

	/*
		WM_NOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NOTIFY = 0x4e

	/*
		WM_NOTIFYFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NOTIFYFORMAT = 0x55

	/*
		WM_NULL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_NULL = 0x0

	/*
		WM_PAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PAINT = 0xf

	/*
		WM_PAINTCLIPBOARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PAINTCLIPBOARD = 0x309

	/*
		WM_PAINTICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PAINTICON = 0x26

	/*
		WM_PALETTECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PALETTECHANGED = 0x311

	/*
		WM_PALETTEISCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PALETTEISCHANGING = 0x310

	/*
		WM_PARENTNOTIFY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PARENTNOTIFY = 0x210

	/*
		WM_PASTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PASTE = 0x302

	/*
		WM_PENWINFIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PENWINFIRST = 0x380

	/*
		WM_PENWINLAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PENWINLAST = 0x38f

	/*
		WM_POINTERACTIVATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERACTIVATE = 0x24b

	/*
		WM_POINTERCAPTURECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERCAPTURECHANGED = 0x24c

	/*
		WM_POINTERDEVICECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERDEVICECHANGE = 0x238

	/*
		WM_POINTERDEVICEINRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERDEVICEINRANGE = 0x239

	/*
		WM_POINTERDEVICEOUTOFRANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERDEVICEOUTOFRANGE = 0x23a

	/*
		WM_POINTERDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERDOWN = 0x246

	/*
		WM_POINTERENTER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERENTER = 0x249

	/*
		WM_POINTERHWHEEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERHWHEEL = 0x24f

	/*
		WM_POINTERLEAVE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERLEAVE = 0x24a

	/*
		WM_POINTERROUTEDAWAY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERROUTEDAWAY = 0x252

	/*
		WM_POINTERROUTEDRELEASED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERROUTEDRELEASED = 0x253

	/*
		WM_POINTERROUTEDTO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERROUTEDTO = 0x251

	/*
		WM_POINTERUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERUP = 0x247

	/*
		WM_POINTERUPDATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERUPDATE = 0x245

	/*
		WM_POINTERWHEEL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POINTERWHEEL = 0x24e

	/*
		WM_POWER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POWER = 0x48

	/*
		WM_POWERBROADCAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_POWERBROADCAST = 0x218

	/*
		WM_PRINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PRINT = 0x317

	/*
		WM_PRINTCLIENT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_PRINTCLIENT = 0x318

	/*
		WM_QUERYDRAGICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUERYDRAGICON = 0x37

	/*
		WM_QUERYENDSESSION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUERYENDSESSION = 0x11

	/*
		WM_QUERYNEWPALETTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUERYNEWPALETTE = 0x30f

	/*
		WM_QUERYOPEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUERYOPEN = 0x13

	/*
		WM_QUERYUISTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUERYUISTATE = 0x129

	/*
		WM_QUEUESYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUEUESYNC = 0x23

	/*
		WM_QUIT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_QUIT = 0x12

	/*
		WM_RBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_RBUTTONDBLCLK = 0x206

	/*
		WM_RBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_RBUTTONDOWN = 0x204

	/*
		WM_RBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_RBUTTONUP = 0x205

	/*
		WM_RENDERALLFORMATS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_RENDERALLFORMATS = 0x306

	/*
		WM_RENDERFORMAT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_RENDERFORMAT = 0x305

	/*
		WM_SETCURSOR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETCURSOR = 0x20

	/*
		WM_SETFOCUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETFOCUS = 0x7

	/*
		WM_SETFONT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETFONT = 0x30

	/*
		WM_SETHOTKEY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETHOTKEY = 0x32

	/*
		WM_SETICON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETICON = 0x80

	/*
		WM_SETREDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETREDRAW = 0xb

	/*
		WM_SETTEXT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETTEXT = 0xc

	/*
		WM_SETTINGCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SETTINGCHANGE = 0x1a

	/*
		WM_SHOWWINDOW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SHOWWINDOW = 0x18

	/*
		WM_SIZE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SIZE = 0x5

	/*
		WM_SIZECLIPBOARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SIZECLIPBOARD = 0x30b

	/*
		WM_SIZING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SIZING = 0x214

	/*
		WM_SPOOLERSTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SPOOLERSTATUS = 0x2a

	/*
		WM_STYLECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_STYLECHANGED = 0x7d

	/*
		WM_STYLECHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_STYLECHANGING = 0x7c

	/*
		WM_SYNCPAINT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYNCPAINT = 0x88

	/*
		WM_SYSCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYSCHAR = 0x106

	/*
		WM_SYSCOLORCHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYSCOLORCHANGE = 0x15

	/*
		WM_SYSCOMMAND from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYSCOMMAND = 0x112

	/*
		WM_SYSDEADCHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYSDEADCHAR = 0x107

	/*
		WM_SYSKEYDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYSKEYDOWN = 0x104

	/*
		WM_SYSKEYUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_SYSKEYUP = 0x105

	/*
		WM_TABLET_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TABLET_FIRST = 0x2c0

	/*
		WM_TABLET_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TABLET_LAST = 0x2df

	/*
		WM_TCARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TCARD = 0x52

	/*
		WM_THEMECHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_THEMECHANGED = 0x31a

	/*
		WM_TIMECHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TIMECHANGE = 0x1e

	/*
		WM_TIMER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TIMER = 0x113

	/*
		WM_TOOLTIPDISMISS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TOOLTIPDISMISS = 0x345

	/*
		WM_TOUCH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TOUCH = 0x240

	/*
		WM_TOUCHHITTESTING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_TOUCHHITTESTING = 0x24d

	/*
		WM_UNDO from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_UNDO = 0x304

	/*
		WM_UNICHAR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_UNICHAR = 0x109

	/*
		WM_UNINITMENUPOPUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_UNINITMENUPOPUP = 0x125

	/*
		WM_UPDATEUISTATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_UPDATEUISTATE = 0x128

	/*
		WM_USER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_USER = 0x400

	/*
		WM_USERCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_USERCHANGED = 0x54

	/*
		WM_VKEYTOITEM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_VKEYTOITEM = 0x2e

	/*
		WM_VSCROLL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_VSCROLL = 0x115

	/*
		WM_VSCROLLCLIPBOARD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_VSCROLLCLIPBOARD = 0x30a

	/*
		WM_WINDOWPOSCHANGED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_WINDOWPOSCHANGED = 0x47

	/*
		WM_WINDOWPOSCHANGING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_WINDOWPOSCHANGING = 0x46

	/*
		WM_WININICHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_WININICHANGE = 0x1a

	/*
		WM_WTSSESSION_CHANGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_WTSSESSION_CHANGE = 0x2b1

	/*
		WM_XBUTTONDBLCLK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_XBUTTONDBLCLK = 0x20d

	/*
		WM_XBUTTONDOWN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_XBUTTONDOWN = 0x20b

	/*
		WM_XBUTTONUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_XBUTTONUP = 0x20c

	/*
		WSF_VISIBLE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WSF_VISIBLE = 0x1

	/*
		WTS_CONSOLE_CONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_CONSOLE_CONNECT = 0x1

	/*
		WTS_CONSOLE_DISCONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_CONSOLE_DISCONNECT = 0x2

	/*
		WTS_REMOTE_CONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_REMOTE_CONNECT = 0x3

	/*
		WTS_REMOTE_DISCONNECT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_REMOTE_DISCONNECT = 0x4

	/*
		WTS_SESSION_CREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_CREATE = 0xa

	/*
		WTS_SESSION_LOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_LOCK = 0x7

	/*
		WTS_SESSION_LOGOFF from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_LOGOFF = 0x6

	/*
		WTS_SESSION_LOGON from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_LOGON = 0x5

	/*
		WTS_SESSION_REMOTE_CONTROL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_REMOTE_CONTROL = 0x9

	/*
		WTS_SESSION_TERMINATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_TERMINATE = 0xb

	/*
		WTS_SESSION_UNLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WTS_SESSION_UNLOCK = 0x8

	/*
		WVR_ALIGNBOTTOM from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_ALIGNBOTTOM = 0x40

	/*
		WVR_ALIGNLEFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_ALIGNLEFT = 0x20

	/*
		WVR_ALIGNRIGHT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_ALIGNRIGHT = 0x80

	/*
		WVR_ALIGNTOP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_ALIGNTOP = 0x10

	/*
		WVR_HREDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_HREDRAW = 0x100

	/*
		WVR_REDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_REDRAW = 0x300

	/*
		WVR_VALIDRECTS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_VALIDRECTS = 0x400

	/*
		WVR_VREDRAW from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WVR_VREDRAW = 0x200

	/*
		XBUTTON1 from Windows.Win32 metadata (originally typed uint16). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XBUTTON1 = 0x1

	/*
		XBUTTON2 from Windows.Win32 metadata (originally typed uint16). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XBUTTON2 = 0x2
)

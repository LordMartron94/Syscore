//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_system_shutdown

const (
	/*
		MAX_NUM_REASONS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_NUM_REASONS = 0x100

	/*
		MAX_REASON_BUGID_LEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_REASON_BUGID_LEN = 0x20

	/*
		MAX_REASON_COMMENT_LEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_REASON_COMMENT_LEN = 0x200

	/*
		MAX_REASON_DESC_LEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_REASON_DESC_LEN = 0x100

	/*
		MAX_REASON_NAME_LEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_REASON_NAME_LEN = 0x40

	/*
		POLICY_SHOWREASONUI_ALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POLICY_SHOWREASONUI_ALWAYS = 0x1

	/*
		POLICY_SHOWREASONUI_NEVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POLICY_SHOWREASONUI_NEVER = 0x0

	/*
		POLICY_SHOWREASONUI_SERVERONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POLICY_SHOWREASONUI_SERVERONLY = 0x3

	/*
		POLICY_SHOWREASONUI_WORKSTATIONONLY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	POLICY_SHOWREASONUI_WORKSTATIONONLY = 0x2

	/*
		SHUTDOWN_TYPE_LEN from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SHUTDOWN_TYPE_LEN = 0x20

	/*
		SNAPSHOT_POLICY_ALWAYS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SNAPSHOT_POLICY_ALWAYS = 0x1

	/*
		SNAPSHOT_POLICY_NEVER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SNAPSHOT_POLICY_NEVER = 0x0

	/*
		SNAPSHOT_POLICY_UNPLANNED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	SNAPSHOT_POLICY_UNPLANNED = 0x2
)

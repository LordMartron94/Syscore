//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_system_dataexchange

const (
	/*
		APPCLASS_MASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	APPCLASS_MASK = 0xf

	/*
		APPCMD_MASK from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	APPCMD_MASK = 0xff0

	/*
		CADV_LATEACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CADV_LATEACK = 0xffff

	/*
		CP_WINANSI from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_WINANSI = 0x3ec

	/*
		CP_WINNEUTRAL from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_WINNEUTRAL = 0x4b0

	/*
		CP_WINUNICODE from Windows.Win32 metadata (originally typed int32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	CP_WINUNICODE = 0x4b0

	/*
		DDE_FACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FACK = 0x8000

	/*
		DDE_FACKREQ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FACKREQ = 0x8000

	/*
		DDE_FAPPSTATUS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FAPPSTATUS = 0xff

	/*
		DDE_FBUSY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FBUSY = 0x4000

	/*
		DDE_FDEFERUPD from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FDEFERUPD = 0x4000

	/*
		DDE_FNOTPROCESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FNOTPROCESSED = 0x0

	/*
		DDE_FRELEASE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FRELEASE = 0x2000

	/*
		DDE_FREQUESTED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DDE_FREQUESTED = 0x1000

	/*
		DMLERR_ADVACKTIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_ADVACKTIMEOUT = 0x4000

	/*
		DMLERR_BUSY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_BUSY = 0x4001

	/*
		DMLERR_DATAACKTIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_DATAACKTIMEOUT = 0x4002

	/*
		DMLERR_DLL_NOT_INITIALIZED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_DLL_NOT_INITIALIZED = 0x4003

	/*
		DMLERR_DLL_USAGE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_DLL_USAGE = 0x4004

	/*
		DMLERR_EXECACKTIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_EXECACKTIMEOUT = 0x4005

	/*
		DMLERR_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_FIRST = 0x4000

	/*
		DMLERR_INVALIDPARAMETER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_INVALIDPARAMETER = 0x4006

	/*
		DMLERR_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_LAST = 0x4011

	/*
		DMLERR_LOW_MEMORY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_LOW_MEMORY = 0x4007

	/*
		DMLERR_MEMORY_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_MEMORY_ERROR = 0x4008

	/*
		DMLERR_NOTPROCESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_NOTPROCESSED = 0x4009

	/*
		DMLERR_NO_CONV_ESTABLISHED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_NO_CONV_ESTABLISHED = 0x400a

	/*
		DMLERR_NO_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_NO_ERROR = 0x0

	/*
		DMLERR_POKEACKTIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_POKEACKTIMEOUT = 0x400b

	/*
		DMLERR_POSTMSG_FAILED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_POSTMSG_FAILED = 0x400c

	/*
		DMLERR_REENTRANCY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_REENTRANCY = 0x400d

	/*
		DMLERR_SERVER_DIED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_SERVER_DIED = 0x400e

	/*
		DMLERR_SYS_ERROR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_SYS_ERROR = 0x400f

	/*
		DMLERR_UNADVACKTIMEOUT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_UNADVACKTIMEOUT = 0x4010

	/*
		DMLERR_UNFOUND_QUEUE_ID from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	DMLERR_UNFOUND_QUEUE_ID = 0x4011

	/*
		HDATA_APPOWNED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	HDATA_APPOWNED = 0x1

	/*
		MAX_MONITORS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MAX_MONITORS = 0x4

	/*
		MF_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MF_MASK = 0xff000000

	/*
		MH_CLEANUP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MH_CLEANUP = 0x4

	/*
		MH_CREATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MH_CREATE = 0x1

	/*
		MH_DELETE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MH_DELETE = 0x3

	/*
		MH_KEEP from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MH_KEEP = 0x2

	/*
		MSGF_DDEMGR from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	MSGF_DDEMGR = 0x8001

	/*
		QID_SYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	QID_SYNC = 0xffffffff

	/*
		TIMEOUT_ASYNC from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	TIMEOUT_ASYNC = 0xffffffff

	/*
		WM_DDE_ACK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_ACK = 0x3e4

	/*
		WM_DDE_ADVISE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_ADVISE = 0x3e2

	/*
		WM_DDE_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_DATA = 0x3e5

	/*
		WM_DDE_EXECUTE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_EXECUTE = 0x3e8

	/*
		WM_DDE_FIRST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_FIRST = 0x3e0

	/*
		WM_DDE_INITIATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_INITIATE = 0x3e0

	/*
		WM_DDE_LAST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_LAST = 0x3e8

	/*
		WM_DDE_POKE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_POKE = 0x3e7

	/*
		WM_DDE_REQUEST from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_REQUEST = 0x3e6

	/*
		WM_DDE_TERMINATE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_TERMINATE = 0x3e1

	/*
		WM_DDE_UNADVISE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WM_DDE_UNADVISE = 0x3e3

	/*
		XCLASS_BOOL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XCLASS_BOOL = 0x1000

	/*
		XCLASS_DATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XCLASS_DATA = 0x2000

	/*
		XCLASS_FLAGS from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XCLASS_FLAGS = 0x4000

	/*
		XCLASS_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XCLASS_MASK = 0xfc00

	/*
		XCLASS_NOTIFICATION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XCLASS_NOTIFICATION = 0x8000

	/*
		XTYPF_ACKREQ from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XTYPF_ACKREQ = 0x8

	/*
		XTYPF_NOBLOCK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XTYPF_NOBLOCK = 0x2

	/*
		XTYPF_NODATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XTYPF_NODATA = 0x4

	/*
		XTYP_MASK from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XTYP_MASK = 0xf0

	/*
		XTYP_SHIFT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	XTYP_SHIFT = 0x4
)

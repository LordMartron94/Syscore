//go:build windows

// This file was auto-generated by 'syscore window bindings generator' at 2026-06-02 14:49:52 CEST

package windows_win32_system_errorreporting

const (
	/*
		WER_DUMP_AUXILIARY from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_DUMP_AUXILIARY = 0x2

	/*
		WER_DUMP_MASK_START from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_DUMP_MASK_START = 0x1

	/*
		WER_DUMP_NOHEAP_ONQUEUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_DUMP_NOHEAP_ONQUEUE = 0x1

	/*
		WER_FAULT_REPORTING_CRITICAL from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FAULT_REPORTING_CRITICAL = 0x200

	/*
		WER_FAULT_REPORTING_DISABLE_SNAPSHOT_CRASH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FAULT_REPORTING_DISABLE_SNAPSHOT_CRASH = 0x80

	/*
		WER_FAULT_REPORTING_DISABLE_SNAPSHOT_HANG from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FAULT_REPORTING_DISABLE_SNAPSHOT_HANG = 0x100

	/*
		WER_FAULT_REPORTING_DURABLE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FAULT_REPORTING_DURABLE = 0x400

	/*
		WER_FAULT_REPORTING_FLAG_NO_HEAP_ON_QUEUE from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FAULT_REPORTING_FLAG_NO_HEAP_ON_QUEUE = 0x40

	/*
		WER_FAULT_REPORTING_NO_UI from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FAULT_REPORTING_NO_UI = 0x20

	/*
		WER_FILE_COMPRESSED from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_FILE_COMPRESSED = 0x4

	/*
		WER_MAX_APPLICATION_NAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_APPLICATION_NAME_LENGTH = 0x80

	/*
		WER_MAX_BUCKET_ID_STRING_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_BUCKET_ID_STRING_LENGTH = 0x104

	/*
		WER_MAX_DESCRIPTION_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_DESCRIPTION_LENGTH = 0x200

	/*
		WER_MAX_EVENT_NAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_EVENT_NAME_LENGTH = 0x40

	/*
		WER_MAX_FRIENDLY_EVENT_NAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_FRIENDLY_EVENT_NAME_LENGTH = 0x80

	/*
		WER_MAX_LOCAL_DUMP_SUBPATH_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_LOCAL_DUMP_SUBPATH_LENGTH = 0x40

	/*
		WER_MAX_PARAM_COUNT from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_PARAM_COUNT = 0xa

	/*
		WER_MAX_PARAM_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_PARAM_LENGTH = 0x104

	/*
		WER_MAX_PREFERRED_MODULES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_PREFERRED_MODULES = 0x80

	/*
		WER_MAX_PREFERRED_MODULES_BUFFER from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_PREFERRED_MODULES_BUFFER = 0x100

	/*
		WER_MAX_REGISTERED_DUMPCOLLECTION from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_REGISTERED_DUMPCOLLECTION = 0x4

	/*
		WER_MAX_REGISTERED_ENTRIES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_REGISTERED_ENTRIES = 0x200

	/*
		WER_MAX_REGISTERED_METADATA from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_REGISTERED_METADATA = 0x8

	/*
		WER_MAX_REGISTERED_RUNTIME_EXCEPTION_MODULES from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_REGISTERED_RUNTIME_EXCEPTION_MODULES = 0x10

	/*
		WER_MAX_SIGNATURE_NAME_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_SIGNATURE_NAME_LENGTH = 0x80

	/*
		WER_MAX_TOTAL_PARAM_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_MAX_TOTAL_PARAM_LENGTH = 0x6b8

	/*
		WER_METADATA_KEY_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_METADATA_KEY_MAX_LENGTH = 0x40

	/*
		WER_METADATA_VALUE_MAX_LENGTH from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_METADATA_VALUE_MAX_LENGTH = 0x80

	/*
		WER_P0 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P0 = 0x0

	/*
		WER_P1 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P1 = 0x1

	/*
		WER_P2 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P2 = 0x2

	/*
		WER_P3 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P3 = 0x3

	/*
		WER_P4 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P4 = 0x4

	/*
		WER_P5 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P5 = 0x5

	/*
		WER_P6 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P6 = 0x6

	/*
		WER_P7 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P7 = 0x7

	/*
		WER_P8 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P8 = 0x8

	/*
		WER_P9 from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_P9 = 0x9

	/*
		WER_SUBMIT_BYPASS_NETWORK_COST_THROTTLING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_SUBMIT_BYPASS_NETWORK_COST_THROTTLING = 0x8000

	/*
		WER_SUBMIT_BYPASS_POWER_THROTTLING from Windows.Win32 metadata (originally typed uint32). Emitted untyped so callers convert to the receiving Go type at the use site.
	*/
	WER_SUBMIT_BYPASS_POWER_THROTTLING = 0x4000
)
